OPML file upload creates feed subscriptions with category-as-tag preservation, app settings page configures reader preferences (articlesPerPage, markReadOnOpen), 52 new tests across 3 tasks. parse_opml() pure function handles flat/nested OPML with category nesting via recursive walk. process_opml_import() async helper calls subscribe() per feed with category→bpkm:tags patching. Settings helpers (get_settings_context, save_settings, validate_articles_per_page) separate logic from routes. Manifest declares permissions.settings: true with 2 settings. All 140 RSS-related tests pass with zero regressions.