# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M002 | scope | Items from CONCERNS.md already fixed | Close: datetime.now timezone, label cache invalidation, CORS conditional, IRI validation, debug SPARQL owner-only, EventStore DI, Alembic startup, SMTP, session cleanup, ViewSpec cache, cookie_secure | Verified in codebase — fixed in v2.1 through v2.6 | No |
| D002 | M002 | scope | MCP server for AI agent access | Defer to future milestone | Feature work, not polish — doesn't belong in hardening milestone | Yes — when hardening complete |
| D003 | M002 | scope | Admin model detail stats and charts | Defer to future milestone | Cosmetic admin enhancements, low priority | Yes |
| D004 | M002 | scope | Browser router refactor approach | Split into domain sub-routers, zero behavior change | Preserves all URL paths and htmx wiring; E2E tests are the safety net | No |
| D005 | M002 | scope | Federation testing approach | Dual-instance docker-compose for E2E testing | Single instance can't test federation; permanent dev setup not needed | Yes — if federation becomes daily workflow |
| D006 | M002 | scope | Obsidian import testing approach | Manual user-driven import of real Ideaverse vault, fix bugs found | Automated testing of 905-note vault import is fragile; user judgment needed for mapping quality | No |
| D007 | M002/S01 | tech | Rate limiting library | slowapi with in-memory backend, per-route decorators (no global default) | Battle-tested (wraps flask-limiter), no Redis needed for single-instance, supports `get_remote_address` for real client IP behind nginx | Yes — add Redis backend if scaling to multiple workers |
| D008 | M002/S01 | tech | SPARQL regex escaping placement | Standalone `backend/app/sparql/utils.py` module | Must be importable without pulling in views service; S03 will unit-test it directly | No |
| D009 | M002/S02 | tech | SPARQL string-stripping helper placement | Private `_strip_sparql_strings()` in `sparql/client.py`, not in `sparql/utils.py` | This is keyword-detection preprocessing specific to `scope_to_current_graph` and `check_member_query_safety`, not general SPARQL escaping. Keeping it module-private avoids polluting the shared utils API. | No |
| D010 | M002/S02 | tech | View spec source_model query strategy | `GRAPH ?g { ... }` with VALUES clause constraining graph IRIs | Matches existing codebase pattern (events/query.py), avoids scanning unrelated graphs, captures per-spec provenance. Reverse map from graph IRI to model ID uses the same `urn:sempkm:model:{id}:views` convention as models/registry.py. | No |
| D011 | M002/S03 | tech | Backend test strategy | Pure-function unit tests via `.venv/bin/pytest`, no Docker/triplestore/server | All S03 targets are sync pure functions or functions with injectable settings. No integration test infra needed. Tests run in <5s locally. | No |
| D012 | M002/S03 | tech | Token expiry testing approach | Use `max_age_seconds=0` parameter instead of `time.sleep()` | Avoids slow/flaky sleep-based tests. Tests the same verify API with immediate expiry. | No |
| D013 | M002/S03 | tech | `_serializer` singleton isolation | Autouse pytest fixture resets `tokens._serializer = None` after each test | Prevents stale serializer from masking key-resolution bugs across tests. | No |
| D014 | M002/S04 | tech | Sub-router prefix strategy | Sub-routers get NO prefix; routes keep full sub-paths (e.g. `@router.get("/settings")`). Parent coordinator provides `/browser/` prefix. | Avoids prefix stacking bugs (`/browser/browser/settings`). Matches how existing codebase works. | No |
| D015 | M002/S04 | tech | `_validate_iri` test import path | Update `test_iri_validation.py` to import from `app.browser._helpers` instead of re-exporting from `router.py` | Cleaner — no re-export indirection. Single source of truth in `_helpers.py`. | No |
| D016 | M002/S04 | tech | Sub-router include order | Register sub-routers in same order as routes appeared in original monolith file | Preserves FastAPI route matching order; prevents path-parameter shadowing regressions. | No |
| D017 | M002/S06 | tech | Federation E2E invitation strategy | Hybrid: simulate invitation via direct SPARQL insertion on instance B, bypass HTTP Signatures/WebFinger for invitation delivery | Full cross-instance signing requires TLS (WebFinger hardcodes HTTPS) and key management in tests. Hybrid still tests accept → sync with real API calls. Upgrade to real signing if federation becomes a daily workflow. | Yes — add real signing test when WebFinger supports HTTP scheme |
| D018 | M002/S06 | tech | Federation test ports | Instance A: 3911/8911, Instance B: 3912/8912 | Avoids conflict with dev (3000/8001) and existing E2E test (3901/8901) stacks | No |
| D019 | M002/S06 | tech | Federation test APP_BASE_URL | Use Docker service name URLs (`http://frontend-a:80`, `http://frontend-b:80`) | WebID URLs must be fetchable by the other instance. Docker service names resolve on the shared network. External localhost URLs wouldn't work for cross-container requests. | No |
| D020 | M003 | arch | Explorer mode switching mechanism | Dropdown replaces OBJECTS section content in-place via htmx | User confirmed: single section with mode dropdown, not multiple collapsible sections per mode | No |
| D021 | M003 | arch | Favorites storage layer | SQL (user_favorites table) not RDF | Favorites are user preferences, not knowledge graph data. Same layer as auth/sessions. | Yes — if favorites need graph queries |
| D022 | M003 | arch | Comments storage layer | RDF via EventStore operations | Comments are knowledge about objects — they belong in the knowledge graph. Event-sourced for auditability. | No |
| D023 | M003 | arch | Comment threading model | Threaded with reply-to nesting | User chose threaded over flat. Modeled via sempkm:replyTo predicate in RDF. | No |
| D024 | M003 | arch | Gist visibility in ontology viewer | Fully visible — gist classes on equal footing with model classes | User chose fully visible over grayed/hidden. Power users can explore the full hierarchy. | No |
| D025 | M003 | arch | Gist version | Pin to 14.0.0, update deliberately | Avoids breaking changes from upstream. CC BY 4.0 license. | Yes — when gist 15.x releases |
| D026 | M003 | arch | Gist named graph IRI | `urn:sempkm:ontology:gist` | Namespaced under sempkm convention. Separate from model graphs. | No |
| D027 | M003 | arch | User-created types graph | `urn:sempkm:user-types` named graph | Separate from model graphs. Discoverable by ShapesService. | No |
| D028 | M003 | scope | M003 class creation scope | Name, icon, parent class, basic properties. No advanced SHACL editor. | Full shape editing deferred to later milestone (TYPE-03). | Yes — when TYPE-03 planned |
| D029 | M003 | arch | Hierarchy nesting depth | Arbitrary depth, no hard cap | Most real usage will be 2-3 levels. Lazy expansion prevents performance issues. | No |
| D030 | M003 | arch | VFS strategy reuse for explorer | Adapt VFS strategies.py SPARQL builders for htmx tree rendering | Same queries, different output format. Avoids duplicating SPARQL logic. | No |
| D031 | M003/S01 | tech | Explorer mode endpoint pattern | Single `GET /browser/explorer/tree?mode={mode}` with `hx-include="this"` on `<select name="mode">` | htmx-native: select value auto-included in request. Simpler than dynamic URL building. Registry pattern allows downstream slices to register handlers. | No |
| D032 | M003/S01 | tech | Explorer mode persistence | localStorage key `sempkm_explorer_mode` | Lightweight, per-browser, no server state needed for UI preference. Read on workspace init, trigger htmx load if non-default. | Yes — if multi-device sync needed |
| D033 | M003/S01 | tech | Existing `/browser/nav-tree` endpoint kept for backwards compat | Delegates internally to by-type handler | Prevents breakage during incremental rollout. Will be deprecated once all callers use mode-aware endpoint. | Yes — remove when safe |
| D034 | M003/S01 | tech | Selection clearing strategy on mode switch | `clearSelection()` + `lastClickedLeaf = null` on dropdown change event | Prevents stale selection state referencing IRIs not visible in the new tree. Proactive clear before htmx swap. | No |
| D035 | M003/S02 | tech | Hierarchy node rendering: all nodes as expandable `.tree-node` | Every hierarchy node rendered as `.tree-node` with lazy `hx-trigger="click once"`, plus label `onclick` for `handleTreeLeafClick` | Any object could have children — rendering all as expandable avoids a sub-query to detect children upfront. If children endpoint returns empty, the expanded area shows "No children". Simpler than ASK queries per node. | Yes — add child-count sub-query if UX feedback demands toggle-only-when-children |
| D036 | M003/S02 | tech | Children endpoint as separate route from `tree_children` | New `GET /browser/explorer/children?parent={iri}` rather than reusing `/browser/tree/{type_iri}` | `tree_children` queries by `rdf:type`, hierarchy queries by `dcterms:isPartOf` — fundamentally different SPARQL. Separate endpoint avoids overloading the type-children route with mode-conditional logic. | No |
| D037 | M003/S03 | tech | Mount mode value encoding in explorer dropdown | `<option value="mount:{uuid}">` with server-side prefix parsing | Single `<select>` value carries both mode type and mount identity. Avoids hidden inputs or second query param. Parser strips `mount:` prefix to extract UUID. | No |
| D038 | M003/S03 | tech | Mount handler as prefix match, not EXPLORER_MODES entry | `explorer_tree` checks `mode.startswith("mount:")` before registry lookup | Avoids polluting the static EXPLORER_MODES dict with per-mount entries. Single handler function handles all mounts via mount_id dispatch. | No |
| D039 | M003/S03 | tech | Dynamic mount dropdown injection timing | Fetch mounts after `initExplorerMode()`, then re-run mode restore for `mount:` prefixed stored modes | Avoids blocking initial page load. Built-in modes work immediately; mount options appear asynchronously. Re-check handles the case where stored mode is a mount that wasn't in the DOM during first restore. | No |
| D040 | M003/S03 | tech | Separate mount-children endpoint from explorer/children | New `GET /browser/explorer/mount-children?mount_id={id}&folder={folder}` | Mount folder expansion dispatches by VFS strategy (5 strategies × multiple depth levels), fundamentally different from hierarchy `dcterms:isPartOf` queries. Separate endpoint keeps dispatch clean. | No |
| D041 | M003/S04 | tech | Tag property detection heuristic | `'tags' in iri` or `'keywords' in iri` substring match | Only `bpkm:tags` and `schema:keywords` exist as tag properties. Simple substring match is sufficient and consistent across templates, save path, and SPARQL queries. | Yes — add explicit IRI set if more properties appear |
| D042 | M003/S04 | tech | Multi-property by-tag SPARQL uses UNION, not VFS reuse | Custom SPARQL with UNION across `bpkm:tags` and `schema:keywords` instead of reusing VFS `query_tag_folders` | VFS functions take single `tag_property` and `scope_filter` params — explorer needs multi-property, unscoped queries. Adapting would be more complex than writing fresh queries following the same pattern. | No |
| D043 | M003/S04 | tech | Tag migration as admin endpoint, not startup hook | `POST /browser/admin/migrate-tags` requiring owner role | No existing SPARQL migration infrastructure. Explicit endpoint avoids running migration on every startup. Idempotent — safe to call multiple times. | Yes — move to startup migration runner if one is built |
| D044 | M003/S04 | tech | Tag-children as separate endpoint from tree_children and explorer/children | `GET /browser/explorer/tag-children?tag={value}` | Tag queries use UNION across two properties with string-match FILTER, fundamentally different from type-based tree_children or isPartOf-based explorer/children. Follows D036/D040 pattern of separate endpoints per query shape. | No |
| D045 | M003/S05 | tech | Favorites router as separate browser sub-module | `backend/app/browser/favorites.py` with `favorites_router` included in `browser/router.py` | Follows D014 sub-router pattern. Favorites has its own endpoints (toggle, list) separate from workspace routes. Keeps workspace.py from growing further. | No |
| D046 | M003/S05 | tech | FAVORITES section position in explorer pane | Above OBJECTS section, not below | Favorites are high-traffic shortcuts — placing them above the main object list gives immediate visibility. Follows UX convention for quick-access sections. | Yes — move below if user feedback prefers |
| D047 | M003/S05 | tech | Star icon toggle strategy | Two `<i>` elements (outline + filled) with visibility toggle, not Lucide re-render | Avoids Lucide `createIcons()` re-render timing issues. Simpler DOM manipulation — toggle a class to swap which icon is visible. | No |
| D048 | M003/S05 | tech | FAVORITES refresh via htmx trigger event | `HX-Trigger: favoritesRefreshed` on toggle response + `hx-trigger="load, favoritesRefreshed from:body"` on section | htmx-native pattern for cross-component refresh. Toggle endpoint sets header; since star uses fetch (not htmx), JS dispatches `htmx.trigger(document.body, 'favoritesRefreshed')` after successful toggle. | No |
| D049 | M003/S06 | tech | Comment soft-delete strategy | Replace body with "[deleted]" and remove author triple, preserve comment IRI and replyTo structure | Avoids orphaned reply chains. Thread structure stays intact for readers. Simpler than cascade delete or tombstone patterns. | Yes — add hard-delete if storage becomes a concern |
| D050 | M003/S06 | tech | Comment thread query strategy | Flat SPARQL SELECT + Python tree assembly, not recursive SPARQL | RDF4J property paths can't carry metadata (author, timestamp) along the path. Flat query is simple, fast, and lets Python build the tree with full control over ordering and depth capping. | No |
| D051 | M003/S06 | tech | Comments router as separate browser sub-module | `backend/app/browser/comments.py` with `comments_router` included in `browser/router.py` | Follows D014/D045 sub-router pattern. Comments have their own CRUD endpoints separate from workspace routes. | No |
| D052 | M003/S06 | tech | Comments router include order | `comments_router` included before `objects_router` in `browser/router.py` | FastAPI's `:path` parameter in `objects_router` (`/object/{iri:path}`) is greedy and consumes `/comments` suffix, preventing comments endpoints from matching. Must register more-specific routes first. | No |
| D053 | M003/S07 | tech | Ontology service as separate module | `backend/app/ontology/` module (service.py + router.py), not added to existing services/ | Ontology loading + TBox/ABox/RBox queries are a distinct domain boundary with startup responsibilities. Separate module keeps workspace.py from growing further and follows the domain-module pattern (canvas/, lint/, vfs/). | No |
| D054 | M003/S07 | tech | Gist loading via batched INSERT DATA, not RDF4J REST upload | Parse gist with rdflib → batch INSERT DATA (≤500 triples) via existing TriplestoreClient | Avoids adding a new RDF4J REST API method to TriplestoreClient. Batch size of 500 stays well under RDF4J's 2MB request body limit. Transaction-scoped for atomicity. Trade-off: slightly slower than direct Turtle upload but simpler and uses proven patterns. | Yes — switch to RDF4J REST graph upload if batch loading is too slow |
| D055 | M003/S07 | tech | Ontology viewer as three-tab htmx layout in special-panel | TBox/ABox/RBox as htmx-driven tabs inside a single `ontology_page.html`, TBox loads on open, ABox/RBox lazy-load on tab click | Simpler than dockview sub-panels. Consistent with how other workspace tabs work (canvas, VFS browser). htmx tab switching avoids JS complexity. | No |
| D056 | M003/S07 | tech | TBox label resolution inline via SPARQL COALESCE, not LabelService batch | TBox SPARQL uses `COALESCE(?skosLabel, ?rdfsLabel, localname)` directly in query | Gist uses `skos:prefLabel`, model classes use `rdfs:label`. Resolving in-query avoids a second round-trip to LabelService. LabelService.resolve_batch() is still used for ABox instances. | Yes — switch to LabelService if label resolution needs caching |
| D057 | M003/S07 | tech | ABox shows only types with instances > 0 | Filter out classes with zero instances from ABox browser display | Gist has ~96 classes; most have zero instances in a typical SemPKM database. Showing all would overwhelm. D024 says gist is "fully visible" in TBox — ABox filtering by instance count is about data presence, not schema visibility. | Yes — add "show all types" toggle if users request |
| D058 | M003/S07 | tech | Ontology router included in browser router coordinator | `ontology_router` included before `objects_router` in `browser/router.py` | Same reason as D052: objects_router's `:path` parameter is greedy. Ontology routes use `/ontology/` prefix which would be consumed. | No |
| D059 | M003/S08 | tech | User class IRI minting with UUID suffix | `urn:sempkm:user-types:{Slug}-{uuid4hex[:8]}` (e.g. `urn:sempkm:user-types:MyTask-a1b2c3d4`) | Prevents IRI collision when multiple users create classes with the same name. Slug provides readability; UUID provides uniqueness. 8-hex-char prefix gives ~4 billion combinations — sufficient for user-created types. | No |
| D060 | M003/S08 | tech | No EventStore for class/shape creation | Direct INSERT DATA to `urn:sempkm:user-types`, same pattern as gist loading | Class creation is schema modification, not data modification. EventStore is for object-level operations. Consistent with how gist and ontology triples are loaded (D054). | Yes — add EventStore if class version history becomes a requirement |
| D061 | M003/S08 | tech | IconService user-type icon resolution via triplestore triples | Store `sempkm:typeIcon` and `sempkm:typeColor` triples on class IRI in `urn:sempkm:user-types`, with lazy-load cache on IconService | IconService is filesystem-only (manifest.yaml). User types have no filesystem manifest. Storing icon metadata as RDF triples is consistent with the RDF-first architecture and avoids introducing a separate JSON/YAML file for user types. | Yes — refactor if icon loading becomes a performance bottleneck |
| D062 | M003/S08 | tech | Class deletion as DELETE WHERE, create-only scope expanded to include delete | Minimum error recovery: `DELETE WHERE` removes class + shape triples from `urn:sempkm:user-types` | D028 scopes S08 as create-only, but without delete there's no recovery from broken classes. Delete is a single SPARQL operation, low effort, high value. Does not include edit (TYPE-03 scope). | No |
| D063 | M003/S08 | tech | htmx form data serialization: use config-request not before-request | `hx-on::config-request` with explicit `event.detail.parameters` update for dynamic form fields | htmx seals form parameters before `htmx:beforeRequest` fires. Hidden input values updated by JS during `beforeRequest` are not included in the request. `configRequest` fires during parameter collection phase, allowing explicit parameter injection. | No |
| D064 | M003/S08 | tech | Shape delete uses two separate SPARQL UPDATE calls | `_build_delete_shape_sparql()` returns tuple of (bnode cleanup, shape removal) queries | RDF4J rejects `OPTIONAL` inside `DELETE WHERE` (returns 400). Semicolon-separated updates in a single call also fail. Two sequential calls are reliable and equivalent. | No |
| D065 | M003/S09 | tech | Chart.js 4.x via CDN, per-page only | Chart.js 4.4 loaded via `{% block scripts %}` in model_detail.html only | Research recommendation. Battle-tested, ~60KB gzipped, supports sparkline + bar chart. No global loading — only admin model detail page needs it. | Yes — switch to bundled if build pipeline changes |
| D066 | M003/S09 | tech | Lazy chart init on flip transitionend | Charts created in flipCard() transitionend callback, not on page load | Chart.js reads canvas dimensions on init. Canvas inside `display:none` (face-hidden) has 0×0 dimensions → empty charts. Lazy init after flip ensures canvas is visible. | No |
| D067 | M003/S09 | tech | Link distribution buckets: 0, 1-2, 3-5, 6-10, 11+ | Fixed 5-bucket histogram for link count distribution | Covers the typical power-law distribution of knowledge graph connectivity. Simple to compute in SPARQL with CASE/BIND. Readable in a small bar chart. | Yes — adjust buckets if real usage shows different distribution |
| D068 | M003/S09 | tech | Growth trend window: 8 weeks | Query last 8 weeks of object.create events with timestamp filter | Balances sparkline readability (8 data points) with query performance (tight time bound avoids full event scan). | Yes — extend if users want longer history |
| D069 | M003/S10 | tech | Rate-limit e2e tests run last via 99- directory prefix | Rate-limiting spec in `e2e/tests/99-rate-limiting/` | Rate-limit tests exhaust the per-IP endpoint budget (slowapi in-memory), breaking auth fixtures in subsequent tests. Moving to 99- ensures Playwright's alphabetical ordering runs them after all auth-dependent tests. | No |
| D070 | M004/S02 | tech | Two-step class delete confirmation via check endpoint | `GET /ontology/delete-class-check` returns HTML fragment with instance/subclass counts, rendered in modal overlay. Confirm button triggers existing `DELETE /ontology/delete-class`. | `hx-confirm` uses browser-native `window.confirm()` which cannot show dynamic content. Two-step pattern follows existing modal conventions (class creation, property creation, class edit). | No |
| D071 | M004/S02 | tech | `_property_source` returns `'user'` for user-types IRIs | Check for `urn:sempkm:user-types:` prefix before generic `SEMPKM_NS` check | Fixes RBox badge showing "sempkm" instead of "user" for user-created properties. IRI prefix ordering matters since user-types IRIs also start with `urn:sempkm:`. | No |
| D072 | M004/S02 | tech | Property deletion: simple `hx-confirm`, no modal | Properties have no instances and no dependent shapes to warn about. Static `hx-confirm` string is sufficient. | Avoids over-engineering. Class deletion needs dynamic counts; property deletion doesn't. | Yes — add modal if property deletion gains cascade concerns |
| D073 | M004/S03 | tech | Property type immutable on edit | Edit form shows property type (Object/Datatype) as read-only; cannot switch after creation | Switching between ObjectProperty and DatatypeProperty changes IRI semantics and breaks SHACL shapes that reference the property. Safer to require delete + re-create. | No |
| D074 | M004/S03 | tech | Custom section server-rendered in admin_models(), not htmx lazy-load | Extend `admin_models()` to call `list_user_types()` and pass result to template | The `list_user_types()` query hits a single small graph (<100ms). Lazy-load adds unnecessary complexity. | Yes — switch to htmx lazy-load if query becomes slow |
| D075 | M004/S03 | tech | SHACL shapes NOT updated on property edit | When domain/range changes, existing SHACL shapes referencing the property are not auto-updated | SHACL shapes reference properties by IRI via `sh:path`. Domain/range are OWL-level constraints, not SHACL-level. Acceptable because shapes still point to the correct IRI. | Yes — add shape cascade if it causes user confusion |
| D076 | M004/S03 | tech | Edit property form uses `epf-` ID prefix | Edit property form element IDs prefixed `epf-` to avoid collision with create form `cpf-` IDs | Both forms can coexist in the DOM (ontology page has both modals). ID collision would break JS handlers. | No |
| D077 | M005 | process | Pre-commit hook rejects conflict markers | `.githooks/pre-commit` scans staged files for `^<<<<<<< ` / `^=======` / `^>>>>>>> ` and exits non-zero if found | 17 consecutive squash merges (M002/S03–M005/S01) committed conflict markers to main undetected. Docker volume mounts hid the damage by serving clean branch code. Pre-commit hook is the last line of defense. | No |
| D078 | M005 | infra | Migrations volume-mounted in docker-compose.yml | `./backend/migrations:/app/migrations` added to api service volumes | Without this mount, migration file edits required a full `docker compose build api`. Consistent with how `./backend/app` is already mounted. Discovered when conflict-marker fix in `env.py` wasn't picked up by the running container. | No |
| D079 | M005/S02 | tech | Ops log calls in router layer, not service layer | `log_activity()` called from admin router, inference router, and validation queue — not from `ModelService`/`InferenceService` | Router layer already has user context from `require_role()`. Threading user IRIs through service methods would change signatures and create coupling. Consistent with how `EventStore.commit()` receives `performed_by` from routers. | No |
| D080 | M005/S02 | tech | Ops log uses direct SPARQL INSERT DATA, not EventStore | Direct SPARQL INSERT DATA to `urn:sempkm:ops-log` named graph | Ops log records metadata _about_ operations, not data belonging in `urn:sempkm:current`. Same pattern as `QueryService` writes to `urn:sempkm:queries`. EventStore would create per-event graphs and materialize into current — wrong semantics. | No |
| D081 | M005/S02 | tech | Single `urn:sempkm:ops-log` named graph for all entries | All ops log activities in one graph, not per-entry graphs | Matches `urn:sempkm:queries` pattern from S01. Log entries are simple metadata — no data isolation needed. Single graph simplifies querying. Low volume (10-50 entries/day max). | No |
| D082 | M005/S02 | tech | Ops log calls are fire-and-forget with try/except | All `log_activity()` calls wrapped in try/except — failures logged at WARNING but never block primary operation | Ops log is observability tooling. A triplestore write failure during model install must not prevent the install from succeeding. Same philosophy as webhook dispatch. | No |
| D083 | M005/S02 | tech | htmx target-aware block rendering for ops log route | Check `HX-Target` header to decide which Jinja2 block to return: `app-content` → `content` block (sidebar nav), anything else → `table_rows` block (filter/pagination) | Single endpoint serves three use cases (full page, sidebar nav, filter/pagination). Without target detection, returning `content` block for filter swaps caused duplicate heading/controls inside `#ops-log-table`. | No |
| D084 | M005/S03 | tech | Hierarchical tag tree: fetch flat, nest in Python, lazy-load children | Existing SPARQL fetches all tags flat; `build_tag_tree()` pure function groups into hierarchy; `tag_children` endpoint with `prefix` param for lazy sub-folder loading | Reuses existing SPARQL (no new string functions for roots), uses `STRSTARTS` for children (well-supported), pure function is unit-testable without Docker. Follows proven htmx lazy-load pattern (D035, D036, D040, D044). | No |
| D085 | M005/S03 | tech | `tag_children` endpoint extended with `prefix` parameter, not new endpoint | Add `prefix` query param to existing `GET /explorer/tag-children` rather than creating a separate `/explorer/tag-prefix-children` route | Existing endpoint already handles tag expansion. Prefix queries are the same domain — sub-folder expansion is conceptually "children of a tag prefix". `tag` param = exact match objects, `prefix` param = sub-folder tree. Avoids endpoint proliferation while staying clean. | Yes — split if logic diverges significantly |
| D086 | M005/S03 | tech | `build_tag_tree()` as separate module, not inline in workspace.py | New `backend/app/browser/tag_tree.py` module for pure function | workspace.py is already 1063 lines. Pure function has no dependencies on FastAPI/triplestore — separate module keeps it testable and workspace.py from growing. | No |
| D087 | M005/S04 | tech | Tag autocomplete q parameter via htmx:configRequest, not hx-vals | `htmx:configRequest` event listener rewrites params to `{q: triggerEl.value}` for tag-suggestions requests | `hx-vals='js:{"q": this.value}'` did not reliably pass the `q` query parameter on htmx GET requests — requests were sent without `q`, returning all tags unfiltered. `htmx:configRequest` fires during parameter collection and allows explicit injection. | No |
| D088 | M005/S05 | tech | Refresh clears 4 artifact graphs inline, not via `clear_model_graphs()` | Build 4 individual `CLEAR SILENT GRAPH` statements for ontology/shapes/views/rules, not reuse `clear_model_graphs()` | `clear_model_graphs()` clears all 5 graphs including seed. Refresh must NOT touch seed (user data). Adding an `exclude` parameter would change the function signature for one caller. Inline CLEAR is simpler and explicit. | No |
| D089 | M005/S05 | tech | Refresh uses hx-confirm, not two-step modal | Simple `hx-confirm` dialog on Refresh button | Refresh is non-destructive (transactional — rollback on failure). Lower risk than delete. Two-step modal (D070) is for operations with permanent data loss potential. | Yes — add modal if refresh gains destructive side effects |
| D090 | M005/S06 | arch | PROV-O Starting Point terms only — no Qualified or Extended | Use `prov:Activity`, `prov:startedAtTime`, `prov:wasAssociatedWith`, etc. Do not use `prov:qualifiedAssociation`, `prov:hadRole`, or Extended terms | Qualified terms add blank nodes and multi-triple patterns for marginal information gain. Starting Point covers 90%+ of SemPKM's provenance needs. Ops log already validates this approach. | Yes — reconsider if provenance queries need role-on-association detail |
| D091 | M005/S06 | arch | Existing event graphs retain sempkm: predicates forever — no retroactive migration | Immutable event named graphs keep their original `sempkm:` predicates. Alignment applies only to newly written data via dual-write. | Event-sourcing requires immutable events. Federation sync transmits events as serialized patches — changing predicates would desynchronize instances. Dual-write + COALESCE queries handle mixed-era data without rewriting history. | No |
| D092 | M005/S06 | tech | COALESCE pattern for dual-predicate SPARQL queries (over UNION or VALUES) | Use `OPTIONAL { ?e sempkm:timestamp ?old } OPTIONAL { ?e prov:startedAtTime ?new } BIND(COALESCE(?new, ?old) AS ?ts)` | VALUES creates cross-products on dual-write events. UNION requires deduplication. COALESCE with OPTIONAL is clean, single-row-per-event, and performs well on most triplestores. | Yes — revisit if triplestore performance shows issues with OPTIONAL chains |
| D093 | M005/S07 | arch | Generic views registered in-memory at startup, not as RDF | Register 3 generic ViewSpecs (Table/Cards/Graph) in ViewSpecService at startup, not in a system named graph | Avoids a "system views" named graph. Generic views are code-defined behavior, not data-defined. Simpler lifecycle — no migration needed for view definition changes. | Yes — move to RDF if user-customizable generic views become a requirement |
| D094 | M005/S07 | arch | SHACL shapes via ShapesService as column discovery source | Use ShapesService._extract_node_shape() → PropertyShape.path/name/order for dynamic table columns | Already extracts sh:path, sh:name, sh:order — everything needed for dynamic columns. Fallback to default column set (label, type, created, modified) for types with ≤2 properties. | No |
| D095 | M005/S07 | arch | Type filter pills for cross-type generic views | Pills (not dropdown, not carousel) for type selection in generic views | Carousel assumes single target_class. Dropdown hides available types. Pills show all types at a glance with active state. Consistent with tag pill UX pattern. | Yes — reconsider if type count exceeds ~15 |
| D096 | M005/S07 | arch | sempkm:scopeQuery for view-to-query scope binding | Distinct from sempkm:fromQuery: scopeQuery = runtime filter, fromQuery = provenance | Extends existing promoted view pattern. scopeQuery injects VALUES clause or subquery to limit view results to query output IRIs. Clean separation of concerns. | No |
| D097 | M005/S07 | process | 3-phase views migration, additive-first | Phase 1 (generic views alongside existing tree), Phase 2 (consolidate explorer), Phase 3 (cleanup). Phase 1 is milestone deliverable. | No big-bang. Phase 1 is strictly additive — zero risk to existing views. Phase 2 changes navigation only. Phase 3 is dead code removal. | No |
| D098 | M005/S08 | tech | Direct SPARQL via SyncTriplestoreClient for VFS saved query resolution | `SyncTriplestoreClient` queries `urn:sempkm:queries` graph directly, not async `QueryService` | WebDAV runs in sync WSGI context. `QueryService` is async (uses `httpx.AsyncClient`). Calling async from sync would require `asyncio.run()` or thread bridging — fragile in production. `SyncTriplestoreClient` already used by VFS provider code. | No |
| D099 | M005/S08 | arch | VFS adopts `sempkm:scopeQuery` with full IRI (aligning with Views Rethink) | Replace bare UUID `sempkm:savedQueryId` with `sempkm:scopeQuery` predicate pointing to full `urn:sempkm:query:{uuid}` IRI | Matches Views Rethink D096 pattern. Full IRI enables SPARQL joins without string manipulation. One predicate used by both views and VFS for scope binding. Migration: one SPARQL UPDATE renames predicate + wraps values. | Yes — if model query IRIs need different treatment |
| D100 | M005/S08 | arch | Chain model (Option A) for composable VFS strategies, max depth 3 | Ordered list of strategies, each level nests inside the previous. Max 3 levels enforced. | Chain is simpler than tree or graph composition. 3 levels covers all practical use cases (e.g. by-tag → by-type → flat). Provider path dispatch needs extension from 4 to 6 segments. | No |
| D101 | M005/S08 | tech | type_filter AND saved_query compose via AND | Both `sempkm:typeFilter` VALUES clause and saved query scope filter applied simultaneously | AND semantics match user mental model: "show me Notes (type) from this query (scope)". OR would produce confusing results. Implementation: both clauses injected into the same WHERE block. | No |
| D102 | M005/S08 | tech | Defer auto-refresh for VFS, use existing 30s TTLCache | No file-system watcher or push invalidation. Rely on existing `TTLCache(maxsize=64, ttl=30)` in VFS provider. | Auto-refresh adds complexity (filesystem watchers, WebSocket push) for marginal benefit. 30s TTL means changes appear within half a minute. Revisit when write-support lands. | Yes — reconsider with write-support milestone |
