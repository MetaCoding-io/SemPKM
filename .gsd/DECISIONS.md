# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D283 | M030/S03/T04 | UX | Whether lint violations should be dismissable in the UI | No — violations are structural issues that must be resolved; only warnings and infos get dismiss buttons | Violations indicate real SHACL conformance failures (e.g., missing required properties, wrong datatypes). Allowing users to dismiss them would hide actionable errors. Warnings and infos are advisory data quality signals where user judgment determines relevance — appropriate for dismiss/suppress. | Yes | agent |
| D263 | M028 planning | arch | LLM proxy auth strategy for browser extension — new /api/llm/stream endpoint with dual-auth | Create new POST /api/llm/stream endpoint using get_current_user_or_api (dual-auth). Leave existing /browser/settings/llm/chat/stream unchanged for htmx frontend. | Existing LLM proxy uses get_current_user (cookie-only). Modifying it risks breaking the htmx Settings page. Creating a parallel endpoint with get_current_user_or_api is ~20 lines copying the SSE pattern with the auth swap. Same approach used for POST /api/commands (D165). | No | agent |
| D264 | M028 planning | arch | Progressive loading strategy for AI Insights in extension sidebar | Sequential fetch calls with incremental DOM rendering — no SSE from extension to backend for progressive loading | SSE adds complexity (EventSource, reconnection, error handling) for marginal benefit. The extension makes 4 sequential API calls (detect-claims → match-claims → suggest-relationships → summarize) and renders each section as it completes. This achieves the same UX (claims first, then matches, then suggestions, then summary) with simpler implementation. Each call is independent — failure of one doesn't block others. | Yes — switch to SSE if latency proves unacceptable | agent |
| D265 | M028 planning | scope | M028 requirement IDs use EXT- prefix continuing from Phase 2 | EXT-22 through EXT-33 for all M028 requirements, continuing from EXT-21 (last M015 requirement) | Browser extension requirements form a continuous sequence across Phase 1 (EXT-01–EXT-13), Phase 2 (EXT-14–EXT-21), and Phase 3 (EXT-22–EXT-33). Continuing the sequence maintains traceability across phases. | No | agent |
| D266 | M028 planning | arch | Claim-to-graph matching uses FTS keyword search, not embeddings | FTS keyword search via LuceneSail with score ranking, cap at 5 matches per claim. No embedding-based semantic search. | CONTEXT.md explicitly scopes out embedding-based semantic search. LuceneSail FTS is already operational (v2.2) and the context-query endpoint already uses it. Ranking by FTS relevance score and capping at 5 matches per claim reduces false positives. Can upgrade to embeddings in a future milestone without API changes. | Yes — add embedding search when pgvector is adopted | agent |
| D267 | M029 planning | tech | Build tool choice — esbuild over Vite | esbuild for all JS/CSS bundling, minification, and content-hashing. No Vite. | For a vanilla JS + htmx project with no ES module imports, esbuild's speed (~10ms builds) and simplicity outweigh Vite's HMR/plugin ecosystem. Vite targets React/Vue SPAs; SemPKM is server-rendered with htmx. esbuild handles concatenation, minification, and content-hashing natively. Install via npm in multi-stage Docker build. | No | agent |
| D268 | M029 planning | tech | Vendoring strategy — npm + esbuild vendor bundle | Create frontend/package.json with all 18 CDN deps as npm packages. esbuild bundles them into vendor.min.js (and vendor.min.css where applicable). Eliminates CDN dependency completely. | npm is the standard JS package manager, version-pinning is built-in, and esbuild can bundle from node_modules. No custom download scripts or manual file management. | No | agent |
| D269 | M029 planning | tech | Compression strategy — nginx gzip + pre-compressed .gz files, no brotli for v1 | Build step generates .gz files alongside originals. nginx gzip_static on serves pre-compressed when available, gzip on as fallback for dynamic responses. Brotli deferred. | nginx:stable-alpine doesn't include the brotli module without a custom build. gzip provides ~70-80% compression. Adding brotli would require a custom nginx image increasing maintenance burden for marginal benefit (~5% smaller). | Yes — add brotli when nginx:stable-alpine includes the module | agent |
| D270 | M029 planning | arch | Content-hashed filename resolution — JSON manifest + Jinja2 filter | esbuild's metafile output transformed to manifest.json mapping original names to hashed names. Jinja2 template filter `asset_url` resolves via manifest. In dev mode (no manifest), filter returns original path. | Django's staticfiles manifest.json pattern is proven. Jinja2 filter is ~20 lines and integrates cleanly with existing template infrastructure. Dev mode fallback preserves volume-mount hot-reload workflow. | No | agent |
| D271 | M029 planning | arch | CSS splitting strategy — template block inheritance, not build tool | base.html loads only shared CSS (theme.css). workspace.html extends with workspace CSS stack. Admin templates load only admin CSS. This is a template-level change. | CSS splitting by route doesn't require sophisticated build tooling — it's a matter of which <link> tags appear in which template. Jinja2 block inheritance already supports this pattern. Build tool handles minification; templates handle route assignment. | No | agent |
| D272 | M029 planning | arch | Yasgui and Chart.js stay as separate lazy-loaded bundles, not in vendor bundle | Yasgui (loaded on SPARQL tab click) and Chart.js (loaded on admin model detail page only per D065) remain page-specific bundles, vendored locally but not in the global vendor bundle. | Yasgui bundles CodeMirror + YASQE + YASR (~800KB). Chart.js is admin-only (~60KB). Including them in the vendor bundle would bloat every page load for features used on 1-2 pages. Lazy loading preserves current behavior. | No | agent |
| D273 | M029 planning | scope | PERF requirement IDs for M029 | Requirements numbered PERF-02 through PERF-10 (PERF-01 exists from M002). Covers local vendoring, build pipeline, compression, caching, CSS splitting, Lighthouse target, backend profiling, backend cache headers, QUIC/HTTP/3 decision. | PERF prefix established by PERF-01 in M002. Continuing the sequence maintains traceability. | No | agent |
| D274 | M029 planning | scope | QUIC/HTTP/3 deferred — document decision only | Research and document decision. nginx:stable-alpine lacks HTTP/3 support. Self-hosted single-user over localhost gets minimal benefit from QUIC multiplexing. HTTP/2 over h2c has limited browser support. | Implementing HTTP/3 requires a custom nginx build or switching to Caddy. The cost exceeds the benefit for the self-hosted Docker deployment model. Caddy exists for the demo instance (D246) but switching the main stack is a separate decision. | Yes — revisit when nginx:stable-alpine adds HTTP/3 or Caddy migration is separately motivated | agent |
| D275 | M029 planning | arch | Dev vs production asset path divergence handled by Jinja2 filter mode | Dev serves /js/workspace.js (raw via volume mount). Production serves /assets/workspace-a1b2c3d4.min.js (hashed). The asset_url Jinja2 filter checks for manifest.json existence — if present, resolves via manifest; if absent, returns original path. | Single filter handles both modes. No environment variable needed — manifest file presence is the signal. Docker build produces the manifest; dev mode volume mounts don't include it. | No | agent |
| D276 | M029/S01/T04 | architecture | Manifest sharing between frontend and API containers in Docker | Named Docker volume (frontend_assets) at /srv/built-assets/ populated by entrypoint script, mounted read-only in API container at /app/frontend_assets/ | The frontend bind mount (./frontend/static:/usr/share/nginx/html:ro) is read-only, so a sub-volume at /assets/ fails. A separate named volume at /srv/built-assets/ avoids the conflict. The entrypoint script copies built assets from the image on every container start, ensuring rebuilds take effect without docker compose down -v. nginx serves /assets/ via alias directive to the shared volume. | Yes | agent |
| D215 | M019 planning | arch | Todoist sync authentication — PAT-only for v1, no OAuth | Personal API Token only. No OAuth 2.0 code exchange flow. | Todoist PAT is simpler and matches the individual user focus stated in CONTEXT. GitHub sync (M017) uses the same PAT-only approach. OAuth adds client_id/secret management and callback complexity for the same functional outcome. The research doc notes OAuth as secondary — deferring it keeps M019 lean. Can add OAuth later if multi-user production deployments need it. | Yes — add Todoist OAuth when multi-user production deployments need it | agent |
| D216 | M019 planning | scope | Todoist sync requirement ID prefix | TD- prefix for all Todoist sync requirements (TD-01 through TD-08) | Follows D209 convention — provider-specific prefix avoids collision with existing SYNC- (Linear), GH- (GitHub), GCAL- (Google Calendar) sequences. | No | agent |
| D217 | M020 planning | arch | Outlook sync authentication — full OAuth 2.0 with /common/ tenant | Microsoft Identity Platform OAuth 2.0 with `/common/` endpoint for multi-tenant support (personal + work accounts). Client ID/secret from Azure AD app registration. | Microsoft Graph API requires OAuth for user-data access (no API key path). `/common/` supports both personal Microsoft accounts and organizational accounts, maximizing compatibility. Follows M018 Google OAuth pattern. | No | agent |
| D218 | M020 planning | arch | Polling-only sync for Outlook v1, no webhook subscriptions | Delta query-based incremental polling via `@odata.deltaLink`. No webhook subscription management. | Same constraint as D200/D211 — App Platform doesn't expose external webhook routes. Delta queries are highly efficient (return only changes since last token). Webhook subscriptions expire after ~3 days requiring auto-renewal — complexity deferred to v2. | Yes — add webhook subscriptions when platform supports external webhook routing | agent |
| D219 | M020 planning | arch | Hand-rolled recurrence pattern→RRULE converter as pure function | Manual conversion per INTEGRATION-DOMAIN-MAPPING.md tables. No external library. 18 combinations covered by exhaustive unit tests. | No library maps Outlook's structured pattern object to RFC 5545 RRULE. The conversion is a deterministic pure function with well-defined mapping tables. Unit test coverage (6 patterns × 3 ranges + edge cases) is the safety net. | No | agent |
| D220 | M020 planning | tech | markdownify for HTML→Markdown body conversion | Use `markdownify` Python library for Outlook `body.content` when `body.contentType` is `html`. Plain text bodies pass through unchanged. | Outlook body content defaults to HTML (unlike Google's plain text). markdownify is lightweight with no heavy C dependencies. Fallback: strip HTML tags if markdownify fails to install. | Yes — switch to alternative if markdownify has Docker install issues | agent |
| D221 | M020 planning | scope | Outlook sync requirement IDs use OL- prefix | OL- prefix for all Outlook sync requirements (OL-01 through OL-09) | Follows D209/D214/D216 convention — provider-specific prefix avoids collision. | No | agent |
| D222 | M020 planning | scope | RSVP-only push scope for Outlook v1 | Push sync limited to responseStatus changes, same as M018 Google Calendar (D213). Title, description, time edits NOT pushed to Outlook. | Consistent with Google Calendar push scope. Narrow push reduces complexity and risk. Full event editing push requires field-level conflict resolution exceeding v1 scope. | Yes — expand push scope if users request bidirectional event editing | agent |
| D223 | M021 planning | arch | CalDAV sync authentication — HTTP Basic primary, optional OAuth 2.0 | HTTP Basic auth (URL + username + password) as primary method. OAuth 2.0 as secondary path for Google CalDAV and enterprise setups. | CalDAV's primary audience is self-hosters (Fastmail, Nextcloud, Synology, Radicale) who all use HTTP Basic auth. No complex OAuth redirect dance needed for the primary use case. Simpler than M018/M020 OAuth-only approaches. OAuth can layer on later without breaking the Basic auth path. | Yes — add OAuth 2.0 flow when Google CalDAV or enterprise users need it | agent |
| D224 | M021 planning | tech | CalDAV protocol implementation — hand-crafted XML over httpx, no caldav library | Hand-crafted XML with stdlib xml.etree.ElementTree + httpx via SDK HttpClient. No caldav library. | The caldav Python library (PyPI) has inconsistent API, limited maintenance, and brings its own HTTP client conflicting with SDK's HttpClient. CalDAV XML requests are small, well-defined documents. stdlib ET handles namespace-aware XML cleanly. Full control over request/response cycle is critical for debugging server compatibility issues. | No | agent |
| D225 | M021 planning | arch | CalDAV network permission — wildcard for user-configured server URLs | Manifest uses network: ["*"] wildcard. fnmatch('any.domain', '*') returns True — verified during planning. | CalDAV servers are user-configured (any domain). Unlike Google/Outlook which target fixed API domains, CalDAV must reach arbitrary hosts. SDK's HttpClient uses fnmatch for domain enforcement — verified that '*' matches any domain string. This is the first app to use wildcard network permissions. | No | agent |
| D226 | M021 planning | scope | CalDAV sync requirement ID prefix | CalDAV sync requirement IDs use CDAV- prefix (CDAV-01 through CDAV-10) | Follows D209/D214/D216/D221 convention — provider-specific prefix avoids collision with SYNC- (Linear), GH- (GitHub), GCAL- (Google), TD- (Todoist), OL- (Outlook) sequences. | No | agent |
| D227 | M022 planning | arch | Asana sync authentication — OAuth 2.0 primary with PAT fallback | Support both Asana OAuth 2.0 (app.asana.com OAuth endpoints, 1h access tokens, long-lived refresh) and Personal Access Token. OAuth for production, PAT for local dev. | Matches the dual-auth pattern from M016 (D199). OAuth is the standard Asana auth path; PAT simplifies local dev without callback routing. Asana OAuth has no explicit scopes — all permissions are implicit. | No | agent |
| D228 | M022 planning | arch | Configurable field mapping as explicit setup step before sync | User must configure status/priority field mapping after project selection and before first sync. Configuration stored as JSON in StateClient. Three status modes: completed_only, custom_field, section. | Asana has no native status or priority fields — status comes from custom enum fields or section membership, priority from custom enum fields. This is fundamentally different from Linear/GitHub/Todoist where mappings are fixed. The "configure before sync" pattern is new to the platform and will be reused by Monday.com and other custom-field-heavy providers (M024+). | No | agent |
| D229 | M022 planning | arch | Subtask recursion bounded at 5 levels with per-level API calls | Each subtask level requires GET /tasks/{gid}/subtasks with its own opt_fields. Max depth 5 per CONTEXT. Depth counter prevents infinite recursion. Rate limit backoff on 429 Retry-After. | Subtask API returns direct children only — no deep query. 5 levels covers all practical use. Bounding prevents cost-based rate limit exhaustion (each level is N API calls where N is parent count). | No | agent |
| D230 | M022 planning | scope | Asana sync requirement IDs use ASANA- prefix | ASANA- prefix for all Asana sync requirements (ASANA-01 through ASANA-11) | Follows D209/D214/D216/D221/D226 convention — provider-specific prefix avoids collision with existing sync app requirement sequences. | No | agent |
| D231 | M022 planning | arch | Section-based status push via addTask API, not field PATCH | When status_source is "section", push sync uses POST /sections/{gid}/addTask instead of PATCH on a custom field. Requires reverse-mapping bpkm:taskStatus to section GID via stored mapping config. | Asana sections are organizational containers, not field values. Moving a task between sections requires a dedicated API call. This is a different push pattern than custom-field PATCH used by prior sync apps. Both paths coexist based on the user's status_source configuration. | No | agent |
| D232 | M022/S01 | arch | Asana REST client uses _raw_request/_request two-layer pattern for data envelope APIs | Split _raw_request (returns full JSON body with next_page) from _request (unwraps data envelope). _paginated_get uses _raw_request to access offset cursor without duplicating error handling. | Asana's API wraps data in {"data": [...], "next_page": {"offset": "..."}} — pagination metadata is a sibling of data, not nested inside it. A single-layer _request that unwraps data loses the next_page cursor. The two-layer split keeps error handling (401, 429, 5xx) in one place while allowing pagination to read the full response structure. Applicable to any API with response wrappers containing metadata alongside the data array. | Yes | agent |
| D171 | M009/S01/T03 | arch | AppManager restart count tracking strategy | Restart counts tracked in-memory (_restart_counts dict) and synced to DB; reset to 0 on manual start/restart but preserved across crash-recovery restarts via _start_for_recovery() | Avoids extra DB round-trips in the crash recovery hot path. DB sync happens after each recovery attempt for persistence. Memory-first is safe because a single AppManager instance owns each app's lifecycle — no concurrent writers. | No | agent |
| D172 | M009/S01 | arch | How platform shutdown handles app DB status for auto-restart | shutdown_all() preserves DB status='running' so auto_start() picks apps up on next boot. Only explicit user stop() sets status='stopped'. | Platform restarts (Docker restart, deploy) should not lose the user's intended app state. If an app was running before shutdown, it should auto-start on boot. This is the same pattern as systemd 'enabled' services — the desired state is declarative, not just current state. | No | agent |
| D173 | M009/S02 | arch | SDK token validation mechanism (platform↔app authentication) | Shared-secret string comparison — platform passes token at startup, SDK validates incoming requests by comparing X-SemPKM-App-Token header to stored token. Not JWT decode on the app side. | Simpler model where platform and app share the same token string. Apps don't need PyJWT to decode — just string equality. Platform still generates JWT (for structured claims on its own side, including grace period renewal), but the app only ever compares the opaque string. Avoids SDK needing secret key access or clock sync for JWT expiry checks. | Yes — could switch to JWT decode if apps need to inspect claims (e.g. permissions in S05) | agent |
| D174 | M009/S02 | tech | AppProxy response forwarding strategy | Non-streaming proxy (v1) — uses httpx client.request() instead of stream=True for response forwarding | Simpler implementation. App fragment responses are small HTML chunks (KB range). Streaming adds complexity (async generator, chunked transfer encoding, error handling mid-stream) for no measurable benefit at current scale. Can upgrade to streaming later if large responses (file downloads, SSE) become a requirement. | Yes — add streaming when large responses or SSE proxying is needed | agent |
| D175 | M009/S03 | arch | Admin router inclusion order relative to app proxy router | app_admin_router included immediately before app_proxy_router in main.py to prevent the proxy's catch-all {path:path} pattern from consuming /admin/apps/* URLs | app_proxy_router uses a greedy path parameter that matches any sub-path under /app/. If it were registered first, requests to /admin/apps/ would be intercepted by the proxy. FastAPI matches routes in registration order, so placing the admin router first ensures admin paths are resolved before the catch-all. | No | agent |
| D176 | M009/S03 | tech | How to serve per-app static assets via nginx | nginx alias directive with /app/data/apps-static/{app_id}/ path, with trailing slashes on both location and alias. Static assets copied from app source to shared data volume during install. | App static files live in app source directories under the API container. nginx serves them via the shared sempkm_data volume. The alias directive (not root) handles path remapping correctly. Trailing slashes on both location and alias are critical for nginx alias to work without path traversal issues. Cache-Control: public, immutable headers set since app assets are versioned by install. | No | agent |
| D177 | M009/S05 | arch | SDK permission enforcement model — default-deny with manifest-driven allowlists | Default-deny on all SDK clients: CommandClient rejects unpermitted command types + enforces IRI prefix, GraphClient gates on sparql_read boolean, HttpClient uses fnmatch domain globs. Permissions threaded from manifest through AppContext to each client constructor. | Without enforcement, apps can execute any command type, create IRIs outside their prefix, query any graph, and call any external URL. Default-deny is the safe baseline — apps must explicitly declare permissions. IRI prefix scoped to urn:sempkm:app:{appId}: to namespace all app-created data. fnmatch for domain matching supports both exact domains and *.example.com glob patterns. Permission checks are stateless and synchronous — raises PermissionError with self-documenting messages including the offending value and allowed list. | No | agent |
| D001 | M002 | scope | Items from CONCERNS.md already fixed | Close: datetime.now timezone, label cache invalidation, CORS conditional, IRI validation, debug SPARQL owner-only, EventStore DI, Alembic startup, SMTP, session cleanup, ViewSpec cache, cookie_secure | Verified in codebase — fixed in v2.1 through v2.6 | No | agent |
| D002 | M002 | scope | MCP server for AI agent access | Defer to future milestone | Feature work, not polish — doesn't belong in hardening milestone | Yes — when hardening complete | agent |
| D003 | M002 | scope | Admin model detail stats and charts | Defer to future milestone | Cosmetic admin enhancements, low priority | Yes | agent |
| D004 | M002 | scope | Browser router refactor approach | Split into domain sub-routers, zero behavior change | Preserves all URL paths and htmx wiring; E2E tests are the safety net | No | agent |
| D005 | M002 | scope | Federation testing approach | Dual-instance docker-compose for E2E testing | Single instance can't test federation; permanent dev setup not needed | Yes — if federation becomes daily workflow | agent |
| D006 | M002 | scope | Obsidian import testing approach | Manual user-driven import of real Ideaverse vault, fix bugs found | Automated testing of 905-note vault import is fragile; user judgment needed for mapping quality | No | agent |
| D007 | M002/S01 | tech | Rate limiting library | slowapi with in-memory backend, per-route decorators (no global default) | Battle-tested (wraps flask-limiter), no Redis needed for single-instance, supports `get_remote_address` for real client IP behind nginx | Yes — add Redis backend if scaling to multiple workers | agent |
| D008 | M002/S01 | tech | SPARQL regex escaping placement | Standalone `backend/app/sparql/utils.py` module | Must be importable without pulling in views service; S03 will unit-test it directly | No | agent |
| D009 | M002/S02 | tech | SPARQL string-stripping helper placement | Private `_strip_sparql_strings()` in `sparql/client.py`, not in `sparql/utils.py` | This is keyword-detection preprocessing specific to `scope_to_current_graph` and `check_member_query_safety`, not general SPARQL escaping. Keeping it module-private avoids polluting the shared utils API. | No | agent |
| D010 | M002/S02 | tech | View spec source_model query strategy | `GRAPH ?g { ... }` with VALUES clause constraining graph IRIs | Matches existing codebase pattern (events/query.py), avoids scanning unrelated graphs, captures per-spec provenance. Reverse map from graph IRI to model ID uses the same `urn:sempkm:model:{id}:views` convention as models/registry.py. | No | agent |
| D011 | M002/S03 | tech | Backend test strategy | Pure-function unit tests via `.venv/bin/pytest`, no Docker/triplestore/server | All S03 targets are sync pure functions or functions with injectable settings. No integration test infra needed. Tests run in <5s locally. | No | agent |
| D012 | M002/S03 | tech | Token expiry testing approach | Use `max_age_seconds=0` parameter instead of `time.sleep()` | Avoids slow/flaky sleep-based tests. Tests the same verify API with immediate expiry. | No | agent |
| D013 | M002/S03 | tech | `_serializer` singleton isolation | Autouse pytest fixture resets `tokens._serializer = None` after each test | Prevents stale serializer from masking key-resolution bugs across tests. | No | agent |
| D014 | M002/S04 | tech | Sub-router prefix strategy | Sub-routers get NO prefix; routes keep full sub-paths (e.g. `@router.get("/settings")`). Parent coordinator provides `/browser/` prefix. | Avoids prefix stacking bugs (`/browser/browser/settings`). Matches how existing codebase works. | No | agent |
| D015 | M002/S04 | tech | `_validate_iri` test import path | Update `test_iri_validation.py` to import from `app.browser._helpers` instead of re-exporting from `router.py` | Cleaner — no re-export indirection. Single source of truth in `_helpers.py`. | No | agent |
| D016 | M002/S04 | tech | Sub-router include order | Register sub-routers in same order as routes appeared in original monolith file | Preserves FastAPI route matching order; prevents path-parameter shadowing regressions. | No | agent |
| D017 | M002/S06 | tech | Federation E2E invitation strategy | Hybrid: simulate invitation via direct SPARQL insertion on instance B, bypass HTTP Signatures/WebFinger for invitation delivery | Full cross-instance signing requires TLS (WebFinger hardcodes HTTPS) and key management in tests. Hybrid still tests accept → sync with real API calls. Upgrade to real signing if federation becomes a daily workflow. | Yes — add real signing test when WebFinger supports HTTP scheme | agent |
| D018 | M002/S06 | tech | Federation test ports | Instance A: 3911/8911, Instance B: 3912/8912 | Avoids conflict with dev (3000/8001) and existing E2E test (3901/8901) stacks | No | agent |
| D019 | M002/S06 | tech | Federation test APP_BASE_URL | Use Docker service name URLs (`http://frontend-a:80`, `http://frontend-b:80`) | WebID URLs must be fetchable by the other instance. Docker service names resolve on the shared network. External localhost URLs wouldn't work for cross-container requests. | No | agent |
| D020 | M003 | arch | Explorer mode switching mechanism | Dropdown replaces OBJECTS section content in-place via htmx | User confirmed: single section with mode dropdown, not multiple collapsible sections per mode | No | agent |
| D021 | M003 | arch | Favorites storage layer | SQL (user_favorites table) not RDF | Favorites are user preferences, not knowledge graph data. Same layer as auth/sessions. | Yes — if favorites need graph queries | agent |
| D022 | M003 | arch | Comments storage layer | RDF via EventStore operations | Comments are knowledge about objects — they belong in the knowledge graph. Event-sourced for auditability. | No | agent |
| D023 | M003 | arch | Comment threading model | Threaded with reply-to nesting | User chose threaded over flat. Modeled via sempkm:replyTo predicate in RDF. | No | agent |
| D024 | M003 | arch | Gist visibility in ontology viewer | Fully visible — gist classes on equal footing with model classes | User chose fully visible over grayed/hidden. Power users can explore the full hierarchy. | No | agent |
| D025 | M003 | arch | Gist version | Pin to 14.0.0, update deliberately | Avoids breaking changes from upstream. CC BY 4.0 license. | Yes — when gist 15.x releases | agent |
| D026 | M003 | arch | Gist named graph IRI | `urn:sempkm:ontology:gist` | Namespaced under sempkm convention. Separate from model graphs. | No | agent |
| D027 | M003 | arch | User-created types graph | `urn:sempkm:user-types` named graph | Separate from model graphs. Discoverable by ShapesService. | No | agent |
| D028 | M003 | scope | M003 class creation scope | Name, icon, parent class, basic properties. No advanced SHACL editor. | Full shape editing deferred to later milestone (TYPE-03). | Yes — when TYPE-03 planned | agent |
| D029 | M003 | arch | Hierarchy nesting depth | Arbitrary depth, no hard cap | Most real usage will be 2-3 levels. Lazy expansion prevents performance issues. | No | agent |
| D030 | M003 | arch | VFS strategy reuse for explorer | Adapt VFS strategies.py SPARQL builders for htmx tree rendering | Same queries, different output format. Avoids duplicating SPARQL logic. | No | agent |
| D031 | M003/S01 | tech | Explorer mode endpoint pattern | Single `GET /browser/explorer/tree?mode={mode}` with `hx-include="this"` on `<select name="mode">` | htmx-native: select value auto-included in request. Simpler than dynamic URL building. Registry pattern allows downstream slices to register handlers. | No | agent |
| D032 | M003/S01 | tech | Explorer mode persistence | localStorage key `sempkm_explorer_mode` | Lightweight, per-browser, no server state needed for UI preference. Read on workspace init, trigger htmx load if non-default. | Yes — if multi-device sync needed | agent |
| D033 | M003/S01 | tech | Existing `/browser/nav-tree` endpoint kept for backwards compat | Delegates internally to by-type handler | Prevents breakage during incremental rollout. Will be deprecated once all callers use mode-aware endpoint. | Yes — remove when safe | agent |
| D034 | M003/S01 | tech | Selection clearing strategy on mode switch | `clearSelection()` + `lastClickedLeaf = null` on dropdown change event | Prevents stale selection state referencing IRIs not visible in the new tree. Proactive clear before htmx swap. | No | agent |
| D035 | M003/S02 | tech | Hierarchy node rendering: all nodes as expandable `.tree-node` | Every hierarchy node rendered as `.tree-node` with lazy `hx-trigger="click once"`, plus label `onclick` for `handleTreeLeafClick` | Any object could have children — rendering all as expandable avoids a sub-query to detect children upfront. If children endpoint returns empty, the expanded area shows "No children". Simpler than ASK queries per node. | Yes — add child-count sub-query if UX feedback demands toggle-only-when-children | agent |
| D036 | M003/S02 | tech | Children endpoint as separate route from `tree_children` | New `GET /browser/explorer/children?parent={iri}` rather than reusing `/browser/tree/{type_iri}` | `tree_children` queries by `rdf:type`, hierarchy queries by `dcterms:isPartOf` — fundamentally different SPARQL. Separate endpoint avoids overloading the type-children route with mode-conditional logic. | No | agent |
| D037 | M003/S03 | tech | Mount mode value encoding in explorer dropdown | `<option value="mount:{uuid}">` with server-side prefix parsing | Single `<select>` value carries both mode type and mount identity. Avoids hidden inputs or second query param. Parser strips `mount:` prefix to extract UUID. | No | agent |
| D038 | M003/S03 | tech | Mount handler as prefix match, not EXPLORER_MODES entry | `explorer_tree` checks `mode.startswith("mount:")` before registry lookup | Avoids polluting the static EXPLORER_MODES dict with per-mount entries. Single handler function handles all mounts via mount_id dispatch. | No | agent |
| D039 | M003/S03 | tech | Dynamic mount dropdown injection timing | Fetch mounts after `initExplorerMode()`, then re-run mode restore for `mount:` prefixed stored modes | Avoids blocking initial page load. Built-in modes work immediately; mount options appear asynchronously. Re-check handles the case where stored mode is a mount that wasn't in the DOM during first restore. | No | agent |
| D040 | M003/S03 | tech | Separate mount-children endpoint from explorer/children | New `GET /browser/explorer/mount-children?mount_id={id}&folder={folder}` | Mount folder expansion dispatches by VFS strategy (5 strategies × multiple depth levels), fundamentally different from hierarchy `dcterms:isPartOf` queries. Separate endpoint keeps dispatch clean. | No | agent |
| D041 | M003/S04 | tech | Tag property detection heuristic | `'tags' in iri` or `'keywords' in iri` substring match | Only `bpkm:tags` and `schema:keywords` exist as tag properties. Simple substring match is sufficient and consistent across templates, save path, and SPARQL queries. | Yes — add explicit IRI set if more properties appear | agent |
| D042 | M003/S04 | tech | Multi-property by-tag SPARQL uses UNION, not VFS reuse | Custom SPARQL with UNION across `bpkm:tags` and `schema:keywords` instead of reusing VFS `query_tag_folders` | VFS functions take single `tag_property` and `scope_filter` params — explorer needs multi-property, unscoped queries. Adapting would be more complex than writing fresh queries following the same pattern. | No | agent |
| D043 | M003/S04 | tech | Tag migration as admin endpoint, not startup hook | `POST /browser/admin/migrate-tags` requiring owner role | No existing SPARQL migration infrastructure. Explicit endpoint avoids running migration on every startup. Idempotent — safe to call multiple times. | Yes — move to startup migration runner if one is built | agent |
| D044 | M003/S04 | tech | Tag-children as separate endpoint from tree_children and explorer/children | `GET /browser/explorer/tag-children?tag={value}` | Tag queries use UNION across two properties with string-match FILTER, fundamentally different from type-based tree_children or isPartOf-based explorer/children. Follows D036/D040 pattern of separate endpoints per query shape. | No | agent |
| D045 | M003/S05 | tech | Favorites router as separate browser sub-module | `backend/app/browser/favorites.py` with `favorites_router` included in `browser/router.py` | Follows D014 sub-router pattern. Favorites has its own endpoints (toggle, list) separate from workspace routes. Keeps workspace.py from growing further. | No | agent |
| D046 | M003/S05 | tech | FAVORITES section position in explorer pane | Above OBJECTS section, not below | Favorites are high-traffic shortcuts — placing them above the main object list gives immediate visibility. Follows UX convention for quick-access sections. | Yes — move below if user feedback prefers | agent |
| D047 | M003/S05 | tech | Star icon toggle strategy | Two `<i>` elements (outline + filled) with visibility toggle, not Lucide re-render | Avoids Lucide `createIcons()` re-render timing issues. Simpler DOM manipulation — toggle a class to swap which icon is visible. | No | agent |
| D048 | M003/S05 | tech | FAVORITES refresh via htmx trigger event | `HX-Trigger: favoritesRefreshed` on toggle response + `hx-trigger="load, favoritesRefreshed from:body"` on section | htmx-native pattern for cross-component refresh. Toggle endpoint sets header; since star uses fetch (not htmx), JS dispatches `htmx.trigger(document.body, 'favoritesRefreshed')` after successful toggle. | No | agent |
| D049 | M003/S06 | tech | Comment soft-delete strategy | Replace body with "[deleted]" and remove author triple, preserve comment IRI and replyTo structure | Avoids orphaned reply chains. Thread structure stays intact for readers. Simpler than cascade delete or tombstone patterns. | Yes — add hard-delete if storage becomes a concern | agent |
| D050 | M003/S06 | tech | Comment thread query strategy | Flat SPARQL SELECT + Python tree assembly, not recursive SPARQL | RDF4J property paths can't carry metadata (author, timestamp) along the path. Flat query is simple, fast, and lets Python build the tree with full control over ordering and depth capping. | No | agent |
| D051 | M003/S06 | tech | Comments router as separate browser sub-module | `backend/app/browser/comments.py` with `comments_router` included in `browser/router.py` | Follows D014/D045 sub-router pattern. Comments have their own CRUD endpoints separate from workspace routes. | No | agent |
| D052 | M003/S06 | tech | Comments router include order | `comments_router` included before `objects_router` in `browser/router.py` | FastAPI's `:path` parameter in `objects_router` (`/object/{iri:path}`) is greedy and consumes `/comments` suffix, preventing comments endpoints from matching. Must register more-specific routes first. | No | agent |
| D053 | M003/S07 | tech | Ontology service as separate module | `backend/app/ontology/` module (service.py + router.py), not added to existing services/ | Ontology loading + TBox/ABox/RBox queries are a distinct domain boundary with startup responsibilities. Separate module keeps workspace.py from growing further and follows the domain-module pattern (canvas/, lint/, vfs/). | No | agent |
| D054 | M003/S07 | tech | Gist loading via batched INSERT DATA, not RDF4J REST upload | Parse gist with rdflib → batch INSERT DATA (≤500 triples) via existing TriplestoreClient | Avoids adding a new RDF4J REST API method to TriplestoreClient. Batch size of 500 stays well under RDF4J's 2MB request body limit. Transaction-scoped for atomicity. Trade-off: slightly slower than direct Turtle upload but simpler and uses proven patterns. | Yes — switch to RDF4J REST graph upload if batch loading is too slow | agent |
| D055 | M003/S07 | tech | Ontology viewer as three-tab htmx layout in special-panel | TBox/ABox/RBox as htmx-driven tabs inside a single `ontology_page.html`, TBox loads on open, ABox/RBox lazy-load on tab click | Simpler than dockview sub-panels. Consistent with how other workspace tabs work (canvas, VFS browser). htmx tab switching avoids JS complexity. | No | agent |
| D056 | M003/S07 | tech | TBox label resolution inline via SPARQL COALESCE, not LabelService batch | TBox SPARQL uses `COALESCE(?skosLabel, ?rdfsLabel, localname)` directly in query | Gist uses `skos:prefLabel`, model classes use `rdfs:label`. Resolving in-query avoids a second round-trip to LabelService. LabelService.resolve_batch() is still used for ABox instances. | Yes — switch to LabelService if label resolution needs caching | agent |
| D057 | M003/S07 | tech | ABox shows only types with instances > 0 | Filter out classes with zero instances from ABox browser display | Gist has ~96 classes; most have zero instances in a typical SemPKM database. Showing all would overwhelm. D024 says gist is "fully visible" in TBox — ABox filtering by instance count is about data presence, not schema visibility. | Yes — add "show all types" toggle if users request | agent |
| D058 | M003/S07 | tech | Ontology router included in browser router coordinator | `ontology_router` included before `objects_router` in `browser/router.py` | Same reason as D052: objects_router's `:path` parameter is greedy. Ontology routes use `/ontology/` prefix which would be consumed. | No | agent |
| D059 | M003/S08 | tech | User class IRI minting with UUID suffix | `urn:sempkm:user-types:{Slug}-{uuid4hex[:8]}` (e.g. `urn:sempkm:user-types:MyTask-a1b2c3d4`) | Prevents IRI collision when multiple users create classes with the same name. Slug provides readability; UUID provides uniqueness. 8-hex-char prefix gives ~4 billion combinations — sufficient for user-created types. | No | agent |
| D060 | M003/S08 | tech | No EventStore for class/shape creation | Direct INSERT DATA to `urn:sempkm:user-types`, same pattern as gist loading | Class creation is schema modification, not data modification. EventStore is for object-level operations. Consistent with how gist and ontology triples are loaded (D054). | Yes — add EventStore if class version history becomes a requirement | agent |
| D061 | M003/S08 | tech | IconService user-type icon resolution via triplestore triples | Store `sempkm:typeIcon` and `sempkm:typeColor` triples on class IRI in `urn:sempkm:user-types`, with lazy-load cache on IconService | IconService is filesystem-only (manifest.yaml). User types have no filesystem manifest. Storing icon metadata as RDF triples is consistent with the RDF-first architecture and avoids introducing a separate JSON/YAML file for user types. | Yes — refactor if icon loading becomes a performance bottleneck | agent |
| D062 | M003/S08 | tech | Class deletion as DELETE WHERE, create-only scope expanded to include delete | Minimum error recovery: `DELETE WHERE` removes class + shape triples from `urn:sempkm:user-types` | D028 scopes S08 as create-only, but without delete there's no recovery from broken classes. Delete is a single SPARQL operation, low effort, high value. Does not include edit (TYPE-03 scope). | No | agent |
| D063 | M003/S08 | tech | htmx form data serialization: use config-request not before-request | `hx-on::config-request` with explicit `event.detail.parameters` update for dynamic form fields | htmx seals form parameters before `htmx:beforeRequest` fires. Hidden input values updated by JS during `beforeRequest` are not included in the request. `configRequest` fires during parameter collection phase, allowing explicit parameter injection. | No | agent |
| D064 | M003/S08 | tech | Shape delete uses two separate SPARQL UPDATE calls | `_build_delete_shape_sparql()` returns tuple of (bnode cleanup, shape removal) queries | RDF4J rejects `OPTIONAL` inside `DELETE WHERE` (returns 400). Semicolon-separated updates in a single call also fail. Two sequential calls are reliable and equivalent. | No | agent |
| D065 | M003/S09 | tech | Chart.js 4.x via CDN, per-page only | Chart.js 4.4 loaded via `{% block scripts %}` in model_detail.html only | Research recommendation. Battle-tested, ~60KB gzipped, supports sparkline + bar chart. No global loading — only admin model detail page needs it. | Yes — switch to bundled if build pipeline changes | agent |
| D066 | M003/S09 | tech | Lazy chart init on flip transitionend | Charts created in flipCard() transitionend callback, not on page load | Chart.js reads canvas dimensions on init. Canvas inside `display:none` (face-hidden) has 0×0 dimensions → empty charts. Lazy init after flip ensures canvas is visible. | No | agent |
| D067 | M003/S09 | tech | Link distribution buckets: 0, 1-2, 3-5, 6-10, 11+ | Fixed 5-bucket histogram for link count distribution | Covers the typical power-law distribution of knowledge graph connectivity. Simple to compute in SPARQL with CASE/BIND. Readable in a small bar chart. | Yes — adjust buckets if real usage shows different distribution | agent |
| D068 | M003/S09 | tech | Growth trend window: 8 weeks | Query last 8 weeks of object.create events with timestamp filter | Balances sparkline readability (8 data points) with query performance (tight time bound avoids full event scan). | Yes — extend if users want longer history | agent |
| D069 | M003/S10 | tech | Rate-limit e2e tests run last via 99- directory prefix | Rate-limiting spec in `e2e/tests/99-rate-limiting/` | Rate-limit tests exhaust the per-IP endpoint budget (slowapi in-memory), breaking auth fixtures in subsequent tests. Moving to 99- ensures Playwright's alphabetical ordering runs them after all auth-dependent tests. | No | agent |
| D070 | M004/S02 | tech | Two-step class delete confirmation via check endpoint | `GET /ontology/delete-class-check` returns HTML fragment with instance/subclass counts, rendered in modal overlay. Confirm button triggers existing `DELETE /ontology/delete-class`. | `hx-confirm` uses browser-native `window.confirm()` which cannot show dynamic content. Two-step pattern follows existing modal conventions (class creation, property creation, class edit). | No | agent |
| D071 | M004/S02 | tech | `_property_source` returns `'user'` for user-types IRIs | Check for `urn:sempkm:user-types:` prefix before generic `SEMPKM_NS` check | Fixes RBox badge showing "sempkm" instead of "user" for user-created properties. IRI prefix ordering matters since user-types IRIs also start with `urn:sempkm:`. | No | agent |
| D072 | M004/S02 | tech | Property deletion: simple `hx-confirm`, no modal | Properties have no instances and no dependent shapes to warn about. Static `hx-confirm` string is sufficient. | Avoids over-engineering. Class deletion needs dynamic counts; property deletion doesn't. | Yes — add modal if property deletion gains cascade concerns | agent |
| D073 | M004/S03 | tech | Property type immutable on edit | Edit form shows property type (Object/Datatype) as read-only; cannot switch after creation | Switching between ObjectProperty and DatatypeProperty changes IRI semantics and breaks SHACL shapes that reference the property. Safer to require delete + re-create. | No | agent |
| D074 | M004/S03 | tech | Custom section server-rendered in admin_models(), not htmx lazy-load | Extend `admin_models()` to call `list_user_types()` and pass result to template | The `list_user_types()` query hits a single small graph (<100ms). Lazy-load adds unnecessary complexity. | Yes — switch to htmx lazy-load if query becomes slow | agent |
| D075 | M004/S03 | tech | SHACL shapes NOT updated on property edit | When domain/range changes, existing SHACL shapes referencing the property are not auto-updated | SHACL shapes reference properties by IRI via `sh:path`. Domain/range are OWL-level constraints, not SHACL-level. Acceptable because shapes still point to the correct IRI. | Yes — add shape cascade if it causes user confusion | agent |
| D076 | M004/S03 | tech | Edit property form uses `epf-` ID prefix | Edit property form element IDs prefixed `epf-` to avoid collision with create form `cpf-` IDs | Both forms can coexist in the DOM (ontology page has both modals). ID collision would break JS handlers. | No | agent |
| D077 | M005 | process | Pre-commit hook rejects conflict markers | `.githooks/pre-commit` scans staged files for `^<<<<<<< ` / `^=======` / `^>>>>>>> ` and exits non-zero if found | 17 consecutive squash merges (M002/S03–M005/S01) committed conflict markers to main undetected. Docker volume mounts hid the damage by serving clean branch code. Pre-commit hook is the last line of defense. | No | agent |
| D078 | M005 | infra | Migrations volume-mounted in docker-compose.yml | `./backend/migrations:/app/migrations` added to api service volumes | Without this mount, migration file edits required a full `docker compose build api`. Consistent with how `./backend/app` is already mounted. Discovered when conflict-marker fix in `env.py` wasn't picked up by the running container. | No | agent |
| D079 | M005/S02 | tech | Ops log calls in router layer, not service layer | `log_activity()` called from admin router, inference router, and validation queue — not from `ModelService`/`InferenceService` | Router layer already has user context from `require_role()`. Threading user IRIs through service methods would change signatures and create coupling. Consistent with how `EventStore.commit()` receives `performed_by` from routers. | No | agent |
| D080 | M005/S02 | tech | Ops log uses direct SPARQL INSERT DATA, not EventStore | Direct SPARQL INSERT DATA to `urn:sempkm:ops-log` named graph | Ops log records metadata _about_ operations, not data belonging in `urn:sempkm:current`. Same pattern as `QueryService` writes to `urn:sempkm:queries`. EventStore would create per-event graphs and materialize into current — wrong semantics. | No | agent |
| D081 | M005/S02 | tech | Single `urn:sempkm:ops-log` named graph for all entries | All ops log activities in one graph, not per-entry graphs | Matches `urn:sempkm:queries` pattern from S01. Log entries are simple metadata — no data isolation needed. Single graph simplifies querying. Low volume (10-50 entries/day max). | No | agent |
| D082 | M005/S02 | tech | Ops log calls are fire-and-forget with try/except | All `log_activity()` calls wrapped in try/except — failures logged at WARNING but never block primary operation | Ops log is observability tooling. A triplestore write failure during model install must not prevent the install from succeeding. Same philosophy as webhook dispatch. | No | agent |
| D083 | M005/S02 | tech | htmx target-aware block rendering for ops log route | Check `HX-Target` header to decide which Jinja2 block to return: `app-content` → `content` block (sidebar nav), anything else → `table_rows` block (filter/pagination) | Single endpoint serves three use cases (full page, sidebar nav, filter/pagination). Without target detection, returning `content` block for filter swaps caused duplicate heading/controls inside `#ops-log-table`. | No | agent |
| D084 | M005/S03 | tech | Hierarchical tag tree: fetch flat, nest in Python, lazy-load children | Existing SPARQL fetches all tags flat; `build_tag_tree()` pure function groups into hierarchy; `tag_children` endpoint with `prefix` param for lazy sub-folder loading | Reuses existing SPARQL (no new string functions for roots), uses `STRSTARTS` for children (well-supported), pure function is unit-testable without Docker. Follows proven htmx lazy-load pattern (D035, D036, D040, D044). | No | agent |
| D085 | M005/S03 | tech | `tag_children` endpoint extended with `prefix` parameter, not new endpoint | Add `prefix` query param to existing `GET /explorer/tag-children` rather than creating a separate `/explorer/tag-prefix-children` route | Existing endpoint already handles tag expansion. Prefix queries are the same domain — sub-folder expansion is conceptually "children of a tag prefix". `tag` param = exact match objects, `prefix` param = sub-folder tree. Avoids endpoint proliferation while staying clean. | Yes — split if logic diverges significantly | agent |
| D086 | M005/S03 | tech | `build_tag_tree()` as separate module, not inline in workspace.py | New `backend/app/browser/tag_tree.py` module for pure function | workspace.py is already 1063 lines. Pure function has no dependencies on FastAPI/triplestore — separate module keeps it testable and workspace.py from growing. | No | agent |
| D087 | M005/S04 | tech | Tag autocomplete q parameter via htmx:configRequest, not hx-vals | `htmx:configRequest` event listener rewrites params to `{q: triggerEl.value}` for tag-suggestions requests | `hx-vals='js:{"q": this.value}'` did not reliably pass the `q` query parameter on htmx GET requests — requests were sent without `q`, returning all tags unfiltered. `htmx:configRequest` fires during parameter collection and allows explicit injection. | No | agent |
| D088 | M005/S05 | tech | Refresh clears 4 artifact graphs inline, not via `clear_model_graphs()` | Build 4 individual `CLEAR SILENT GRAPH` statements for ontology/shapes/views/rules, not reuse `clear_model_graphs()` | `clear_model_graphs()` clears all 5 graphs including seed. Refresh must NOT touch seed (user data). Adding an `exclude` parameter would change the function signature for one caller. Inline CLEAR is simpler and explicit. | No | agent |
| D089 | M005/S05 | tech | Refresh uses hx-confirm, not two-step modal | Simple `hx-confirm` dialog on Refresh button | Refresh is non-destructive (transactional — rollback on failure). Lower risk than delete. Two-step modal (D070) is for operations with permanent data loss potential. | Yes — add modal if refresh gains destructive side effects | agent |
| D090 | M005/S06 | arch | PROV-O Starting Point terms only — no Qualified or Extended | Use `prov:Activity`, `prov:startedAtTime`, `prov:wasAssociatedWith`, etc. Do not use `prov:qualifiedAssociation`, `prov:hadRole`, or Extended terms | Qualified terms add blank nodes and multi-triple patterns for marginal information gain. Starting Point covers 90%+ of SemPKM's provenance needs. Ops log already validates this approach. | Yes — reconsider if provenance queries need role-on-association detail | agent |
| D091 | M005/S06 | arch | Existing event graphs retain sempkm: predicates forever — no retroactive migration | Immutable event named graphs keep their original `sempkm:` predicates. Alignment applies only to newly written data via dual-write. | Event-sourcing requires immutable events. Federation sync transmits events as serialized patches — changing predicates would desynchronize instances. Dual-write + COALESCE queries handle mixed-era data without rewriting history. | No | agent |
| D092 | M005/S06 | tech | COALESCE pattern for dual-predicate SPARQL queries (over UNION or VALUES) | Use `OPTIONAL { ?e sempkm:timestamp ?old } OPTIONAL { ?e prov:startedAtTime ?new } BIND(COALESCE(?new, ?old) AS ?ts)` | VALUES creates cross-products on dual-write events. UNION requires deduplication. COALESCE with OPTIONAL is clean, single-row-per-event, and performs well on most triplestores. | Yes — revisit if triplestore performance shows issues with OPTIONAL chains | agent |
| D093 | M005/S07 | arch | Generic views registered in-memory at startup, not as RDF | Register 3 generic ViewSpecs (Table/Cards/Graph) in ViewSpecService at startup, not in a system named graph | Avoids a "system views" named graph. Generic views are code-defined behavior, not data-defined. Simpler lifecycle — no migration needed for view definition changes. | Yes — move to RDF if user-customizable generic views become a requirement | agent |
| D094 | M005/S07 | arch | SHACL shapes via ShapesService as column discovery source | Use ShapesService._extract_node_shape() → PropertyShape.path/name/order for dynamic table columns | Already extracts sh:path, sh:name, sh:order — everything needed for dynamic columns. Fallback to default column set (label, type, created, modified) for types with ≤2 properties. | No | agent |
| D095 | M005/S07 | arch | Type filter pills for cross-type generic views | Pills (not dropdown, not carousel) for type selection in generic views | Carousel assumes single target_class. Dropdown hides available types. Pills show all types at a glance with active state. Consistent with tag pill UX pattern. | Yes — reconsider if type count exceeds ~15 | agent |
| D096 | M005/S07 | arch | sempkm:scopeQuery for view-to-query scope binding | Distinct from sempkm:fromQuery: scopeQuery = runtime filter, fromQuery = provenance | Extends existing promoted view pattern. scopeQuery injects VALUES clause or subquery to limit view results to query output IRIs. Clean separation of concerns. | No | agent |
| D097 | M005/S07 | process | 3-phase views migration, additive-first | Phase 1 (generic views alongside existing tree), Phase 2 (consolidate explorer), Phase 3 (cleanup). Phase 1 is milestone deliverable. | No big-bang. Phase 1 is strictly additive — zero risk to existing views. Phase 2 changes navigation only. Phase 3 is dead code removal. | No | agent |
| D098 | M005/S08 | tech | Direct SPARQL via SyncTriplestoreClient for VFS saved query resolution | `SyncTriplestoreClient` queries `urn:sempkm:queries` graph directly, not async `QueryService` | WebDAV runs in sync WSGI context. `QueryService` is async (uses `httpx.AsyncClient`). Calling async from sync would require `asyncio.run()` or thread bridging — fragile in production. `SyncTriplestoreClient` already used by VFS provider code. | No | agent |
| D099 | M005/S08 | arch | VFS adopts `sempkm:scopeQuery` with full IRI (aligning with Views Rethink) | Replace bare UUID `sempkm:savedQueryId` with `sempkm:scopeQuery` predicate pointing to full `urn:sempkm:query:{uuid}` IRI | Matches Views Rethink D096 pattern. Full IRI enables SPARQL joins without string manipulation. One predicate used by both views and VFS for scope binding. Migration: one SPARQL UPDATE renames predicate + wraps values. | Yes — if model query IRIs need different treatment | agent |
| D100 | M005/S08 | arch | Chain model (Option A) for composable VFS strategies, max depth 3 | Ordered list of strategies, each level nests inside the previous. Max 3 levels enforced. | Chain is simpler than tree or graph composition. 3 levels covers all practical use cases (e.g. by-tag → by-type → flat). Provider path dispatch needs extension from 4 to 6 segments. | No | agent |
| D101 | M005/S08 | tech | type_filter AND saved_query compose via AND | Both `sempkm:typeFilter` VALUES clause and saved query scope filter applied simultaneously | AND semantics match user mental model: "show me Notes (type) from this query (scope)". OR would produce confusing results. Implementation: both clauses injected into the same WHERE block. | No | agent |
| D102 | M005/S08 | tech | Defer auto-refresh for VFS, use existing 30s TTLCache | No file-system watcher or push invalidation. Rely on existing `TTLCache(maxsize=64, ttl=30)` in VFS provider. | Auto-refresh adds complexity (filesystem watchers, WebSocket push) for marginal benefit. 30s TTL means changes appear within half a minute. Revisit when write-support lands. | Yes — reconsider with write-support milestone | agent |
| D103 | M006/S04 | tech | Dashboard builder submits via fetch(), not htmx form | Builder form uses `fetch()` with `Content-Type: application/json` to POST/PATCH the existing JSON API endpoints | JSON API expects structured `{name, layout, blocks}` body. htmx form submission sends form-encoded data which doesn't map cleanly to nested JSON. `fetch()` also allows JS-side payload construction from dynamic block rows. Same pattern as VFS mount form. | No | agent |
| D104 | M006/S04 | tech | Explorer refresh via JS-dispatched htmx trigger, not HX-Trigger header | Builder's `fetch()` success handler calls `htmx.trigger(document.body, 'dashboardsRefreshed')` | JSON API endpoints return JSON, not HTML. `HX-Trigger` response header only works with htmx-initiated requests. Since builder uses `fetch()`, JS must dispatch the event manually. Follows D048 pattern (favorites). | No | agent |
| D105 | M006/S05 | arch | Single implicit IRI context variable per dashboard, not Retool-style named variables | Dashboard has one context variable — the selected IRI. Source blocks export it, consumer blocks bind it to a named SPARQL variable via `listens_to_context`. | Simplest viable approach. Single IRI covers primary use case (select project → filter notes). Multiple named variables can layer on later without breaking the single-IRI API. | Yes — add named variables if dashboards need multiple independent filters | agent |
| D106 | M006/S05 | tech | htmx:configRequest for context_iri URL injection, not hx-vals | Consumer blocks use `htmx:configRequest` event listener to inject `context_iri` and `context_var` params into re-fetch URLs | `hx-vals` with `js:` prefix is unreliable for dynamic values in this codebase (see D087). `htmx:configRequest` fires during parameter collection and allows explicit injection. Proven pattern from tag autocomplete. | No | agent |
| D107 | M006/S05 | tech | Context emission limited to table renderer for v1 | Only table rows emit `sempkm:dashboard-context-changed`. Cards (flip animation conflict) and graphs (Cytoscape events) deferred. | Table is the primary use case. Card clicks trigger flip, not selection. Graph node clicks need Cytoscape event listeners — separate concern. Both can be added later without changing the event contract. | Yes — add card/graph context emission when needed | agent |
| D108 | M006/S05 | tech | dashboard_mode query parameter for conditional row behavior | `dashboard_mode=1` query param on view endpoints tells table_view template to emit context events instead of openTab() | Clean separation: same template renders correctly in both dashboard-embedded and standalone contexts. No global state or CSS class sniffing. Passed from render_block through the view URL. | No | agent |
| D109 | M006/S05/T02 | tech | Dashboard context event uses camelCase, not colon-namespaced | Event name `dashboardContextChanged` instead of `sempkm:dashboard-context-changed` | htmx's hx-trigger parser uses colons as modifier separators (like `from:body`). Custom event names containing colons fail to register as trigger listeners. camelCase matches existing codebase pattern (dashboardsRefreshed, favoritesRefreshed). | No | agent |
| D110 | M006/S05/T02 | tech | Context params forwarded server-side in render_block, not purely client-side | render_block accepts context_iri/context_var and embeds them in the view URL | htmx:configRequest on nested elements (inner view div inside slot div) causes duplicate parameter injection. Server-side forwarding gives clean, single-injection chain: configRequest on slot → render_block → view URL. | No | agent |
| D111 | M007/S01/T02 | tech | Default SELECT query uses mandatory rdf:type binding | `?s rdf:type ?type .` is non-OPTIONAL in `_build_default_select()` | All-OPTIONAL patterns left `?s` unbound — SPARQL engines return zero rows because there's no triple pattern to ground the subject variable. Making type mandatory ensures subjects have at least one typed assertion. | No | agent |
| D112 | M007/S01/T02 | tech | Generic graph view uses separate data endpoint | `GET /browser/views/generic/{renderer}/data` instead of retrofitting existing `/graph/{spec_iri}/data` | Generic specs have empty `sparql_query` — queries are built dynamically per request. The existing data endpoint calls `execute_graph_query(spec)` which returns empty when `spec.sparql_query` is empty. Separate endpoint keeps both paths clean. | Yes — could unify if generic specs stored built queries | agent |
| D113 | M007/S01/T02 | pattern | pagination_base_url template variable for URL construction | All pagination/sort/filter templates accept `pagination_base_url \ | default(old_pattern)` | Generic views use `?type=` query params instead of spec IRI path segments. The `\ | agent |
| D114 | M007/S01/T04 | tech | Generic view tabs use special-panel dockview component, not view-panel | `openGenericViewTab()` creates a `special-panel` with `specialType: 'generic-view'` and `renderer` param | Generic views don't have a spec IRI — `view-panel`'s URL construction assumes `/{renderer}/{encoded_iri}` which doesn't fit. `special-panel` pattern already proven by canvas, ontology, dashboard, and workflow tabs. | No | agent |
| D115 | M007/S01/T04 | tech | Saved Views replaces MY VIEWS as htmx lazy-loaded folder inside VIEWS section | Saved Views folder in views_explorer.html uses `hx-trigger="intersect once"` loading from `/browser/my-views` | Removes a sidebar section. `/browser/my-views` endpoint stays unchanged. `#my-views-tree` → `#saved-views-tree` ID update in `my_views.html` and `sparql-console.js`. | No | agent |
| D116 | M007/S02/T01 | tech | type_filter VALUES uses ?iri a ?type binding | `VALUES ?type { ... }` with `?iri a ?type .` in scope filter | Connects the VALUES constraint to the object pattern via rdf:type triple. Empty list treated same as None (no-op). | No | agent |
| D117 | M007/S02/T02 | arch | scope_query stores full IRI, not bare UUID | `<urn:sempkm:query:{uuid}>` as RDF IRI type | Enables SPARQL joins without string manipulation. Frontend constructs IRI by prefixing `urn:sempkm:query:` to UUID. Aligns with D099. | No | agent |
| D118 | M007/S02/T04 | tech | Two-query approach for list_mounts type_filter | Separate SELECT for typeFilter triples, merged in Python | More portable across SPARQL 1.1 stores than GROUP_CONCAT subquery. GROUP_CONCAT used for single-resource queries (get_by_id, get_by_prefix) where it's safe. | Yes — consolidate if store supports GROUP_CONCAT reliably | agent |
| D119 | M007/S02/T03 | tech | Preview returns HTTP 404 for unresolvable scope_query | Fail-fast with clear error instead of empty scope | Dead code previously produced empty scope_filter regardless of input. 404 makes the failure visible to API callers. WebDAV path silently omits scope (warning logged) since crashing a directory listing is worse. | No | agent |
| D120 | M007/S03 | tech | Chain strategies stored as pipe-delimited string literal | `"by-tag\ | by-date"` stored as single string via `sempkm:directoryStrategy` | RDF doesn't have native ordered lists without blank nodes. Pipe-delimited string avoids sequence predicate complexity. Parsed via `strategy.split("\ | agent |
| D121 | M007/S03 | tech | Chain scope narrowing is cumulative across all parent levels | Each chain depth adds WHERE clauses for ALL parent groupings, not just the immediate parent | Objects at depth 2 must match both depth-0 and depth-1 groupings. Prevents leaked results from sibling branches. Narrowing filters composed as additional WHERE clause fragments via `build_chain_narrowing_filter()`. | No | agent |
| D122 | M007/S03 | tech | Filename template expansion before slugification | Template variables expanded to raw text, then entire string slugified | Preserves filesystem safety. `{date}-{title}` → `"2024-01-15-My Note"` → `"2024-01-15-my-note"`. Dedup suffix applies after slugification as before. | No | agent |
| D123 | M007/S03/T02 | tech | Chain by-type narrowing uses SPARQL local name FILTER, not pre-resolved IRI | `FILTER(REPLACE(STR(?type), ".*[/:#]", "") = "Label")` instead of `?iri a <resolved-iri>` | `build_chain_narrowing_filter()` is a pure function without triplestore access. Resolving type label→IRI requires a SPARQL query (done in StrategyFolderCollection._resolve_type_iri_from_label). The FILTER approach works at query time and avoids coupling the narrowing helper to the sync client. | Yes — could pre-resolve if perf matters | agent |
| D124 | M008 | arch | Dual-layer rendering for embed nodes | Embed iframes in a separate persistent DOM layer; regular nodes in the innerHTML-rebuilt dynamic layer | `renderNodes()` rebuilds `state.layer.innerHTML` every drag frame. Iframes recreated via innerHTML lose their loaded page state. Dual-layer avoids this: embed nodes are created/moved via `style.left`/`style.top` updates, never innerHTML-rebuilt. | No | agent |
| D125 | M008 | arch | Separate `base_embed.html` template for iframe content | New minimal base template, not conditional hiding in `base.html` | `base.html` loads 15+ CDN scripts (Cytoscape, dockview, CodeMirror, etc.) and the sidebar. Embed pages need only htmx + theme CSS + content. Separate template avoids iframe payload bloat and fragility of hiding elements in a shared template. | No | agent |
| D126 | M008 | arch | Property flip uses inline rendering, not iframes | Compact label/value table built from JSON API response, rendered directly in the node DOM | SHACL-derived property data is lightweight (10-30 key/value pairs). Inline rendering is faster, lighter, and avoids iframe overhead for the most common flip action. Only views/dashboards/SPARQL/object-read use iframe embeds. | No | agent |
| D127 | M008 | arch | CSS resize handles over native CSS `resize: both` | Custom pointer-event resize handles on node corners/edges | CSS `resize: both` only provides a bottom-right handle, doesn't emit events for state persistence, and can't enforce min/max constraints. Custom handles provide better UX and integrate with the existing pointer event system. | No | agent |
| D128 | M008 | arch | Max 8 simultaneous iframe embeds, hard limit | Hard cap on embed count with user warning at placement time | Each iframe is a full page load. 5+ embeds measurably impacts performance. Hard limit for v1 prevents browser tab crashes. Lazy loading deferred to follow-up. | Yes — add lazy loading or raise limit if needed | agent |
| D129 | M008 | arch | Embed link routing via `window.parent` | Links inside embed iframes open targets in parent workspace via `window.parent.openTab()` | Same-origin iframes can access parent window. This routes clicks to dockview tabs in the main workspace instead of navigating the iframe away from embedded content. | No | agent |
| D130 | M008/S01 | tech | Resize updates inline style during drag, full renderNodes on pointerUp | During resize: update `style.width`/`style.height` directly on the DOM element + update node model. On pointerUp: call `renderNodes()` once to finalize edges and DOM state. | `renderNodes()` rebuilds `state.layer.innerHTML` on every call. Calling it on every resize frame would be expensive and cause visual flicker (SVG edges rebuilt). Direct style manipulation during drag is smooth; final `renderNodes()` ensures edges re-calculate from final box dimensions. | Yes — if dual-layer rendering (S03) changes the rebuild model | agent |
| D131 | M008/S02 | tech | Property cache is memory-only, not serialized | `state.propertyCache` keyed by nodeId, re-fetched on session load via `applyDocument()` triggering `fetchNodeProperties()` for nodes with `showProperties: true` | Serializing property data would bloat canvas session JSON and stale quickly. Re-fetching on load ensures fresh data. Follows the `fetchNodeBody` pattern where markdown is also re-fetched. | No | agent |
| D132 | M008/S02 | tech | `build_property_list()` extracted as pure function | Core property-building logic in standalone function taking parsed bindings, form, and labels as input | Enables 26 unit tests with zero mocking — all inputs are plain dicts/lists. Endpoint handler orchestrates async calls then delegates to pure function. Pattern reusable for future canvas data transformations. | No | agent |
| D133 | M008/S03 | tech | New `GET /browser/sparql-result/{query_id}` endpoint for server-rendered query results | Fetch saved query, execute via `_execute_sparql()`, render HTML table in `base_embed.html` | Existing SPARQL console renders results client-side (CodeMirror + custom JS). No server-rendered endpoint exists for saved query output. Embeds need an HTML page, not a JS-rendered SPA. Reuses existing query execution helpers. | No | agent |
| D134 | M008/S03 | tech | Embed layer uses `pointer-events: none` with `auto` on individual embeds | Container `.spatial-canvas-embed-layer` has `pointer-events: none`; individual `.spatial-node-embed` elements have `pointer-events: auto` | Lets clicks pass through empty areas of the embed layer to reach regular nodes on the layer below. Without this, the embed layer's higher DOM position would intercept all pointer events over the canvas. | No | agent |
| D135 | M008/S03-T01 | tech | Fragment-to-string rendering for embed wrapping | `_embed_response()` calls `templates.env.get_template(name).render(context)` to get fragment HTML, passes it as `content` to `embed_wrapper.html` | Jinja2 `{% include inner_template %}` with a variable template name has scoping issues and requires the template name to be a string constant in some Jinja2 configurations. Rendering to string first is simpler, works with any fragment template, and the embed wrapper just needs `{{ content | safe }}`. | agent |
| D136 | M008/S03-T01 | tech | SPARQL result router registered as browser sub-module before objects_router | `sparql_result_router` in `browser/router.py` mounted before `objects_router` | The objects_router has `{object_iri:path}` catch-all patterns that would consume `/sparql-result/*` URLs. Registration order in FastAPI determines route matching priority. Same pattern as ontology_router and comments_router. | No | agent |
| D137 | M008/S03-T02 | arch | Dual-layer rendering: regular nodes via innerHTML, embeds via persistent DOM | `state.layer` rebuilt via innerHTML each `renderNodes()` call; `state.embedLayer` only updates CSS position/size properties on existing DOM elements | iframes lose their loaded state when recreated via innerHTML. Persistent DOM for embeds means the iframe never gets destroyed during regular node interactions. Orphan cleanup runs each render to remove stale embed elements. Embed layer applies same CSS transform as regular layer for pan/zoom sync. | No | agent |
| D138 | M009 | arch | Apps as sandboxed subprocesses, not in-process or Docker containers | Each app runs in its own Python subprocess with its own venv, communicating via HTTP over unix domain socket | Subprocess gives dependency isolation and crash isolation without Docker-in-Docker complexity. HTTP/UDS is debuggable with standard tooling. In-process would allow apps to crash the platform; Docker containers add networking/orchestration overhead. | Yes — upgrade to containers if untrusted marketplace apps are added | agent |
| D139 | M009 | arch | Mental models are always shared — apps never bundle their own model | Models published independently, versioned on own timeline, reusable by any app. App declares model dependencies with semver ranges. | Composability: multiple apps can share the same model. Independent versioning prevents tight coupling. Data survives app removal. | No | agent |
| D140 | M009 | arch | App data lives in urn:sempkm:current, not a separate graph | Apps produce data through standard EventStore (including bulk mode). Data typed by shared models is first-class — browsable, queryable, linkable. IRI prefix for traceability/cleanup. | Separate graphs would fragment the knowledge graph. Shared graph means articles are as queryable as user-created notes. IRI prefix enables clean uninstall. | No | agent |
| D141 | M009 | arch | Platform owns scheduling — apps declare tasks, platform runs them | AppScheduler in main process triggers app tasks via HTTP. Admin has full visibility and control (pause, adjust interval, view history). | Centralized control prevents rogue apps from overwhelming the system. Admin can pause all tasks. Consistent monitoring surface. | No | agent |
| D142 | M009 | arch | All app UI is fragments — platform controls the page shell | Apps return HTML fragments loaded via htmx. Platform renders sidebar, header, theming. Apps cannot break platform navigation. | Consistent UX, theme compliance, no iframe overhead. Apps can't hijack navigation or break workspace layout. | No | agent |
| D143 | M009 | arch | App SDK as in-repo package, not published to PyPI | SDK lives at backend/sdk/ in the SemPKM repo. Installed into app venvs at install time. | Simpler development loop. SDK and platform evolve together. No package publishing infrastructure needed for v1. | Yes — publish to PyPI when marketplace launches | agent |
| D144 | M009 | arch | browserVisible field on Mental Model types (default true) | Types with `browserVisible: false` are hidden from object browser but remain queryable via SPARQL and linkable via edges. | Apps create internal bookkeeping types (ReadActivity, sync cursors) that clutter the object browser. Hiding them improves UX without losing data access. Backward compatible (default true). | No | agent |
| D145 | M009 | arch | Bulk EventStore with summary metadata, not per-operation | `commit_bulk()` records ~10 triples per batch vs. ~5N for standard. All-or-nothing undo. | Feed polling creates 50-150 operations per update. Per-operation metadata overhead is unacceptable. Bulk undo semantics are fine for batch ingestion. | No | agent |
| D146 | M009 | arch | First-party apps in apps/ directory alongside models/ | Apps committed to repo. Platform discovers them at startup. Consistent with models/ pattern for Mental Models. | Simple installation for bundled apps. No marketplace infrastructure needed for v1. | Yes — add marketplace when app ecosystem grows | agent |
| D147 | M009/S06 | arch | Right pane sections dynamic vs. hardcoded | Single dynamic endpoint merging platform sections + app contributions, replacing 3 hardcoded <details> blocks | App contributions need to appear alongside Relations/Lint/Comments. Dynamic endpoint allows runtime merging based on object type and running apps. AbortController cancellation prevents stale sections on rapid tab switching. Platform sections always present as baseline; app sections gracefully degraded on failure. | No | agent |
| D148 | M009/S06 | arch | Object renderer override dispatch pattern | Registry checks type match → pref table overrides → template swap with context augmentation; failure falls back silently to default SHACL form | Apps need to replace default SHACL form for specific types with custom read/edit renderers. Three-layer dispatch (registry → pref → template) gives clear precedence. AppRendererPref table resolves multi-app conflicts via admin UI. Silent fallback to default on any error ensures renderer bugs never break object viewing. | No | agent |
| D149 | M011 | scope | M011 is pure content — no platform code changes | All 4 models shipped as .sempkm-model archives using existing manifest/loader/validator/inference/validation pipeline. Zero Python, JS, or CSS changes. | Research confirmed every pattern needed (SPARQLConstraint, PropertyGroups, editHelpText, inverseOf, SavedQuery) is already exercised by basic-pkm and PPV. Adding platform features would increase risk for no benefit. | No | agent |
| D150 | M011 | scope | Dashboard bundling deferred — document recommended configs | Dashboards described in design doc cannot be shipped in model archives (DashboardSpec is SQLite JSON per D105). Document recommended dashboard configurations in user guide instead. | DashboardSpec-to-RDF migration is tracked tech debt. Adding manifest dashboard support is a platform change that violates M011's pure-content scope. | Yes — add dashboard manifest support when DashboardSpec moves to RDF | agent |
| D151 | M011 | arch | S01-S04 parallelizable — no inter-model dependencies | Each model slice can execute independently. Cross-model verification deferred to S05. | Models must work standalone by design. Cross-model edges use shared gist hierarchy, not direct model-to-model references. Parallel execution cuts calendar time. | No | agent |
| D152 | M011 | arch | basic-pkm v2 is additive-only, no Event type in v2.0 | v2.0 adds Task and Milestone only. Event type (bpkm:Event) deferred — the design doc's Calendar Hub section is forward-looking and depends on calendar provider apps (M016+). | Event type has recurrence (RRULE), attendees, and timezone complexity. Without a calendar provider app to exercise it, it's dead schema. Ship what's usable now. | Yes — add Event type when calendar provider apps are built | agent |
| D153 | M011 | arch | Validation rules use sh:sparql SPARQLConstraint on separate NodeShapes | Validation-only rules (warnings) placed in rules/*.ttl on dedicated NodeShapes with sh:severity sh:Warning, separate from inference SPARQLRule NodeShapes | pyshacl requires severity on parent NodeShape, not constraint node. Mixing inference and validation on one shape causes unpredictable behavior. Separate shapes is the proven pattern. | No | agent |
| D154 | M011 | arch | Seed data pre-populates both sides of owl:inverseOf pairs | Forward and inverse sides both present in seed JSON-LD. Inference produces 0 new triples for seed data but inverse properties display correctly without inference. | Matches basic-pkm and PPV pattern. Ensures data appears correctly even if inference hasn't run yet. Inference correctness tested by adding objects with only one side and checking materialization. | No | agent |
| D155 | M012/S03 | arch | Layout-only personas for v1 — no settings overrides | Personas store dockview layout JSON + sidebar panel positions + explorer mode. No theme, font size, or other settings. | Reduces scope and complexity. Settings overrides can layer on later without breaking the persona API. Core value is layout switching. | Yes — add settings overrides if users request | agent |
| D156 | M012/S03 | arch | Dedicated `personas` table, not `user_settings` key-value | New `personas` table with proper schema following DashboardSpec model pattern | Personas have CRUD lifecycle + activation semantics that don't fit key-value storage. Dedicated table enables proper FK, indexing, and schema evolution. | No | agent |
| D157 | M012/S03 | arch | Explicit save only — no auto-save to server on layout change | Save current persona state on: explicit "Save" action, before switching to another persona, on `beforeunload`. No server save on `onDidLayoutChange`. | `onDidLayoutChange` fires on every panel resize/move — auto-saving would create excessive writes. localStorage auto-save continues independently for crash recovery. | Yes — add debounced auto-save if users request | agent |
| D158 |  | testing | How to handle auth rate limiting in E2E test environments | Added RATE_LIMIT_ENABLED env var (default true) passed to slowapi Limiter's enabled parameter. Set to false in docker-compose.test.yml. Rate-limiting E2E test updated to skip gracefully when disabled. | E2E auth fixtures create sessions rapidly (one per test), exhausting the 5/minute magic-link rate limit and causing cascading test failures. Disabling rate limiting in test stack is cleaner than increasing limits or adding delays. Production defaults remain unchanged (enabled=true). | Yes | agent |
| D159 | M013 | arch | Dual-auth dependency approach for M013 API endpoints | New `get_current_user_or_api` dependency that tries session cookie first, then Bearer token via existing AuthService.verify_api_token(). Existing `get_current_user` left unchanged for htmx routes. | External clients (browser extension) send Bearer tokens; htmx frontend sends cookies. Both must work on the same endpoints. Cookie-first ordering means the htmx frontend path is unchanged. AuthService.verify_api_token() already exists and is tested — no new auth logic needed, only wiring. | No | agent |
| D160 | M013 | tech | SHACL shape JSON serialization strategy | Shape serialization via dataclasses.asdict() mapped to Pydantic response models. No Pydantic migration of ShapesService internals. | Research confirmed PropertyShape/PropertyGroup/NodeShapeForm are plain Python dataclasses with simple types (str, int, float, list[str], None). dataclasses.asdict() serializes them cleanly — no custom serializers needed. Pydantic models on the response side provide OpenAPI docs and validation. Migrating ShapesService internals to Pydantic would be high-risk refactoring with no benefit to M013. | Yes — if ShapesService internals need Pydantic for other reasons | agent |
| D161 | M013 | arch | CORS policy for API endpoints | CORS `Access-Control-Allow-Origin: *` on all `/api/` and `/.well-known/sempkm` responses via nginx headers. No origin registration required. | SemPKM is self-hosted — the user controls their instance. Requiring extension origin registration adds complexity with no security benefit for local instances. Browser extensions have unpredictable origins (chrome-extension://random-id). Wildcard CORS is standard for local APIs. Can add configurable origin restrictions later for cloud-hosted instances. | Yes — add configurable CORS origins for cloud instances | agent |
| D162 | M013 | arch | Context-query scope — literal matches only for v1 | Context-query v1 searches literal values only (URL exact match via SPARQL FILTER + FTS keyword search). No edge traversal. | URL matching (find objects containing a given URL as a property value) and FTS keyword matching (existing LuceneSail) cover the primary browser extension use case. Edge traversal (objects linked to matching objects) adds SPARQL complexity and potentially noisy results. Ship the simple version; add traversal later if needed. | Yes — add edge traversal when browser extension Phase 2 (M015) needs it | agent |
| D163 | M013/S01/T03 | architecture | How to handle 401 responses for JSON API endpoints outside /api/ prefix | Extend _is_html_route() in main.py to exclude /.well-known/ paths alongside /api/, so 401 responses return JSON instead of 302 redirects | The exception handler converts 401s to 302 login redirects for HTML routes. Discovery endpoints at /.well-known/ are JSON APIs that must return structured error responses. Any future JSON API path prefix outside /api/ must also be added to the _is_html_route exclusion list. | Yes | agent |
| D164 | M013/S02 | tech | How /api/types endpoint accesses IconService | Create IconService ad-hoc in endpoint handler (matching codebase pattern) rather than expecting app.state.icon_service | IconService is instantiated inline everywhere in the codebase (admin router, browser helpers, SPARQL router) — it's never registered on app.state. T01-T02 executor assumed app.state wiring, causing runtime 500. Fixed by following existing pattern: `IconService(models_dir=_MODELS_DIR)` with user_type_icons from app.state. | Yes — if IconService is centralized on app.state in a future refactor | agent |
| D165 | M014 | arch | Backend auth approach for commands endpoint to support browser extension | Create `require_role_or_api(*roles)` factory in `dependencies.py` that chains to `get_current_user_or_api` instead of `get_current_user`. Update `POST /api/commands` to use it. Leave existing `require_role` unchanged for htmx routes. | Parallel factory avoids breaking existing htmx routes (which rely on cookie-only `require_role`). The `get_current_user_or_api` dependency already exists and is tested (M013). This is a ~20-line change with no risk to existing auth flows. Same pattern can be applied selectively to SPARQL endpoints if needed later. | No | agent |
| D166 | M014 | arch | Extension form rendering approach — client-side vs server-rendered | Client-side JS SHACL renderer (Option A from design doc). Extension fetches shape JSON from `GET /api/shapes/{type_iri}` and renders forms in vanilla JS. No server-side HTML rendering for extension forms. | Avoids a network round-trip for form HTML, enables form rendering from cached shapes (works offline-ish), keeps the extension self-contained with no dependency on server-rendered partials. The JS renderer handles the common SHACL property types used in standard Mental Models — not every edge case. Reference implementation is the 205-line Jinja2 `_field.html` macro. | No | agent |
| D167 | M014 | arch | Relationship picker search backend — context-query vs SPARQL endpoint | Use context-query endpoint (`POST /api/context-query`) for relationship picker search in v1. Skip SPARQL endpoint Bearer auth fix — not needed for Phase 1. | Context-query already supports Bearer auth (M013). Adding Bearer auth to 19 SPARQL router endpoints is high-effort with low Phase 1 benefit. Context-query returns objects with IRIs, labels, and types — sufficient for relationship search. Client-side type filtering is acceptable for v1 result sets. SPARQL auth can be added in Phase 2 (M015) if needed. | Yes — add SPARQL Bearer auth when Phase 2 (M015) needs flexible queries | agent |
| D168 | M014 | scope | Extension distribution method for Phase 1 | Sideload-only distribution for Phase 1. No Chrome Web Store or Firefox Add-ons submission. | Web Store review adds latency, code review requirements, and privacy disclosures. `http://localhost:*/*` in host_permissions may be rejected. Sideloading is sufficient for self-hosted users and development iteration. Web Store submission planned for later when stable. | Yes — submit to Web Store once stable | agent |
| D169 | M014 | tech | Extension tech stack and build approach | Vanilla JS only — no React, Vue, or build step. Plain HTML/CSS/JS in the extension directory. No bundler, no transpiler, no npm. | Chrome MV3 Content Security Policy forbids `eval()` and inline scripts. A build step adds complexity for a <2k LOC extension. Vanilla JS is consistent with the SemPKM frontend (htmx + vanilla JS). The SHACL renderer is imperative DOM manipulation which is natural in vanilla JS. No framework dependency to manage or update. | Yes — add build step if extension grows beyond Phase 1 scope | agent |
| D170 | M014 | tech | Object + edge creation sequencing in extension | Two-step sequential creation: object.create first → get IRI from response → edge.create second. No client-side IRI minting. | Client-side IRI minting requires replicating `mint_object_iri` logic and is fragile if the minting scheme changes. Two-step sequential is simpler, guaranteed correct, and the latency of two API calls is negligible for a user-initiated save action. If the first succeeds but the second fails, the user has an object without the relationship — show a clear error and let them retry. | Yes — switch to batch with client-minted IRIs if latency becomes an issue | agent |
| D178 | M009/S07 | architecture | App uninstall triplestore data cleanup strategy | Best-effort SPARQL cleanup (try/except with WARNING log). Uninstall proceeds even if triplestore queries fail. Three queries: DELETE subjects with app IRI prefix, DELETE objects with app IRI prefix, CLEAR app state graph. | Uninstall must never be blocked by a transient triplestore error. Orphaned triples are harmless (namespaced under urn:sempkm:app:{id}:). Admin can re-run cleanup manually if needed. The alternative — rolling back the DB deletion on SPARQL failure — would leave the system in an inconsistent state where the app row exists but the process is already stopped. | Yes | agent |
| D179 | M010/S01/T01 | architecture | IRI prefix enforcement scope in SDK CommandClient | _check_iri_prefix() only enforces prefix on urn:sempkm:app:* and urn:sempkm:data:* namespaces. Model types (urn:sempkm:model:*), user-types (urn:sempkm:user-types:*), and standard vocabularies (http://, https://) pass through unchecked. | The original implementation rejected ALL IRIs not matching the app's own prefix, blocking apps from referencing model-defined types (the #1 platform risk for M010). Scoping enforcement to only app/data namespaces preserves sandboxing while allowing legitimate cross-namespace references. | No | agent |
| D180 | M010/S01/T03 | architecture | RSS article IRI minting strategy | Article IRI minted via SHA-256 hash of (feed_iri + entry_id) producing urn:sempkm:app:rss-reader:article-{hash16} | Using feed_iri (not feed_url) makes IRIs stable even if the feed URL redirects. The feed_iri is the canonical subscription object IRI, which is fixed. SHA-256 prevents collision while keeping IRIs deterministic for dedup. | No | agent |
| D181 | M010/S01/T02 | architecture | FeedSubscription browser visibility in rss-feeds model | FeedSubscription has browserVisible: false in the rss-feeds model manifest | FeedSubscriptions are managed by the RSS reader app, not manually created by users. Showing them in the object browser would clutter the user's workspace with implementation-detail objects. They remain SPARQL-queryable and editable by the app. | Yes | agent |
| D182 | M010/S02/T03 | architecture | Subscription unsubscribe strategy for RSS feeds | Soft-delete via isActive=False patch on FeedSubscription, not hard delete | Existing articles reference their feed subscription IRI via rss:fromFeed. Hard-deleting the subscription would orphan those references. Soft-delete preserves article provenance chain while preventing future polling. Admin can still query inactive subscriptions via SPARQL. | Yes — switch to hard delete + cascade if article cleanup is implemented | agent |
| D183 | M010/S04/T02 | architecture | Navigate command enrichment for app pages in command palette | commands_list() iterates manifest.ui.pages comparing page.path == cmd.path; on match adds appId and pageId to JSON. JS handler branches on cmd.appId to call openAppPageTab() instead of window.location.href | Exact path matching (no prefix/fuzzy) is simple and correct — app pages have unique paths. Adding appId/pageId to the JSON response lets the frontend make the right choice (dockview tab vs full navigation) without hardcoding app-specific logic in JS. This pattern works for all future apps with navigate commands. | Yes | agent |
| D184 | M010/S01/T02 | arch | FeedSubscription browserVisible setting | FeedSubscription has `browserVisible: false` in rss-feeds model manifest — hidden from object browser | FeedSubscriptions are managed by the RSS reader app, not by users directly. Showing them in the object browser would clutter the UI with infrastructure objects. They remain queryable via SPARQL and linkable via edges, just not browsable. | Yes — make visible if users need to manually manage subscriptions | agent |
| D185 | M010/S02/T01 | architecture | Feed format dispatch strategy for RSS reader | parse_feed_content() dispatches by HTTP Content-Type: JSON types → custom parse_json_feed() parser, all XML types → feedparser. JSON Feed entries are normalized to SimpleNamespace objects with feedparser-compatible attribute names. | feedparser doesn't support JSON Feed format. A content-type dispatcher keeps the parsing layer format-agnostic — callers don't need to know whether the feed was RSS, Atom, or JSON Feed. SimpleNamespace provides attribute access (.title, .link) matching feedparser's FeedParserDict, so entry_to_article() works identically for all formats. | No | agent |
| D186 | M010/S02/T02 | architecture | trafilatura optional dependency strategy for article content extraction | Module-level try/except import guard with HAS_TRAFILATURA boolean flag. When trafilatura is not installed, extract_article_content() returns None immediately (graceful degradation). Feed summary is used as fallback content. | trafilatura has heavy C extension dependencies (lxml). Making it optional ensures the RSS reader app starts and functions even if trafilatura fails to install in the Docker container. The HAS_TRAFILATURA flag is inspectable at runtime for diagnostics. Feed-provided summaries are always available as fallback content. | Yes — make required once Docker build is proven stable with trafilatura | agent |
| D187 | M010/S02/T02 | architecture | Conditional GET (ETag/Last-Modified) for feed polling bandwidth optimization | fetch_feed() sends If-None-Match and If-Modified-Since headers from stored per-subscription etag/lastModifiedHeader values. On 304, returns (None, headers, 304) — caller skips parsing. On success, update_subscription_state() persists new etag/lastModified for next cycle. | Polling feeds every 5 minutes generates redundant traffic when feed content hasn't changed. Conditional GET lets servers return 304 Not Modified (no body), saving bandwidth and parsing time. Per-subscription state stored as RDF predicates on FeedSubscription objects so conditional GET state persists across app restarts. | No | agent |
| D188 | M010/S06/T02 | documentation | RSS Reader user guide chapter number | Numbered as Chapter 32 (not Chapter 30 as originally planned) — chapters 30 (Workspace Personas) and 31 (API Surface) already existed when S06 executed | The S06 plan assumed ch.29 was the last numbered chapter, but M012/S04 and M013/S03 had already created chapters 30 and 31 respectively. Maintaining sequential numbering required ch.32. Navigation chain adjusted: ch.31 → ch.32 → Appendix A. | No | agent |
| D189 | M014/S02 | tech | Title extraction strategy for dynamic SHACL forms in extension popup | 4-priority cascade: (1) property path containing "title" or "dcterms:title", (2) path containing "name" (firstName, lastName), (3) first required field value, (4) any non-empty value. Concatenates name parts for CRM-style types. | Different Mental Model types use different title conventions — basic-pkm uses dcterms:title, CRM Contact has firstName/lastName but no title field, Task has dcterms:title. A cascade handles all cases without type-specific branching. Falls through gracefully — validation only requires a non-empty title, the cascade almost always finds something. | Yes | agent |
| D190 | M014/S03 | architecture | Content script injection strategy for browser extension page data extraction | chrome.scripting.executeScript({func: extractPageData}) with imported function reference — single return value, no message-passing, no persistent content script | Cleaner than {files:} approach which requires message-passing to get data back. The func parameter serializes/deserializes the function body, so the extractor must be fully self-contained (var declarations, no closures, no imports). This avoids a persistent content script that would run on every page load. The popup injects on-demand only when opened. | Yes | agent |
| D191 | M014/S03 | architecture | Context menu to popup data bridge for "Save to SemPKM" right-click action | Context menu handler stores {selectionText, pageUrl, pageTitle} in chrome.storage.session, then opens popup via chrome.action.openPopup(). Popup checks session storage on init, consumes and clears data. | Service worker and popup are separate execution contexts — no shared memory. chrome.storage.session is ephemeral (cleared on browser restart), fast, and doesn't pollute sync storage. The consume-and-clear pattern prevents stale data from affecting subsequent popup opens. chrome.action.openPopup() is the standard MV3 approach; chrome.windows.create() fallback handles the case where openPopup() isn't available. | Yes | agent |
| D192 | M014/S03 | architecture | Schema.org to SHACL form property mapping priority | Cross-namespace mappings (CROSS_NS_MAP) take priority over direct schema.org namespace matches in mapSchemaOrgToFormValues — first-write-wins prevents overwriting specific mappings with generic ones | A schema.org Person has both schema:givenName and a CRM model might have crm:firstName. The cross-namespace map explicitly encodes domain-specific intent (givenName → crm:firstName), which should take priority over a generic direct-namespace match. First-write-wins means the cross-namespace mapping is checked first, and if it populates a path, the direct match skips it. | Yes | agent |
| D193 | M014/S04 | architecture | Edge creation error handling strategy in extension two-step save | Per-edge try/catch with warning toast showing failure count — edge failures never block object creation success | Object creation is the primary user intent. Edges are supplementary relationship metadata. If the API is partially available or a single edge call fails due to a transient error, losing the entire object would be worse than losing a relationship. The user sees a clear warning toast with the failure count, so they know to re-add the relationship manually. Console logs identify exactly which edge failed for debugging. | Yes | agent |
| D194 | M015 | arch | Sidebar implementation strategy — Chrome Side Panel API over content-script Shadow DOM injection | Use Chrome's native `chrome.sidePanel` API for Chrome and `sidebar_action` manifest key for Firefox. Sidebar HTML/JS/CSS shared between both browsers. | Side Panel API provides a dedicated browser surface that doesn't conflict with page CSS, persists across navigation, and has full access to Chrome extension APIs. Shadow DOM injection requires fighting with arbitrary page CSS, doesn't persist across navigation, and has limited API access. The cross-browser gap is manageable with the existing dual-manifest pattern from M014. | No | agent |
| D195 | M015 | arch | Popup and sidebar coexistence strategy — keep popup for capture, sidebar for context | Do NOT use `sidePanel.setPanelBehavior({openPanelOnActionClick: true})` which replaces the popup. Instead, open sidebar programmatically via Alt+K keyboard shortcut (new `commands` entry) or from a "Show Context" button inside the popup. Icon click continues to open the capture popup. | Users need both flows: quick capture (popup via icon click / Alt+S) and context browsing (sidebar via Alt+K). Replacing one with the other reduces functionality. Separate entry points serve distinct user intents. | No | agent |
| D196 | M015 | arch | Context query signal splitting — separate URL, title, and keywords fields | Add `contextQuery({url, title, keywords})` method to SemPKMClient that sends each field separately to POST /api/context-query, replacing the single-string `searchObjects()` approach | The context-query endpoint already accepts `url`, `title`, and `keywords` as separate fields. Sending the same string as both title and keywords (current behavior) misses URL matching entirely and produces suboptimal keyword results. Separate signals enable URL exact match (highest confidence) alongside keyword search. | No | agent |
| D197 | M015 | arch | Client-side result ranking strategy — URL match > title match > keyword match | Results ranked on the extension side before rendering: URL matches first (exact property match), then title matches, then keyword matches. Truncate to top 10. | The context-query endpoint returns flat results with match_type field but no ranking. Client-side ranking is simpler than modifying the backend endpoint and can be tuned per-UI without API changes. Top 10 prevents sidebar overflow from false positives. | Yes — move ranking to backend if needed across multiple clients | agent |
| D198 | M015 | tech | Service worker context cache — in-memory Map with LRU eviction, no persistence | URL→results cache as a plain Map in service worker memory. Max 100 entries with LRU eviction. Lost on service worker termination (MV3 lifecycle). No chrome.storage persistence. | Cache is a performance optimization, not a correctness requirement. Re-querying on cache miss is cheap against a local instance. Persisting to chrome.storage would add read/write latency and storage management for ephemeral data. MV3 service workers restart cleanly. | Yes — add chrome.storage.session persistence if re-query latency is noticeable | agent |
| D199 | M016 | arch | Linear sync app authentication strategy — OAuth vs API key | Support both OAuth 2.0 (with mandatory refresh tokens) and personal API key auth. OAuth for multi-user/production, API key for personal/local dev. API key entered in settings page; OAuth via redirect flow through app proxy callback URL. | Linear mandates refresh tokens for OAuth2 apps created since Oct 2025, with 24h access token expiry. API key auth is simpler and avoids OAuth callback routing complexity for local dev. Supporting both maximizes usability: API key for quick personal setup, OAuth for production deployments where multiple users authenticate independently. | No | agent |
| D200 | M016 | arch | Linear sync mechanism — polling vs webhooks | Polling-only for v1. No webhook endpoint. poll-tasks scheduled task fetches issues via GraphQL with updatedAt filter for delta sync. | The App Platform doesn't expose app routes to external traffic — only platform-proxied internal routes. Linear webhooks can't reach localhost for local dev. Polling with updatedAt delta cursor covers the core sync UX. Webhooks can be added when the platform gains external webhook routing. | Yes — add webhooks when platform supports external webhook routing | agent |
| D201 | M016 | tech | Linear GraphQL client approach — httpx direct vs gql library | Use httpx directly via SDK's HttpClient for GraphQL POST requests to https://api.linear.app/graphql. No gql library. | Linear's GraphQL API is a single POST endpoint. The SDK's HttpClient already wraps httpx with domain enforcement. Adding the gql library brings dependency weight (graphql-core, etc.) for no benefit over raw POST with JSON query strings. Query construction is simpler as string templates with variable interpolation. | Yes — add gql if query complexity warrants it | agent |
| D202 | M016 | arch | Linear sync conflict resolution strategy | Status: provider wins (Linear state.type is authoritative). Title/description: last-write-wins based on dcterms:modified vs Linear updatedAt. Compare lastSyncedAt to detect which side changed. | Linear's state.type enum is a cleaner status model than free-text — letting the provider win for status avoids lossy round-trips. For title/description, last-write-wins is pragmatic for a single-user self-hosted app where true conflicts are rare. lastSyncedAt timestamp comparison detects which side changed since last sync. | Yes — add field-level merge if multi-user conflicts become common | agent |
| D203 | M016/S01 | tech | Linear app unit test approach — importlib loading from apps/ directory | Use importlib.util.spec_from_file_location to load app modules from apps/linear-sync/services/ into backend pytest tests. Mock SDK clients (HttpClient, StateClient) with simple async classes. | App code lives in apps/ (outside backend/), not importable via normal Python paths. The test-app has no unit tests, but the research doc recommends the importlib pattern used by M010 RSS Reader. Mocking SDK clients avoids platform runtime dependencies and makes tests fast and deterministic. | Yes | agent |
| D204 | M016/S02 | arch | Sync engine bulk creation strategy for platform-minted IRIs | Two-phase bulk: phase 1 submits object.create commands (no IRI needed, platform mints it), phase 2 re-queries SPARQL to discover the minted IRIs then submits body.set/edge.create commands referencing those IRIs. All commands bypass SDK's CommandClient via direct POST to /api/commands/bulk to avoid IRI prefix checking on patch/body/edge commands. | The App SDK's CommandClient enforces an IRI prefix check (urn:sempkm:app:{appId}:) on object.patch, body.set, body.diff, and edge.create commands. Platform-minted Task IRIs use urn:sempkm:object: prefix, which the SDK rejects. Bypassing via ctx.commands._client (the underlying httpx.AsyncClient) avoids the prefix check while still using the platform's bulk command endpoint. The two-phase approach is needed because object.create returns no IRI — the platform assigns it — so body/edge commands for new objects must wait until the IRI is discoverable via SPARQL. | Yes — once the SDK supports an IRI prefix override or the platform returns created IRIs in bulk response | agent |
| D205 | M016/S03/T02 | arch | Push sync loop prevention mechanism | pull_sync compares issue updatedAt against task's bpkm:lastSyncedAt — skips issues where updatedAt ≤ lastSyncedAt. String comparison on ISO-8601 timestamps. push_sync updates lastSyncedAt after each successful issueUpdate mutation. | When push_sync sends a change to Linear, the next poll sees that issue as updated. Without loop detection, pull_sync would re-import the pushed change. Comparing timestamps is simple and correct because both Linear updatedAt and bpkm:lastSyncedAt use ISO-8601 format where lexicographic ordering equals temporal ordering. lastSyncedAt is set after the mutation completes, so the next poll's updatedAt will be ≤ lastSyncedAt for push-originated changes. | Yes — may need server-side mutation tracking if timestamp granularity causes issues | agent |
| D206 | M017 | arch | GitHub sync authentication — PAT-only for v1, no OAuth App | Personal Access Token only for v1. No GitHub OAuth App registration or OAuth code exchange flow. | PAT is simpler, matches the M016 API key path, and avoids OAuth callback routing (same localhost limitation as D200). Users create a PAT with `repo` scope in GitHub Settings. OAuth App flow adds client_id/secret management, callback URL configuration, and token refresh complexity — all for the same functional outcome (API access). Can add OAuth later if multi-user production deployments need it. | Yes — add GitHub OAuth App flow for production multi-user | agent |
| D207 | M017 | tech | GitHub REST pagination — manual Link header parsing, no library | Parse `Link: <url>; rel="next"` from response headers with simple regex/split. No pagination library. | Link header format is trivial (~5 lines to parse). Adding a library for one regex is overhead. httpx exposes response.headers directly. Same approach used throughout SemPKM codebase — keep it simple. | No | agent |
| D208 | M017 | arch | PR-to-issue linking via timeline API cross-referenced events | Use GET `/repos/{owner}/{repo}/issues/{number}/timeline` to find `cross-referenced` events where `source.issue.pull_request` exists. Create `bpkm:dependsOn` edges from PR task → issue task. | Text parsing of "Closes #42" in PR bodies is fragile (multiple variations, localization, false positives). The timeline API returns structured events that definitively identify cross-references. The `cross-referenced` event type includes the source PR reference, making link direction unambiguous. | No | agent |
| D209 | M017 | scope | GitHub sync requirement IDs use GH- prefix | New requirements numbered GH-01 through GH-07 instead of continuing SYNC- sequence | SYNC-05 through SYNC-07 are already taken by M016 (Linear sync) with different meanings. GH- prefix avoids collision and makes requirements clearly identifiable by provider. Future sync apps should follow: TD- (Todoist), AS- (Asana), JR- (Jira), etc. | No | agent |
| D210 | M018 | arch | Google Calendar sync authentication — full OAuth 2.0, no API key alternative | OAuth 2.0 code exchange with refresh tokens. Client ID/secret from Google Cloud Console. Unverified client for dev. Token refresh automatic on 401 (access tokens expire after 1h). | Google Calendar API v3 has no API key path for user-data access — OAuth is mandatory. First real OAuth in a sync app (Linear used API key D199, GitHub used PAT D206). Callback URL routes through app proxy at /app/google-calendar/_fragments/oauth-callback. | No | agent |
| D211 | M018 | arch | Polling-only sync for Google Calendar v1, no push notification channels | syncToken-based incremental polling. No webhook channels. | Same constraint as D200 — App Platform doesn't expose external webhook routes. Polling with syncToken is highly efficient (returns only changes). Push notification channels require publicly reachable URL + 7-day renewal. Polling matches M016/M017 pattern. | Yes — add push channels when platform supports external webhook routing | agent |
| D212 | M018 | arch | bpkm:Event property set designed as cross-provider superset | Event type includes all enum values from Google + Outlook + CalDAV even if Google doesn't use them all (e.g. showAs: out-of-office, working-elsewhere are Outlook-specific). | The Event type must serve M018 (Google), M020 (Outlook), and M021 (CalDAV). Building the superset now avoids a breaking ontology change when Outlook sync lands. Unused enum values are harmless. | No | agent |
| D213 | M018 | arch | RSVP-only push scope for v1 — no full event creation/edit push | Push sync limited to responseStatus changes. Title, description, time edits NOT pushed to Google. | Narrow push scope reduces complexity and risk. RSVP is the most common bidirectional action. Full event editing push requires field-by-field conflict resolution that exceeds v1 scope. Meeting notes explicitly listed as SemPKM-only in CONTEXT. | Yes — expand push scope if users request bidirectional event editing | agent |
| D214 | M018 | scope | Google Calendar requirement IDs use GCAL- prefix, Event type uses EVENT- | GCAL-01 through GCAL-09 for sync app. EVENT-01 for bpkm:Event type (shared across calendar providers). | Follows D209 pattern — provider-specific prefix avoids collision. EVENT-01 is provider-neutral because the type serves Google, Outlook, and CalDAV. | No | agent |
| D233 | M023 | tech | Jira status normalization strategy | Always use statusCategory.key (new/indeterminate/done) for mapping, never status.name. Store status.name in bpkm:externalStatus for display. | Status names are arbitrarily customizable per Jira project. statusCategory.key is the only reliable normalization point — maps every custom status to exactly one of three categories. This is Jira's designed abstraction for cross-project status comparison. | No | agent |
| D234 | M023 | scope | Jira sync requirement ID prefix | Requirements numbered JIRA-01 through JIRA-12 following D209 (GH-) and D230 (ASANA-) provider-specific prefix pattern. | Provider-specific prefix avoids collision and makes requirements identifiable by provider. | No | agent |
| D235 | M023/S01 | tech | Jira status normalization via statusCategory.key, not status name | Always use statusCategory.key (new/indeterminate/done) for status mapping, never status.name. Store status.name in bpkm:externalStatus for display. | Status names are arbitrarily customizable per Jira project ("In Review", "QA Testing", etc.). statusCategory.key is the only reliable normalization point — maps every custom status to exactly one of three categories. This is Jira's designed abstraction for cross-project status comparison. | No | agent |
| D236 | M023/S01 | arch | Jira sync authentication — API token (email + token as Basic auth) for v1, no OAuth 2.0 3LO | API token only. User generates token at id.atlassian.com/manage-profile/security/api-tokens. Stored as email + token pair via StateClient. Basic auth header: base64(email:token). | Atlassian OAuth 2.0 (3LO) requires registering a Jira app with Atlassian developer console, managing client_id/secret, handling complex callback routing, and scope negotiation. API token is simpler and matches D206 (GitHub PAT) pattern. Sufficient for self-hosted single-user use. OAuth can be added in v2. | Yes — add OAuth 2.0 3LO for multi-user production | agent |
| D237 | M023/S01 | scope | Push sync limited to title/description/priority for v1 — no status transitions | Push sync handles title and priority only. No status transition push — Jira requires discovering valid workflow transitions per issue via GET /transitions then POST /transitions with transition ID. | Jira requires valid transition IDs for status changes (unlike Linear/GitHub where you set any status directly). Per-project transition discovery and validation is a separate concern from basic field sync. Title/description/priority PATCH is sufficient for v1 bidirectional capability. | Yes — add status transition push in v2 | agent |
| D238 | M023/S01 | tech | Jira person resolution requires extra API call per unique accountId | Jira v3 API uses opaque accountId (not email). Getting email requires GET /rest/api/3/user?accountId=X. Cache aggressively via PersonMatcher LRU cache — each unique user looked up once per sync run. | Jira's GDPR-compliant API removed email from issue response data. The extra API call is unavoidable but the LRU cache (same pattern as all other sync apps) means each unique user is looked up only once per sync run. | No | agent |
| D239 | M023/S01 | tech | ADF ↔ Markdown conversion — custom recursive converter, not library | Hand-roll a ~400-line recursive converter covering ~12 common ADF node types (paragraph, heading, bulletList, orderedList, codeBlock, blockquote, table, text with marks, mention, inlineCard, mediaGroup, rule) plus reverse direction. Unknown node types emit [unsupported: {type}] placeholder. | No well-maintained Python ADF↔MD library exists. The node types are well-documented and finite. A focused converter covering common types is more reliable than depending on an unmaintained package. The reverse direction (MD→ADF) only needs to handle the Markdown subset SemPKM produces. | Yes — switch to library if a well-maintained one appears | agent |
| D240 | M023/S03 | tech | Jira issue link deduplication strategy for dependsOn edges | Process only inwardIssue entries from "Blocks" links (the blocker→blocked direction). Skip outwardIssue to avoid creating duplicate edges. | Each Jira blocking relationship appears in both issues' issuelinks arrays — as outwardIssue on the blocker and as inwardIssue on the blocked. Processing both directions would create duplicate bpkm:dependsOn edges. Filtering to inward-only (current issue "is blocked by" the inward issue) ensures one edge per blocking relationship while maintaining the correct dependency semantics. | No | agent |
| D241 | M024 planning | arch | LoopGuard as lightweight TTL cache module for echo prevention | Standalone loop_guard.py with LoopGuard class tracking (item_id, column_id) → timestamp with configurable TTL (default 30s). In-memory only. | Monday.com has no webhook suppression. For v1 polling, push→poll echo occurs within same process lifetime. Simple in-memory TTL dict is sufficient and highly testable. | Yes — promote to shared platform utility if multiple apps need echo prevention | agent |
| D242 | M024/S02 | arch | Monday.com column mapping per-board storage keys | Column mapping stored at `column_mapping_{board_id}` and label mapping at `label_mapping_{board_id}` as JSON in settings. Label mapping contains nested `status_label_mapping` and `priority_label_mapping` sub-dicts. | Monday.com boards are independently configurable — each board has unique columns, status labels, and priority labels. Per-board keys allow a single app installation to sync multiple boards with different mapping configurations. Follows the D228 "configure before sync" pattern established by Asana. Nested label mapping structure keeps status and priority mappings co-located per board for atomic read/write. | No | agent |
| D243 | M024/S02 | arch | Monday.com group→taskGroup sourced from item.group, not column_values | Group title sourced from `item["group"]["title"]` in GraphQL response. Subitems fetched via separate `get_subitems(item_ids)` call. Group is structural metadata, not a column value. | Monday.com groups are structural containers, not user-configurable columns. They appear in the items query under `group { id title }` but are absent from `column_values`. Using `item.group.title` directly (not via column mapping) is correct because the group assignment comes from item placement, not from a column value the user can remap. Similarly, subitems are separate items requiring a distinct API call, matching the structural relationship model. | No | agent |
| D244 | M025 planning | arch | DEMO_MODE env var for anonymous access — synthetic guest user bypass | `DEMO_MODE=true` env var makes `get_current_user` return a synthetic read-only guest user without session/cookie. No login page shown. | Option B from research — cleaner than pre-seeded demo user with auto-login cookie. Synthetic user gets `role: "guest"` which already has restricted permissions. Combined with nginx write-blocking, this is defense-in-depth. ~15 lines in auth/dependencies.py, no new endpoints needed. | No | agent |
| D245 | M025 planning | arch | Read-only enforcement via nginx default-deny on non-GET methods | Block all POST/PUT/DELETE/PATCH at nginx level for all routes except health check. Return 403 JSON `{"error": "Demo instance is read-only"}`. | Default-deny is safer than endpoint-by-endpoint allowlisting — missing one POST route allows data corruption across visitors. Research identified 20+ mutation endpoints; blocking by HTTP method catches all current and future write routes with ~10 lines of nginx config. | No | agent |
| D246 | M025 planning | arch | Caddy reverse proxy for SSL termination, not nginx SSL config | Caddy runs on host, proxies to Docker's nginx on port 3000. Handles Let's Encrypt automatically with zero config. | Avoids modifying nginx.conf for SSL — Caddy's automatic HTTPS is simpler than Let's Encrypt + certbot + nginx SSL blocks. Caddy also handles certificate renewal automatically. Keeps the demo nginx.conf identical to the local dev config. | No | agent |
| D247 | M025 planning | scope | Demo requirement IDs use DEMO- prefix | 10 requirements numbered DEMO-01 through DEMO-10 covering anonymous access, read-only, sample data, tour, dashboard, CTA, deployment, reset, monitoring, validation. | Follows established provider-specific prefix convention (D209, D214, D216, D221, D226, D230, D234). | No | agent |
| D248 | M025 planning | scope | M025 is deployment + content — no new platform features | DEMO_MODE is a configuration flag (~15 lines), not a feature. Same Docker Compose 3-service stack. No new containers except optional Caddy for SSL. | Research confirmed every pattern needed (Driver.js tours, dashboard builder, seed data JSON-LD) is already exercised. Platform feature changes would increase risk for no benefit. | No | agent |
| D249 | M025/S01/T01 | architecture | How to bypass FastAPI Depends() chain in get_current_user for DEMO_MODE | Replaced Depends(get_session_token) with inline Cookie(None) extraction in get_current_user, so demo_mode check runs before any 401 can fire. get_session_token preserved for logout endpoint. | FastAPI evaluates Depends() arguments before the function body runs. The old get_session_token dependency raises 401 when no cookie is present, which would fire before any demo_mode check in the function body. Inlining the cookie extraction as an optional parameter lets the function body execute first, where the demo_mode guard returns the synthetic user immediately. | Yes | agent |
| D250 | M025/S01/T03 | architecture | How to handle setup wizard redirect in DEMO_MODE | Short-circuit /api/auth/status to return setup_complete=true, setup_mode=false when DEMO_MODE=true | DEMO_MODE auth bypass (T01) prevents 401s but doesn't prevent the client-side JS setup wizard redirect. Fresh demo instances have setup_mode=true, causing auth.js to redirect to /setup.html before auth can take effect. The fix is at the status endpoint (not in JS or middleware) because it's the single source of truth the client checks, and it avoids modifying shared frontend code. | Yes | agent |
| D251 | M025/S01/T02 | tech | nginx JSON response for write-blocking in demo mode | error_page 495 + named @read_only location pattern — custom error code 495 (unused by nginx) triggers internal redirect to named location that sets default_type application/json and returns 403 with JSON body | Bare `return 403` inside nginx `if` blocks always sends `text/plain` content-type — there's no way to set Content-Type: application/json with that pattern. The `limit_except` directive doesn't work at server level. The error_page + named location approach is the canonical nginx solution for returning JSON error responses from method guards. | Yes | agent |
| D252 | M025/S02 planning | architecture | How the demo seed script bypasses HTTP auth and nginx write-blocking | Script runs inside the API container via `docker compose exec` using direct Python module imports (TriplestoreClient, EventStore, ModelService) — bypasses both HTTP auth and nginx | Three alternatives were considered: (1) HTTP API calls to port 8902 (bypasses nginx but requires owner auth — DEMO_MODE guest user lacks install/command permissions), (2) two-phase deployment (start without DEMO_MODE, seed, restart with DEMO_MODE — fragile, compose file hardcodes DEMO_MODE=true), (3) direct module imports inside container (works regardless of DEMO_MODE since model install and EventStore.commit don't check the flag). Option 3 is simplest and most reliable — follows the same pattern as ensure_starter_model() at app startup. | No | agent |
| D253 | M025/S03 | architecture | Deterministic UUID for demo dashboard shared between tour JS and seed script | Well-known UUID `aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee` hardcoded in both `tutorials.js` (tour step 6) and `seed-demo-data.py` (Phase 4/5 dashboard creation) | The demo tour's step 6 calls `openDashboardTab(dashboardId, name)` which needs to know the dashboard ID at JS runtime. Using a deterministic UUID rather than discovering it at runtime (which would need an API call) keeps the tour code simple and avoids async complexity. The seed script uses the same UUID when creating the dashboard, making it a shared constant. This is a pattern already used in the codebase (demo user UUID `00000000-...`). | Yes | agent |
| D254 | M026 planning | arch | No static site generator — vanilla HTML/CSS/JS for homepage rewrite | Rewrite docs/index.html in-place with vanilla HTML/CSS/JS. Extract shared CSS to docs/styles.css. Add 3 persona sub-pages as separate HTML files. No Jekyll, Hugo, Astro, or any SSG. | The existing docs/ directory is served directly by GitHub Pages with no build step. This is consistent with the project's "no build step" frontend philosophy (htmx + vanilla JS). At most 4 HTML files sharing one CSS file — an SSG adds tooling complexity for negligible benefit. | No | agent |
| D255 | M026 planning | scope | Homepage messaging grounded in USER-CONVERSION-STRATEGY.md, not invented | All hero copy, persona paths, competitive positioning, and feature framing derived from .gsd/design/USER-CONVERSION-STRATEGY.md. No ad-hoc marketing copy. | The strategy doc contains researched competitive analysis, persona definitions, and messaging shifts. Using it as the content source ensures consistency and avoids subjective copywriting. Can iterate on copy post-launch based on user feedback. | No | agent |
| D256 | M026 planning | scope | SITE requirement IDs for homepage-specific requirements | New requirements SITE-01 through SITE-07 covering homepage rewrite, persona paths, competitive positioning, domain kits, screenshots, mobile/performance, and SEO. | These are new scope not covered by existing requirements. SITE- prefix clearly identifies them as marketing site concerns separate from app functionality. SITE-04 through SITE-07 are quality attributes naturally embedded in the main work. | No | agent |
| D257 | M026 planning | scope | Screenshots captured from M025 demo stack, not fresh Docker build | Use the M025 demo Docker stack (docker-compose.demo.yml with seed data) as the screenshot source, not a dev stack or production instance. | The demo stack has 74 pre-populated objects across 4 Mental Models, a demo dashboard, and represents the "best foot forward" state of the app. Screenshots from this stack match what the "Try the Demo" CTA links to. | No | agent |
| D258 | M027 planning | arch | Notion importer follows Obsidian pattern — parallel module, not shared imports/ extraction | New `backend/app/notion/` module (scanner, executor, router, models, broadcast) mirroring `backend/app/obsidian/` structure. Copy-and-adapt broadcast.py rather than extracting shared `imports/` module. | Copy-and-adapt is lower risk than refactoring the working Obsidian importer. Both modules are small (~5 files each). Shared extraction can happen in a follow-up if a third importer is added. | Yes — extract shared imports/ module if a third importer is built | agent |
| D259 | M027 planning | scope | Classify step deferred — auto-classify databases as structured, standalone pages as content | Research doc proposes a 5-way classification step (structured/content/dashboard/skip/reference). M027 auto-classifies: CSV databases = structured types, standalone .md pages = content (Notes). Users can skip individual items in the type mapping step. | Classification adds wizard complexity without proportional value for v1. Auto-classification covers 90%+ of real Notion exports. Users who need finer control can skip items. Dashboard/rollup classification deferred per CONTEXT.md scope. | Yes — add explicit classification step if user feedback shows auto-classify is insufficient | agent |
| D260 | M027 planning | scope | NOTION requirement IDs for Notion import | 3 requirements: NOTION-01 (ZIP import), NOTION-02 (database→type mapping), NOTION-03 (relation→edge resolution). NOTION-01 was already registered as deferred — M027 activates it. | Follows provider-specific prefix convention. NOTION-01 already exists in REQUIREMENTS.md as deferred. | No | agent |
| D261 | M027 planning | tech | CSV parsing uses stdlib csv with utf-8-sig encoding, dates via python-dateutil | Python stdlib `csv` module with `encoding='utf-8-sig'` for BOM handling. Date parsing via `dateutil.parser.parse()` (already in deps). No new dependencies. | Notion exports CSVs in UTF-8 with BOM. dateutil handles locale-dependent date formats robustly. Both are proven tools already in the project. | No | agent |
| D262 | M027 planning | arch | Relation resolution is title-based with ambiguity warnings | CSV exports contain page title strings, not Notion IDs. Scanner detects relations by checking if column values are a subset (>80%) of another DB's row titles. Executor resolves by title→IRI dict lookup after Pass 1. Duplicate titles produce warnings in import summary, with first-match behavior. | This is a fundamental limitation of Notion's ZIP export format — no alternative exists for ZIP-only import. The 80% heuristic for detection is conservative. Warning users about ambiguous matches is the right UX — they can fix duplicates before re-importing. | Yes — switch to ID-based resolution when Notion API integration is added | agent |
| D277 |  | tech | QUIC/HTTP/3 for self-hosted Docker deployment | Defer — nginx:stable-alpine lacks HTTP/3 support. Document rationale, revisit when nginx stable adds HTTP/3 or Caddy migration is separately motivated. | nginx:stable-alpine does NOT include HTTP/3 module (requires --with-http_v3_module + BoringSSL). nginx:mainline-alpine has experimental HTTP/3 but not stable. Self-hosted single-user tool over localhost gets minimal benefit from QUIC multiplexing and 0-RTT. HTTP/2 over h2c (cleartext) has limited browser support. Caddy supports HTTP/3 out of the box and is already used for the demo instance (D246), but switching the main Docker stack is a separate decision with its own trade-offs. Cost of implementing exceeds benefit for the current deployment model. | Yes — revisit when nginx:stable-alpine adds HTTP/3 or Caddy migration is separately motivated | agent |
| D278 | M030 planning | arch | Cross-model data quality rule placement — attach to basic-pkm with broad target patterns | Place cross-model rules (comma-in-tags, titleless objects, orphan objects) in basic-pkm's rules/basic-pkm.ttl with broad sh:targetSubjectsOf or rdfs:Resource patterns, not in a new platform rules graph | basic-pkm is auto-installed as the starter model — always present. A platform rules graph (Option A from research) would require loader changes (new graph IRI pattern, separate loading logic). Attaching to basic-pkm is zero-infrastructure and follows the existing pattern where basic-pkm already has cross-cutting rules (overdue tasks). Migrate to platform rules graph later if a non-basic-pkm-only deployment becomes possible. | Yes — migrate to platform rules graph if basic-pkm becomes optional | agent |
| D279 | M030 planning | arch | Lint filter storage in SQLite with server-side Python filtering | Three new SQLite tables (lint_suppressions, lint_dismissals, lint_presets) via Alembic migration. LintService applies Python post-filtering after SPARQL query returns results. | Suppressions/dismissals are user preferences, not knowledge graph data — SQLite is the natural home (alongside auth, favorites, personas). Server-side Python filtering is clean because the SPARQL queries for lint results are already complex; adding SQL-sourced filter lists to SPARQL would require cross-database joins (impossible). Python post-filtering is performant for typical result sets (~50-200 results). | No | agent |
| D280 | M030 planning | arch | Lint filter preset schema — additive suppression model | Presets store a list of suppressed rule source shape IRIs (start from "show all", suppress some). Not restrictive (start from "show none", enable some). | Additive is simpler and matches the natural "hide the noisy ones" workflow. Users start seeing everything and progressively suppress rules they don't care about. The preset captures what to hide, not what to show. | Yes — switch to restrictive if users need "show only X" workflows | agent |
| D281 | M030 planning | scope | LINT requirement IDs for data quality linting | New requirements LINT-08 through LINT-20 covering pipeline fix (LINT-08), 9 data quality rules (LINT-09 through LINT-17), and lint filter system (LINT-18 suppress, LINT-19 dismiss, LINT-20 presets). Continues from LINT-07 (last M011/v2.4 lint requirement). | LINT prefix established by v2.4 lint dashboard requirements (LINT-01 through LINT-07). Continuing the sequence maintains traceability. | No | agent |
| D282 | M030 planning | scope | Orphan object rule as SHACL-AF with performance monitoring, not SavedQuery | Implement orphan detection as a SHACL-AF SPARQLConstraint rule (same as other 8 rules), not as a SavedQuery. Monitor performance during S02 — if >5s on Ideaverse-scale data, convert to SavedQuery as a follow-up. | Consistency: all 9 rules use the same mechanism. Users see orphan results alongside other lint results without running a separate query. The performance concern is speculative — measure first, optimize if needed. | Yes — convert to SavedQuery if performance is unacceptable | agent |
| D284 | M031 planning | arch | Model-declared view variant access after carousel removal | View toolbar dropdown shows model-declared ViewSpecs when a type filter pill is active. Model-declared views also appear in the Saved Views folder. Two surfaces replace the single carousel. | The carousel is the ONLY current surface for model-declared ViewSpecs (VIEW-05). Simply removing it loses access to type-specific views like "Projects Table". The view toolbar already exists and is the natural home for a variant selector alongside the scope dropdown. Adding entries to Saved Views provides a second discovery path. Both approaches use existing UI patterns with no new components. | No | agent |
| D285 | M031 planning | scope | M031 requirement ID prefixes and numbering | Requirements numbered VIEW-08 through VIEW-14, SQ-01 through SQ-03, SPARQL-09 through SPARQL-11, ONTO-04 through ONTO-06, and DBUIX-01 through DBUIX-04. Continues existing prefix sequences for views, SPARQL, and ontology; introduces SQ- and DBUIX- for new domains. | VIEW- continues from VIEW-07 (M007). SPARQL- continues from SPARQL-08 (v2.6). ONTO- continues from ONTO-03 (M003). SQ- (Saved Queries) and DBUIX- (Dashboard/Builder UX) are new prefixes for new capability areas. 20 requirements total covering all 6 scope areas. | No | agent |
| D286 | M031 planning | arch | Kanban status field discovery strategy | Kanban uses bpkm:taskStatus for v1 column discovery. Falls back to a hardcoded ["todo", "in-progress", "done", "cancelled"] set. Types without status fields show an empty kanban with a message. | bpkm:taskStatus is the only status-like field with sh:in constraints in the current Mental Models. SHACL shape inspection for arbitrary status fields is complex and adds scope. Hardcoding to the known field covers the primary use case (Task kanban). Can upgrade to SHACL-based field discovery in a follow-up if other models add status fields. | Yes — add SHACL-based status field discovery when other models need kanban | agent |
| D287 | M031 planning | tech | Multiple view instance tab ID strategy | Multiple view instance tab IDs use `generic-view-{renderer}-{uuid}` format. dockview addPanel creates a new panel for each unique ID. The scope query and type filter are stored as panel params. | dockview deduplicates tabs by ID. The current `generic-view-table` ID prevents opening a second table view. Appending a UUID creates unique tabs while preserving the prefix for type identification. Scope and type filter stored as panel params so the view can re-fetch on restore. Matches the pattern used by object tabs (which use IRI-based unique IDs). | No | agent |
| D288 | M031/S01/T02 | arch | Scope filter injection via SPARQL sub-select pattern | Sub-select pattern: `{ SELECT ?s WHERE { <scope_filter> } }` injected into WHERE clause, with variable normalization to ?s via extract_scope_where_body() | Follows VFS build_scope_filter() pattern but normalizes saved query primary SELECT variable to ?s for consistency with view query builders. Sub-select composes well with type filters and SHACL-derived columns. Graceful degradation (render unfiltered) when scope_query is invalid avoids user-facing errors. | No | agent |
| D289 | M031/S02 | tech | Tab ID refinement — Date.now() for unscoped, composite key for scoped | Scoped tabs use `generic-view:{renderer}:scope:{queryId}` (deduplicates same query). Unscoped tabs use `generic-view:{renderer}:{Date.now()}` (every click = new tab). Tab labels append scope name or numeric suffix. | Refines D287 (planning used UUID). Date.now() is simpler than UUID generation in browser JS and collision is impossible from user interaction. Scoped deduplication prevents redundant tabs for the same saved query. Numeric suffixes ("Table View (2)") differentiate unscoped instances visually. | No | agent |
| D290 | M031/S02 | arch | Generic saved views route through openGenericViewTab, query-based through openViewTab | my_views.html uses two code paths: generic saved views (no query or query-scoped) call `openGenericViewTab(renderer, scopeQuery)`, while query-based promoted views keep `openViewTab()`. Unpin uses `deleteSavedView()` for generic and `demoteView()` for query-based. | Generic saved views need the new multi-instance tab system with scope binding. Query-based promoted views already work through the existing openViewTab() path. Separate delete methods because generic views may have no associated query_id — `demote_query()` requires one. Two-path pattern keeps backward compatibility while enabling the new feature. | No | agent |
| D291 | M031/S04 | tech | Kanban status field detection implementation — SHACL sh:in scan vs hardcoded field | Implemented general SHACL sh:in PropertyShape scan in _detect_status_field(), preferring properties with "status" in path (case-insensitive), falling back to first property with non-empty in_values. Evolved beyond D286 which planned hardcoding to bpkm:taskStatus. | The SHACL-based approach is only marginally more complex than hardcoding (~30 lines vs ~10 lines) but automatically supports any Mental Model type that declares sh:in enum constraints on a property. The preference for "status" in the path name provides sensible defaults while the fallback ensures broader compatibility. Unit tests cover all edge cases. | No — strictly better than the hardcoded approach | agent |
| D292 | M031/S05/T01 | arch | SPARQL _VOCAB_PREFIXES strategy — broad urn:sempkm: exclusion vs specific sub-namespace allow-list | Explicit allow-list of ~28 specific internal urn:sempkm: sub-namespaces (query, user, workflow, etc.) that should NOT be enriched as data objects. urn:sempkm:model: is intentionally excluded so model ontology IRIs get enriched and rendered as vocab pills. | The broad "urn:sempkm:" entry caused ALL model ontology IRIs to be treated as internal/vocabulary, preventing pill rendering. An allow-list means new model ontology IRIs automatically get pill treatment. Only new internal machinery namespaces require list updates. | No | agent |
| D293 | M031/S05/T04 | arch | Graph popover rendering — document.body with position:fixed vs z-index within container | Append graph popovers to document.body with position:fixed and z-index:9999, using getBoundingClientRect() for viewport-relative positioning. Cleanup removes popovers from body on graph destroy. | .graph-container creates a stacking context via position:relative. Elevating z-index within that context cannot escape dockview chrome. Appending to document.body fully escapes all stacking contexts. Cleanup in registerCleanup prevents orphaned DOM nodes. | No | agent |
| D294 | M031/S05/T04 | tech | Full-height view layout — shared .view-flex-column flex wrapper vs per-view calc() | Shared .view-flex-column CSS class (display:flex; flex-direction:column; height:100%). Expandable children use flex:1; min-height:0. Used by both graph and kanban views. | Per-view calc() (e.g., calc(100% - 90px)) is fragile — breaks when toolbar height changes. Flex column delegates height distribution to the browser. Shared class keeps CSS DRY for future views. Table/cards use natural scrolling and don't need the wrapper. | No | agent |
| D295 | M032 | arch | Dashboard block layout storage — keep SQLite JSON, not RDF | DashboardSpec.blocks_json stays as SQLAlchemy JSON column. No migration to RDF named graphs. | Existing system has 968 lines across 4 backend modules, 1,076 lines of tests, proven GridStack serialization. Dashboard definitions are per-user config data, not knowledge graph entities. RDF migration would be significant effort with no clear benefit. | Yes — if dashboards need to be shared as linked data | agent |
| D296 | M032 | arch | Widget config schema — keep Python type hints, defer JSON Schema (BLK-06) | BlockTypeSpec.config_schema stays as dict[str, type] validated via isinstance(). JSON Schema formalization deferred. | Python type hints are simpler, work for all planned block types (flat config dicts), and the validation code is already well-tested with 30+ registry tests. JSON Schema adds value only for external plugin systems — not needed now. | Yes — if a plugin system needs machine-readable schemas | agent |
| D297 | M033 planning | planning | M033 slice ordering — all six slices are independent, no inter-slice dependencies | S01: Deployment (medium, data-integrity risk), S02: Isometric graph + icon toggle (high, CSS 3D + Cytoscape), S03: Calendar (low), S04: Map (low), S05: Federated SPARQL console (low), S06: App catalog (low). Each slice touches a different subsystem so no slice depends on another. | Deployment eliminates dangerous example.org default. Isometric graph is highest technical uncertainty (Cytoscape issue #1756). Calendar/map follow established view renderer pattern. Federation backend already exists — only console UI is new. App catalog is least coupled. Independence means slices can be reordered or dropped without affecting others. | No | agent |
| D298 | M033 planning | scope | M033 requirement ID prefixes | DEPLOY-01..04 for deployment, CAL-01..03 for calendar, MAP-01..03 for map, ISO-01..02 for isometric, ICON-01 for graph icons, FED-01..03 for federation, CAT-01..02 for app catalog. 18 requirements total. | Follows established provider-specific prefix convention (D209, D214, etc.). Each feature area gets its own prefix for clear traceability. | No | agent |
| D299 | M033 planning | tech | Research correction — SPARQL console has zero federation UI code | Research claimed console JS has detectServiceEndpoints(), fetchMirrorAllowlist(), handleMirrorClick(), and isEndpointAllowed(). Verified by rg: sparql-console.js (1666 lines) contains no 'mirror', 'SERVICE', 'federation', or 'allowlist' references. Backend mirror service/router exist (410 lines). All federation console UI must be built from scratch in S05. | Grep confirmed zero matches for federation-related terms in frontend/static/js/sparql-console.js and all files under frontend/static/js/. This adjusts S05 scope upward from "polish" to "build console integration". | No | agent |
| D300 | M033 planning | arch | Isometric 2.5D approach — CSS 3D transform on Cytoscape container with inverse coordinate correction | Apply rotateX(60deg) + rotateZ(-45deg) to Cytoscape container. Intercept mouse events and apply inverse transform matrix (~20 lines of matrix math) to correct coordinate mismatch. Fall back to parallax depth simulation (node size + opacity variation) if coordinate correction is unreliable across browsers. | Reuses all existing Cytoscape infrastructure (layouts, styling, popovers). Option B (z-position hack) isn't real isometric. Option C (custom renderer) is too much scope. The coordinate correction is well-documented in Cytoscape issue #1756 community workarounds. Ship as a layout option, not a separate renderer. | Yes — fall back to parallax if CSS 3D correction is unreliable | agent |
| D301 | M033 planning | scope | Calendar view is read-only display — no inline drag-to-reschedule | Calendar v1 renders events on the grid. Clicking opens the object tab for editing. No FullCalendar eventDrop/eventResize handlers for inline date modification. | Inline reschedule requires a write-back path for date property changes, which means a SPARQL UPDATE from the frontend or a new API endpoint. This exceeds v1 scope and adds complexity. Users can click through to the object editor to change dates. | Yes — add inline reschedule if users request it | agent |
| D302 | M033 planning | scope | Map view ships functional with empty-state UX — no geo property bootstrapping | No existing Mental Model types define lat/long coordinates. The map renderer ships complete but shows an instructive empty state explaining which RDF properties to add. No model changes to force-fit geo data. | The map view is a capability investment. Forcing geo properties into bpkm:Event (which has a string location field) would be artificial. Future models (Research Workflow, CRM with meeting locations) can add `wgs84:lat`/`wgs84:long` and the view works immediately. | Yes — add geo-enabled model types in a future milestone | agent |
| D303 | M034 planning | arch | FullCalendar interaction approach — enable editable/selectable/droppable on existing CDN bundle | Enable `editable:true`, `selectable:true`, `droppable:true` on the existing FullCalendar 6.1.17 CDN bundle (interaction plugin already included). `eventDrop`/`eventResize` call `object.patch` with `scheduledStart`/`scheduledEnd`. `select` handler opens new task form with pre-filled times. No separate interaction plugin script needed. | Research confirmed the standard FullCalendar CDN bundle includes `@fullcalendar/interaction`. M033 template already lazy-loads FullCalendar with CDN polling. Write-back uses existing `object.patch` command endpoint. D301 deferred inline reschedule — M034 delivers it. | No | agent |
| D304 | M034 planning | tech | Frappe Gantt (MIT, zero deps, ~50KB SVG) for timeline/Gantt renderer | Vendor Frappe Gantt as the timeline renderer. Integrates as the 8th generic view renderer (alongside table, card, graph, kanban, calendar, map, isometric). SVG-based with built-in dependency arrows, drag-to-reschedule, resize, and zoom levels. | vis-timeline has heavier deps and more complex API. FullCalendar Premium timeline costs $480 and is not MIT. Frappe Gantt is purpose-built for task timelines, MIT licensed, zero runtime deps, and has the exact feature set needed (bars, arrows, drag, zoom). Custom SVG would duplicate what Frappe Gantt already provides. | Yes — switch to custom SVG if Frappe Gantt proves inflexible | agent |
| D305 | M034 planning | arch | Task scheduling properties — bpkm:scheduledStart/scheduledEnd/estimatedDuration complementing ppv:doDate | Add `bpkm:scheduledStart` (xsd:dateTime), `bpkm:scheduledEnd` (xsd:dateTime), `bpkm:estimatedDuration` (xsd:string, ISO 8601 duration) to TaskShape. These complement `bpkm:dueDate` (when it's due) and `ppv:doDate` (which day to do it, date-only). | dueDate = deadline, doDate = day-level intent, scheduledStart/End = hour-level time block. All three are useful at different granularities. Calendar needs dateTime precision for time-grid placement. Duration enables block sizing independent of explicit end time. Additive change only — no existing properties modified, basic-pkm version bumps to 2.2.0. | No | agent |
| D306 | M034 planning | tech | python-dateutil for backend RRULE expansion | Add `python-dateutil~=2.9.0` to backend dependencies for `rrule.rrulestr()` to expand recurrence rules into virtual calendar events at query time. | Event model already has `bpkm:recurrenceRule` (xsd:string, RFC 5545 RRULE format) from calendar sync apps. python-dateutil is the standard Python library for RRULE parsing. Virtual expansion at query time (not stored objects) follows the Google Calendar pattern and avoids phantom object pollution in the triplestore. | No | agent |
| D307 | M034 planning | arch | Cross-panel drag protocol — text/x-sempkm-iri dataTransfer type with IRI + title + duration | Kanban and explorer drag handlers set `dataTransfer.setData('text/x-sempkm-iri', JSON.stringify({iri, title, duration}))` alongside existing `text/plain` (backward compat). Calendar's FullCalendar `drop` handler reads this to create a scheduled task at the drop time. | Extends the proven kanban drag pattern (currently uses `text/plain` with bare IRI). JSON payload carries title and estimated duration for immediate calendar block rendering before the server roundtrip. FullCalendar's `externalDrop` event provides the date/time from the drop target. `stopPropagation()` prevents dockview interference (proven in kanban M031 and canvas M008). | No | agent |
| D308 | M034 planning | scope | M034 requirement ID prefixes — PLAN-01..PLAN-10 | Requirements PLAN-01 (time-blocking) through PLAN-10 (timeline scoping). 10 requirements total covering scheduling, calendar interaction, timeline, recurring tasks, templates, workflows, and composability. | New PLAN- prefix for the planning domain. Follows established prefix convention (D209, D285, D298). | No | agent |
| D309 | M034/S02 | library | Frappe Gantt loaded from CDN, not vendored | CDN-loaded from jsdelivr (`frappe-gantt@1.2.2` UMD bundle + CSS), not vendored in `frontend/static/vendor/`. Follows exact lazy-load IIFE pattern used by FullCalendar, Leaflet, Cytoscape, and Chart.js. Refines D304 which said "vendor". | The codebase consistently uses CDN loading for all external visualization libraries. Vendoring would diverge from the established pattern. Pinned to @1.2.2 for reproducibility. | Yes — can vendor if CDN availability becomes a concern | agent |
| D310 | M039 planning | arch | RDF import: store imported triples directly with original IRIs, no IRI reminting | Imported RDF subjects keep their original IRIs. No reminting to SemPKM `{namespace}/{Type}/{slug}` pattern. EventStore accepts arbitrary IRIs. | RDF import users expect their IRIs to be preserved. The object view queries `<iri> ?p ?o` and renders any IRI. Reminting would lose external linkability and violate user expectations for RDF data. | No | agent |
| D311 | M039 planning | arch | RDF import: direct triple insertion without edge reification | All parsed triples go into `urn:sempkm:current` as-is. URIRef-to-URIRef triples (e.g., `ex:alice schema:knows ex:bob`) are stored as direct triples, not reified as `sempkm:Edge` resources. | Reification is lossy and complex — some URIRef objects are vocabulary terms, not importable subjects. Edge detection is ambiguous without a heuristic. Direct triples are valid RDF, queryable via SPARQL, and preserve data fidelity. Edge Inspector won't show these — acceptable for v1. | No | agent |
| D312 | M039 planning | arch | RDF import: one Operation per subject, commit_bulk for batches | Group parsed triples by subject. Each subject becomes one Operation with operation_type="rdf.import". Use commit_bulk() for ≤1000 subjects, chunk into batches of 500 for larger imports. | Per-subject Operations provide fine-grained provenance. commit_bulk keeps event overhead low (~10 summary triples vs N*5). Direct Operation construction preserves original RDF data faithfully — imported data already has IRIs and typed literals. | No | agent |
| D313 | M039 planning | arch | RDF import module: separate `/browser/rdf-import` router, not nested under Obsidian's `/browser/import` | New `backend/app/rdf_import/` module with router prefix `/browser/rdf-import`. | The Obsidian router at `/browser/import` has `/{import_id}/...` path patterns. A nested `/browser/import/rdf` path could collide with import_id route matching. Separate prefix follows the Notion pattern (separate module at `/browser/notion`). | No | agent |
| D314 | M040 planning | documentation | Where to place M034 feature documentation in the user guide | Extend existing chapters: Calendar/Timeline/Map views go in chapter 7 (Browsing and Visualizing), Task Templates and Review Workflows go in chapter 28 (Dashboards and Workflows). | Calendar, Timeline, and Map are renderers — they belong in the renderers chapter (7) alongside Table, Cards, Graph, and Kanban. Task Templates and Review Workflows are planning tools built on the workflow system — they belong with existing workflow docs in chapter 28. Preserves existing chapter structure without creating thin standalone chapters. Alternative (dedicated "Planning & Time-Blocking" chapter) rejected because it would scatter renderer documentation across two chapters. | No | agent |
| D315 | M040 planning | documentation | Orphan chapter renumbering strategy — sequential from 39 onward | Rename 9 orphan files to chapters 39–47 (after current highest linked chapter 38). No renumbering of existing linked chapters. | Avoids touching any existing chapter numbers, which would break cross-references. Orphans get the next available numbers. Mental Model Catalog (currently duplicating 29) moves to 39, preserving its content as-is. Sync apps cluster together (41–44, 47) for logical grouping. | No | agent |
| D316 | M036 planning | arch | Model packaging for business planning frameworks | Single `business-planning` model archive with namespace `urn:sempkm:model:bp:`, containing all framework types in one installable package | Single model is simpler to install and enables cross-framework SPARQL queries without cross-model graph concerns. PPV model (11 types, 2186 lines) provides precedent for large models. All frameworks share base types (QuadrantItem, FrameworkSection) in a single namespace. | Yes — split into strategic-frameworks + operational-tools if single archive exceeds ~5000 lines | agent |
| D317 | M036 planning | arch | Custom renderer dispatch — extend elif pattern, don't refactor registry | Add new renderer type strings (`quadrant`, `bmc`, `okr`, `decision-matrix`) to `_VALID_RENDERERS` set and add elif branches in `generic_view()`. Don't refactor the dead `register_renderer()` into a real dispatch system. | The elif pattern is ugly but proven across 7 renderers, each with unique data fetching and template context needs. Refactoring to registry-based dispatch is a large tangent that doesn't deliver user value. The 4 new renderers can follow the exact same pattern. Wire the registry in a future cleanup milestone. | Yes — refactor to registry dispatch when renderer count exceeds 15 | agent |
| D318 | M036 planning | arch | Generic quadrant renderer parameterized by axis labels | One `quadrant_view.html` template + `quadrant.js` + `quadrant.css` used by 6+ frameworks (Eisenhower, SWOT, BCG, Ansoff, Stakeholder Map, Risk Matrix). Axis labels, colors, and quadrant names passed as template context from the ViewSpec's SPARQL query and config. | Building 6 separate renderers for the same 2×2 layout would be massive duplication. The only difference is axis labels and quadrant names. A single parameterized renderer with config from the ViewSpec keeps CSS/JS DRY. Each framework's ViewSpec declares `quadrant` renderer with framework-specific config. | No | agent |
| D319 | M036 planning | scope | M036 requirement ID prefix — BIZ-01 through BIZ-10 | Requirements BIZ-01 (model archive) through BIZ-10 (documentation). 10 requirements covering model install, 4 custom renderers, extended frameworks, cross-model edges, SPARQL queryability, E2E tests, and docs. | New BIZ- prefix for the business planning domain. Follows established prefix convention (D209, D285, D298, D308). | No | agent |
| D320 | M036 planning | tech | Computed fields (OKR progress, Decision Matrix scores) via server-side Python in data endpoints | OKR progress percentage = (current_value / target_value) × 100. Decision Matrix weighted score = Σ(weight × score). Computed in the Python data endpoint before template rendering. Not SHACL-AF rules, not client-side JS. | SHACL-AF computation would make values SPARQL-queryable (ideal) but adds significant rule complexity and requires inference re-run on every edit. Client-side JS loses SPARQL queryability. Server-side computation is the pragmatic middle: computed values are available in the rendered view and can be cached or materialized later. | Yes — migrate to SHACL-AF inference rules for SPARQL queryability when AI copilot needs it | agent |
| D321 | M035 planning | arch | New `POST /api/copilot/chat` endpoint with dual-auth, separate from settings LLM proxy | The copilot needs server-side system prompt injection (schema context, persona prompt, conversation history) before proxying to the LLM. The existing `llm_chat_stream` in settings.py is a simple passthrough. New endpoint uses `get_current_user_or_api` dual-auth and handles conversation persistence, context injection, and SPARQL generation internally. Settings proxy remains for raw LLM access. | Copilot logic (schema injection, self-correction, conversation history) shouldn't pollute the generic LLM proxy. Separation of concerns. | No | agent |
| D322 | M035 planning | arch | SQLite per-message rows for conversation persistence — `copilot_conversations` + `copilot_messages` tables via Alembic | Conversations are user state, not knowledge graph data — triplestore would pollute the RDF model. sessionStorage loses history on reload. Per-message rows (vs JSON blob) enable pagination, search, and per-conversation deletion. Follows persona module pattern (SQLAlchemy + Alembic + CRUD service). | SQLite is the right balance: persistent, queryable, schema-migrated. One row per message allows future conversation search and message-level linking. | No | agent |
| D323 | M035 planning | arch | Separate `ai_personas` table from workspace `personas` — same CRUD pattern, different schema | Workspace personas (M012) store dockview layout state. AI personas store system prompt templates, model preference, temperature. Different data, different purpose. Separate tables with same service pattern keep models clean. | Sharing a table would require nullable columns for both use cases and type discrimination. persona/ module is the pattern reference, not the storage target. | No | agent |
| D324 | M035 planning | arch | SPARQL self-correction: max 2 retries (3 total), error message fed back to LLM | LLMs frequently generate plausible but wrong SPARQL. 2 retries balances quality improvement vs latency. Error feedback includes parse error + available schema predicates. After 3 failures, show error and suggest rephrasing. | Well-documented pattern for LLM code generation. 3 attempts catches syntax and predicate errors. More indicates fundamental misunderstanding. | No | agent |
| D325 | M035 planning | scope | M035 requirement IDs: AI-01 through AI-10 | 10 requirements covering copilot chat UI, SPARQL generation, query approval, graph context injection, conversation persistence, AI personas, object creation from chat, and 3-tier test harness (mock/Ollama/cloud). | New AI- prefix for the AI copilot domain. Follows established prefix convention (D209, D285, D298, D308, D319). | No | agent |
| D326 | M035 planning | tech | Character-based token estimation (~4 chars/token), no tiktoken dependency | tiktoken requires ~3MB data download and is OpenAI-specific. Character estimation is within 10-15% for English, sufficient for soft context budgets. Configurable default 4000 tokens for graph context. | Avoids heavy dependency for approximate arithmetic. Budget is a soft limit — precision not needed. Works across all LLM providers (OpenAI, Anthropic, Ollama/Llama). | Yes — add tiktoken if token-precise budgeting becomes necessary | agent |
| D327 | M035/S01 planning | arch | Copilot chat endpoint — new POST /api/copilot/chat SSE with dedicated CopilotService | New endpoint with server-side system prompt injection (schema context), SPARQL detection in streamed content, and query approval via POST /api/copilot/approve. Separate from existing /browser/settings/llm/chat/stream and /api/llm/stream. ai_router from M028 wired into main.py alongside new copilot_router. | Copilot needs schema context injection, SPARQL detection, and approval state — none supported by existing LLM proxies. Dedicated service class keeps copilot logic isolated from extension AI endpoints. | No | agent |
| D328 | M035/S01 | arch | SPARQL predicate validation — non-blocking warnings for unknown predicates, blocking rejection for mutation keywords | Unknown predicates (not in installed model SHACL shapes) log a warning but do not reject the query. Mutation keywords (INSERT, DELETE, DROP, CLEAR, LOAD, CREATE, COPY, MOVE, ADD) are strict rejections. Mutation check is regex-based — catches keywords in string literals too (known limitation, low risk). | SHACL shape coverage is incomplete — rdf:type, rdfs:label, and other common predicates aren't declared in shapes. Blocking on unknowns would false-negative on legitimate queries. Mutation blocking is strict because the copilot should never modify the graph. | Yes — tighten predicate validation as shape coverage improves | agent |
| D329 | M035/S01 | tech | SSE client uses ReadableStream reader instead of EventSource API | Frontend copilot.js parses SSE manually from a fetch() ReadableStream rather than using the browser's EventSource API. | EventSource only supports GET requests. The copilot chat endpoint requires POST with a JSON body (messages array, model selection). ReadableStream gives full control over request method, headers, and body while parsing the same SSE wire format. | No | agent |
| D330 | M035/S01 | arch | CopilotService lives in `backend/app/copilot/` module, not `backend/app/services/` | CopilotService, schemas, and __init__.py placed in a dedicated `backend/app/copilot/` package rather than as a single file in the services directory. | The copilot module has its own Pydantic schemas, will gain conversation persistence and persona integration in S02/S03 — a package structure accommodates growth. Follows the same pattern as `backend/app/commands/`, `backend/app/sparql/`, `backend/app/auth/`. | No | agent |
| D331 | M035/S02 | arch | Graph context serialization: human-readable grouped text with labels, not raw RDF triples | serialize_context() resolves IRIs to labels, compacts predicates, groups by relationship type, outputs natural language text (e.g. "Project 'Q1 Planning' has tasks: 'Review Goals'"). | LLMs process natural language far better than RDF syntax. Raw triples waste token budget on angle brackets and full IRIs. Matches the schema context pattern from S01's build_schema_context(). | Yes | agent |
| D332 | M035/S02 | data-model | Conversation persistence: per-message rows in copilot_messages table, not JSON blob | Each message stored as a separate row with FK to copilot_conversations, not as a JSON blob column on the conversation. | Per-message rows enable pagination, cross-conversation search, and per-message metadata (SPARQL query attachment, approval status). Matches the SparqlQueryHistory per-record pattern. JSON blob requires full deserialization for any operation. | No | agent |
| D333 | M035/S02 | arch | Separate 2000-token budget for graph context, independent of 4000-token schema context budget | Graph context gets its own DEFAULT_GRAPH_TOKEN_BUDGET = 2000 (~8000 chars), separate from schema context's DEFAULT_TOKEN_BUDGET = 4000. | Schema context (type structure) and graph context (instance data) vary independently — installing more models shouldn't shrink instance context space. Combined total ~6000 tokens (24KB) stays well within LLM context limits. | Yes — budgets are configurable | agent |
| D334 | M035/S04 | arch | 3-tier LLM test strategy: mock (CI), Ollama (local), cloud (budget-capped) | Mock server for CI determinism (<5s), Ollama compose for local real inference, cloud tier with CostTracker budget cap ($1.00 default). Tier auto-selected via env vars in getLlmTier(). | Mock gives deterministic assertions for CI. Ollama gives real inference without API costs. Cloud gives production-realistic testing with budget guardrails. Each tier has a dedicated compose file or configuration path. | No | agent |
| D335 | M035/S04 | arch | Mock LLM server pattern matching priority: claims first for backward compat | _select_response() checks routes in order: claims > SPARQL > create-object > summarize > generic. Claims route stays first because M028 browser extension tests depend on it. | Adding copilot routes to the existing mock server must not break M028 claim detection tests. Priority ordering with keyword matching ensures backward compatibility while supporting the new copilot flows. | No | agent |
| D336 | M037 planning | arch | Context data in SQLite `user_context` table — single-row-per-user upsert, not RDF | Context is ephemeral state (current location, activity, time period), not knowledge. Single-row upsert keeps exactly one current-state record per user. TTL-based staleness via `updated_at`. No history. | Triplestore is for knowledge graph data. Context is transient device state that changes every few minutes. SQLite is the right home for ephemeral per-user state. | No | agent |
| D337 | M037 planning | tech | Expo managed workflow with development builds for mobile app | `expo-location` provides `startGeofencingAsync` + `TaskManager.defineTask` for background geofencing. `expo-notifications` for unified push. `expo-calendar` for calendar read. Development builds support all native modules. JS/TS ecosystem aligns with existing project. | Flutter rejected (Dart, no ecosystem overlap). Bare React Native rejected (Expo wraps the hard native parts). Expo Go rejected (doesn't support native modules). | Yes — if Expo geofencing proves unreliable, consider react-native-background-geolocation (paid) | agent |
| D338 | M037 planning | arch | Direct FCM via `firebase-admin` Python SDK for push notifications | Mobile app uses `getDevicePushTokenAsync()` for native FCM/APNs token. Backend dispatches via `firebase-admin` `messaging.send()`. | Aligns with self-hosted philosophy — no dependency on Expo's push servers. firebase-admin is Google's official SDK. | Yes — can switch to Expo Push Service if direct FCM setup proves too complex | agent |
| D339 | M037 planning | arch | `ContextBroadcast` SSE fan-out — same pattern as `LintBroadcast` | New class with asyncio.Queue subscribers, publish-to-all, full-queue client eviction. Context-specific event types: context_update, persona_switched, context_stale. | LintBroadcast is proven. No need for shared base class — the two broadcasts serve different event streams. Extract base if a third broadcast is needed. | Yes | agent |
| D340 | M037 planning | scope | CTX-01 through CTX-19 requirement IDs for user context domain | 19 requirements covering context API, SSE streaming, rules engine, mobile app, geofencing, calendar, activity, push notifications, and settings UI. | New CTX- prefix follows established convention (D209, D285, D298, D308, D319, D325). | No | agent |
| D341 | M037 planning | arch | Rules evaluate on context update POST, not on a timer | ContextService.update() calls RulesEngine.evaluate() synchronously after persisting. Separate TTL checker on timer for staleness only. | Context updates are naturally event-driven (geofence transitions, calendar boundaries). Polling wastes CPU. | No | agent |
| D342 | M037 planning | arch | Manual persona switch suppresses auto-switching until next context change | Override flag on user_context row. Cleared on next context update POST. | Users expect manual choice to stick. "Until next context change" is a natural reset — the next geofence transition signals a meaningful state change. | Yes — could add configurable lock duration | agent |
| D343 | M037/S02 planning | arch | Context rule conditions: JSON dict with AND semantics, first-match-wins by priority | Conditions are field-name→value equality pairs — all must match. Rules sorted by priority DESC, first match wins. No OR logic or complex expressions. | Covers stated use case (location + time → persona) with minimal complexity. JSON conditions are schema-free and extensible. Priority ordering gives users predictable control. OR/complex expressions deferred. | Yes — can evolve JSON structure to support OR logic later | agent |
| D344 | M037/S02 planning | arch | Rule evaluation runs synchronously in context update request handler, not as broadcast subscriber | RulesEngine.evaluate() called directly in update_context() after broadcast.publish(). Not decoupled via broadcast subscription. | Synchronous evaluation is simpler, avoids subscriber lifecycle management, and rule evaluation is fast (one DB query + in-memory matching). Decoupling adds complexity without benefit at this scale. | Yes — extract to subscriber if evaluation becomes expensive | agent |
| D345 | M037/S04 | UX | Geofence sync failures in mobile app are non-blocking — zone CRUD succeeds even if registerGeofences() fails | Log geofence registration failures as warnings (zones.geofence_sync_failed), show no user-facing error. Zone data is persisted server-side regardless. | Geofence registration is an OS-level operation that can fail for many reasons (permissions revoked, region limit exceeded, background restrictions). Blocking zone CRUD on geofence registration would make the UI feel broken when the backend is healthy. The zone data is the source of truth; geofences are re-registered on next app launch or zone mutation. | Yes — could add user-visible retry button if geofence sync failures are common | agent |
| D346 | M038 planning | arch | Schedule rules in StateClient (JSON), media items + daily plans in RDF | Rules are ephemeral app configuration stored via StateClient key-value. MediaItem, DailyMediaPlan stored as RDF objects via CommandClient. | Rules change frequently and don't benefit from graph querying. Media items are knowledge artifacts queryable by SPARQL ("what did I listen to this week?") and linkable to other objects. Consistent with M037 context rules pattern (SQLite config, not RDF). | Yes — could move rules to RDF if rule reasoning becomes valuable | agent |
| D347 | M038 planning | arch | Podcast polling reuses feedparser + conditional GET from RSS Reader (M010), not a new dependency | Feed polling in media-scheduler app reuses the same feedparser library and conditional GET pattern (ETag/Last-Modified) from apps/rss-reader/services/feed_service.py. No shared library extraction — code is duplicated into the media-scheduler app. | feedparser is already a project dependency. The RSS Reader's feed_service.py is a proven pattern (subscribe, fetch, parse, dedup). Extracting a shared library adds coupling between two apps that should be independently deployable. Duplication is cheaper than abstraction at this point. | Yes — could extract shared feed-polling library if a third app needs it | agent |
| D348 | M038 planning | arch | External API clients use raw httpx (via SDK HttpClient), no Python SDK wrappers | YouTube Data API v3 and Spotify Web API accessed via direct HTTP requests through the App SDK's HttpClient, not via google-api-python-client or spotipy. | Consistent with all existing sync apps (Linear, GitHub, Jira, Monday, Google Calendar) which use raw HTTP. Avoids new dependencies, keeps the API surface explicit, and the SDK's HttpClient enforces domain allowlists. | Yes — could adopt SDK wrappers if API complexity grows | agent |
| D349 | M038 planning | arch | Context-driven plan adaptation uses 2-minute debounce on context SSE events | Media scheduler subscribes to GET /api/context/stream SSE. Context changes trigger rule re-evaluation after a 2-minute debounce window. Immediate re-evaluation only for geofence transitions (location_zone changes). | Prevents thrashing from rapid activity detection changes (walking↔stationary during a meeting). Location zone changes are discrete events (geofence transitions) that warrant immediate response. Activity/calendar changes are noisy and benefit from settling time. | Yes — debounce duration configurable via app settings | agent |
| D350 | M038 planning | UX | Media playback via deep links to native apps, not embedded players | For v1, all media types open via deep links: Spotify URI (spotify:track:...), YouTube URL, podcast app URL scheme. No embedded web player or audio player in the SemPKM mobile app. | Building a reliable audio/video player is out of scope. Users already have preferred apps for each media type. Deep links provide the best UX — one tap to start playing in the right app. Podcast audio player could be added later as a progressive enhancement. | Yes — could add HTML5 audio player for podcasts in future slice | agent |
| D351 | M038/S01 planning | arch | Media scheduler ontology: flat enum (sh:in) for sourceType, not OWL subclasses | Namespace `urn:sempkm:model:media-scheduler:` with prefix `ms:`. Three types: MediaSource, MediaItem, MediaCategory. sourceType uses sh:in ["podcast","youtube","spotify"]. | Flat enum simplifies SPARQL (single type, filter by sourceType) vs subclass hierarchy (PodcastSource, YouTubeSource, SpotifySource). Consistent with other model patterns in the codebase. | Yes | agent |
| D352 | M038/S01 planning | arch | Media scheduler app owns its model (media-scheduler), does not extend rss-feeds | MediaItem ≠ Article (different semantics: playback status vs read/starred, duration, enclosure). MediaSource ≠ FeedSubscription (sourceType discriminator). Code pattern reused, not the RDF types. | Independent evolution of both models. No confusing type overlap. Consistent with app-per-model separation. | Yes | agent |
| D353 | M038/S04 (early) | integration | Spotify OAuth requires HTTPS redirect URI — HTTP rejected | The Spotify developer portal enforces HTTPS on redirect URIs. User confirmed this during setup. S04 must plan for HTTPS callback routing. | Discovered by user during Spotify portal configuration. Unlike some OAuth providers, Spotify does not allow HTTP even for localhost development. | No — Spotify enforces this | agent |
| D354 | M038/S02/T01 | arch | Time range rule evaluation behavior when current_time is missing from context | Fail-closed — rule does not match. If the system can't determine the current time, a time-scoped rule should not fire. | A time-scoped rule matching when there's no time data could trigger media playback at inappropriate times. Fail-closed is the safe default for context-driven automation. Wildcard behavior is already available via null conditions for fields where "match any" is intentional. | Yes | agent |
| D355 | M038/S02/T02 | arch | How to handle old daily plan entries when regenerating a plan | Patch old entries to entryStatus="replaced" via object.patch. No object.delete. | object.delete is a separate permission the app may not have. Patching status preserves history (what was planned before regeneration) and is consistent with the event-sourced write model. The today view SPARQL excludes "replaced" entries. | No | agent |
| D356 | M042 Security Audit findings | security | Object-level ownership model (F-002) | Document shared-data model only. Multi-tenant isolation is not a goal — sharing is handled via federation and shared named graphs. | Single-user/small-team is the target. Federation provides cross-instance sharing. Per-object ownership would touch every CRUD endpoint for a use case that doesn't exist. | Yes | human |
| D357 | M042 Security Audit findings | security | Session management hardening (F-013) | All three: wire revoke_all_sessions() to Settings UI, cap concurrent sessions at 10 per user, add daily background cleanup task. | revoke_all_sessions() already exists but is unwired. Session cap prevents unbounded accumulation. Daily cleanup replaces startup-only cleanup for long-running instances. | Yes | human |
| D358 | M042 Security Audit findings | security | Demo mode safeguard (F-019) | Startup warning log when DEMO_MODE=true and APP_BASE_URL is non-localhost. No hard block. | Demo mode is intentional for the hosted demo. A warning surfaces misconfiguration without blocking legitimate use. Hard block adds override-flag complexity for an unlikely scenario. | Yes | human |
| D359 | M043 Security Hardening planning | security/architecture | Central SPARQL query builder approach for injection prevention | Build a custom SPARQLBuilder in backend/app/sparql/builder.py using rdflib's URIRef.n3() and Literal.n3() for safe serialization. Provides iri(), literal(), values_clause(), triple_pattern(), and where_block() methods. Not a full query DSL — a parameterized string builder that guarantees safe interpolation of IRIs and literals into SPARQL templates. No external library — the Python SPARQL ecosystem has no mature parameterized query builder for remote endpoints. | Research findings: (1) SPARQLWrapper has no parameterized query support (open issue since 2018). (2) Apache Jena's ParameterizedSparqlString is Java-only with known limitations. (3) rdflib's prepareQuery/initBindings doesn't work with remote SPARQLStore endpoints. (4) The pudo/sparqlquery project is abandoned. (5) rdflib's .n3() method IS the right primitive — URIRef.n3() rejects malicious IRIs with exceptions, Literal.n3() properly escapes all injection characters. Building on .n3() gives us type-safe serialization without a new dependency. The builder consolidates 9 scattered escape functions + _validate_iri into one authoritative module. | Yes | collaborative |
| D360 | M041 S02 frontend audit | frontend / htmx conventions | Whether to standardize htmx patterns: 14 trigger types, inconsistent debounce (200ms vs 300ms), revealed vs intersect, and undocumented conventions | Document conventions (innerHTML default, 300ms standard debounce, custom event naming pattern). Fix the 200ms debounce outlier to 300ms. Migrate revealed triggers to intersect once. No heavy refactoring. | htmx patterns are already 88% consistent — formalizing the de facto standard prevents drift. The 200ms vs 300ms debounce difference is likely accidental (same intent, different author). revealed is a legacy htmx trigger; intersect once is the documented modern equivalent. | Yes | human |
| D361 | M043/S01/T04 | security | SPARQL injection defense strategy — validate early at HTTP boundary | Call safe_iri() at HTTP parameter extraction point (router layer), not deep in service call chains. Return 400 with warning-level security log on invalid IRIs. Defense-in-depth: safe_iri() also called on system-derived IRIs in service layer. | If safe_iri() is only called deep in the service layer, ValueError propagates as 500 instead of 400. Early validation at the router gives clean error responses and security audit logging. The service-layer calls catch edge cases where system data becomes corrupted or an internal caller bypasses the router. | No | agent |
| D362 | M043/S02/T02 | security/infrastructure | CORS header ownership — single source of truth | FastAPI CORSMiddleware is the sole CORS authority. All CORS add_header directives removed from nginx.conf and nginx.demo.conf. Caddy passes through without adding CORS. A dedicated _WellKnownCORSMiddleware overrides CORSMiddleware specifically for /.well-known/sempkm to always return Access-Control-Allow-Origin: * (browser extension discovery must work from any origin). | Dual CORS headers (nginx + FastAPI) caused duplicate header issues and confusion about which layer controlled access. Single-sourcing in FastAPI makes CORS testable via unit tests and keeps proxy layers as pure pass-through. The well-known override uses BaseHTTPMiddleware with `del response.headers[key]` (not .pop() — Starlette MutableHeaders doesn't implement pop). | No | agent |
| D363 | M043/S02/T02 | security/infrastructure | HTTP security headers standard set for all proxy layers | 6 security headers added to nginx.conf, nginx.demo.conf, and Caddyfile.cloud: X-Content-Type-Options (nosniff), X-Frame-Options (DENY), Referrer-Policy (strict-origin-when-cross-origin), Permissions-Policy (camera=(),microphone=(),geolocation=()), Content-Security-Policy (self + CDN allowlist), server_tokens off. CSP allows scripts/styles from unpkg.com, cdn.jsdelivr.net, cdnjs.cloudflare.com plus 'unsafe-inline' for existing inline scripts. | F-021 finding. Headers applied at proxy layer (nginx/Caddy) so they cover all responses including static files, not just FastAPI routes. CSP allowlist matches the three CDNs used by FullCalendar, Leaflet, and Yasgui — would be removed when those are vendored (M029 pipeline). 'unsafe-inline' is required for existing htmx inline event handlers and inline styles. | Yes — tighten CSP when CDN deps are vendored and inline scripts are eliminated | agent |
| D364 | M043/S02/T02 | security | Obsidian vault upload size cap | Obsidian upload location capped at 500m via client_max_body_size. Default nginx locations remain at 10m. | F-027 finding — the Obsidian import endpoint had client_max_body_size 0 (unlimited), allowing unbounded uploads. 500MB accommodates large Obsidian vaults while preventing abuse. Other endpoints stay at nginx default 10m. | Yes | agent |
