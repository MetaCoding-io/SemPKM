"""API surface module for external client endpoints (M013).

Contains the well-known discovery endpoint and the API surface router
for types, shapes, context-query, and other external-facing endpoints.
"""
