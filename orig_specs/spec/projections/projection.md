# Virtual Filesystem Projection (v1)

- Read-only
- Root maps to user base namespace
- IRI path becomes file path
- One markdown per object (`X.md`)
- Sidecars: `X.meta.json`, `X.edges.json`
- No global edge index in v1