---
phase: 21-research-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/DECISIONS.md
autonomous: true
requirements:
  - SYN-01

must_haves:
  truths:
    - "DECISIONS.md exists at .planning/DECISIONS.md"
    - "DECISIONS.md opens with a summary table of all 4 decisions (technology, status, target milestone)"
    - "DECISIONS.md has a cross-cutting concerns section covering auth scoping, SPARQL query patterns, and CSS token usage"
    - "DECISIONS.md proposes a concrete v2.2 phase structure with named phases, requirements, and sequencing rationale"
    - "DECISIONS.md includes a tech debt schedule mapping remaining TECH items to target milestones"
    - "A reviewer reading only DECISIONS.md can understand all committed architectural choices and implementation order"
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "Consolidated architectural decision record for v2.2 planning"
      contains: "## Decision Summary"
  key_links:
    - from: "Decision Summary table"
      to: "4 RESEARCH.md files"
      via: "Each row references the source research document"
      pattern: "LuceneSail|yasgui|wsgidav|dockview-core"
    - from: "v2.2 Phase Structure"
      to: "SYN-01 requirement"
      via: "Concrete phase breakdown derived from all 4 Handoff sections"
      pattern: "Phase 2"
---

<objective>
Produce DECISIONS.md — a single consolidated architectural decision record that synthesizes all 4 committed decisions (FTS/Vector, SPARQL UI, VFS, UI Shell), surfaces cross-cutting implementation concerns, and derives a concrete v2.2 phase structure and implementation order.

Purpose: Phase 20 committed each decision in isolation. This phase creates the unified view that enables v2.2 implementation planning — a single document that answers "what are we building in v2.2 and in what order?"

Output: .planning/DECISIONS.md — the definitive v2.2 architecture reference document.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/phase-20-fts-vector/RESEARCH.md
@.planning/research/phase-21-sparql-ui/RESEARCH.md
@.planning/research/phase-22-vfs/RESEARCH.md
@.planning/research/phase-23-ui-shell/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DECISIONS.md — Decision Summary and Cross-Cutting Concerns</name>
  <files>.planning/DECISIONS.md</files>
  <action>
    Read all 4 RESEARCH.md files (especially their ## Decision and ## v2.2 Handoff sections).
    Then read ROADMAP.md for SYN-01 success criteria and tech debt schedule context.

    Create .planning/DECISIONS.md with the following sections:

    **1. Frontmatter**
    ```yaml
    ---
    created: {today's date}
    phase: 21-research-synthesis
    requirement: SYN-01
    status: committed
    ---
    ```

    **2. Title + Purpose**
    # Architectural Decisions — SemPKM v2.2

    One paragraph: what this document is, why it exists, what decisions it captures.

    **3. ## Decision Summary**
    A table with columns: Decision | Technology Chosen | Status | Target Milestone | Source
    Rows for all 4 decisions:
    - FTS/Keyword Search | RDF4J LuceneSail | Committed | v2.2 | phase-20-fts-vector/RESEARCH.md
    - SPARQL Console UI | @zazuko/yasgui v4.5.0 (CDN) | Committed | v2.2 | phase-21-sparql-ui/RESEARCH.md
    - Virtual Filesystem (WebDAV) | wsgidav + a2wsgi | Committed | v2.2 | phase-22-vfs/RESEARCH.md
    - UI Shell / Panel Layout | dockview-core (incremental Split.js migration) | Committed (Phase A v2.3) | v2.3 | phase-23-ui-shell/RESEARCH.md

    **4. ## Decision Details**
    For each of the 4 decisions, a subsection (### FTS/Vector Search, etc.) with:
    - **Committed approach:** one sentence
    - **Key rationale:** 3-4 bullet points (distilled from RESEARCH.md rationale)
    - **Explicitly rejected:** 2-3 alternatives with one-phrase reason each
    - **Prerequisites before implementation:** from the v2.2 Handoff section
    - **First implementation steps:** numbered list (3-5 steps from Handoff)

    **5. ## Cross-Cutting Concerns**
    Identify concerns that span multiple decisions. Cover at minimum:

    - **Authentication scoping**: wsgidav uses API token Basic auth (Phase 22c design); SPARQL console uses session cookie (same as app). How do auth patterns differ across features?
    - **SPARQL query patterns**: LuceneSail adds FTS-indexed SPARQL predicates; yasgui exposes direct SPARQL access; how do query patterns interact with existing TriplestoreClient?
    - **CSS token usage**: dockview-core uses --dv-* tokens; SemPKM theme.css uses --color-* and --spacing-* tokens. Token expansion (40 → 91) is cross-cutting — applies before any panel migration.
    - **Dependency footprint**: LuceneSail (JVM, no new containers), yasgui (CDN, no build step), wsgidav (3 new Python pkgs), dockview-core (vendor bundle). Describe the combined new dependency surface.
    - **Incremental delivery**: Which features can ship independently vs which require coordination? (yasgui and wsgidav are independent; LuceneSail and dockview-core are also independent but dockview-core is v2.3)
  </action>
</task>

<task type="auto">
  <name>Task 2: Add v2.2 Phase Structure and Tech Debt Schedule to DECISIONS.md</name>
  <files>.planning/DECISIONS.md</files>
  <action>
    Read the existing DECISIONS.md (from Task 1) and the 4 RESEARCH.md Handoff sections.
    Also read REQUIREMENTS.md to understand remaining tech debt items.

    Append the following sections to DECISIONS.md:

    **6. ## v2.2 Phase Structure**
    Propose a concrete v2.2 implementation order derived from prerequisites and dependencies in the 4 Handoff sections.

    Present as a table: Phase | Name | Delivers | Requirements | Depends On | Rationale

    Consider the following as inputs when deriving the phase order:
    - LuceneSail prerequisites: JAR verification, Turtle config validation, FROM clause scoping
    - yasgui prerequisites: no backend changes needed — can ship early
    - wsgidav prerequisites: SyncTriplestoreClient, API token auth design, 3 new Python packages
    - dockview-core: Phase A (inner editor-pane split) is v2.3, not v2.2 — CSS token expansion IS v2.2
    - CSS token expansion (40 → 91) is prerequisite for dockview migration but valuable independently

    Suggested structure to consider (adjust based on actual prerequisite analysis):
    - Phase 23: SPARQL Console (yasgui CDN embed) — no dependencies, ships first
    - Phase 24: FTS Keyword Search (LuceneSail) — needs JAR/config validation first
    - Phase 25: CSS Token Expansion — prerequisite for UI shell work, independent of FTS/VFS
    - Phase 26: Virtual Filesystem MVP (wsgidav read-only) — needs SyncTriplestoreClient
    - Phase 27: VFS Write + Auth (wsgidav write path, API token auth) — depends on Phase 26
    Note: dockview-core full migration deferred to v2.3; Phase A may appear as v2.3 Phase 1.

    Add sequencing rationale for each phase explaining WHY it comes before/after adjacent phases.

    **7. ## Tech Debt Schedule**
    Map all known tech debt items (from REQUIREMENTS.md and STATE.md Known Tech Debt) to target milestones. Group as:

    ### Completed in v2.1
    Items resolved in Phase 22 (Alembic, SMTP, session cleanup, ViewSpec cache).

    ### Deferred to v2.2
    Items not yet scheduled — review STATE.md Known Tech Debt section.

    ### Deferred to v2.3+
    Items tied to deferred features (e.g., dockview full migration, VFS write path, pgvector semantic search).

    **8. ## Implementation Readiness Checklist**
    A single checklist confirming what must be verified before v2.2 development begins:
    - [ ] LuceneSail JAR present in Docker image (lucene-sail-*.jar in RDF4J distribution)
    - [ ] Turtle config syntax validated for RDF4J 5.x unified namespace (urn:eclipse.rdf4j.model)
    - [ ] yasgui CDN availability confirmed (unpkg.com/@zazuko/yasgui/build/yasgui.*)
    - [ ] wsgidav ≥4.3.3 available on PyPI
    - [ ] CSS token count measured (current baseline before expansion)
    - [ ] dockview-core bundle size measured before deciding CDN vs vendor

    Commit DECISIONS.md after this task.
  </action>
</task>

</tasks>
