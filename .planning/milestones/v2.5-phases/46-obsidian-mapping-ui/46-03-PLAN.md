---
phase: 46-obsidian-mapping-ui
plan: 03
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - backend/app/templates/obsidian/partials/preview.html
  - frontend/static/css/import.css
autonomous: false
requirements: [OBSI-05]

must_haves:
  truths:
    - "User can see a summary table showing each mapped type with object count and properties mapped count"
    - "User can see 2-3 sample objects per type rendered as key-value lists"
    - "User can navigate back to property mapping to iterate"
    - "Import button is present and clearly labeled for Phase 47"
  artifacts:
    - path: "backend/app/templates/obsidian/partials/preview.html"
      provides: "Preview wizard step showing mapped objects before import"
      contains: "preview"
  key_links:
    - from: "backend/app/templates/obsidian/partials/preview.html"
      to: "backend/app/obsidian/router.py"
      via: "hx-get for back navigation, hx-post for import trigger"
      pattern: "hx-get.*step/property-mapping"
---

<objective>
Preview wizard step template showing mapped objects before import commitment.

Purpose: Let users verify their mapping configuration by seeing concrete sample objects rendered as key-value lists before triggering the batch import (Phase 47).
Output: Preview template with summary table and sample objects, plus a visual verification checkpoint.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-obsidian-mapping-ui/46-CONTEXT.md
@.planning/phases/46-obsidian-mapping-ui/46-RESEARCH.md
@.planning/phases/46-obsidian-mapping-ui/46-01-SUMMARY.md
@.planning/phases/46-obsidian-mapping-ui/46-02-SUMMARY.md
@backend/app/templates/obsidian/partials/type_mapping.html
@frontend/static/css/import.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create preview template and CSS</name>
  <files>backend/app/templates/obsidian/partials/preview.html, frontend/static/css/import.css</files>
  <action>
1. Create `preview.html`:
   - Include step_bar partial with `current_step=5`
   - Summary table at top: columns Type | Objects | Properties Mapped. One row per mapped type from the `previews` context list. Each row shows type_label, total_count, mapped_properties count.
   - Below summary: one section per type with heading (type label). Each section shows 2-3 sample objects as lightweight key-value cards:
     - Card shows note path as title
     - Key-value pairs from mapped properties (property_label: value)
     - "Has body content" indicator (checkmark or text) if has_body is true
     - Style as simplified object cards (not full SHACL forms) -- clean key-value list with muted keys and regular values
   - If no types are mapped (all skipped), show an info message: "No types are mapped. Go back to type mapping to map at least one group."
   - Navigation: "Back: Property Mapping" button (`hx-get` to property-mapping step). "Import" button that will trigger Phase 47 batch import (for now, just a styled button with `disabled` attribute and tooltip "Import will be available after Phase 47" or simply link to a placeholder endpoint).
   - Call `lucide.createIcons()` at end.

2. Add CSS to `import.css`:
   - `.preview-summary-table` (clean table matching lint dashboard aesthetic)
   - `.preview-type-section` (section per type with heading)
   - `.preview-sample-card` (lightweight card: border, padding, slight shadow)
   - `.preview-kv-row` (key-value pair row: key in muted color, value in regular)
   - `.preview-body-indicator` (small badge/icon indicating body content exists)
   - `.preview-empty` (info message when nothing mapped)
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && test -f backend/app/templates/obsidian/partials/preview.html && grep -q "preview-sample-card" frontend/static/css/import.css && grep -q "step/property-mapping" backend/app/templates/obsidian/partials/preview.html && echo "Preview checks passed"</automated>
  </verify>
  <done>Preview template shows summary table and sample objects as key-value cards. Back button returns to property mapping. Import button is present (placeholder for Phase 47). CSS styles all preview elements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete mapping wizard flow</name>
  <files>none</files>
  <action>Human verification of the complete Obsidian mapping wizard (steps 3-5). No code changes -- visual and functional verification only.</action>
  <verify>
    <automated>echo "Manual verification checkpoint"</automated>
  </verify>
  <done>User confirms wizard flow works: type mapping, property mapping, and preview steps all render correctly with auto-save and navigation.</done>
  <what-built>Complete Obsidian mapping wizard (steps 3-5): type mapping with auto-save dropdowns, property mapping with SHACL property selection and custom IRI input, and preview with sample objects.</what-built>
  <how-to-verify>
    1. Start Docker stack: `docker compose up -d`
    2. Navigate to http://localhost:3000/browser/import
    3. Upload an Obsidian vault zip (or use existing import if one is present)
    4. Trigger scan and wait for results
    5. Verify step bar appears at top showing steps 1-6 with step 2 (Scan) highlighted
    6. Click "Next: Type Mapping" to advance to step 3
    7. Verify type mapping table shows detected groups with dropdowns
    8. Select a Mental Model type for at least one group -- verify dropdown auto-saves (no explicit save button needed)
    9. Click "Next: Property Mapping" to advance to step 4
    10. Verify per-type property tables appear with SHACL properties in dropdowns
    11. Try selecting "Custom IRI..." and verify text input appears
    12. Click "Next: Preview" to advance to step 5
    13. Verify summary table shows mapped types with counts
    14. Verify sample objects render as key-value cards
    15. Click "Back: Property Mapping" and verify you return to step 4 with mappings preserved
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Preview summary table shows correct counts per type
- Sample objects display mapped property values
- Full wizard flow works: upload -> scan -> type map -> property map -> preview -> back
- Mappings persist across step navigation (auto-saved to mapping_config.json)
</verification>

<success_criteria>
- Preview step shows summary table with type, count, and properties mapped
- 2-3 sample objects per type render as key-value lists
- Back button returns to property mapping with mappings preserved
- Import button is present (functional implementation in Phase 47)
- Complete wizard flow from upload through preview works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/46-obsidian-mapping-ui/46-03-SUMMARY.md`
</output>
