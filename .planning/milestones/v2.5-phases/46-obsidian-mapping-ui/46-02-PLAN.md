---
phase: 46-obsidian-mapping-ui
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - backend/app/templates/obsidian/import.html
  - backend/app/templates/obsidian/partials/step_bar.html
  - backend/app/templates/obsidian/partials/type_mapping.html
  - backend/app/templates/obsidian/partials/property_mapping.html
  - frontend/static/css/import.css
autonomous: true
requirements: [OBSI-03, OBSI-04]

must_haves:
  truths:
    - "Step bar shows 6 numbered steps with current step highlighted and completed steps showing checkmarks"
    - "Type mapping table shows one row per NoteTypeGroup with dropdown of Mental Model types and Skip option"
    - "Property mapping shows per-type tables with SHACL property dropdown and custom IRI input"
    - "Dropdowns auto-save on change via htmx POST"
    - "Next button navigates forward, Back button navigates backward"
  artifacts:
    - path: "backend/app/templates/obsidian/partials/step_bar.html"
      provides: "Reusable wizard step indicator"
      contains: "step-bar"
    - path: "backend/app/templates/obsidian/partials/type_mapping.html"
      provides: "Type mapping wizard step with dropdown per group"
      contains: "mapping-select"
    - path: "backend/app/templates/obsidian/partials/property_mapping.html"
      provides: "Property mapping wizard step with per-type tables"
      contains: "property-mapping"
    - path: "frontend/static/css/import.css"
      provides: "Wizard step bar and mapping table styles"
      contains: "step-bar"
  key_links:
    - from: "backend/app/templates/obsidian/partials/type_mapping.html"
      to: "backend/app/obsidian/router.py"
      via: "hx-post for auto-save, hx-get for step navigation"
      pattern: "hx-post.*mapping/type"
    - from: "backend/app/templates/obsidian/partials/property_mapping.html"
      to: "backend/app/obsidian/router.py"
      via: "hx-post for auto-save, hx-get for step navigation"
      pattern: "hx-post.*mapping/property"
---

<objective>
Frontend templates and CSS for the type mapping and property mapping wizard steps.

Purpose: Give users the visual interface to map Obsidian note categories to Mental Model types and frontmatter keys to RDF properties, with auto-save on each change.
Output: Step bar partial, type mapping template, property mapping template, and CSS for wizard steps.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-obsidian-mapping-ui/46-CONTEXT.md
@.planning/phases/46-obsidian-mapping-ui/46-RESEARCH.md
@.planning/phases/46-obsidian-mapping-ui/46-01-SUMMARY.md
@backend/app/templates/obsidian/import.html
@frontend/static/css/import.css
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create step bar partial and update import.html base layout</name>
  <files>backend/app/templates/obsidian/partials/step_bar.html, backend/app/templates/obsidian/import.html, frontend/static/css/import.css</files>
  <action>
1. Create `backend/app/templates/obsidian/partials/step_bar.html`:
   - 6 steps: Upload, Scan, Types, Properties, Preview, Import
   - Each step: numbered circle + label. Current step gets `step-active` class. Steps before current get `step-complete` class with Lucide check icon (`<i data-lucide="check"></i>`).
   - Steps connected by horizontal lines (`.step-connector`). Completed connectors get `step-connector-complete`.
   - Template receives `current_step` variable (1-6).
   - Use the exact pattern from 46-RESEARCH.md step bar template.

2. Update `import.html` to include the step bar above `#import-content`. The step bar should be present on all wizard steps. The existing upload/scan steps (1-2) should also show the step bar with `current_step=1` or `current_step=2`.

3. Add CSS to `import.css` for the step bar:
   - `.import-step-bar`: flex row, centered, gap between items
   - `.step-item`: flex column, align center
   - `.step-circle`: 32px circle, border, centered text. Active: accent background. Complete: green/success background with white check.
   - `.step-label`: small text below circle
   - `.step-connector`: horizontal line between circles, 2px height. Complete: accent color.
   - Responsive: on mobile, hide labels, reduce circle size

4. Call `lucide.createIcons()` at the end of the step bar partial via inline `<script>` so check icons render after htmx swap (Research Pitfall 2).

Per CLAUDE.md: Lucide icons inside flex containers must have CSS rules with `flex-shrink: 0` on the SVG. Add `.step-circle svg { width: 16px; height: 16px; flex-shrink: 0; stroke: currentColor; }`.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && test -f backend/app/templates/obsidian/partials/step_bar.html && grep -q "step-active" backend/app/templates/obsidian/partials/step_bar.html && grep -q "step-bar" frontend/static/css/import.css && grep -q "flex-shrink" frontend/static/css/import.css && echo "Step bar checks passed"</automated>
  </verify>
  <done>Step bar partial renders 6 wizard steps with correct active/complete states. import.html includes step bar. CSS styles the step bar with proper Lucide icon handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create type mapping and property mapping step templates</name>
  <files>backend/app/templates/obsidian/partials/type_mapping.html, backend/app/templates/obsidian/partials/property_mapping.html, frontend/static/css/import.css</files>
  <action>
1. Create `type_mapping.html`:
   - Include step_bar partial with `current_step=3`
   - Table with columns: Detected Group | Count | Signal | Map To
   - Each row: one NoteTypeGroup from scan_result.type_groups
   - "Detected Group" column uses `<details><summary>` for expandable sample notes (up to 10 from group.sample_notes)
   - "Signal" column shows signal_source as a badge
   - "Map To" column: `<select>` dropdown with installed types from `available_types` list. First option: `<option value="">-- Skip --</option>`. Pre-select if mapping_config has an existing mapping for this group_key (`type_name|signal_source`).
   - Each `<select>` has: `hx-post="/browser/import/{{ import_id }}/mapping/type"`, `hx-include="this"`, `hx-vals='{"group_key": "{{ group.type_name }}|{{ group.signal_source }}"}'`, `hx-swap="none"`, `hx-trigger="change"`. Use `name="target_type"`.
   - "Uncategorized" rows get a `.uncategorized` class for muted styling
   - Navigation: "Back: Scan Results" button (`hx-get` to results endpoint), "Next: Property Mapping" button (`hx-get` to property-mapping step). Next button enabled when at least one group is mapped (use a small inline script that checks if any select has a non-empty value, or always enable and validate server-side).
   - Call `lucide.createIcons()` at end of template.

2. Create `property_mapping.html`:
   - Include step_bar partial with `current_step=4`
   - One section per mapped type. Each section has heading with type label and a table.
   - Table columns: Frontmatter Key | Occurrences | Sample Values | Map To
   - Rows: one per frontmatter key from the merged per-group keys for this type
   - "Sample Values" shows up to 3 sample values in muted text
   - "Map To" column: `<select>` with SHACL properties from the type's NodeShapeForm. Options: `-- Skip --`, then each PropertyShape (value=path IRI, label=name). Last option: `Custom IRI...` which, when selected, shows an inline text input below the select for entering a custom IRI.
   - Custom IRI UX: use a simple pattern - when "Custom IRI..." is selected, show a text `<input>` field below the select. The auto-save POST sends the custom IRI value instead. Use a small inline script or htmx `hx-trigger="change"` to toggle visibility of the custom input.
   - Each `<select>` auto-saves via `hx-post="/browser/import/{{ import_id }}/mapping/property"` with `hx-vals` containing `type_iri`, `fm_key`. Include the select value and optionally the custom IRI input.
   - Body mapping note: show a non-interactive info row stating "Markdown body content will be imported as the object body" (not configurable per user decision).
   - Navigation: "Back: Type Mapping" and "Next: Preview" buttons.
   - Call `lucide.createIcons()` at end.

3. Add CSS to `import.css`:
   - `.type-mapping-row`, `.type-mapping-row.uncategorized` (muted opacity)
   - `.type-group-name` (clickable summary style)
   - `.type-group-notes` (indented list, small text)
   - `.import-signal-badge` (small pill/badge, matches existing app badges)
   - `.mapping-select` (styled select dropdown)
   - `.property-mapping-section` (per-type section with heading)
   - `.custom-iri-input` (text input for custom property IRI)
   - `.body-mapping-note` (info row, muted, italic)
   - `.mapping-nav` (button row for Back/Next)
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && test -f backend/app/templates/obsidian/partials/type_mapping.html && test -f backend/app/templates/obsidian/partials/property_mapping.html && grep -q "hx-post" backend/app/templates/obsidian/partials/type_mapping.html && grep -q "hx-post" backend/app/templates/obsidian/partials/property_mapping.html && grep -q "mapping-select" frontend/static/css/import.css && echo "Template checks passed"</automated>
  </verify>
  <done>Type mapping template shows dropdown per group with auto-save. Property mapping template shows per-type tables with SHACL property dropdown and custom IRI input. Both templates include step bar and navigation buttons. CSS covers all mapping UI elements.</done>
</task>

</tasks>

<verification>
- Step bar renders with correct step states
- Type mapping dropdowns list installed Mental Model types
- Property mapping shows per-type tables with SHACL properties
- Auto-save fires on dropdown change (htmx POST with hx-swap="none")
- Lucide icons render after htmx swaps
- Navigation buttons work between steps
</verification>

<success_criteria>
- Step bar shows at top of all wizard steps with numbered circles and checkmarks
- Type mapping table renders one row per NoteTypeGroup with Map To dropdown
- Property mapping shows per-type sections with SHACL property dropdowns
- Custom IRI input appears when "Custom IRI..." is selected
- All dropdowns auto-save on change
- Back/Next navigation works across steps 3 and 4
</success_criteria>

<output>
After completion, create `.planning/phases/46-obsidian-mapping-ui/46-02-SUMMARY.md`
</output>
