---
phase: 45-obsidian-vault-scanner
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - backend/app/templates/browser/import_page.html
  - backend/app/templates/browser/_import_upload.html
  - backend/app/templates/browser/_import_results.html
  - backend/app/templates/browser/_import_scanning.html
  - frontend/static/css/import.css
  - frontend/static/js/workspace.js
  - backend/app/templates/components/_sidebar.html
  - e2e/tests/14-obsidian-import/vault-upload.spec.ts
  - e2e/tests/14-obsidian-import/scan-results.spec.ts
  - e2e/fixtures/test-vault.zip
autonomous: true
requirements: [OBSI-01, OBSI-02]

must_haves:
  truths:
    - "User can open Import Vault tab from sidebar Apps section"
    - "User can open Import Vault tab from Command Palette"
    - "User can upload a ZIP file and see a progress bar during scan"
    - "Scan results display big stat numbers at top (notes, tags, links, attachments)"
    - "Scan results have collapsible sections for Frontmatter Keys, Tags, Folders, Wiki-link Targets"
    - "Notes are grouped by detected category with expandable note lists"
    - "Uncategorized group is visually distinct"
    - "Warnings section shows broken links, empty notes, malformed frontmatter"
  artifacts:
    - path: "backend/app/templates/browser/import_page.html"
      provides: "Full-page wrapper for import tab"
      contains: "import-container"
    - path: "backend/app/templates/browser/_import_upload.html"
      provides: "Upload form with file input and submit button"
      contains: "multipart/form-data"
    - path: "backend/app/templates/browser/_import_results.html"
      provides: "Scan results dashboard with stat cards and collapsible sections"
      contains: "type-groups"
    - path: "frontend/static/css/import.css"
      provides: "Stat card, collapsible section, and progress bar styles"
      contains: ".import-stat-card"
    - path: "frontend/static/js/workspace.js"
      provides: "openImportTab() function"
      contains: "openImportTab"
  key_links:
    - from: "frontend/static/js/workspace.js"
      to: "backend/app/obsidian/router.py"
      via: "openImportTab() triggers dockview special-panel which loads /browser/import"
      pattern: "openImportTab"
    - from: "backend/app/templates/browser/_import_upload.html"
      to: "backend/app/obsidian/router.py"
      via: "hx-post to /browser/import/upload"
      pattern: "hx-post.*import/upload"
    - from: "backend/app/templates/browser/_import_scanning.html"
      to: "backend/app/obsidian/router.py"
      via: "EventSource to /browser/import/scan/{id}/stream"
      pattern: "EventSource"
---

<objective>
Build the frontend UI for the Obsidian vault import tab: upload form, scan progress with SSE, results dashboard, and integration points (sidebar link, command palette entry). Plus e2e tests.

Purpose: Users interact with the vault scanner through this UI. Renders the data produced by Plan 01's backend.
Output: Working import tab accessible from sidebar and command palette, showing upload form -> progress -> scan results dashboard.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-obsidian-vault-scanner/45-CONTEXT.md
@.planning/phases/45-obsidian-vault-scanner/45-RESEARCH.md
@.planning/phases/45-obsidian-vault-scanner/45-01-SUMMARY.md

@frontend/static/js/workspace.js
@frontend/static/js/workspace-layout.js
@backend/app/templates/components/_sidebar.html
@backend/app/templates/browser/vfs_page.html
@frontend/static/css/workspace.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create import tab templates, CSS, and JS integration</name>
  <files>backend/app/templates/browser/import_page.html, backend/app/templates/browser/_import_upload.html, backend/app/templates/browser/_import_results.html, backend/app/templates/browser/_import_scanning.html, frontend/static/css/import.css, frontend/static/js/workspace.js, backend/app/templates/components/_sidebar.html</files>
  <action>
**import_page.html** — Full page template (extends base.html) with htmx partial support (following vfs_page.html pattern):
- `{% block content %}` contains `<div id="import-container"><div id="import-content">{% include "browser/_import_upload.html" %}</div></div>`
- If an existing import is in progress (passed from router context), include `_import_results.html` or `_import_scanning.html` instead
- Link `import.css` stylesheet

**_import_upload.html** — Upload form partial:
- Drag-and-drop zone with visual feedback (dashed border, icon)
- `<form hx-post="/browser/import/upload" hx-target="#import-content" hx-swap="innerHTML" hx-encoding="multipart/form-data">`
- File input accepting `.zip` files
- Upload button with Lucide upload icon
- Brief instructions: "Upload your Obsidian vault as a ZIP file"
- If existing import detected, show "Resume previous import" and "Discard & start over" buttons

**_import_scanning.html** — Scanning progress partial:
- Progress bar (animated, CSS-driven)
- "Scanning... {current_file}" text area updated by SSE
- Files scanned counter: "{scanned} / {total} files"
- Inline `<script>` that creates EventSource to `/browser/import/scan/{import_id}/stream`:
  - On `scan_progress`: update progress bar width, file counter, current file name
  - On `scan_complete`: `htmx.ajax('GET', '/browser/import/{import_id}/results', {target: '#import-content', swap: 'innerHTML'})`
  - On `scan_error`: show error message, offer retry button
  - Close EventSource on complete/error

**_import_results.html** — Scan results dashboard:
- **Stat cards row** at top (inspired by lint dashboard): 4 cards showing Notes (markdown count), Tags (unique tag count), Links (unique wiki-link targets), Attachments (attachment count). Big number, small label. Use CSS grid, 4 columns on desktop, 2 on mobile.
- **Detected Types section**: grouped by detected category. Each group: type name + count + signal source badge (frontmatter/folder/tag). Expandable note list per group (first 10 notes shown, "and N more" if over 10). "Uncategorized" group gets distinct styling (amber/warning background tint) so user knows these need attention in Phase 46.
- **Collapsible sections** (use `<details><summary>` for simplicity):
  - Frontmatter Keys: table with key name, count, sample values (comma-separated, max 5)
  - Tags: tag name + count, sorted by count descending
  - Folders: folder path list
  - Wiki-link Targets: target name + count, sorted by count descending
- **Warnings section** (if any): severity badges (warning=amber, info=blue), grouped by category. Each warning shows file path and message. Expandable per category.
- **Action buttons**: "Continue to Mapping" (disabled, Phase 46 will enable), "Discard Import" (hx-post to discard endpoint)
- Scan duration shown at bottom: "Scanned in {duration}s"

**import.css** — Styles for the import tab:
- `.import-stat-cards` — CSS grid, 4 columns, gap
- `.import-stat-card` — border, border-radius, padding, big number (font-size: 2rem), small label
- `.import-stat-card .stat-number` — large, bold, `var(--color-text-primary)`
- `.import-stat-card .stat-label` — small, `var(--color-text-muted)`
- `.import-type-group` — border, padding, margin-bottom
- `.import-type-group.uncategorized` — amber left-border or background tint for visual distinction
- `.import-signal-badge` — small pill badge showing signal source (frontmatter/folder/tag)
- `.import-upload-zone` — dashed border, centered content, drag hover state
- `.import-progress-bar` — height, background, animated fill
- `.import-warning` — severity-colored left border
- Collapsible sections use standard `<details>` styling
- Responsive: 2-column stat cards below 768px
- Use existing CSS custom properties (--color-*, --border-radius, etc.)
- Follow CLAUDE.md rules: Lucide SVG icons sized via CSS with `flex-shrink: 0`

**workspace.js** — Add `openImportTab()` function following `openSettingsTab`/`openVfsTab` pattern:
```javascript
function openImportTab() {
    var tabKey = 'special:import';
    var dv = window._dockview;
    if (!dv) return;
    var existing = dv.panels.find(function(p) { return p.id === tabKey; });
    if (existing) { existing.api.setActive(); return; }
    if (!window._tabMeta) window._tabMeta = {};
    window._tabMeta[tabKey] = { label: 'Import Vault', dirty: false };
    dv.api.addPanel({
        id: tabKey,
        component: 'special-panel',
        params: { specialType: 'import', isView: false, isSpecial: true },
        title: 'Import Vault'
    });
}
window.openImportTab = openImportTab;
```
Also add Command Palette entry to the commands array: `{ id: 'import-vault', title: 'Import Vault', icon: 'upload', section: 'Navigation', handler: function() { openImportTab(); } }`

**_sidebar.html** — Add "Import Vault" link in the Apps section (alongside Object Browser, File Browser):
- `<a href="#" onclick="openImportTab(); return false;" class="nav-link">` with Lucide `upload` icon
- Place after the File Browser link
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && ls backend/app/templates/browser/import_page.html backend/app/templates/browser/_import_upload.html backend/app/templates/browser/_import_results.html backend/app/templates/browser/_import_scanning.html frontend/static/css/import.css && grep -c "openImportTab" frontend/static/js/workspace.js && grep -c "Import Vault" backend/app/templates/components/_sidebar.html</automated>
  </verify>
  <done>All import templates exist. Upload form uses hx-encoding multipart/form-data. Results dashboard has stat cards, type groups with Uncategorized distinction, collapsible detail sections, and warnings. openImportTab() registered in workspace.js and command palette. Sidebar has Import Vault link.</done>
</task>

<task type="auto">
  <name>Task 2: Create e2e test fixture and tests for upload and scan results</name>
  <files>e2e/fixtures/test-vault.zip, e2e/tests/14-obsidian-import/vault-upload.spec.ts, e2e/tests/14-obsidian-import/scan-results.spec.ts</files>
  <action>
**test-vault.zip** — Create a small test fixture Obsidian vault as a ZIP file containing:
- `.obsidian/app.json` (minimal, marks it as Obsidian vault)
- `Projects/project-alpha.md` — frontmatter: `type: project, status: active, tags: [planning]`, body with `[[meeting-notes]]` wiki-link
- `Projects/project-beta.md` — frontmatter: `type: project, status: draft`, body with `[[project-alpha]]` link
- `Notes/meeting-notes.md` — frontmatter: `title: Weekly Sync, tags: [meetings, planning]`, body with `#important` tag and `[[project-alpha]]` link
- `Notes/quick-note.md` — no frontmatter, body: "Just a quick thought #fleeting"
- `Concepts/knowledge-graph.md` — frontmatter: `category: concept, tags: [tech]`, body with `[[project-alpha]]` link
- `random-file.md` — no frontmatter, empty body (triggers empty_note warning)
- `attachments/diagram.png` — tiny 1x1 PNG (counts as attachment)

Use a script to generate this ZIP: `cd /home/james/Code/SemPKM/e2e && python3 -c "..."` that creates the vault structure in a temp dir and zips it.

Known scan results for assertions:
- 6 markdown files, 1 attachment, 0 other
- Type groups: "project" (2, frontmatter:type), "meeting-notes" or folder-based (1-2, folder), "concept" (1, frontmatter:category), "Uncategorized" (1-2)
- Tags: planning (2+), meetings (1), tech (1), important (1), fleeting (1)
- Wiki-link targets: project-alpha (3), meeting-notes (1)
- Warnings: 1 empty note (random-file.md)
- Frontmatter keys: type, status, tags, title, category

**vault-upload.spec.ts** — E2e test for OBSI-01:
- Navigate to workspace
- Click Import Vault in sidebar (or use command palette)
- Verify import tab opens with upload form
- Upload test-vault.zip via file input
- Wait for scanning to start (progress bar visible)
- Wait for scan to complete (results appear)
- Verify stat cards show correct counts

**scan-results.spec.ts** — E2e test for OBSI-02:
- (Assumes upload already done — may need to upload first or use a pre-scanned state)
- Verify stat cards: notes count, tags count, links count, attachments count
- Verify type groups section exists with detected types
- Verify "Uncategorized" group is visually distinct
- Verify collapsible sections exist: Frontmatter Keys, Tags, Wiki-link Targets
- Expand a collapsible section and verify content (key names, counts)
- Verify warnings section shows at least one warning
- Verify "Discard Import" button exists
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM/e2e && ls fixtures/test-vault.zip tests/14-obsidian-import/vault-upload.spec.ts tests/14-obsidian-import/scan-results.spec.ts && npx playwright test --project=chromium tests/14-obsidian-import/</automated>
  </verify>
  <done>Test fixture ZIP contains 6 markdown files with known frontmatter/links/tags plus 1 attachment. Both e2e tests pass: upload flow opens tab and completes scan, results display correct stats/types/sections/warnings.</done>
</task>

</tasks>

<verification>
- Import tab opens from sidebar link and command palette
- Upload form accepts ZIP and shows progress during scan
- Scan results dashboard displays stat cards, type groups, collapsible sections, warnings
- Uncategorized notes are visually distinct
- Discard button removes import data
- E2e tests pass: `cd e2e && npx playwright test --project=chromium tests/14-obsidian-import/`
- Full e2e suite still passes: `cd e2e && npx playwright test --project=chromium`
</verification>

<success_criteria>
- User can open Import Vault tab from sidebar Apps section and command palette
- User can upload ZIP and see real-time scan progress via SSE
- Scan results show big stat numbers, type groups with Uncategorized distinction, collapsible detail sections, and warnings
- E2e tests for OBSI-01 and OBSI-02 pass
</success_criteria>

<output>
After completion, create `.planning/phases/45-obsidian-vault-scanner/45-02-SUMMARY.md`
</output>
