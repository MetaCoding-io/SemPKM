---
phase: 04-admin-shell-and-object-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/nginx.conf
  - backend/app/main.py
  - backend/app/templates/base.html
  - backend/app/templates/dashboard.html
  - backend/app/templates/health.html
  - backend/app/shell/__init__.py
  - backend/app/shell/router.py
  - frontend/static/css/style.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Root URL (/) serves a dashboard page rendered by FastAPI via Jinja2 with a left sidebar and right content area"
    - "Sidebar contains links to Admin, Object Browser, and Health Check apps"
    - "Clicking a sidebar link loads that app's content into the right area via htmx partial swap"
    - "Health Check page displays the existing health status information"
    - "Static assets (CSS, JS) are still served directly by nginx for performance"
  artifacts:
    - path: "frontend/nginx.conf"
      provides: "Updated routing: static assets via nginx, everything else proxied to FastAPI"
    - path: "backend/app/templates/base.html"
      provides: "Base Jinja2 template with htmx, dashboard shell layout, sidebar"
    - path: "backend/app/templates/dashboard.html"
      provides: "Dashboard home page extending base"
    - path: "backend/app/shell/router.py"
      provides: "FastAPI router serving template-rendered pages at /, /admin/, /browser/"
  key_links:
    - from: "frontend/nginx.conf"
      to: "backend/app/main.py"
      via: "nginx proxies non-static routes to FastAPI"
      pattern: "proxy_pass.*api:8000"
    - from: "backend/app/shell/router.py"
      to: "backend/app/templates/base.html"
      via: "Jinja2Blocks TemplateResponse"
      pattern: "templates\\.TemplateResponse"
---

<objective>
Set up the Jinja2 template infrastructure and dashboard shell that all Phase 4 UI plans depend on.

Purpose: Phase 4 introduces server-rendered pages via Jinja2, replacing the static-only nginx frontend. The dashboard shell provides the app navigation structure (Admin, Object Browser, Health) that all subsequent plans plug into.

Output: Working dashboard at `/` with sidebar navigation, nginx routing updated to proxy template-rendered routes to FastAPI, Jinja2Blocks configured for htmx partial rendering.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-shell-and-object-creation/04-RESEARCH.md
@frontend/nginx.conf
@frontend/static/index.html
@frontend/static/css/style.css
@backend/app/main.py
@backend/app/dependencies.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nginx routing update and Jinja2 template infrastructure</name>
  <files>
    frontend/nginx.conf
    backend/app/main.py
    backend/app/templates/base.html
    backend/app/templates/dashboard.html
    backend/app/templates/health.html
  </files>
  <action>
    1. Update `frontend/nginx.conf` to proxy all routes to FastAPI EXCEPT static assets:
       - Keep `location /css/` and `location /js/` serving from `/usr/share/nginx/html` (nginx static)
       - Keep `location /api/` proxying to `http://api:8000/api/`
       - Change root `location /` to proxy to `http://api:8000/` (FastAPI renders templates)
       - This follows Research Pitfall 5 guidance

    2. Update `backend/app/main.py`:
       - Import `Jinja2Blocks` from `jinja2_fragments.fastapi`
       - Create `templates = Jinja2Blocks(directory=Path(__file__).parent / "templates")` after app creation
       - Store as `app.state.templates` for access in routers via dependency injection

    3. Create `backend/app/templates/base.html`:
       - HTML5 boilerplate with htmx 2.0.4 CDN script tag (already used in project)
       - Two-column CSS layout: left sidebar (fixed ~200px) + right content area (flex grow)
       - Sidebar contains three nav links with simple icon + label (per CONTEXT.md decision):
         * Admin (`/admin/`) with gear icon
         * Object Browser (`/browser/`) with folder icon
         * Health (`/health/`) with heart icon
       - Each sidebar link uses `hx-get="{url}" hx-target="#app-content" hx-swap="innerHTML" hx-push-url="true"` for SPA-like navigation
       - Right area: `<div id="app-content">{% block content %}{% endblock %}</div>`
       - Use Jinja2 block names: `title`, `head`, `content`
       - Include inline CSS for the shell layout (sidebar + content grid) or link to style.css
       - Brand/logo at top of sidebar: "SemPKM"

    4. Create `backend/app/templates/dashboard.html`:
       - Extends `base.html`
       - Content block shows a welcome/landing page with brief description of SemPKM
       - Links to get started (Admin to install models, Browser to explore)

    5. Create `backend/app/templates/health.html`:
       - Extends `base.html` (or standalone block for htmx partial)
       - Content block renders health check info by calling `/api/health` via htmx on load
       - Replaces the old static `index.html` health page functionality
       - Use `hx-get="/api/health" hx-trigger="load" hx-swap="innerHTML"` to load health data
       - Render the response as a styled status card (similar to existing dev console)
  </action>
  <verify>
    Run `docker compose up --build -d` and verify:
    - `curl http://localhost:3000/` returns HTML with sidebar and dashboard content
    - `curl http://localhost:3000/css/style.css` returns CSS (nginx static serving)
    - `curl http://localhost:3000/api/health` returns JSON health data
    - Template response includes htmx script tag
  </verify>
  <done>
    Root URL serves a Jinja2-rendered dashboard with sidebar navigation. Static assets served by nginx. API routes proxied correctly. Jinja2Blocks configured in FastAPI app.
  </done>
</task>

<task type="auto">
  <name>Task 2: Shell router with dashboard navigation endpoints</name>
  <files>
    backend/app/shell/__init__.py
    backend/app/shell/router.py
    backend/app/main.py
    frontend/static/css/style.css
  </files>
  <action>
    1. Create `backend/app/shell/__init__.py` (empty).

    2. Create `backend/app/shell/router.py`:
       - Import `Jinja2Blocks` from `jinja2_fragments.fastapi`, `APIRouter`, `Request`
       - Create router with no prefix (serves root routes)
       - Endpoint: `GET /` -> render `dashboard.html` (full page)
       - Endpoint: `GET /health/` -> render `health.html`
         * If `HX-Request` header present, return only the `content` block (htmx partial)
         * Otherwise return full page (direct navigation)
       - Endpoint: `GET /admin/` -> render `admin/index.html` (placeholder for now -- just a "coming soon" heading in a content block)
       - Endpoint: `GET /browser/` -> render `browser/workspace.html` (placeholder for now)
       - Create placeholder templates:
         * `backend/app/templates/admin/index.html` extending base.html with "Admin Portal" heading
         * `backend/app/templates/browser/workspace.html` extending base.html with "Object Browser" heading
       - Each endpoint checks for HX-Request header to decide full page vs partial block rendering

    3. Update `backend/app/main.py`:
       - Import and include the shell router: `from app.shell.router import router as shell_router`
       - Include it LAST (after API routers) so `/api/*` routes take precedence: `app.include_router(shell_router)`

    4. Update `frontend/static/css/style.css`:
       - Add dashboard shell styles: sidebar fixed width, content area flex, nav link hover states
       - Add active sidebar link styling (`.nav-link.active`)
       - Keep existing dev console styles (they'll be phased out but don't break them)
       - Ensure the dashboard looks clean and professional with good spacing (Claude's discretion per CONTEXT.md)
  </action>
  <verify>
    With containers running:
    - `curl http://localhost:3000/` returns dashboard page with sidebar
    - `curl http://localhost:3000/admin/` returns admin placeholder page
    - `curl -H "HX-Request: true" http://localhost:3000/admin/` returns only the content block (partial)
    - `curl http://localhost:3000/health/` returns health page
    - `curl http://localhost:3000/browser/` returns workspace placeholder
  </verify>
  <done>
    Dashboard shell router serves all app pages. htmx partial rendering works for sidebar navigation (clicking sidebar links swaps content area without full page reload). Placeholder pages exist for Admin and Browser that subsequent plans will replace.
  </done>
</task>

</tasks>

<verification>
1. Navigate to `http://localhost:3000/` -- dashboard renders with sidebar showing Admin, Object Browser, Health links
2. Click sidebar links -- content area updates via htmx without full page reload
3. Direct navigation to `/admin/`, `/browser/`, `/health/` renders full pages with sidebar
4. Static CSS and JS files load correctly from nginx
5. API endpoints at `/api/*` continue to work (no routing regression)
6. Health page displays system status information
</verification>

<success_criteria>
- Jinja2Blocks configured in FastAPI with template directory
- nginx proxies template routes to FastAPI, static assets served directly
- Dashboard shell renders at `/` with working sidebar navigation
- htmx partial rendering works for SPA-like navigation between apps
- Placeholder pages exist at `/admin/` and `/browser/` for subsequent plans
- Existing API endpoints unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-shell-and-object-creation/04-01-SUMMARY.md`
</output>
