---
phase: 08-integration-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/validation/queue.py
  - backend/app/main.py
  - backend/app/commands/router.py
autonomous: true
requirements: [ADMN-03, VIEW-02]

must_haves:
  truths:
    - "Configured webhooks subscribed to validation.completed receive an HTTP POST after each SHACL validation run"
    - "Opening a cards view from workspace tabs, command palette, or view type switcher renders correctly with no 404"
  artifacts:
    - path: "backend/app/validation/queue.py"
      provides: "Completion callback mechanism on AsyncValidationQueue"
      contains: "on_complete"
    - path: "backend/app/main.py"
      provides: "Webhook dispatch wired as validation queue completion callback"
      contains: "validation.completed"
  key_links:
    - from: "backend/app/validation/queue.py"
      to: "backend/app/services/webhooks.py"
      via: "on_complete callback invoked after _worker() validates"
      pattern: "on_complete"
    - from: "frontend/static/js/workspace.js"
      to: "backend/app/views/router.py"
      via: "loadViewContent builds /browser/views/card/ URL"
      pattern: "/browser/views/card/"
---

<objective>
Wire the validation.completed webhook dispatch and verify the cards view URL fix, closing the final two integration gaps from the v1.0 milestone audit.

Purpose: ADMN-03 requires webhooks to fire on validation.completed events, but the AsyncValidationQueue has no completion callback mechanism — validation finishes silently. VIEW-02 cards view had a URL mismatch (plural /cards/ vs singular /card/) that was fixed during Phase 5 UAT but needs explicit verification.

Output: AsyncValidationQueue supports an on_complete callback, wired in main.py to dispatch validation.completed webhooks. Cards view URL confirmed correct.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/app/validation/queue.py
@backend/app/main.py
@backend/app/services/webhooks.py
@backend/app/commands/router.py
@frontend/static/js/workspace.js
@backend/app/views/router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add completion callback to AsyncValidationQueue and wire webhook dispatch</name>
  <files>
    backend/app/validation/queue.py
    backend/app/main.py
    backend/app/commands/router.py
  </files>
  <action>
1. In backend/app/validation/queue.py:
   - Add an optional `on_complete` callback parameter to `AsyncValidationQueue.__init__()`. Type: `Optional[Callable[[ValidationReportSummary, str, str], Awaitable[None]]]` (receives report_summary, event_iri, timestamp). Default to None.
   - Store as `self._on_complete`.
   - In `_worker()`, after `self._latest_report = report.summary()` and the logger.info line (around line 125-133), add a block that calls `self._on_complete` if set:
     ```python
     if self._on_complete:
         try:
             await self._on_complete(self._latest_report, job.event_iri, job.timestamp)
         except Exception:
             logger.warning("Validation completion callback failed", exc_info=True)
     ```
   - The callback is fire-and-forget (exception logged, never propagated), consistent with webhook dispatch semantics elsewhere in the codebase.
   - Import `Callable` and `Awaitable` from typing.

2. In backend/app/main.py:
   - After creating the `webhook_service` (around line 110-111) and BEFORE creating the `validation_queue` (around line 97), reorder so webhook_service is created first.
   - Actually, looking at the code flow: webhook_service is created at line 110 which is AFTER validation_queue at line 97. Reorder: move webhook_service creation to before validation_queue creation.
   - Define an async callback function that dispatches validation.completed:
     ```python
     async def on_validation_complete(report_summary, event_iri, timestamp):
         await webhook_service.dispatch("validation.completed", {
             "event_iri": event_iri,
             "timestamp": timestamp,
             "conforms": report_summary.conforms,
             "violations": report_summary.violation_count,
             "warnings": report_summary.warning_count,
         })
     ```
   - Pass `on_complete=on_validation_complete` to `AsyncValidationQueue(validation_service, on_complete=on_validation_complete)`.

3. In backend/app/commands/router.py:
   - Remove the TODO comment block at lines 146-149 (the one about validation.completed webhook) since it's now implemented via the queue callback.
  </action>
  <verify>
    - `python -c "from app.validation.queue import AsyncValidationQueue"` succeeds (import check).
    - Grep for "on_complete" in queue.py confirms the callback parameter and invocation exist.
    - Grep for "validation.completed" in main.py confirms the webhook dispatch is wired.
    - Grep for "TODO.*validation.completed" in commands/router.py returns no results (comment removed).
    - `docker compose up --build -d` starts without errors (if Docker is available).
  </verify>
  <done>
    AsyncValidationQueue accepts an on_complete callback. main.py wires a callback that dispatches validation.completed webhooks with event_iri, timestamp, conforms status, and violation/warning counts. The TODO comment in commands/router.py is removed. Fire-and-forget semantics maintained (callback failures logged, not propagated).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify cards view URL consistency across codebase</name>
  <files>
    frontend/static/js/workspace.js
  </files>
  <action>
1. Verify that workspace.js loadViewContent() uses `/browser/views/card/` (singular, NOT /cards/ plural) for the card view type. This was fixed during Phase 5 UAT (test 6) but needs explicit verification as a Phase 8 deliverable.

2. Run a codebase-wide grep for any remaining `/cards/` (plural) references in .js, .html, and .py files (excluding .planning/, .claude/, node_modules, __pycache__). If any are found, fix them to use `/card/` (singular) to match the router endpoint `@router.get("/card/{spec_iri:path}")`.

3. Verify the full chain is consistent:
   - View spec data: `sempkm:rendererType` = "card" (singular) in model JSON-LD
   - Views router: `@router.get("/card/{spec_iri:path}")` at backend/app/views/router.py line 166
   - workspace.js loadViewContent: `'/browser/views/card/'` at line 131
   - view_toolbar.html switchViewType: uses `rendererType` variable which is "card" (singular)

4. If all references are already correct (expected based on Phase 5 UAT fix), document the verification in the SUMMARY. No code changes needed if already consistent.
  </action>
  <verify>
    - `grep -rn '/cards/' frontend/ backend/app/ --include='*.js' --include='*.html' --include='*.py'` returns no results (no plural /cards/ references).
    - workspace.js line 131 contains `/browser/views/card/` (singular).
    - view_toolbar.html switchViewType builds URLs using rendererType which is "card" (singular).
  </verify>
  <done>
    The entire URL chain for cards view is verified consistent: model data uses "card" singular, router exposes /card/ singular, workspace.js requests /card/ singular, and view_toolbar.html switchViewType uses rendererType "card" singular. No remaining /cards/ (plural) references exist in the codebase.
  </done>
</task>

</tasks>

<verification>
1. Import validation: `python -c "from app.validation.queue import AsyncValidationQueue; print('OK')"` succeeds
2. Webhook wiring: `grep -n "validation.completed" backend/app/main.py` shows dispatch call
3. Callback mechanism: `grep -n "on_complete" backend/app/validation/queue.py` shows parameter and invocation
4. TODO removed: `grep -rn "TODO.*validation.completed" backend/app/commands/` returns empty
5. Cards URL consistency: `grep -rn "/cards/" frontend/ backend/app/ --include="*.js" --include="*.html" --include="*.py"` returns empty
6. Docker build (if available): `docker compose build api` succeeds
</verification>

<success_criteria>
- AsyncValidationQueue has on_complete callback mechanism
- main.py wires validation.completed webhook dispatch via the callback
- No TODO comment about validation.completed remains in commands/router.py
- Zero /cards/ (plural) URL references in frontend/ or backend/app/ source files
- All verification commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-bug-fixes/08-01-SUMMARY.md`
</output>
