---
phase: 09-provenance-and-redirect-micro-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/commands/router.py
  - frontend/static/js/auth.js
autonomous: true
requirements:
  - PROV-02-partial
  - AUTH-03-cosmetic

must_haves:
  truths:
    - "API command endpoint records performed_by_role in event metadata alongside performed_by user IRI"
    - "Invite acceptance redirects to ?next= URL when present instead of always redirecting to /"
  artifacts:
    - path: "backend/app/commands/router.py"
      provides: "API command endpoint with full user provenance (performed_by + performed_by_role)"
      contains: "performed_by_role=user.role"
    - path: "frontend/static/js/auth.js"
      provides: "Invite acceptance with ?next= redirect support"
      contains: "nextUrl"
  key_links:
    - from: "backend/app/commands/router.py"
      to: "backend/app/events/store.py"
      via: "EventStore.commit() call with performed_by_role kwarg"
      pattern: "event_store\\.commit.*performed_by_role"
    - from: "frontend/static/js/auth.js"
      to: "invite.html URL params"
      via: "URLSearchParams reading ?next= in handleInviteAccept"
      pattern: "params\\.get.*next"
---

<objective>
Close the 2 remaining low-severity integration gaps from the v1.0 re-audit: add performed_by_role to the API command commit path, and verify the invite acceptance ?next= redirect is working.

Purpose: Achieve full parity between browser write provenance (which already passes performed_by + performed_by_role) and API command write provenance. Ensure all auth redirect paths honor ?next= for consistent post-auth navigation.

Output: Two micro-fixes closing PROV-02-partial and AUTH-03-cosmetic, completing all v1.0 gap closures.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@backend/app/commands/router.py
@backend/app/browser/router.py
@frontend/static/js/auth.js
@backend/app/events/store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add performed_by_role to API command EventStore.commit()</name>
  <files>backend/app/commands/router.py</files>
  <action>
In backend/app/commands/router.py, line 124, the EventStore.commit() call currently reads:

```python
event_result = await event_store.commit(operations, performed_by=user_iri)
```

Add the `performed_by_role` argument to match the pattern used in browser/router.py (lines 211, 527, 630):

```python
event_result = await event_store.commit(operations, performed_by=user_iri, performed_by_role=user.role)
```

This is a one-line change. The `user` variable is already available via `Depends(require_role("owner", "member"))` on line 85. The `user.role` attribute is a string ("owner" or "member") from the User model. The EventStore.commit() method already accepts `performed_by_role: str | None = None` (store.py line 78).

No other changes needed — the import of User is already present (line 17), and the user dependency is already injected.
  </action>
  <verify>
1. `grep -n "performed_by_role" backend/app/commands/router.py` shows the new kwarg on the commit() call
2. `grep -c "performed_by_role=user.role" backend/app/commands/router.py` returns 1
3. Verify consistency: `grep "performed_by_role=user.role" backend/app/browser/router.py backend/app/commands/router.py` shows matching pattern in both files
  </verify>
  <done>POST /api/commands passes performed_by_role=user.role to EventStore.commit(), matching the pattern in browser/router.py. All user-initiated writes (both browser and API paths) now record full provenance with user IRI and role.</done>
</task>

<task type="auto">
  <name>Task 2: Verify invite acceptance ?next= redirect is functional</name>
  <files>frontend/static/js/auth.js</files>
  <action>
The v1.0 re-audit (AUTH-03-cosmetic) flagged that handleInviteAccept in auth.js does not honor ?next=. However, Phase 7 Plan 01 (commit 8af1e65) already added ?next= redirect-back flow to all auth paths including handleInviteAccept.

Verify that the current code in handleInviteAccept's success path (around line 309-312) contains:

```javascript
var nextParams = new URLSearchParams(window.location.search);
var nextUrl = nextParams.get("next");
window.location.href = nextUrl || "/";
```

If this code IS present: The gap is already closed. No code changes needed. Document in the summary that AUTH-03-cosmetic was resolved by Phase 7 Plan 01 (commit 8af1e65).

If this code is NOT present (i.e., the success path just does `window.location.href = "/"`): Add the ?next= reading before the redirect, matching the pattern used in handleLoginForm (line 169-171) and handleVerifyToken (line 236-238):

```javascript
setTimeout(function () {
  var nextParams = new URLSearchParams(window.location.search);
  var nextUrl = nextParams.get("next");
  window.location.href = nextUrl || "/";
}, 2000);
```
  </action>
  <verify>
1. `grep -A3 "Welcome to SemPKM" frontend/static/js/auth.js` shows the setTimeout block with nextParams/nextUrl reading
2. `grep -c 'nextUrl' frontend/static/js/auth.js` returns at least 3 (login, verify, invite paths)
3. Verify all three auth success paths use the same ?next= pattern: `grep -B2 -A2 "nextUrl" frontend/static/js/auth.js`
  </verify>
  <done>handleInviteAccept reads ?next= parameter from URL and redirects to the original URL after successful invitation acceptance, consistent with login and magic link verify flows.</done>
</task>

</tasks>

<verification>
1. All user-initiated write paths (browser and API) pass both performed_by and performed_by_role to EventStore.commit()
2. All auth success paths (login, magic link verify, invite accept) honor ?next= redirect parameter
3. No regressions in existing functionality — changes are additive only
</verification>

<success_criteria>
1. `grep "performed_by_role=user.role" backend/app/commands/router.py` returns a match
2. `grep "performed_by_role=user.role" backend/app/browser/router.py` returns matches (existing, no regression)
3. All three auth success paths in auth.js use consistent ?next= redirect pattern
4. Both PROV-02-partial and AUTH-03-cosmetic gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/09-provenance-and-redirect-micro-fixes/09-01-SUMMARY.md`
</output>
