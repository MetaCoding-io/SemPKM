---
phase: 58-federation
plan: 04
type: execute
wave: 4
depends_on: [58-03]
files_modified:
  - backend/app/templates/browser/workspace.html
  - backend/app/templates/browser/partials/inbox_panel.html
  - backend/app/templates/browser/partials/collaboration_panel.html
  - backend/app/templates/browser/partials/shared_nav_section.html
  - frontend/static/js/federation.js
  - frontend/static/css/federation.css
  - backend/app/federation/router.py
  - backend/app/browser/router.py
autonomous: false
requirements: [FED-08, FED-10]

must_haves:
  truths:
    - "User can see inbox notifications in a sidebar panel with badge count for unread"
    - "User can view notification details and take actions (accept invitation, import recommendation, dismiss)"
    - "Collaboration panel shows registered remote contacts, shared graphs, and their sync status"
    - "Shared graphs appear in a SHARED section of the nav tree with their objects grouped by type"
    - "Sync Now button triggers sync and shows toast with pulled/pushed counts"
    - "Sync status shows green (<24h), yellow (>24h), or gray (never) dot"
  artifacts:
    - path: "backend/app/templates/browser/partials/inbox_panel.html"
      provides: "Inbox sidebar panel with notification list and per-type actions"
      min_lines: 40
    - path: "backend/app/templates/browser/partials/collaboration_panel.html"
      provides: "Collaboration sidebar panel with shared graphs, contacts, and sync controls"
      min_lines: 50
    - path: "backend/app/templates/browser/partials/shared_nav_section.html"
      provides: "SHARED section for nav tree showing shared graph objects by type"
      min_lines: 30
    - path: "frontend/static/js/federation.js"
      provides: "Federation UI interactions: sync, invitations, notifications, badge updates"
      min_lines: 80
    - path: "frontend/static/css/federation.css"
      provides: "Styles for federation UI components"
      min_lines: 50
  key_links:
    - from: "frontend/static/js/federation.js"
      to: "/api/federation/shared-graphs/{id}/sync"
      via: "fetch POST for Sync Now button"
      pattern: "federation.*sync"
    - from: "frontend/static/js/federation.js"
      to: "/api/inbox"
      via: "fetch GET for inbox badge count"
      pattern: "api/inbox"
    - from: "backend/app/templates/browser/partials/inbox_panel.html"
      to: "/api/inbox"
      via: "htmx hx-get for notification list"
      pattern: "hx-get.*inbox"
    - from: "backend/app/templates/browser/workspace.html"
      to: "partials/inbox_panel.html"
      via: "include for sidebar panel"
      pattern: "inbox_panel"
---

<objective>
Inbox notification UI, collaboration panel, and nav tree shared section.

Purpose: Give users a workspace interface for federation features -- viewing and acting on notifications, managing shared graphs and remote contacts, seeing shared data in the nav tree, and triggering manual sync. This completes the user-facing federation experience.

Output: Inbox sidebar panel (notification list with per-type actions), Collaboration sidebar panel (shared graphs, contacts, sync controls), SHARED nav tree section (shared graph objects by type), federation.js/css for interactions.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-federation/58-CONTEXT.md
@.planning/phases/58-federation/58-RESEARCH.md
@.planning/phases/58-federation/58-01-SUMMARY.md
@.planning/phases/58-federation/58-02-SUMMARY.md
@.planning/phases/58-federation/58-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 -->
From backend/app/federation/router.py:
```python
# POST /api/federation/shared-graphs — create shared graph
# GET /api/federation/shared-graphs — list shared graphs
# POST /api/federation/shared-graphs/{graph_id}/sync — trigger sync
# POST /api/federation/invitations — send invitation
# POST /api/federation/invitations/{id}/accept — accept invitation
# POST /api/federation/notifications/send — send notification
# GET /api/federation/contacts — list contacts
```

From backend/app/federation/inbox.py:
```python
# GET /api/inbox — list notifications (supports ?state=unread)
# PATCH /api/inbox/{notification_id} — update state
```

From backend/app/federation/schemas.py:
```python
class SharedGraphResponse(BaseModel):
    graph_iri: str; name: str; description: str; created: str
    members: list[str]; last_sync: str | None; pending_count: int

class SyncResult(BaseModel):
    pulled: int; applied: int; errors: list[str]

class ContactInfo(BaseModel):
    webid: str; label: str; instance_url: str; last_seen: str | None
```

<!-- Existing sidebar panel pattern -->
From backend/app/templates/browser/workspace.html:
```html
<!-- Left pane: nav tree with explorer-section panels -->
<div class="explorer-section expanded" id="section-objects" data-panel-name="objects">
  <div class="explorer-section-header" draggable="true" onclick="...">
    <i data-lucide="chevron-right" class="section-chevron"></i>
    <span>OBJECTS</span>
  </div>
  <div class="explorer-section-content">...</div>
</div>

<!-- Right pane: details sections -->
<details class="right-section" data-panel-name="relations" open>
  <summary class="right-section-header" draggable="true">...</summary>
  <div class="right-section-content">...</div>
</details>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inbox panel, collaboration panel, and backend partials endpoints</name>
  <files>
    backend/app/templates/browser/partials/inbox_panel.html,
    backend/app/templates/browser/partials/collaboration_panel.html,
    backend/app/templates/browser/partials/shared_nav_section.html,
    backend/app/templates/browser/workspace.html,
    backend/app/browser/router.py,
    backend/app/federation/router.py
  </files>
  <action>
    1. Create `backend/app/templates/browser/partials/inbox_panel.html`:
       - Template receives `notifications` list and `unread_count` int.
       - Renders as a right-pane `<details>` section (matching existing right-section pattern):
         ```html
         <details class="right-section" data-panel-name="inbox" open>
           <summary class="right-section-header" draggable="true">
             <i data-lucide="grip-vertical" ...></i>
             INBOX
             <span class="inbox-badge" id="inbox-badge" {% if unread_count == 0 %}style="display:none"{% endif %}>{{ unread_count }}</span>
           </summary>
           <div class="right-section-content" id="inbox-content"
                hx-get="/api/federation/inbox-partial" hx-trigger="load, every 60s" hx-swap="innerHTML">
           </div>
         </details>
         ```
       - Notification list items show: icon per type (Offer=mail-plus, Announce=megaphone, Update=refresh-cw, Note=message-square), actor label, summary text, timestamp, state indicator.
       - Per-type action buttons:
         - Offer (invitation): "Accept" (POST /api/federation/invitations/{id}/accept) and "Decline" (POST /api/federation/invitations/{id}/decline)
         - Announce (recommendation): "Import" (copies object to private graph), "Open Remote" (link), "Dismiss"
         - Update (sync alert): "Sync Now" button
         - Note (message): "Mark Read" / "Dismiss"
       - All actions use htmx hx-post/hx-patch with hx-swap to refresh the notification item.
       - Empty state: "No notifications" with inbox icon.

    2. Create `backend/app/templates/browser/partials/collaboration_panel.html`:
       - Template receives `shared_graphs` list and `contacts` list.
       - Renders as a right-pane `<details>` section:
         ```html
         <details class="right-section" data-panel-name="collaboration">
           <summary class="right-section-header" draggable="true">
             COLLABORATION
           </summary>
           <div class="right-section-content" id="collab-content"
                hx-get="/api/federation/collab-partial" hx-trigger="load" hx-swap="innerHTML">
           </div>
         </details>
         ```
       - **Shared Graphs subsection:**
         - Each graph card shows: name, member count, sync status dot (green/yellow/gray), last sync time, pending count.
         - Green dot: `last_sync` within 24 hours. Yellow: >24 hours. Gray: never synced.
         - "Sync Now" button per graph (htmx POST to /api/federation/shared-graphs/{id}/sync).
         - "New Shared Graph" button at top opening a form (name, description input fields).
         - "Invite" button per graph opening recipient handle input.
       - **Contacts subsection:**
         - List of known remote contacts with WebID label, instance URL, last interaction.
       - Empty state: "No shared graphs yet. Create one or accept an invitation."

    3. Create `backend/app/templates/browser/partials/shared_nav_section.html`:
       - Template receives `shared_graphs` list (each with name, graph_iri, objects grouped by type).
       - Renders as an `explorer-section` in the left nav tree:
         ```html
         <div class="explorer-section" id="section-shared" data-panel-name="shared">
           <div class="explorer-section-header" draggable="true" onclick="...">
             <i data-lucide="chevron-right" class="section-chevron"></i>
             <span>SHARED</span>
           </div>
           <div class="explorer-section-content">
             {% for graph in shared_graphs %}
             <div class="shared-graph-group">
               <div class="shared-graph-header">
                 <i data-lucide="share-2"></i>
                 <span>{{ graph.name }}</span>
                 <span class="sync-dot sync-{{ graph.sync_status }}"></span>
               </div>
               {% for type_group in graph.objects_by_type %}
               <div class="tree-type-group">
                 <span class="type-label">{{ type_group.label }}</span>
                 {% for obj in type_group.objects %}
                 <div class="tree-leaf shared-object" data-iri="{{ obj.iri }}">
                   <i data-lucide="share-2" class="shared-icon"></i>
                   <span>{{ obj.label }}</span>
                 </div>
                 {% endfor %}
               </div>
               {% endfor %}
             </div>
             {% endfor %}
           </div>
         </div>
         ```
       - Clicking a shared object opens it in a workspace tab (same as regular objects).
       - Shared objects show a subtle share icon to distinguish from private objects.

    4. Modify `backend/app/templates/browser/workspace.html`:
       - Include the shared nav section after the existing "my-views" section in the left pane nav tree:
         ```html
         {% include "browser/partials/shared_nav_section.html" %}
         ```
       - Include inbox and collaboration panels in the right pane after existing sections:
         ```html
         {% include "browser/partials/inbox_panel.html" %}
         {% include "browser/partials/collaboration_panel.html" %}
         ```
       - Add `<script src="/static/js/federation.js"></script>` to scripts.
       - Add `<link rel="stylesheet" href="/static/css/federation.css">` to styles.

    5. Add partial rendering endpoints to `backend/app/federation/router.py` (or `backend/app/browser/router.py`):
       - `GET /api/federation/inbox-partial` — renders inbox notification list HTML partial (htmx).
       - `GET /api/federation/collab-partial` — renders collaboration panel content HTML partial (htmx).
       - `GET /api/federation/shared-nav` — renders shared nav section HTML partial (htmx, for refresh).
       - These endpoints query the FederationService and render the respective Jinja2 templates.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && docker compose exec -T api python -c "
import os
templates_dir = '/app/app/templates/browser/partials'
required = ['inbox_panel.html', 'collaboration_panel.html', 'shared_nav_section.html']
for t in required:
    path = os.path.join(templates_dir, t)
    assert os.path.exists(path), f'Missing template: {path}'
    with open(path) as f:
        content = f.read()
    assert len(content) > 100, f'Template too short: {t}'
print('PASS: All federation templates exist with content')
"
    </automated>
  </verify>
  <done>
    - Inbox panel shows notifications with per-type action buttons
    - Collaboration panel shows shared graphs with sync status and contacts
    - SHARED nav tree section shows shared graph objects grouped by type
    - All panels use htmx for dynamic content loading
    - Templates integrated into workspace.html
  </done>
</task>

<task type="auto">
  <name>Task 2: Federation JS interactions and CSS styling</name>
  <files>
    frontend/static/js/federation.js,
    frontend/static/css/federation.css
  </files>
  <action>
    1. Create `frontend/static/js/federation.js` as an IIFE:

       **Inbox badge polling:**
       - On page load, fetch `GET /api/inbox?state=unread` to get unread count.
       - Update `#inbox-badge` text and visibility.
       - Poll every 60 seconds (or rely on htmx hx-trigger="every 60s" on the panel).

       **Sync Now handler:**
       - `window.syncSharedGraph = async function(graphId)`:
         a. Show loading state on the sync button (spinner icon).
         b. POST to `/api/federation/shared-graphs/${graphId}/sync`.
         c. Parse SyncResult response.
         d. Show toast notification: "Synced: {pulled} pulled, {applied} applied" using existing toast pattern (if project has one) or a simple `showToast()` function.
         e. Refresh the collaboration panel content and shared nav section via htmx trigger.
         f. If any open tabs show objects from this shared graph, trigger a refresh.

       **Toast notification function:**
       - `function showToast(message, type='info', duration=4000)`:
         a. Create a `<div class="federation-toast federation-toast-{type}">` with the message.
         b. Append to `<body>`, animate in (CSS transition), auto-remove after duration.
         c. Types: 'info' (blue), 'success' (green), 'error' (red).

       **Create shared graph form:**
       - `window.showCreateSharedGraph = function()`:
         a. Show inline form in collaboration panel (name input, description textarea, Create button).
         b. On submit, POST to `/api/federation/shared-graphs`, refresh panel.

       **Send invitation form:**
       - `window.showInviteForm = function(graphIri)`:
         a. Show inline input for recipient handle (user@domain or WebID URL).
         b. On submit, POST to `/api/federation/invitations` with graph_iri and recipient_handle.
         c. Show toast on success/error.

       **Notification actions (delegation from htmx):**
       - Listen for `htmx:afterSwap` on inbox content to re-initialize Lucide icons.
       - `window.acceptInvitation`, `window.declineInvitation`, `window.dismissNotification` — thin wrappers calling respective endpoints and refreshing inbox.

       **Shared nav object click:**
       - Delegate click on `.shared-object[data-iri]` to `openTab(iri, label)` (existing workspace function).

    2. Create `frontend/static/css/federation.css`:

       **Inbox badge:**
       ```css
       .inbox-badge {
         background: var(--color-accent);
         color: var(--color-bg);
         border-radius: 50%;
         min-width: 18px;
         height: 18px;
         font-size: 11px;
         display: inline-flex;
         align-items: center;
         justify-content: center;
         margin-left: auto;
       }
       ```

       **Sync status dots:**
       ```css
       .sync-dot {
         width: 8px;
         height: 8px;
         border-radius: 50%;
         display: inline-block;
         margin-left: 6px;
         flex-shrink: 0;
       }
       .sync-green { background: var(--color-success, #4caf50); }
       .sync-yellow { background: var(--color-warning, #ff9800); }
       .sync-gray { background: var(--color-text-muted); }
       ```

       **Shared object icon:**
       ```css
       .shared-object .shared-icon svg {
         width: 12px;
         height: 12px;
         flex-shrink: 0;
         stroke: currentColor;
         opacity: 0.5;
       }
       ```

       **Toast notifications:**
       ```css
       .federation-toast {
         position: fixed;
         bottom: 20px;
         right: 20px;
         padding: 10px 18px;
         border-radius: 6px;
         color: var(--color-bg);
         font-size: 13px;
         z-index: 10000;
         opacity: 0;
         transform: translateY(10px);
         transition: opacity 0.3s, transform 0.3s;
       }
       .federation-toast.show {
         opacity: 1;
         transform: translateY(0);
       }
       .federation-toast-info { background: var(--color-accent); }
       .federation-toast-success { background: var(--color-success, #4caf50); }
       .federation-toast-error { background: var(--color-error, #f44336); }
       ```

       **Notification items:**
       ```css
       .notification-item {
         padding: 8px 10px;
         border-bottom: 1px solid var(--color-border);
         display: flex;
         flex-direction: column;
         gap: 4px;
       }
       .notification-item.unread {
         background: var(--color-bg-hover);
       }
       .notification-actions {
         display: flex;
         gap: 6px;
         margin-top: 4px;
       }
       .notification-actions button {
         font-size: 11px;
         padding: 2px 8px;
         border-radius: 3px;
         border: 1px solid var(--color-border);
         background: var(--color-bg);
         color: var(--color-text);
         cursor: pointer;
       }
       .notification-actions button:hover {
         background: var(--color-bg-hover);
       }
       .notification-actions button.primary {
         background: var(--color-accent);
         color: var(--color-bg);
         border-color: var(--color-accent);
       }
       ```

       **Shared graph cards:**
       ```css
       .shared-graph-card {
         padding: 8px 10px;
         border: 1px solid var(--color-border);
         border-radius: 4px;
         margin-bottom: 6px;
       }
       .shared-graph-header {
         display: flex;
         align-items: center;
         gap: 6px;
         font-weight: 500;
       }
       ```

       **Collaboration and inbox panel spacing/layout** matching existing right-section patterns.

       Follow CLAUDE.md rules: Lucide SVGs in flex containers get `flex-shrink: 0` and size via CSS, not inline styles. SVG stroke inheritance via `stroke: currentColor`.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && test -f frontend/static/js/federation.js && test -f frontend/static/css/federation.css && echo "PASS: Federation JS and CSS files exist" && wc -l frontend/static/js/federation.js frontend/static/css/federation.css</automated>
  </verify>
  <done>
    - federation.js handles sync, toast, create shared graph, invite, and notification actions
    - federation.css styles inbox badge, sync dots, toast notifications, notification items, shared graph cards
    - Shared nav objects are clickable and open in workspace tabs
    - Inbox badge updates with unread count
    - Toast notifications appear after sync with pulled/pushed counts
    - All Lucide icons follow flex-shrink and stroke rules from CLAUDE.md
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify federation UI in workspace</name>
  <files>n/a</files>
  <action>
    Human verifies the federation UI is rendered correctly in the workspace.
    What was built: Inbox panel with notification actions, collaboration panel with shared graphs and sync status, SHARED nav tree section, toast notifications for sync results.
  </action>
  <verify>
    How to verify:
    1. Open workspace at http://localhost:3000/browser
    2. Verify right sidebar has "INBOX" and "COLLABORATION" sections
    3. Verify left nav tree has "SHARED" section (may be empty if no shared graphs)
    4. In Collaboration panel, click "New Shared Graph" and create one (name: "Test Graph")
    5. Verify the shared graph appears in both the Collaboration panel and the SHARED nav section
    6. Verify sync status dot appears (gray for never synced)
    7. Verify inbox panel shows "No notifications" empty state
    8. Check that federation.css styles are applied (badge styling, sync dots, etc.)
  </verify>
  <done>Type "approved" or describe issues</done>
</task>

</tasks>

<verification>
- Inbox panel visible in right sidebar with notification list
- Collaboration panel shows shared graphs and contacts
- SHARED section in nav tree lists shared graph objects
- Sync Now button triggers sync and shows toast
- Sync status dots show correct color based on last_sync time
- Shared objects in nav tree open workspace tabs when clicked
</verification>

<success_criteria>
- User can view and act on notifications in the inbox panel
- User can manage shared graphs and trigger sync from the collaboration panel
- Shared graph data appears in the nav tree SHARED section
- Toast notifications provide feedback on sync results
- All federation UI follows existing workspace patterns and CLAUDE.md guidelines
</success_criteria>

<output>
After completion, create `.planning/phases/58-federation/58-04-SUMMARY.md`
</output>
