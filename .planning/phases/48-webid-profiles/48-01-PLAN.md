---
phase: 48-webid-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/versions/005_webid_columns.py
  - backend/app/auth/models.py
  - backend/app/webid/__init__.py
  - backend/app/webid/service.py
  - backend/app/webid/schemas.py
  - backend/app/webid/router.py
  - backend/app/main.py
  - backend/app/templates/browser/_webid_settings.html
  - backend/app/templates/browser/settings_page.html
autonomous: true
requirements:
  - WBID-01
  - WBID-05

must_haves:
  truths:
    - "User can navigate to Settings and see a WebID Profile section"
    - "User can choose a username (lowercase, alphanumeric + hyphens, unique)"
    - "Choosing a username generates an Ed25519 key pair and stores it encrypted"
    - "User can toggle profile published/unpublished"
    - "User can add and remove rel='me' links"
    - "User can regenerate their key pair"
  artifacts:
    - path: "backend/migrations/versions/005_webid_columns.py"
      provides: "username, public_key_pem, private_key_encrypted, webid_links, webid_published columns on users table"
      contains: "add_column"
    - path: "backend/app/webid/service.py"
      provides: "Key generation, encryption, username management, profile graph construction"
      exports: ["generate_ed25519_keypair", "encrypt_private_key", "build_webid_uri", "build_profile_graph"]
    - path: "backend/app/webid/router.py"
      provides: "Settings API endpoints for username setup, link management, key regeneration"
    - path: "backend/app/templates/browser/_webid_settings.html"
      provides: "Settings UI partial for WebID profile management"
  key_links:
    - from: "backend/app/webid/router.py"
      to: "backend/app/webid/service.py"
      via: "service function calls"
      pattern: "generate_ed25519_keypair|encrypt_private_key"
    - from: "backend/app/webid/router.py"
      to: "backend/app/auth/models.py"
      via: "SQLAlchemy User model"
      pattern: "User\\.username|User\\.public_key_pem"
    - from: "backend/app/templates/browser/settings_page.html"
      to: "backend/app/templates/browser/_webid_settings.html"
      via: "Jinja2 include"
      pattern: "include.*_webid_settings"
---

<objective>
Create the WebID data model, cryptographic service layer, settings API, and settings UI for managing WebID profiles.

Purpose: Establishes the foundation for WebID identity -- users can claim a username, generate keys, manage rel="me" links, and control profile publishing. This is the data/settings layer that Plan 02's public profile endpoint will serve.

Output: Alembic migration, WebID service module, settings API endpoints, settings UI in the Settings page.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-webid-profiles/48-CONTEXT.md
@.planning/phases/48-webid-profiles/48-RESEARCH.md

@backend/app/auth/models.py
@backend/app/services/llm.py
@backend/app/config.py
@backend/app/main.py
@backend/app/templates/browser/settings_page.html
@backend/app/templates/browser/_llm_settings.html

<interfaces>
<!-- Existing patterns the executor needs -->

From backend/app/auth/models.py:
```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    email: Mapped[str] = mapped_column(String(255), unique=True, index=True)
    display_name: Mapped[str | None] = mapped_column(String(255))
    role: Mapped[str] = mapped_column(String(20), default="member")
    # ... sessions, api_tokens relationships
```

From backend/app/services/llm.py (Fernet encryption pattern):
```python
_LLM_KDF_SALT = b"sempkm-llm-config-v1"

def _get_fernet(secret_key: str) -> Fernet:
    kdf = PBKDF2HMAC(algorithm=hashes.SHA256(), length=32, salt=_LLM_KDF_SALT, iterations=100_000)
    key = base64.urlsafe_b64encode(kdf.derive(secret_key.encode()))
    return Fernet(key)

def encrypt_api_key(api_key: str, secret_key: str) -> str:
    return _get_fernet(secret_key).encrypt(api_key.encode()).decode()
```

From backend/app/config.py:
```python
class Settings(BaseSettings):
    secret_key: str = ""
    app_base_url: str = ""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration, User model columns, and WebID service</name>
  <files>
    backend/migrations/versions/005_webid_columns.py,
    backend/app/auth/models.py,
    backend/app/webid/__init__.py,
    backend/app/webid/service.py,
    backend/app/webid/schemas.py
  </files>
  <action>
1. Create Alembic migration `005_webid_columns.py`:
   - Add columns to `users` table: `username` (String(63), nullable=True), `public_key_pem` (Text, nullable=True), `private_key_encrypted` (Text, nullable=True), `webid_links` (Text, nullable=True, stores JSON array), `webid_published` (Boolean, nullable=True, server_default=false)
   - Create unique index `ix_users_username` on `username`
   - Downgrade drops columns and index

2. Add columns to User model in `backend/app/auth/models.py`:
   - `username: Mapped[str | None] = mapped_column(String(63), unique=True, index=True, nullable=True)`
   - `public_key_pem: Mapped[str | None] = mapped_column(Text(), nullable=True)`
   - `private_key_encrypted: Mapped[str | None] = mapped_column(Text(), nullable=True)`
   - `webid_links: Mapped[str | None] = mapped_column(Text(), nullable=True)` (JSON array as string)
   - `webid_published: Mapped[bool | None] = mapped_column(Boolean(), server_default=sa.false(), nullable=True)`

3. Create `backend/app/webid/__init__.py` (empty).

4. Create `backend/app/webid/service.py` with:
   - `_WEBID_KDF_SALT = b"sempkm-webid-keys-v1"` (different salt from LLM)
   - `_get_webid_fernet(secret_key: str) -> Fernet` -- same PBKDF2 pattern as llm.py
   - `encrypt_private_key(private_pem: str, secret_key: str) -> str`
   - `decrypt_private_key(ciphertext: str, secret_key: str) -> str | None`
   - `generate_ed25519_keypair() -> tuple[str, str]` -- returns (public_pem, private_pem) using `cryptography.hazmat.primitives.asymmetric.ed25519.Ed25519PrivateKey.generate()`, SubjectPublicKeyInfo for public, PKCS8 for private
   - `key_fingerprint(public_key_pem: str) -> str` -- SHA-256 of DER bytes, colon-separated hex
   - `build_webid_uri(username: str, base_url: str) -> str` -- `{base_url}/users/{username}#me`
   - `build_profile_url(username: str, base_url: str) -> str` -- `{base_url}/users/{username}`
   - `build_profile_graph(profile_url, webid_uri, display_name, public_key_pem, rel_me_links) -> rdflib.Graph` -- constructs FOAF PersonalProfileDocument with foaf:Person, sec:Ed25519VerificationKey2020, foaf:account for rel="me" links. Key node URI: `{webid_uri}-key`. Bind foaf and sec (`https://w3id.org/security#`) prefixes.
   - `get_base_url(request: Request) -> str` -- returns `settings.app_base_url` if set, else constructs from `request.base_url`

5. Create `backend/app/webid/schemas.py` with Pydantic models:
   - `UsernameSetup(BaseModel)`: `username: str` with validator (lowercase, alphanumeric + hyphens, 3-63 chars, must start/end with alphanumeric)
   - `RelMeLink(BaseModel)`: `url: HttpUrl`
   - `RelMeLinksUpdate(BaseModel)`: `links: list[HttpUrl]`
   - `WebIDProfileResponse(BaseModel)`: `username: str | None`, `webid_uri: str | None`, `published: bool`, `public_key_fingerprint: str | None`, `public_key_pem: str | None`, `links: list[str]`
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && docker compose exec api python -c "from app.webid.service import generate_ed25519_keypair, build_profile_graph, key_fingerprint; pub, priv = generate_ed25519_keypair(); print('Key gen OK:', len(pub) > 0); fp = key_fingerprint(pub); print('Fingerprint:', fp[:20]); g = build_profile_graph('http://ex.com/users/alice', 'http://ex.com/users/alice#me', 'Alice', pub, ['https://mastodon.social/@alice']); print('Graph triples:', len(g)); assert len(g) >= 8"</automated>
  </verify>
  <done>Migration 005 exists. User model has 5 new columns. WebID service generates Ed25519 keys, encrypts private keys, builds RDF profile graphs, and computes key fingerprints. Pydantic schemas validate username format and URL links.</done>
</task>

<task type="auto">
  <name>Task 2: Settings API endpoints and Settings UI partial</name>
  <files>
    backend/app/webid/router.py,
    backend/app/main.py,
    backend/app/templates/browser/_webid_settings.html,
    backend/app/templates/browser/settings_page.html
  </files>
  <action>
1. Create `backend/app/webid/router.py` with these endpoints:
   - `GET /api/webid/profile` -- returns current user's WebID profile data (username, webid_uri, published, fingerprint, public_key_pem, links). Requires auth via `get_current_user`. Returns `WebIDProfileResponse`.
   - `POST /api/webid/username` -- accepts `UsernameSetup`, validates uniqueness (catch IntegrityError for race condition), generates Ed25519 key pair, encrypts private key with Fernet using `settings.secret_key`, saves username + keys to User row. Returns 409 if username taken, 400 if user already has username (immutable). Returns updated `WebIDProfileResponse`.
   - `PUT /api/webid/links` -- accepts `RelMeLinksUpdate`, stores as JSON string in `webid_links` column. Returns updated profile.
   - `POST /api/webid/publish` -- toggles `webid_published` to True. Returns updated profile.
   - `POST /api/webid/unpublish` -- toggles `webid_published` to False. Returns updated profile.
   - `POST /api/webid/regenerate-key` -- generates new key pair, encrypts and stores, discards old. Requires existing username. Returns updated profile.
   Use `router = APIRouter(prefix="/api/webid", tags=["webid"])`.

2. Register router in `backend/app/main.py`:
   - Add import: `from app.webid.router import router as webid_router`
   - Add: `app.include_router(webid_router)`

3. Create `backend/app/templates/browser/_webid_settings.html`:
   - htmx-driven partial loaded into Settings page
   - On load, fetches profile data from `GET /api/webid/profile` via htmx or inline script
   - **If no username set:** Show username input field (text input with lowercase/hyphen validation hint), "Claim Username" button. Button POSTs to `/api/webid/username` via htmx. Show error message if 409 (taken) or 400 (invalid).
   - **If username set:** Show read-only username display, WebID URI (formatted as a link), published toggle (checkbox), public key fingerprint with "Copy PEM" button (copies `public_key_pem` to clipboard via JS), "Regenerate Key" button with confirmation dialog (JS confirm()).
   - **rel="me" links section:** List of current links with [x] remove button each. [+ Add link] button that appends an input row. Save button PUTs to `/api/webid/links`. URL validation hint on inputs.
   - Use htmx patterns consistent with `_llm_settings.html` and `_vfs_settings.html`.
   - All htmx requests include `hx-headers='{"Content-Type": "application/json"}'`.

4. Update `backend/app/templates/browser/settings_page.html`:
   - Add "WebID Profile" button to the settings sidebar nav (after Virtual Filesystem button):
     ```html
     <button class="settings-category-btn" data-category="webid-profile"
             onclick="showSettingsCategory('webid-profile')">WebID Profile</button>
     ```
   - Add corresponding panel div:
     ```html
     <div class="settings-category-panel" id="category-webid-profile"
          data-category="webid-profile" style="display:none">
       <div class="settings-category-header">
         <h2 class="settings-category-title">WebID Profile</h2>
       </div>
       {% include "browser/_webid_settings.html" %}
     </div>
     ```
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && docker compose exec api python -c "from app.webid.router import router; routes = [r.path for r in router.routes]; print('Routes:', routes); assert '/username' in routes or any('username' in r for r in routes)"</automated>
  </verify>
  <done>Settings page shows "WebID Profile" category. User can claim a username (generates keys), toggle publishing, manage rel="me" links, copy public key PEM, and regenerate keys. All API endpoints return proper JSON responses with error handling.</done>
</task>

</tasks>

<verification>
- Docker stack starts without errors after migration: `docker compose up -d && docker compose logs api | tail -20`
- Settings page loads with WebID Profile category visible
- Username claim flow works end-to-end (claim -> keys generated -> fingerprint shown)
- Link management: add, remove, save
- Publish/unpublish toggle works
- Key regeneration works with confirmation
</verification>

<success_criteria>
- User model has 5 new WebID columns (migration 005 applied)
- WebID service generates Ed25519 keys, encrypts/decrypts private keys, builds RDF graphs
- 6 API endpoints under /api/webid/ handle profile management
- Settings UI shows WebID Profile section with full username/key/link/publish management
- Username is validated (lowercase, alphanumeric + hyphens, 3-63 chars), unique, and immutable
</success_criteria>

<output>
After completion, create `.planning/phases/48-webid-profiles/48-01-SUMMARY.md`
</output>
