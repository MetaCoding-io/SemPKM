---
phase: 40-e2e-test-coverage-v24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/09-inference/inference.spec.ts
  - e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "Inference E2E test creates an edge, triggers inference, and verifies the inverse triple appears"
    - "Inference E2E test verifies bottom panel shows inferred triples with badge and filter controls"
    - "Lint dashboard E2E test verifies the dashboard loads with validation results"
    - "Lint dashboard E2E test verifies severity filtering narrows visible results"
    - "Lint dashboard E2E test verifies sorting changes result order"
  artifacts:
    - path: "e2e/tests/09-inference/inference.spec.ts"
      provides: "Inference feature E2E tests"
      min_lines: 80
    - path: "e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts"
      provides: "Lint dashboard E2E tests"
      min_lines: 60
  key_links:
    - from: "e2e/tests/09-inference/inference.spec.ts"
      to: "/api/inference/run"
      via: "API call to trigger inference"
      pattern: "api/inference/run"
    - from: "e2e/tests/09-inference/inference.spec.ts"
      to: "/api/commands"
      via: "edge.create command for test setup"
      pattern: "edge\\.create"
    - from: "e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts"
      to: "/browser/lint-dashboard"
      via: "bottom panel tab loading dashboard HTML"
      pattern: "lint-dashboard"
---

<objective>
Create Playwright E2E tests for the two major v2.4 features: OWL 2 RL inference (Phase 35-36) and the global lint dashboard (Phase 37-38).

Purpose: These are the highest-value test targets covering core v2.4 functionality — bidirectional inference links and the lint dashboard UI with filtering/sorting.
Output: Two new test spec files in `e2e/tests/09-inference/` and `e2e/tests/10-lint-dashboard/`
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-e2e-test-coverage-v24/40-RESEARCH.md

@e2e/fixtures/auth.ts
@e2e/fixtures/seed-data.ts
@e2e/helpers/selectors.ts
@e2e/helpers/wait-for.ts
@e2e/helpers/dockview.ts
@e2e/helpers/api-client.ts
@e2e/tests/04-validation/lint-panel.spec.ts

<interfaces>
<!-- Key imports and patterns the executor needs -->

From e2e/fixtures/auth.ts:
```typescript
import { test, expect } from '../../fixtures/auth';
// Provides: ownerPage (Page), ownerSessionToken (string), anonApi (ApiClient), memberPage (Page)
```

From e2e/fixtures/seed-data.ts:
```typescript
import { SEED, TYPES } from '../../fixtures/seed-data';
// SEED.projects.sempkm.iri, SEED.people.alice.iri, SEED.notes.architecture.iri, etc.
// TYPES.Note, TYPES.Project, TYPES.Person, TYPES.Concept
```

From e2e/helpers/wait-for.ts:
```typescript
import { waitForWorkspace, waitForIdle } from '../../helpers/wait-for';
```

From e2e/helpers/dockview.ts:
```typescript
import { openObjectTab } from '../../helpers/dockview';
```

From e2e/helpers/api-client.ts:
```typescript
// Available on ownerPage.context().request or via ApiClient class
// apiClient.createEdge(source, target, predicate)
// apiClient.executeCommand(command, params)
```

Key selectors (from RESEARCH.md):
- Bottom panel: `#bottom-panel`
- Panel tabs: `.panel-tab[data-panel="inference"]`, `.panel-tab[data-panel="lint-dashboard"]`
- Inference results: `#inference-results`
- Inference summary: `#inference-summary`
- Inference refresh: `.inference-refresh-btn`
- Inferred badge: `.inferred-badge`
- Inference filter (type): `.inference-filter-type`
- Inference filter (status): `.inference-filter-status`
- Lint dashboard: `.lint-dashboard` / `#lint-dashboard-container`
- Lint severity filter: `.lint-dashboard-filter-severity`
- Lint type filter: `.lint-dashboard-filter-type`
- Lint search input: `.lint-dashboard-filter-search`
- Lint sort select: `.lint-dashboard-filter-sort`
- Lint result rows: `.lint-dashboard-row`
- Lint conforms badge: `.lint-conforms`

API endpoints:
- POST `/api/inference/run` — trigger inference
- GET `/api/inference/triples` — list inferred triples
- POST `/api/commands` — execute commands (edge.create)
- GET `/browser/lint-dashboard` — lint dashboard HTML partial
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inference E2E tests</name>
  <files>e2e/tests/09-inference/inference.spec.ts</files>
  <action>
Create `e2e/tests/09-inference/inference.spec.ts` with the following test cases inside a `test.describe('Inference', ...)` block:

1. **"inference panel loads in bottom panel"** — Navigate to workspace, open bottom panel, click inference tab (`.panel-tab[data-panel="inference"]`), verify `#inference-results` or `#inference-summary` becomes visible. Use the bottom-panel-open pattern from RESEARCH.md (check panel height, call `toggleBottomPanel()` if collapsed).

2. **"creating edge and running inference produces inverse triple"** — This is the core inference flow:
   - Use API (`ownerPage.context().request`) to POST `/api/commands` with `edge.create` linking `SEED.projects.sempkm.iri` to `SEED.people.alice.iri` via `http://purl.org/dc/terms/contributor`
   - POST `/api/inference/run` to trigger inference
   - `waitForTimeout(6000)` for inference processing
   - Open bottom panel inference tab
   - Verify `#inference-results` contains at least one row (the inverse triple)
   - OR verify via API: GET `/api/inference/triples` returns results with count > 0

3. **"inference panel filters by entailment type"** — After inference has run (from test 2, tests are sequential):
   - Open inference panel
   - Check that `.inference-filter-type` (entailment_type select) exists
   - Select a filter option if available and verify results update via `waitForIdle()`

4. **"inferred triples show badge in object view"** — Open `SEED.people.alice.iri` in read mode via `openObjectTab()`. Look for `.inferred-badge` elements in the relations panel or properties area. Verify at least one badge is visible (from the inference run in test 2).

5. **"SHACL-AF rule-derived triples appear"** — Verify that SHACL-AF rules from basic-pkm model produce observable triples. Check inference panel for entries with entailment type containing "rule" or "sh:rule". If no rule-derived triples exist yet, create two notes and link them to trigger the `hasRelatedNote` rule, then re-run inference.

All tests follow the established pattern: import from `../../fixtures/auth`, use `ownerPage` and `ownerSessionToken`, `BASE_URL` constant, `waitForWorkspace()` at test start, `waitForIdle()` after htmx actions. Use `const BASE_URL = process.env.TEST_BASE_URL || 'http://localhost:3901';` at module level.

IMPORTANT: Tests are sequential (single worker). Later tests can rely on state from earlier tests (e.g., inference results from test 2 are available in tests 3-5). However, test 1 should be self-contained.

For the bottom panel open helper, create a local async function `openBottomPanelTab(page, tabName)` following the pattern from RESEARCH.md code examples section.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM/e2e && npx playwright test tests/09-inference/ --project=chromium -x</automated>
  </verify>
  <done>All inference tests pass: panel loads, edge creation produces inverse via inference, filters work, badges visible, SHACL-AF rules produce triples</done>
</task>

<task type="auto">
  <name>Task 2: Lint dashboard E2E tests</name>
  <files>e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts</files>
  <action>
Create `e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts` with the following test cases inside a `test.describe('Lint Dashboard', ...)` block:

1. **"lint dashboard loads in bottom panel"** — Navigate to workspace, open bottom panel, click lint-dashboard tab (`.panel-tab[data-panel="lint-dashboard"]`). Wait for `.lint-dashboard` or `#lint-dashboard-container` to appear (htmx lazy-loaded via `hx-trigger="revealed"`, may need `waitForTimeout(5000)` in addition to `waitForIdle()`). Verify the dashboard container has content.

2. **"lint dashboard shows validation results"** — After dashboard loads, check for `.lint-dashboard-row` elements. The seed data should have produced validation results from the async validation queue. If no rows exist, first create an object with missing required fields via API (like test pattern in `04-validation/lint-panel.spec.ts`), wait for validation, then re-check the dashboard.

3. **"severity filter narrows results"** — With the dashboard loaded:
   - Count total rows (`.lint-dashboard-row`)
   - Select "Violation" in `.lint-dashboard-filter-severity`
   - `waitForIdle()` for htmx update
   - Count filtered rows
   - Verify filtered count <= total count
   - Verify all visible rows have `.lint-severity-violation` class (or similar severity indicator)

4. **"sorting changes result order"** — With the dashboard loaded:
   - Get text content of first result row
   - Change sort via `.lint-dashboard-filter-sort` (e.g., select "object" sort)
   - `waitForIdle()`
   - Get text content of first result row again
   - Verify the content changed (or at minimum the sort control accepted the new value)

5. **"search filter works"** — Type a keyword into `.lint-dashboard-filter-search`, wait 500ms for debounce + `waitForIdle()`, verify results update (row count may decrease or remain same if search matches all).

6. **"health badge shows in panel tab"** — Check for `#lint-badge` element in the lint-dashboard panel tab button. Verify it contains a number or status text.

Follow the same import/setup patterns as Task 1. Reuse the `openBottomPanelTab()` helper pattern (define locally or import if refactored).

IMPORTANT: The lint dashboard uses htmx lazy-loading (`hx-trigger="revealed"`). After clicking the tab, the content loads asynchronously. Always wait for the actual content selector (`.lint-dashboard`) not just the panel pane becoming active.

If lint dashboard has no results (empty state), the test should still pass by verifying the empty state message exists (e.g., "No validation results" text), then create test data to populate it.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM/e2e && npx playwright test tests/10-lint-dashboard/ --project=chromium -x</automated>
  </verify>
  <done>All lint dashboard tests pass: dashboard loads, shows results, severity filter narrows rows, sort changes order, search works, health badge visible</done>
</task>

</tasks>

<verification>
Run both test suites together:
```bash
cd /home/james/Code/SemPKM/e2e && npx playwright test tests/09-inference/ tests/10-lint-dashboard/ --project=chromium -x
```

Then run the full suite to confirm no regressions:
```bash
cd /home/james/Code/SemPKM/e2e && npx playwright test --project=chromium
```
</verification>

<success_criteria>
- `e2e/tests/09-inference/inference.spec.ts` exists with 4+ passing tests covering inference flow, panel, filters, badges
- `e2e/tests/10-lint-dashboard/lint-dashboard.spec.ts` exists with 4+ passing tests covering dashboard load, filters, sort, search
- No regressions in existing 124 passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/40-e2e-test-coverage-v24/40-01-SUMMARY.md`
</output>
