---
phase: 04-admin-shell-and-object-creation
plan: 06
type: execute
wave: 3
depends_on:
  - 04-04
  - 04-05
files_modified:
  - backend/app/templates/browser/object_tab.html
  - backend/app/templates/browser/properties.html
  - backend/app/templates/browser/lint_panel.html
  - backend/app/browser/router.py
  - frontend/static/js/editor.js
  - frontend/static/js/workspace.js
  - frontend/static/css/workspace.css
autonomous: true
requirements:
  - VIEW-04
  - OBJ-03
  - SHCL-02
  - SHCL-06

must_haves:
  truths:
    - "User can view an object's details with properties form on top and Markdown body editor below in the center pane"
    - "User can write and edit an object's Markdown body via a CodeMirror 6 editor with syntax highlighting"
    - "User can save the body via Ctrl+S, which dispatches a body.set command"
    - "User can see related objects (inbound and outbound edges) in the right pane"
    - "User can see SHACL validation results in a lint panel tab in the right pane"
    - "Lint panel shows violations (red) and warnings (yellow) grouped by object"
    - "Clicking a violation in the lint panel jumps to and highlights the offending field in the form"
    - "Violations block conformance-required operations (export button disabled when violations exist); warnings never block"
    - "Unsaved changes show a dirty indicator (dot) on the tab"
  artifacts:
    - path: "backend/app/templates/browser/object_tab.html"
      provides: "Object editor tab with split view: properties form + Markdown body editor"
    - path: "backend/app/templates/browser/properties.html"
      provides: "Right pane properties/relations display"
    - path: "backend/app/templates/browser/lint_panel.html"
      provides: "Validation results panel with violations and warnings"
    - path: "frontend/static/js/editor.js"
      provides: "CodeMirror 6 initialization, save handling, dirty state tracking"
  key_links:
    - from: "frontend/static/js/editor.js"
      to: "CodeMirror 6 ESM CDN"
      via: "ESM import from esm.sh with pinned versions"
      pattern: "import.*codemirror"
    - from: "backend/app/browser/router.py"
      to: "backend/app/services/validation.py"
      via: "ValidationService for lint panel data"
      pattern: "get_validation_service|get_validation_queue"
    - from: "backend/app/templates/browser/lint_panel.html"
      to: "frontend/static/js/workspace.js"
      via: "Click handler jumps to offending field"
      pattern: "jumpToField|scrollIntoView"
    - from: "backend/app/browser/router.py"
      to: "backend/app/triplestore/client.py"
      via: "SPARQL query for related objects (edges)"
      pattern: "edge.*SPARQL|outbound|inbound"
---

<objective>
Implement the object page with properties form + Markdown body editor, related objects display in the right pane, and the SHACL validation lint panel with conformance gating.

Purpose: This plan completes the object editing experience -- users can view and edit all aspects of an object (properties, body, relations) and see validation feedback. The lint panel provides the SHACL validation UI that distinguishes violations from warnings and gates conformance operations.

Output: Complete object tab with split view (form + CodeMirror editor), related objects panel, lint panel with color-coded validation results, and export gating based on violation status.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-shell-and-object-creation/04-RESEARCH.md
@.planning/phases/04-admin-shell-and-object-creation/04-04-SUMMARY.md
@.planning/phases/04-admin-shell-and-object-creation/04-05-SUMMARY.md
@backend/app/browser/router.py
@backend/app/services/validation.py
@backend/app/validation/queue.py
@frontend/static/js/workspace.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Object tab with split view and CodeMirror Markdown editor</name>
  <files>
    backend/app/templates/browser/object_tab.html
    backend/app/browser/router.py
    frontend/static/js/editor.js
    frontend/static/css/workspace.css
  </files>
  <action>
    1. Create `backend/app/templates/browser/object_tab.html`:
       - htmx partial for the center pane editor area content
       - Split view layout (per CONTEXT.md: "properties form on top, Markdown body editor below -- both visible at once"):
         * Top section: include the SHACL-driven form from `forms/object_form.html` in edit mode
         * Bottom section: Markdown body editor container with `id="body-editor-{object_iri encoded}"`
         * A horizontal Split.js split between form and editor (nested split within the center pane)
       - Load body content from the server: the route provides the current body text
       - Form section shows the properties form (from Plan 05)
       - Editor section has a toolbar area (bold, italic, heading, link buttons -- Claude's discretion per CONTEXT.md) and the CodeMirror container div
       - Save button (or Ctrl+S only): "Save" button in a toolbar above the form + editor
       - Saving dispatches both `object.patch` (for property changes) and `body.set` (for body changes) as a batch command

    2. Update `backend/app/browser/router.py`:
       - Update `GET /browser/objects/{object_iri}` to return the full object tab:
         * Query the object's current body text via SPARQL (look for sempkm:body or the body content predicate used by body.set command)
         * Query current property values for the form
         * Get the object type and form metadata from ShapesService
         * Render `browser/object_tab.html` with form data + body text
       - Add `POST /browser/objects/{object_iri}/body` endpoint:
         * Accept body content as text/plain or form field
         * Dispatch `body.set` command with the new body content
         * Return success indicator (htmx swap or JS event to mark tab clean)

    3. Create `frontend/static/js/editor.js`:
       - ESM module that initializes CodeMirror 6 (per Research Pitfall 1):
         * Use esm.sh with pinned versions for reliable import:
           ```javascript
           import {EditorView, basicSetup} from "https://esm.sh/codemirror@6.65.7"
           import {markdown} from "https://esm.sh/@codemirror/lang-markdown@6"
           import {keymap} from "https://esm.sh/@codemirror/view@6"
           ```
         * If esm.sh causes issues, fall back to a local bundle strategy (note in code comment)
       - `initEditor(containerId, initialContent, objectIri)`:
         * Create EditorView with extensions: basicSetup, markdown(), custom keymap for Ctrl+S
         * Mount into the container div
         * Track dirty state: set up `updateListener` that detects changes and calls `markDirty(objectIri)` from workspace.js
       - Ctrl+S handler: save body content
         * Get editor content via `view.state.doc.toString()`
         * POST to `/browser/objects/{objectIri}/body` via fetch or htmx
         * On success: call `markClean(objectIri)` from workspace.js
       - Expose `initEditor` globally for use after htmx swaps (called from htmx:afterSwap event)

    4. Handle editor initialization after htmx swap:
       - Add `htmx:afterSwap` event listener in workspace.js or editor.js
       - When content with a `#body-editor-*` container is swapped in, initialize CodeMirror on it
       - Use `htmx.on('htmx:afterSwap', ...)` to detect new editor containers

    5. Update `frontend/static/css/workspace.css`:
       - Horizontal split for form + editor (nested Split.js)
       - CodeMirror container styling: border, min-height, proper sizing
       - Editor toolbar styling
       - Dirty indicator dot on tabs
  </action>
  <verify>
    With containers running:
    - Click an object in the nav tree (e.g., a seed Person object)
    - Center pane shows: properties form on top, Markdown editor below
    - CodeMirror renders with syntax highlighting for Markdown
    - Type in the editor -- tab shows dirty indicator (dot)
    - Ctrl+S saves body -- dirty indicator clears
    - Modified properties can also be saved
    - No JS console errors from CodeMirror ESM imports
  </verify>
  <done>
    Object tab renders with split view: SHACL properties form on top, CodeMirror Markdown editor on bottom. CodeMirror 6 loads via ESM with markdown syntax highlighting. Ctrl+S saves both properties and body. Dirty state tracked and shown on tab. Editor initializes correctly after htmx content swaps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Related objects panel and SHACL lint panel with conformance gating</name>
  <files>
    backend/app/templates/browser/properties.html
    backend/app/templates/browser/lint_panel.html
    backend/app/browser/router.py
    frontend/static/js/workspace.js
  </files>
  <action>
    1. Create `backend/app/templates/browser/properties.html`:
       - htmx partial for the right pane content
       - Two sections with tabs at top: "Relations" | "Lint"
       - **Relations tab** (default visible):
         * Shows related objects: outbound edges (this object -> other) and inbound edges (other -> this object)
         * SPARQL query for relations:
           ```sparql
           # Outbound: this object as subject
           SELECT ?predicate ?object ?objectLabel WHERE {
             <{object_iri}> ?predicate ?object .
             FILTER(isIRI(?object))
             FILTER(?predicate != rdf:type)
           }
           ```
           Plus inbound query (this object as object in triples).
         * Each relation shown as: predicate label -> object label (clickable to open that object in a tab)
         * Group by predicate
         * Use LabelService to resolve IRIs to labels
       - **Lint tab:**
         * Loads lint panel content via `hx-get="/browser/lint/{object_iri}"` on tab click
         * Content from lint_panel.html partial

    2. Create `backend/app/templates/browser/lint_panel.html`:
       - htmx partial showing SHACL validation results for the current object
       - Query the latest validation report from ValidationService/ValidationQueue:
         * Get latest validation report
         * Filter results to the current object (focus node matching the object IRI)
       - Display violations and warnings (per CONTEXT.md decisions):
         * Violations: red circle icon + message + source property
         * Warnings: yellow triangle icon + message + source property
         * Each item is clickable: `onclick="jumpToField('{{ result.source_property }}')"` -- scrolls to and highlights the offending field in the properties form
       - Summary counts at top: "{N} violations, {M} warnings"
       - If no issues: green checkmark "All validations pass"
       - **Conformance gating** (SHCL-06):
         * If violations exist, show notice: "Export blocked: {N} violations must be resolved"
         * Any export/publish action button (if present) should be disabled when violations > 0
         * Warnings show count but explicit note: "Warnings do not block operations"

    3. Add right pane routes to `backend/app/browser/router.py`:
       - `GET /browser/relations/{object_iri}` -> query and render related objects for the right pane
         * Use TriplestoreClient to query outbound and inbound edges
         * Use LabelService to resolve all IRIs to labels
         * Render properties.html relations section
       - `GET /browser/lint/{object_iri}` -> get validation results for this object
         * Get latest report from validation_queue.latest_report (in-memory cache, per 02-02 summary)
         * Parse report to extract results where focus node matches object_iri
         * Determine conforms status for this specific object
         * Render lint_panel.html with violations, warnings, and conformance status

    4. Update `frontend/static/js/workspace.js`:
       - Add `jumpToField(propertyPath)` function:
         * Find the form field by ID: `document.getElementById('field-' + encodeURIComponent(propertyPath))`
         * Scroll into view: `element.scrollIntoView({behavior: 'smooth', block: 'center'})`
         * Add highlight class temporarily (flash yellow background for 2 seconds)
         * Also show the validation message inline below the field
       - When opening an object, also load the right pane:
         * After loading object tab in center, load relations in right pane
         * `htmx.ajax('GET', '/browser/relations/' + encodeURIComponent(objectIri), {target: '#right-content'})`
       - Update `triggerValidation()` (command palette action):
         * POST to `/api/commands` with a validation trigger, or just refresh the lint panel
         * After validation: reload lint panel content

    5. Wire debounced validation trigger (per CONTEXT.md: "SHACL validation runs live as the user types, with debounce"):
       - Per Research Pitfall 4 (two-tier validation):
         * **Client-side**: Check required fields on blur (simple JS, mark empty required fields red immediately)
         * **Server-side**: After save (not on every keystroke), trigger validation queue and poll for results
         * When save completes, auto-refresh the lint panel after a short delay (validation queue processes async)
       - Add a "Validating..." spinner state to the lint panel while validation is pending
       - Use `hx-get="/browser/lint/{object_iri}" hx-trigger="every 3s"` polling while the lint panel is visible, or trigger refresh after save
  </action>
  <verify>
    With containers running:
    - Open an object -- right pane shows Relations tab with outbound/inbound edges
    - Click "Lint" tab -- shows validation results for the object
    - Violations shown in red, warnings in yellow
    - Click a violation -- form scrolls to and highlights the offending field
    - If violations exist: conformance notice shows "Export blocked"
    - If all clear: green checkmark "All validations pass"
    - Save an object with missing required field -- after validation, lint panel updates with violation
    - Related objects are clickable and open in new tabs
  </verify>
  <done>
    Right pane shows related objects (outbound + inbound edges) with labels and clickable navigation. Lint panel tab displays SHACL validation results with color-coded severity (red violations, yellow warnings). Clicking a violation jumps to and highlights the offending field. Conformance gating disables export when violations exist. Validation results refresh after save operations. Two-tier validation: instant client-side checks for required fields, server-side SHACL validation on save.
  </done>
</task>

</tasks>

<verification>
1. Object tab: split view with properties form on top, Markdown editor below
2. CodeMirror 6 loads and provides Markdown syntax highlighting
3. Ctrl+S saves both properties and body content
4. Dirty indicator appears on tab when content is modified
5. Right pane shows related objects grouped by predicate
6. Clicking a related object opens it in a new tab
7. Lint panel shows violations (red) and warnings (yellow)
8. Clicking a violation scrolls to and highlights the field
9. Conformance gating: export blocked message when violations exist
10. Validation results refresh after save
11. Tab management works: open, switch, close, dirty state
</verification>

<success_criteria>
- Objects display with properties form + Markdown body editor in a split view
- CodeMirror 6 provides syntax-highlighted Markdown editing with save shortcut
- Related objects (edges) visible in right pane with navigation
- Lint panel shows per-object SHACL validation with severity-coded results
- Clicking validation issues navigates to offending fields
- Violations block conformance ops; warnings never block
- Two-tier validation: client-side instant checks + server-side SHACL on save
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-shell-and-object-creation/04-06-SUMMARY.md`
</output>
