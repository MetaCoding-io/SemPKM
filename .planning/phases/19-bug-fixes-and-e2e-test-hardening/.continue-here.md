---
phase: 19-bug-fixes-and-e2e-test-hardening
task: post-phase
status: in_progress
last_updated: 2026-02-27T02:05:00.000Z
---

<current_state>
v2.0 complete. Post-milestone bug fixes ongoing via live browser testing with user.
Start Tour fix is done. Continuing site walkthrough for any remaining bugs.
</current_state>

<completed_work>

- Fix 6 (dac697a): Workspace pane close buttons + Split.js size normalization
  - `workspace.html`: added close buttons (panel-left-close / panel-right-close icons) to Explorer and Details pane headers
  - `workspace.js`: when restoring a pane while another pane is hidden, the filtered restoreSizes didn't sum to 100% — added normalization so layout fills full width
  - Root cause: hiding right (saves [a,b,c]) then hiding nav, then restoring right filtered to [b,c] summing to ~75%, leaving 25% dead zone

- Fix 1 (33c0f02): nginx merge_slashes off — IRI path params mangled → 400
- Fix 2 (c0678e5): docs static files path resolution in Docker
- Fix 3 (a753c1c): Phase 19 closed, v2.0 milestone complete
- Fix 4 (d81e2dd): "Docs & Tutorials" sidebar link now opens standalone /guide page instead of workspace tab
  - `_sidebar.html`: changed from onclick="openDocsTab()" to href="/guide" with hx-get
  - `shell/router.py`: added GET /guide and GET /guide/{filename:path} routes
  - `guide.html`: new standalone hub template extending base.html, chapter buttons use hx-target="#app-content"
  - `guide_article.html`: new standalone article template, renderMarkdownFromUrl call in {% block scripts %} (after markdown-render.js loads)
  - Fixed Jinja2 syntax error: removed {% block scripts %} text from JS comment
- Fix 5 (31d03f0): "Start Tour" buttons on /guide now work
  - `guide.html`: onclick navigates to /browser/?tour=welcome (or ?tour=create-object)
  - `workspace.js`: maybeStartTour() reads ?tour= param, strips it from URL, waits 600ms, launches tour
  - `tutorials.js`: fixed getDriver() namespace — IIFE exports window.driver.js.driver, not window['driver.js'].driver

</completed_work>

<remaining_work>

- Pre-existing console error on all non-workspace pages: workspace.js tries to init #nav-pane which doesn't exist → `Error: Selector #nav-pane did not match a DOM element` (workspace.js:1226). Not new, pre-existing.
- User was doing a site tour — may want to continue exploring other features.

</remaining_work>

<decisions_made>

- /guide is a standalone page (not workspace tab) — correct behavior confirmed by user
- guide_article.html uses {% block scripts %} for renderMarkdownFromUrl to ensure markdown-render.js is loaded first
- Chapter-to-chapter links in markdown navigate via full page load to /guide/02-core-concepts.md — works correctly
- Start Tour: navigates to workspace with ?tour= param rather than calling tour fn directly from /guide
- Driver.js IIFE global is window.driver.js.driver (not window['driver.js'].driver as previously documented)

</decisions_made>

<blockers>

None active.

</blockers>

<context>
User is doing a live site walkthrough, finding bugs as they go. We fix them one by one in real-time with Playwright for verification. Dev stack at http://localhost:3000.
</context>

<next_action>
Continue site walkthrough or move on to /gsd:complete-milestone for v2.1 planning.
</next_action>
