---
phase: 19-bug-fixes-and-e2e-test-hardening
plan: "03"
total_plans: 3
status: in_progress
last_updated: 2026-02-26T21:59:13.389Z
---

<current_state>
Wave 1 complete (19-01 and 19-02 both done and committed). Wave 2 (19-03 E2E hardening) has NOT been started — user paused before the 19-03 agent was spawned. Resume by executing plan 19-03 only.
</current_state>

<completed_work>

- Plan 19-01: Backend bug fixes — COMPLETE (commits d0d41e0, 8cd75b5, 9b52026)
  - EventStore DI: 4 ad-hoc EventStore(client) constructions replaced with Depends(get_event_store)
  - Label cache: label_service.invalidate(event_result.affected_iris) after every event_store.commit()
  - UTC datetime: datetime.now() → datetime.now(timezone.utc) at 2 sites in browser/router.py
  - CORS: CORS_ORIGINS env var (empty = wildcard without credentials; non-empty = specific origins with credentials)
  - COOKIE_SECURE: env var controls session cookie secure flag (default True)
  - Debug endpoints: /sparql and /commands now require require_role("owner")
  - IRI validation: _validate_iri helper at all 6 SPARQL interpolation points

- Plan 19-02: UI bug fixes — COMPLETE (commits 3b9ec72, bd421be, 0a65113)
  - Tab active guard: switchTabInGroup returns early if tab already active (no htmx reload)
  - Tag pills: model:basic-pkm:tags values render as #Label rounded pills in read-only (object_read.html) and edit form (_field.html)
  - Nav tree tooltip: initNavTreeTooltips() in workspace.js — event-delegated, type+label in graph style, position:fixed overflow-safe
  - type_label added to tree_children router endpoint context (router.py change)
  - Graph node hover tooltip: confirmed already working, confirmation comment added to graph.js
  - 4 reported bugs (docs tab, tutorial launch, edit button, autocomplete) investigated and confirmed already fixed in prior phases — investigation comments added

</completed_work>

<remaining_work>

- Plan 19-03: E2E test hardening — NOT STARTED
  - Task 1: Add 3 new Playwright spec files:
    - e2e/tests/03-navigation/split-panes.spec.ts (Ctrl+\\ creates split, tabs are independent)
    - e2e/tests/06-settings/event-log.spec.ts (event log panel opens, shows event rows)
    - e2e/tests/06-settings/tutorials.spec.ts (Docs tab opens, tutorial button visible)
  - Task 1 also: Add infrastructure comment to e2e/tests/00-setup/01-setup-wizard.spec.ts explaining the known fresh-stack constraint (do NOT skip or tag the tests)
  - Task 2: Run full chromium suite, verify ≥118/123 passing, fix any regressions from Wave 1 changes

</remaining_work>

<decisions_made>

- 19-01: Conditional CORS — empty CORS_ORIGINS = wildcard without credentials; set CORS_ORIGINS for specific origins with credentials
- 19-01: COOKIE_SECURE defaults True; local dev needs COOKIE_SECURE=false
- 19-01: IRI validation uses urlparse scheme+netloc; urn: IRIs rejected (no netloc) — acceptable since all object IRIs are https://
- 19-02: 4 of 8 reported UI bugs were already fixed in prior phases (docs tab, tutorial launch, edit button first-touch, autocomplete dropdown) — no code changes, just investigation comments
- 19-02: type_label resolved server-side via label_service.resolve_batch in tree_children endpoint to populate nav tree tooltips
- 19-02: Graph node hover already working — typeLabel populated from backend views/service.py

</decisions_made>

<blockers>

None — Wave 1 completed cleanly. 19-03 is unblocked and ready to execute.

</blockers>

<context>
We were mid-execution of /gsd:execute-phase 19. The orchestrator had just completed Wave 1 (spawning 19-01 and 19-02 in parallel, both returned successfully). The orchestrator was about to spawn the Wave 2 agent for 19-03 when the user paused to take the laptop home. The 19-03 agent was NEVER started — the tool use was rejected. 19-03 is a clean slate.

The E2E suite was at 118/123 passing (5 known failures in setup wizard) before Phase 19 work started. Wave 1 changes are backend/frontend fixes that could have introduced regressions — 19-03 Task 2 will verify.
</context>

<next_action>
Run `/gsd:execute-phase 19` — it will detect 19-01 and 19-02 as complete (SUMMARY.md files exist) and execute only the remaining plan 19-03. Alternatively, spawn the 19-03 executor directly using the execute-plan workflow.
</next_action>
