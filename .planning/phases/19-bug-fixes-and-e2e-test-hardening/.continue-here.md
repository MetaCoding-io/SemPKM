---
phase: 19-bug-fixes-and-e2e-test-hardening
task: post-phase
status: in_progress
last_updated: 2026-02-27T01:38:57.204Z
---

<current_state>
Phase 19 is fully CLOSED (all 3 plans, commits, SUMMARY, ROADMAP/STATE updated).
We are now investigating UI bugs the user found while testing v2.0.
Two bugs have been fixed. A third is under investigation.
</current_state>

<completed_work>

- Fix 1 (committed 33c0f02): nginx `merge_slashes off` — nginx was decoding %2F and
  collapsing // to /, mangling https%3A%2F%2F... path params → 400 Invalid IRI.
  Fixed: `frontend/nginx.conf` + hardened `_validate_iri` in `browser/router.py`.
  E2E suite: 124/129 passing (5 known setup-wizard failures only).

- Fix 2 (committed c0678e5): docs static files path resolution — `main.py` resolved
  `/docs/guide` via parent.parent.parent from `/app/app/main.py` = `/`, which doesn't
  exist. Docker volume mounts docs at `/app/docs`, so `parent.parent / "docs" / "guide"`
  = `/app/docs/guide` is correct. Added fallback to parent.parent.parent for local dev.
  Also added `./docs:/app/docs:ro` volume to `docker-compose.test.yml`.
  Result: `/docs/guide/01-what-is-sempkm.md` now returns 200 through nginx.

- Phase 19 closed: ROADMAP updated, STATE.md updated (v2.0 complete), old
  .continue-here.md deleted, committed a753c1c.

</completed_work>

<remaining_work>

- Bug 3 (IN PROGRESS): "the left hand menu does not link to anything!"
  User is in the workspace (/browser/) and clicking sidebar nav items does nothing.

  Investigation so far:
  - `_sidebar.html` is included in `base.html` with nav groups (Admin/Meta/Apps/Debug)
  - Links use `hx-target="#app-content"` which DOES exist in the DOM (base.html:54)
  - BUT: workspace-layout CSS may be hiding `#app-content` or the workspace
    container may cover it visually
  - Some links are explicitly `class="nav-link disabled"` (Users, Teams, SPARQL,
    Commands, Event Log) — these are intentionally non-functional
  - "Docs & Tutorials" uses `onclick="openDocsTab()"` — JS function in workspace
  - Did NOT yet confirm: what specific item the user is clicking that does nothing
  - Did NOT yet confirm: whether the issue is the _sidebar.html nav groups OR the
    workspace Explorer nav tree (clicking objects in the tree to open tabs)

  Most likely cause: when in the workspace, htmx swaps #app-content but the
  workspace-layout CSS makes the result invisible, OR the user is clicking items
  marked disabled, OR the sidebar groups are collapsed.

  Next debugging steps:
  1. Log in to dev stack at http://localhost:3000 via playwright
  2. Navigate to /browser/ and screenshot the left panel
  3. Click a nav item and see what happens (network request? DOM change?)
  4. Check if workspace-layout CSS hides #app-content

</remaining_work>

<decisions_made>

- nginx merge_slashes off: root cause confirmed via API logs showing
  `https%3A/test.example.org` (colon not decoded, slash decoded+merged)
- docs path: parent.parent approach for Docker, parent.parent.parent fallback for local dev

</decisions_made>

<blockers>

- Context was high (85%) when pausing — fresh context needed to continue Bug 3
- User's dev stack is at http://localhost:3000 (docker-compose.yml, not test)
- Need to authenticate via playwright to inspect the workspace UI

</blockers>

<context>
v2.0 is done. We're in cleanup/polish mode fixing whatever the user finds.
Bug 3 is a UX navigation issue — either the sidebar links silently fail in workspace
context, or certain items are disabled and the user doesn't realize it, or the
workspace Explorer nav tree clicks don't open editor tabs. Need a fresh session
to properly investigate with playwright screenshots.
</context>

<next_action>
1. /clear to get fresh context
2. /gsd:resume-work to reload state
3. Log into dev stack at http://localhost:3000 via playwright (magic link flow)
4. Navigate to /browser/ and screenshot the left panel
5. Try clicking sidebar nav items to diagnose "does not link to anything"
</next_action>
