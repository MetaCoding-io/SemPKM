---
phase: 19-bug-fixes-and-e2e-test-hardening
task: post-phase
status: in_progress
last_updated: 2026-02-27T01:55:45.839Z
---

<current_state>
v2.0 complete. Post-milestone bug fixes ongoing via live browser testing with user.
Currently debugging the Docs & Tutorials page and Start Tour functionality.
</current_state>

<completed_work>

- Fix 1 (33c0f02): nginx merge_slashes off — IRI path params mangled → 400
- Fix 2 (c0678e5): docs static files path resolution in Docker
- Fix 3 (a753c1c): Phase 19 closed, v2.0 milestone complete
- Fix 4 (d81e2dd): "Docs & Tutorials" sidebar link now opens standalone /guide page instead of workspace tab
  - `_sidebar.html`: changed from onclick="openDocsTab()" to href="/guide" with hx-get
  - `shell/router.py`: added GET /guide and GET /guide/{filename:path} routes
  - `guide.html`: new standalone hub template extending base.html, chapter buttons use hx-target="#app-content"
  - `guide_article.html`: new standalone article template, renderMarkdownFromUrl call in {% block scripts %} (after markdown-render.js loads)
  - Fixed Jinja2 syntax error: removed {% block scripts %} text from JS comment

</completed_work>

<remaining_work>

- Bug: "Start Tour" buttons on /guide page do nothing
  ROOT CAUSE: Two issues:
  1. `getDriver()` checks `window['driver.js'] && window['driver.js'].driver` — Driver.js CDN may not be loading correctly on /guide (not confirmed yet if CDN is accessible)
  2. Even if Driver.js loads, tour steps reference workspace DOM elements (#nav-pane, .explorer-pane, etc.) that DON'T EXIST on the /guide standalone page

  CORRECT FIX: "Start Tour" buttons should navigate to /browser/ first, then launch the tour.
  Implementation: Change the onclick handlers in guide.html from:
    onclick="if(typeof window.startWelcomeTour==='function'){window.startWelcomeTour();}"
  to something like:
    onclick="window.location.href='/browser/?tour=welcome'"
  Then in workspace.js on load, check for ?tour= query param and auto-start the appropriate tour.
  OR: simpler — just navigate to /browser/ and let the user start the tour from there (remove Start Tour buttons from /guide entirely, or grey them out with a note "Start from the workspace").

- Pre-existing console error on all non-workspace pages: workspace.js tries to init #nav-pane which doesn't exist → `Error: Selector #nav-pane did not match a DOM element` (workspace.js:1226). Not new, pre-existing.

- User was doing a site tour — may want to continue exploring other nav items after tour fix.

</remaining_work>

<decisions_made>

- /guide is a standalone page (not workspace tab) — correct behavior confirmed by user
- guide_article.html uses {% block scripts %} for renderMarkdownFromUrl to ensure markdown-render.js is loaded first
- Chapter-to-chapter links in markdown (e.g. "Core Concepts" at bottom of ch1) navigate via full page load to /guide/02-core-concepts.md — works correctly

</decisions_made>

<blockers>

- Start Tour: tours require workspace DOM — can't run from /guide page without navigation to /browser/ first

</blockers>

<context>
User is doing a live site walkthrough, finding bugs as they go. We fix them one by one in real-time with Playwright for verification. Session key for auth: sempkm_session=2wOA4B8wFMu-M_UqCUpkYHE30PLECURI3EsP_wmBnCc (set as cookie on localhost:3000). Dev stack at http://localhost:3000.
</context>

<next_action>
1. /clear + /gsd:resume-work
2. Set sempkm_session cookie and navigate to http://localhost:3000/guide
3. Fix "Start Tour" buttons: navigate to /browser/?tour=welcome (or similar) instead of calling startWelcomeTour() directly
4. OR: decide with user whether tours should be removed from /guide or redirected to workspace
</next_action>
