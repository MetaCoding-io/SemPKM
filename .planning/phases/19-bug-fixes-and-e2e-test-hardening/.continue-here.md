---
phase: 19-bug-fixes-and-e2e-test-hardening
task: post-phase
status: in_progress
last_updated: 2026-02-27T05:04:02.783Z
---

<current_state>
v2.0 complete. Post-milestone bug fixes via live browser walkthrough with user.
All bugs from this session are fixed and committed. Context window exhausted mid-session.
</current_state>

<completed_work>

- Fix (608b2fb): Autocomplete dropdown position, Cancel button text, live title update on save
  - forms.css: .suggestions-dropdown was position:fixed — overridden by forms.css loading after
    workspace.css (where I'd put the fix). Fixed in forms.css directly: position:absolute; top:100%
  - workspace.js: toggleObjectMode set 'Done' not 'Cancel' — fixed to 'Cancel'
  - workspace.js: objectSaved event now updates toolbar title + tab bar label on save
  - router.py save_object: extracts new label from saved properties, includes in HX-Trigger payload

- Fix (7f13384): Reference fields show labels not IRIs; property tooltips use compact IRIs
  - main.py: added compact_iri Jinja2 filter (full IRI → prefix:localname via COMMON_PREFIXES)
  - _field.html: render_field/render_input accept ref_labels param; reference search inputs
    show resolved human-readable label in visible input, raw IRI in hidden input
  - _field.html: label title attr now uses compact_iri filter (e.g. dcterms:title not full URL)
  - _group.html: passes ref_labels through to render_field; group summary uses compact_iri
  - object_form.html: passes ref_labels from template context to all macro calls
  - router.py save_object: resolves labels for IRI-valued form fields, passes ref_labels to
    re-render context so labels persist after save

- Fix (8a1c661) [previous session]:
  - Autocomplete hx-vals fix (htmx 2.x doesn't bind `this` in js: expressions)
  - _to_rdf_value now detects ISO 8601 datetimes → xsd:dateTime typed literal (SHACL fix)
  - Done → Cancel rename (object_tab.html template, later overridden by workspace.js — fixed above)
  - Button hover colors: green for Cancel/Edit, red for Save

</completed_work>

<remaining_work>

- No known blockers. User was doing a live walkthrough finding bugs.
- Potential follow-up: multi-valued reference fields — save_object only stores values[0] per key
  (e.g. if you add 2 About Concepts, only the first saves). Not reported as bug yet.
- Continue site walkthrough to find more bugs, OR
- /gsd:complete-milestone to archive v2.0 and start v3.0 planning

</remaining_work>

<decisions_made>

- compact_iri filter: sorts by namespace length descending so longer prefixes match first;
  falls back to last '#', '/', or ':' segment
- ref_labels threading: passed explicitly through macro chain (render_group → render_field →
  render_input) rather than using Jinja2 global context (imported macros don't get context)
- save_object ref_labels: scans raw form_data (not just properties dict) to catch all IRI values
  including multi-valued submissions
- forms.css is canonical location for .suggestions-dropdown styles (not workspace.css)

</decisions_made>

<blockers>

None active.

</blockers>

<context>
User is doing a live browser walkthrough, fixing bugs one by one. Dev stack at http://localhost:3000.
The reference field label fix required threading ref_labels through the entire Jinja2 macro chain
(object_form.html → _group.html → _field.html). The compact_iri filter is registered in main.py
alongside dict_without and urlencode filters.

Key architectural note: Jinja2 imported macros ({% from ... import %}) do NOT inherit the calling
template's context — you must pass variables explicitly. This is why ref_labels had to be plumbed
through every macro call.
</context>

<next_action>
Continue live walkthrough or run /gsd:complete-milestone to archive v2.0 and plan v3.0.
If continuing walkthrough: navigate to a note, enter edit mode, verify reference fields show labels,
hover property labels to see compact IRIs in tooltip, test save and verify title updates in tab bar.
</next_action>
