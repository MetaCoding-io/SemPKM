---
phase: 19-bug-fixes-and-e2e-test-hardening
plan: "03"
total_plans: 3
status: in_progress
last_updated: 2026-02-26T22:30:00.000Z
---

<current_state>
ALL 3 PLANS ARE EXECUTED. 19-01, 19-02, 19-03 all have commits and SUMMARY.md files.
ROADMAP and STATE still show Phase 19 as incomplete — needs to be updated.

E2E suite result: 122/129 passing. 5 known setup-wizard failures. 2 NEW regressions remain:
1. tests/01-objects/edit-object.spec.ts:105 — save body via browser endpoint
2. tests/04-validation/lint-panel.spec.ts:85 — lint violation on missing required fields

COOKIE_SECURE fix committed (docker-compose.yml gets COOKIE_SECURE=false default).
Docker stack needs restart for this to take effect.
</current_state>

<completed_work>

- 19-01: Backend bugs — COMPLETE (commits d0d41e0, 8cd75b5, 9b52026)
- 19-02: UI bugs — COMPLETE (commits 3b9ec72, bd421be, 0a65113)
- 19-03: E2E tests — COMPLETE (commits 7d9fc70, ccd22dc) + SUMMARY written (a8f07ed)
- docker-compose.yml: COOKIE_SECURE=false default added (a8f07ed)

</completed_work>

<remaining_work>

1. RESTART DOCKER STACK for COOKIE_SECURE fix to take effect:
   cd /home/james/Code/SemPKM
   docker compose down && docker compose up -d
   (wait for healthy, then rerun failing tests)

2. INVESTIGATE 2 remaining regressions:
   cd e2e && npx playwright test tests/01-objects/edit-object.spec.ts:105 tests/04-validation/lint-panel.spec.ts:85 --project=chromium

   Root cause: likely app state ordering issue (Docker stack not restarted after 19-01 code changes),
   OR the _validate_iri check rejecting something unexpected, OR tests are order-dependent on other test state.

   The _validate_iri current impl (post-fix): only checks scheme + forbidden chars — should accept urn: and https://.
   app.state.event_store IS set in main.py:86 so get_event_store DI is fine.

   After restart: if failures clear → COOKIE_SECURE was the root cause (auth was broken).
   If failures persist → dig into what IRI is being passed and what the 400/500 response says.

3. UPDATE ROADMAP + STATE for Phase 19 completion:
   node /home/james/.claude/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress 19 01 complete
   node /home/james/.claude/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress 19 02 complete
   node /home/james/.claude/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress 19 03 complete
   (then update STATE.md manually or via /gsd:progress)

4. DELETE .continue-here.md when phase is complete.

</remaining_work>

<decisions_made>

- COOKIE_SECURE defaults false in docker-compose (HTTP local dev)
- config.py default remains True (correct for production)
- 19-03 SUMMARY.md documents 2 remaining regressions honestly

</decisions_made>

<blockers>

2 E2E test regressions blocking Phase 19 completion. Most likely fix: restart Docker stack
to pick up backend code changes from 19-01 (backend app is hot-reloaded via volume mount,
but some module-level state may not reload without restart).

</blockers>

<context>
All 3 Phase 19 plans are done code-wise. The only thing left is:
- Fix/confirm the 2 E2E regressions after Docker restart
- Mark Phase 19 complete in ROADMAP + STATE
Then v2.0 is DONE.
</context>

<next_action>
1. docker compose down && docker compose up -d  (restart to pick up COOKIE_SECURE change + fresh app state)
2. Wait for healthy: curl http://localhost:3901/api/health
3. cd e2e && npx playwright test tests/01-objects/edit-object.spec.ts:105 tests/04-validation/lint-panel.spec.ts:85 --project=chromium
4. If passing: run full suite (npx playwright test --project=chromium), then mark Phase 19 complete
5. If still failing: read the actual HTTP response body to understand the 4xx/5xx error
</next_action>
