---
phase: 35-owl2-rl-inference
plan: 03
type: execute
wave: 2
depends_on: ["35-01", "35-02"]
files_modified:
  - backend/app/templates/browser/workspace.html
  - backend/app/templates/browser/inference_panel.html
  - backend/app/templates/browser/inference_triple_row.html
  - frontend/static/css/workspace.css
  - frontend/static/js/workspace-layout.js
autonomous: true
requirements:
  - INF-01

must_haves:
  truths:
    - "New 'Inference' tab appears in the bottom panel alongside SPARQL, Event Log, AI Copilot"
    - "Clicking 'Refresh' button triggers POST /api/inference/run and updates the triple list"
    - "Panel shows a global list of all inferred triples with subject, predicate, object, entailment type"
    - "User can filter triples by object type and date range"
    - "User can group triples by time, object type, or property type"
    - "User can dismiss individual triples (removes from view and inferred graph)"
    - "User can promote individual triples to permanent user-created triples"
    - "Loading spinner shows during inference run"
    - "Last run timestamp displayed in panel header"
  artifacts:
    - backend/app/templates/browser/inference_panel.html
    - backend/app/templates/browser/inference_triple_row.html
  key_links:
    - "Refresh button triggers POST /api/inference/run via htmx"
    - "Triple list loaded via GET /api/inference/triples via htmx"
    - "Dismiss/promote buttons use POST /api/inference/triples/{hash}/dismiss|promote via htmx"
---

<objective>
Build the Inference bottom panel tab — the user's control center for inference. Contains a Refresh button to trigger inference, a global list of all inferred triples, filters (object type, date range), grouping options, and per-triple dismiss/promote actions.

Purpose: This is the primary user interface for interacting with inference. Users trigger inference here, review results, and manage individual triples.
Output: New Inference tab in bottom panel with full interactive triple management via htmx.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-owl2-rl-inference/35-RESEARCH.md
@.planning/phases/35-owl2-rl-inference/35-01-SUMMARY.md (read at execution time)
@.planning/phases/35-owl2-rl-inference/35-02-SUMMARY.md (read at execution time)

Key codebase files to read before implementation:
@backend/app/templates/browser/workspace.html (bottom panel tab pattern — follow exactly for new Inference tab)
@backend/app/templates/browser/event_log.html (reference for table/list layout in bottom panel)
@frontend/static/css/workspace.css (bottom panel styling patterns)
@frontend/static/js/workspace-layout.js (panel tab switching JS — may need to register new tab)

<interfaces>
<!-- API endpoints from Plan 01 that this plan consumes -->

POST /api/inference/run → returns InferenceResult JSON or HTML fragment (if HX-Request header)
GET /api/inference/triples?object_type=&date_from=&date_to=&group_by= → returns HTML fragment with triple list
POST /api/inference/triples/{hash}/dismiss → returns updated row HTML
POST /api/inference/triples/{hash}/promote → returns updated row HTML

<!-- Bottom panel tab pattern from workspace.html -->
- Tab buttons in `.panel-tab-bar`: `<button class="panel-tab" data-panel="name">LABEL</button>`
- Tab panes in `.panel-content`: `<div class="panel-pane" id="panel-name">...</div>`
- JS switches active tab and shows/hides panes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Inference tab to workspace bottom panel and create triple list template</name>
  <files>
    backend/app/templates/browser/workspace.html
    backend/app/templates/browser/inference_panel.html
    backend/app/templates/browser/inference_triple_row.html
    frontend/static/js/workspace-layout.js
  </files>
  <action>
1. **Modify `backend/app/templates/browser/workspace.html`**: Add the Inference tab following the exact pattern of existing bottom panel tabs:
   - Add tab button: `<button class="panel-tab" data-panel="inference">INFERENCE</button>` in the `.panel-tab-bar`
   - Add tab pane: `<div class="panel-pane" id="panel-inference">{% include 'browser/inference_panel.html' %}</div>` in `.panel-content`

2. **Create `backend/app/templates/browser/inference_panel.html`**: The main inference panel template:
   ```html
   <div class="inference-panel">
     <!-- Header bar with Refresh button and last-run timestamp -->
     <div class="inference-panel-header">
       <button class="btn btn-sm inference-refresh-btn"
               hx-post="/api/inference/run"
               hx-target="#inference-results"
               hx-swap="innerHTML"
               hx-indicator="#inference-spinner">
         <i data-lucide="refresh-cw"></i> Refresh
       </button>
       <span class="inference-spinner htmx-indicator" id="inference-spinner">
         <i data-lucide="loader" class="spin"></i> Running inference...
       </span>
       <span class="inference-last-run" id="inference-last-run">
         <!-- Populated after first run -->
       </span>
       <span class="inference-summary" id="inference-summary">
         <!-- e.g., "12 inferred triples" — populated after run -->
       </span>
     </div>

     <!-- Filter bar -->
     <div class="inference-filters">
       <select class="inference-filter-type"
               hx-get="/api/inference/triples"
               hx-target="#inference-results"
               hx-include="[class*='inference-filter']"
               name="object_type">
         <option value="">All types</option>
         <!-- Populated with type options from mental model -->
       </select>
       <input type="date" class="inference-filter-date-from" name="date_from"
              hx-get="/api/inference/triples"
              hx-target="#inference-results"
              hx-trigger="change"
              hx-include="[class*='inference-filter']"
              placeholder="From date">
       <input type="date" class="inference-filter-date-to" name="date_to"
              hx-get="/api/inference/triples"
              hx-target="#inference-results"
              hx-trigger="change"
              hx-include="[class*='inference-filter']"
              placeholder="To date">
       <select class="inference-filter-group"
               hx-get="/api/inference/triples"
               hx-target="#inference-results"
               hx-include="[class*='inference-filter']"
               name="group_by">
         <option value="">No grouping</option>
         <option value="time">By time inferred</option>
         <option value="object_type">By object type</option>
         <option value="property_type">By property type</option>
       </select>
     </div>

     <!-- Triple list (htmx-loaded) -->
     <div class="inference-results" id="inference-results"
          hx-get="/api/inference/triples"
          hx-trigger="load"
          hx-swap="innerHTML">
       <!-- Loaded via htmx on panel open -->
     </div>
   </div>
   ```

3. **Create `backend/app/templates/browser/inference_triple_row.html`**: Template for a single inferred triple row, used by the GET /api/inference/triples response:
   ```html
   <!-- Rendered for each inferred triple -->
   <div class="inference-triple-row {% if triple.status == 'stale' %}inferred-stale{% endif %}"
        id="triple-{{ triple.hash }}">
     <div class="inference-triple-content">
       <a href="/browser/{{ triple.subject_iri }}" class="inference-triple-subject">{{ triple.subject_label }}</a>
       <span class="inference-triple-predicate">{{ triple.predicate_label }}</span>
       <a href="/browser/{{ triple.object_iri }}" class="inference-triple-object">{{ triple.object_label }}</a>
       <span class="inferred-badge" title="{{ triple.entailment_type }}">{{ triple.entailment_type }}</span>
     </div>
     <div class="inference-triple-meta">
       <span class="inference-triple-time">{{ triple.inferred_at }}</span>
     </div>
     <div class="inference-triple-actions">
       <button class="btn btn-xs inference-dismiss-btn"
               hx-post="/api/inference/triples/{{ triple.hash }}/dismiss"
               hx-target="#triple-{{ triple.hash }}"
               hx-swap="outerHTML"
               title="Dismiss this inferred triple">
         <i data-lucide="eye-off"></i>
       </button>
       <button class="btn btn-xs inference-promote-btn"
               hx-post="/api/inference/triples/{{ triple.hash }}/promote"
               hx-target="#triple-{{ triple.hash }}"
               hx-swap="outerHTML"
               title="Promote to permanent user-created triple">
         <i data-lucide="pin"></i>
       </button>
     </div>
   </div>
   ```

4. **Modify `frontend/static/js/workspace-layout.js`**: If the panel tab switching logic needs explicit registration of new tabs, add "inference" to the known panel list. Check how existing tabs (sparql, event-log, copilot) are registered and follow the same pattern. The tab switching should work automatically if it uses `data-panel` attribute matching, but verify.

IMPORTANT: Follow CLAUDE.md icon rules — all Lucide icons sized via CSS (`flex-shrink: 0`, `stroke: currentColor`), no inline styles on `<i data-lucide>` elements. The `.spin` class for the loader icon should use CSS `animation: spin 1s linear infinite`.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && grep -c "panel-inference\|inference-panel" backend/app/templates/browser/workspace.html && grep -c "inference-refresh-btn" backend/app/templates/browser/inference_panel.html && grep -c "inference-dismiss-btn\|inference-promote-btn" backend/app/templates/browser/inference_triple_row.html && echo "All templates exist and contain expected elements"</automated>
  </verify>
  <done>
    - Inference tab appears in bottom panel tab bar
    - Clicking tab shows the inference panel pane
    - Refresh button triggers POST /api/inference/run via htmx
    - Loading spinner shows during inference run
    - Triple list loads via htmx with subject/predicate/object, entailment type, timestamp
    - Filters work: object type dropdown, date range inputs, group-by selector
    - Dismiss button removes triple from view via htmx swap
    - Promote button copies triple to user data via htmx swap
    - Last run timestamp updates after each run
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styles for inference panel, triple rows, filters, and actions</name>
  <files>
    frontend/static/css/workspace.css
  </files>
  <action>
Add comprehensive CSS for the inference panel to `frontend/static/css/workspace.css`. All styles must work in both light and dark themes using existing CSS variables.

1. **Inference panel layout:**
   - `.inference-panel` — full height flex column
   - `.inference-panel-header` — flex row, gap, align-items center, padding, border-bottom
   - `.inference-filters` — flex row, gap, flex-wrap, padding, subtle background
   - `.inference-results` — flex column, overflow-y auto (scrollable), flex: 1

2. **Refresh button and spinner:**
   - `.inference-refresh-btn` — styled like existing panel action buttons
   - `.inference-refresh-btn svg` — width: 14px, height: 14px, flex-shrink: 0, stroke: currentColor
   - `.inference-spinner` — muted color, italic, hidden by default (htmx-indicator)
   - `.spin` animation — `@keyframes spin { to { transform: rotate(360deg) } }`, `.spin svg { animation: spin 1s linear infinite }`
   - `.inference-last-run` — muted text, small font
   - `.inference-summary` — muted text, small font, margin-left auto (pushes to right)

3. **Filter controls:**
   - `.inference-filter-type`, `.inference-filter-group` — styled select dropdowns matching existing panel selects
   - `.inference-filter-date-from`, `.inference-filter-date-to` — date inputs styled consistently

4. **Triple rows:**
   - `.inference-triple-row` — flex row, padding, border-bottom, hover background, align-items center
   - `.inference-triple-content` — flex: 1, flex row, gap, align-items center
   - `.inference-triple-subject`, `.inference-triple-object` — link styling, clickable
   - `.inference-triple-predicate` — muted color, smaller font, code-like font
   - `.inference-triple-meta` — muted, small font
   - `.inference-triple-actions` — flex row, gap, opacity 0 by default, opacity 1 on row hover (reveal on hover pattern)
   - `.inference-dismiss-btn svg`, `.inference-promote-btn svg` — width: 14px, height: 14px, flex-shrink: 0, stroke: currentColor

5. **Group headers** (when grouping is active):
   - `.inference-group-header` — bold, padding, subtle background, sticky top

6. **Empty state:**
   - `.inference-empty` — centered text, muted, padding, "No inferred triples yet. Click Refresh to run inference."

All colors must use existing CSS variables (`--color-text`, `--color-text-muted`, `--color-bg`, `--color-border`, etc.) for light/dark theme compatibility.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && grep -c "inference-panel\|inference-triple-row\|inference-refresh" frontend/static/css/workspace.css && echo "CSS styles added"</automated>
  </verify>
  <done>
    - All inference panel elements are styled consistently with the rest of the workspace
    - Light and dark theme compatibility via CSS variables
    - Hover-reveal pattern for dismiss/promote action buttons
    - Scrollable triple list with proper overflow handling
    - Loading spinner animation works
    - Lucide icons sized via CSS with flex-shrink: 0 per CLAUDE.md rules
  </done>
</task>

</tasks>

<verification>
1. Open workspace, click "INFERENCE" tab in bottom panel — panel appears
2. Click Refresh — spinner shows, then triple list populates (if data exists)
3. Filter by object type — list updates via htmx
4. Click dismiss on a triple — it disappears from the list
5. Click promote on a triple — it becomes a user-created triple
6. All elements render correctly in both light and dark themes
</verification>

<success_criteria>
- Inference tab is a fully functional control center for managing inferred triples
- All interactions are htmx-driven (no page reloads)
- User can see, filter, group, dismiss, and promote inferred triples
- UI is consistent with existing bottom panel tabs in styling and behavior
</success_criteria>

<output>
After completion, create `.planning/phases/35-owl2-rl-inference/35-03-SUMMARY.md`
</output>
