---
phase: 50-user-guide-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/14-system-health-and-debugging.md
  - docs/guide/16-data-model.md
  - docs/guide/21-sparql-console.md
  - docs/guide/22-keyword-search.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Ch 14 documents the Global Lint Dashboard"
    - "Ch 16 documents OWL inference and SHACL-AF rules"
    - "Ch 21 has comprehensive SPARQL Console coverage (~150 lines)"
    - "Ch 22 has comprehensive Keyword Search coverage (~150 lines)"
  artifacts:
    - path: "docs/guide/14-system-health-and-debugging.md"
      provides: "Lint dashboard documentation"
      contains: "lint dashboard"
    - path: "docs/guide/16-data-model.md"
      provides: "Inference and rules documentation"
      contains: "inference"
    - path: "docs/guide/21-sparql-console.md"
      provides: "Expanded SPARQL Console docs"
      min_lines: 120
    - path: "docs/guide/22-keyword-search.md"
      provides: "Expanded keyword search docs"
      min_lines: 100
  key_links:
    - from: "docs/guide/14-system-health-and-debugging.md"
      to: "docs/guide/16-data-model.md"
      via: "lint references data model"
      pattern: "data.model\\|16-data-model"
---

<objective>
Add lint dashboard and inference sections to existing chapters, and expand thin SPARQL/FTS chapters.

Purpose: Chapters 14 and 16 are missing coverage of v2.4 features (lint dashboard, OWL inference, SHACL-AF rules). Chapters 21 and 22 are stub-length and need full task-oriented coverage.
Output: Four updated chapter files covering all v2.2-v2.4 data discovery and analysis features.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-user-guide-documentation/50-CONTEXT.md
@.planning/phases/50-user-guide-documentation/50-RESEARCH.md
@docs/guide/14-system-health-and-debugging.md
@docs/guide/16-data-model.md
@docs/guide/21-sparql-console.md
@docs/guide/22-keyword-search.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lint dashboard to Ch 14 and inference/rules to Ch 16</name>
  <files>docs/guide/14-system-health-and-debugging.md, docs/guide/16-data-model.md</files>
  <action>
Read both chapters fully, then add new sections:

**Ch 14 (System Health and Debugging) — add Global Lint Dashboard section:**
- Describe what the Lint Dashboard is: a page showing all SHACL validation violations across the knowledge base
- How to access: sidebar navigation or `/browser/lint-dashboard`
- What it shows: violation count per type, per severity; individual violation details with the offending object, property, and constraint
- How to use it: click a violation to navigate to the object, fix the issue in the edit form
- Explain SHACL validation: Mental Model shapes define constraints on properties (required, datatype, cardinality); lint checks objects against these shapes
- Add a "For Advanced Users" callout explaining how to write custom SHACL shapes that produce custom lint rules
- Place this section after the existing SPARQL Console mention (or at end of chapter, whichever fits the flow)

**Ch 16 (The Data Model) — add OWL Inference and SHACL-AF Rules sections:**
- Add section on OWL 2 RL Inference:
  - What it does: automatically derives new triples from existing data using OWL axioms (e.g., `owl:inverseOf`, `rdfs:subClassOf`)
  - How to configure: Settings page, Entailment Configuration section
  - What gets inferred: subclass hierarchy, inverse properties, transitive properties, domain/range
  - Where inferred triples live: the inference named graph
- Add section on SHACL-AF Rules:
  - What they are: rules defined in Mental Model shapes that generate new triples (e.g., auto-linking related objects)
  - How they run: triggered after object save, results stored in inference graph
  - Example: a rule that links a Note to related Concepts based on tags
- Place after existing named graphs and SHACL sections

Use Basic PKM examples throughout. Document conventions: **bold** UI elements, `inline code` for paths.
Preserve navigation footer links.
  </action>
  <verify>
    <automated>grep -c "lint dashboard\|Lint Dashboard" docs/guide/14-system-health-and-debugging.md | grep -qv "^0$" && grep -c "inference\|Inference" docs/guide/16-data-model.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Ch 14 has lint dashboard section, Ch 16 has OWL inference and SHACL-AF rules sections</done>
</task>

<task type="auto">
  <name>Task 2: Expand Ch 21 (SPARQL Console) and Ch 22 (Keyword Search)</name>
  <files>docs/guide/21-sparql-console.md, docs/guide/22-keyword-search.md</files>
  <action>
Read both chapters fully, then expand:

**Ch 21 (SPARQL Console) — expand from ~64 lines to ~150 lines:**
- Keep existing content as foundation
- Add "Getting Started" section: how to open the console, write a basic SELECT query
- Add "Multi-Tab Queries" section: describe the tabbed interface, creating new query tabs, renaming tabs
- Add "CodeMirror Editor" section: syntax highlighting, auto-indentation, bracket matching
- Add "Working with Results" section: table view of results, pagination, exporting
- Add "Example Queries" section with 3-4 practical examples using Basic PKM model:
  - List all Notes with their titles: `SELECT ?note ?title WHERE { ?note a <urn:sempkm:Note> ; dcterms:title ?title }`
  - Find all edges from a specific Project
  - Count objects by type
  - Query across named graphs
- Add "For Advanced Users" callout: CONSTRUCT queries, DESCRIBE, UPDATE (if supported), federated queries
- Reference Ch 18 (SPARQL Endpoint) for API access

**Ch 22 (Keyword Search) — expand from ~37 lines to ~150 lines:**
- Keep existing content as foundation
- Add "How Search Works" section: full-text search via RDF4J Lucene index, what gets indexed (labels, titles, body text)
- Add "Using Search" section: `Ctrl+K` to open command palette, type query, result list with type icons and text snippets
- Add "Search Results" section: how results are ranked, what the snippet shows, clicking to open in workspace tab
- Add "Search Scope" section: searches across all objects the user has access to, respects named graph boundaries
- Add "Tips" section: partial matches, case sensitivity behavior, special characters
- Add "For Advanced Users" callout: explain the underlying Lucene index, how FTS integrates with SPARQL via `luc:matches`
- Reference Ch 21 for SPARQL-based search

Use Basic PKM examples throughout. Document conventions apply.
Preserve navigation footer links.
  </action>
  <verify>
    <automated>wc -l docs/guide/21-sparql-console.md docs/guide/22-keyword-search.md | tail -1 | awk '{if ($1 > 200) print "PASS: expanded"; else print "FAIL: still too short"}'</automated>
  </verify>
  <done>Ch 21 is ~150 lines with examples and multi-tab coverage, Ch 22 is ~150 lines with search behavior details</done>
</task>

</tasks>

<verification>
# Verify lint dashboard and inference coverage
grep -l "lint dashboard\|Lint Dashboard" docs/guide/14-system-health-and-debugging.md
grep -l "inference\|entailment" docs/guide/16-data-model.md
# Verify chapter lengths
wc -l docs/guide/21-sparql-console.md docs/guide/22-keyword-search.md
</verification>

<success_criteria>
- Ch 14 contains lint dashboard section with access instructions and violation details
- Ch 16 contains OWL inference and SHACL-AF rules sections
- Ch 21 expanded to ~150 lines with examples and multi-tab coverage
- Ch 22 expanded to ~150 lines with search behavior and tips
- All four chapters use Basic PKM examples and follow document conventions
</success_criteria>

<output>
After completion, create `.planning/phases/50-user-guide-documentation/50-02-SUMMARY.md`
</output>
