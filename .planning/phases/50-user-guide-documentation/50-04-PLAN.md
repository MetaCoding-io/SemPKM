---
phase: 50-user-guide-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 50-01
  - 50-02
  - 50-03
files_modified:
  - docs/guide/appendix-a-environment-variables.md
  - docs/guide/appendix-b-keyboard-shortcuts.md
  - docs/guide/appendix-d-glossary.md
  - docs/guide/appendix-e-troubleshooting.md
  - docs/guide/appendix-f-faq.md
  - docs/guide/README.md
  - docs/USER_GUIDE_OUTLINE.md
  - e2e/tests/screenshots/guide-capture.spec.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "README.md lists Part IX: Identity & Federation with Ch 25 and Ch 26"
    - "Appendix A lists all current environment variables including APP_BASE_URL"
    - "Appendix D defines WebID, IndieAuth, PKCE, carousel view, lint dashboard, entailment"
    - "Appendix E has Obsidian import and WebID troubleshooting sections"
    - "Appendix F answers updated Obsidian and identity questions"
    - "USER_GUIDE_OUTLINE.md reflects actual chapter structure"
    - "Screenshot capture spec exists and runs"
  artifacts:
    - path: "docs/guide/README.md"
      provides: "Updated table of contents"
      contains: "Part IX"
    - path: "docs/guide/appendix-d-glossary.md"
      provides: "Updated glossary"
      contains: "WebID"
    - path: "docs/USER_GUIDE_OUTLINE.md"
      provides: "Updated outline"
      contains: "Part IX"
    - path: "e2e/tests/screenshots/guide-capture.spec.ts"
      provides: "Screenshot automation"
  key_links:
    - from: "docs/guide/README.md"
      to: "docs/guide/25-webid-profiles.md"
      via: "table of contents link"
      pattern: "25-webid-profiles"
    - from: "docs/guide/README.md"
      to: "docs/guide/26-indieauth.md"
      via: "table of contents link"
      pattern: "26-indieauth"
---

<objective>
Update appendices, indexes, and capture screenshots to complete the user guide refresh.

Purpose: After chapter updates (Plans 01-03), the appendices, table of contents, and outline need to reflect the new structure. Screenshots provide visual aids for key features.
Output: Updated appendices, README.md with Part IX, synced outline, and screenshot capture spec.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-user-guide-documentation/50-CONTEXT.md
@.planning/phases/50-user-guide-documentation/50-RESEARCH.md
@docs/guide/README.md
@docs/USER_GUIDE_OUTLINE.md
@docs/guide/appendix-a-environment-variables.md
@docs/guide/appendix-b-keyboard-shortcuts.md
@docs/guide/appendix-d-glossary.md
@docs/guide/appendix-e-troubleshooting.md
@docs/guide/appendix-f-faq.md
@e2e/tests/screenshots/capture.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update appendices and index files</name>
  <files>docs/guide/appendix-a-environment-variables.md, docs/guide/appendix-b-keyboard-shortcuts.md, docs/guide/appendix-d-glossary.md, docs/guide/appendix-e-troubleshooting.md, docs/guide/appendix-f-faq.md, docs/guide/README.md, docs/USER_GUIDE_OUTLINE.md</files>
  <action>
Read all files listed above. Then update each:

**README.md (Table of Contents):**
- Add Part IX: Identity & Federation after current Part VIII
  - 25. [WebID Profiles](25-webid-profiles.md)
  - 26. [IndieAuth](26-indieauth.md)
- Update the Appendix previous/next references if the last chapter before appendices changed (Ch 24 -> Ch 26)
- Verify all existing Part entries still have correct links

**USER_GUIDE_OUTLINE.md:**
- Add Part IX section with Ch 25 and Ch 26 outlines
- Mark v2.2-v2.5 features as implemented in the relevant chapter outlines
- Update any "planned" or "TODO" markers to "implemented"
- Verify chapter numbers and structure match actual files

**Appendix A (Environment Variables):**
- Read `backend/app/config.py` to get the current complete list of env vars
- Add any missing vars: `APP_BASE_URL`, `CORS_ORIGINS`, `COOKIE_SECURE`, `POSTHOG_*`, and any others from WebID/IndieAuth (e.g., `WEBID_*`, `INDIEAUTH_*`)
- Keep existing format (table with name, default, description)

**Appendix B (Keyboard Shortcuts):**
- Cross-reference with the updated Ch 8 (from Plan 01) — must be consistent
- Verify all shortcuts are listed and accurate
- This may be minimal changes if Ch 8 task already caught everything

**Appendix D (Glossary):**
- Add new terms: WebID, IndieAuth, PKCE, Content Negotiation, Carousel View, Lint Dashboard, Entailment, Inference, SHACL-AF Rule, Obsidian Import
- Keep alphabetical order
- Each definition: 1-2 sentences, link to relevant chapter

**Appendix E (Troubleshooting):**
- Add "Obsidian Import Issues" section: common problems (zip too large, unrecognized frontmatter, import stuck)
- Add "WebID / Identity Issues" section: profile not resolving, content negotiation not working, APP_BASE_URL misconfigured
- Keep existing sections intact

**Appendix F (FAQ):**
- Update the Obsidian-related question to reference the in-app import tool (not Python scripts)
- Add Q: "What is WebID and do I need it?" A: brief explanation, link to Ch 25
- Add Q: "Can I sign into other services with SemPKM?" A: yes via IndieAuth, link to Ch 26

**Navigation footers:** Verify the chain from Ch 26 -> Appendix A -> ... -> Appendix F is correct. Update Appendix A's "Previous" to point to Ch 26 (was Ch 24).
  </action>
  <verify>
    <automated>grep -q "Part IX" docs/guide/README.md && grep -q "WebID" docs/guide/appendix-d-glossary.md && grep -q "Part IX" docs/USER_GUIDE_OUTLINE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md has Part IX, all appendices updated with new terms/vars/FAQs, outline synced, navigation footers chain correctly from Ch 26 through all appendices</done>
</task>

<task type="auto">
  <name>Task 2: Create screenshot capture spec and capture guide screenshots</name>
  <files>e2e/tests/screenshots/guide-capture.spec.ts</files>
  <action>
Read the existing `e2e/tests/screenshots/capture.spec.ts` to understand the established pattern (fixtures, helpers, viewport, output paths).

Create `e2e/tests/screenshots/guide-capture.spec.ts` following the same pattern but outputting to `docs/screenshots/`:

```typescript
// Pattern from RESEARCH.md
import { test } from '../../fixtures/auth';
import { waitForWorkspace, waitForIdle } from '../../helpers/wait-for';
import path from 'path';

const GUIDE_SCREENSHOTS = path.resolve(__dirname, '../../../docs/screenshots');
const VIEWPORT = { width: 1440, height: 900 };
```

Capture ~15-20 screenshots covering key features documented in the guide:

1. `guide-workspace-dockview.png` — Workspace with multiple tabs open in dockview groups
2. `guide-object-read-view.png` — Object read view (markdown body + properties)
3. `guide-object-edit-view.png` — Object edit form with helptext visible
4. `guide-carousel-views.png` — Type browser with carousel view tabs visible
5. `guide-nav-tree.png` — Sidebar navigation tree with expanded types
6. `guide-command-palette.png` — Command palette open with search results
7. `guide-sparql-console.png` — SPARQL Console with a query and results
8. `guide-keyword-search.png` — Search results with type icons and snippets
9. `guide-lint-dashboard.png` — Global lint dashboard page
10. `guide-data-model-inference.png` — Settings > Entailment configuration
11. `guide-obsidian-upload.png` — Obsidian import initial upload page
12. `guide-obsidian-mapping.png` — Obsidian type mapping step
13. `guide-webid-profile.png` — WebID profile HTML page
14. `guide-indieauth-consent.png` — IndieAuth consent screen (if accessible without full OAuth flow, otherwise skip)
15. `guide-settings-identity.png` — Settings > Identity section
16. `guide-vfs-browser.png` — VFS file browser view

For each screenshot:
- Set viewport to 1440x900
- Force light mode via `data-theme` attribute and localStorage
- Wait for content to load (`waitForIdle` or specific selectors)
- Add short `waitForTimeout(500)` after idle for CSS transitions to complete
- Use `fullPage: false` for consistent viewport screenshots

Mark the test as `test.describe.serial` so screenshots capture in sequence.

After creating the spec, run it to verify it executes:
```bash
cd /home/james/Code/SemPKM/e2e && npx playwright test tests/screenshots/guide-capture.spec.ts --project=screenshots
```

Note: Some screenshots may fail if the Docker stack isn't running or specific data isn't seeded. That's OK — the spec should be structurally correct and capture what it can. Document any failures in the SUMMARY.
  </action>
  <verify>
    <automated>test -f e2e/tests/screenshots/guide-capture.spec.ts && echo "PASS: spec exists" || echo "FAIL: spec missing"</automated>
  </verify>
  <done>Screenshot capture spec exists at e2e/tests/screenshots/guide-capture.spec.ts with ~15 capture tests, light mode only, outputting to docs/screenshots/</done>
</task>

</tasks>

<verification>
# Verify index files
grep "Part IX" docs/guide/README.md
grep "Part IX" docs/USER_GUIDE_OUTLINE.md

# Verify no stale references across entire guide
grep -r "Split.js\|flip card\|3D flip\|Python script" docs/guide/*.md

# Verify navigation chain integrity (spot check)
grep "26-indieauth" docs/guide/25-webid-profiles.md
grep "appendix-a" docs/guide/26-indieauth.md

# Verify screenshot spec
test -f e2e/tests/screenshots/guide-capture.spec.ts
</verification>

<success_criteria>
- README.md includes Part IX with Ch 25 and Ch 26
- USER_GUIDE_OUTLINE.md reflects current chapter structure
- All 5 appendices updated with new content (env vars, shortcuts, glossary terms, troubleshooting, FAQ)
- Navigation footer chain is unbroken from Ch 1 through Appendix F
- Screenshot capture spec exists and is structurally correct
- Zero grep hits for stale terms across the entire guide
</success_criteria>

<output>
After completion, create `.planning/phases/50-user-guide-documentation/50-04-SUMMARY.md`
</output>
