---
phase: 50-user-guide-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/04-workspace-interface.md
  - docs/guide/05-working-with-objects.md
  - docs/guide/07-browsing-and-visualizing.md
  - docs/guide/08-keyboard-shortcuts.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "Ch 4 describes dockview panel system, not Split.js"
    - "Ch 5 describes crossfade read/edit toggle, not 3D flip card"
    - "Ch 5 includes markdown-first read view and SHACL helptext"
    - "Ch 7 includes carousel view section"
    - "Ch 8 lists all current keyboard shortcuts"
  artifacts:
    - path: "docs/guide/04-workspace-interface.md"
      provides: "Updated workspace layout docs"
      contains: "dockview"
    - path: "docs/guide/05-working-with-objects.md"
      provides: "Updated object editing docs"
      contains: "crossfade"
    - path: "docs/guide/07-browsing-and-visualizing.md"
      provides: "Updated browsing docs with carousel"
      contains: "carousel"
  key_links:
    - from: "docs/guide/04-workspace-interface.md"
      to: "docs/guide/08-keyboard-shortcuts.md"
      via: "cross-reference"
      pattern: "keyboard-shortcuts"
---

<objective>
Update stale workspace and object chapters to reflect current UI state.

Purpose: Chapters 4, 5, 7, and 8 reference superseded UI patterns (Split.js layout, 3D flip cards) that no longer match the application. Users following these instructions would be confused by the mismatch.
Output: Four updated chapter files with current UI descriptions.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-user-guide-documentation/50-CONTEXT.md
@.planning/phases/50-user-guide-documentation/50-RESEARCH.md
@docs/guide/04-workspace-interface.md
@docs/guide/05-working-with-objects.md
@docs/guide/07-browsing-and-visualizing.md
@docs/guide/08-keyboard-shortcuts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Ch 4 (Workspace Interface) and Ch 8 (Keyboard Shortcuts)</name>
  <files>docs/guide/04-workspace-interface.md, docs/guide/08-keyboard-shortcuts.md</files>
  <action>
Read both chapters fully. Then perform targeted surgery:

**Ch 4 (Workspace Interface):**
- Replace all Split.js references with dockview panel system description
- Describe the dockview group model: editor groups, tab bar, drag-to-reorder tabs, drag-to-split into new groups
- Add section on Named Layouts: saving layouts (`Ctrl+Shift+S`), restoring layouts, the layout dropdown
- Update any references to "3-column layout" or fixed panel positions — dockview uses flexible group-based panels
- Keep existing sections about sidebar, nav tree, command palette if still accurate
- Add mention of tab accent colors per type (objects get model-defined accent colors on active tabs)
- Reference the correct shortcuts: `Ctrl+K` for command palette, `Ctrl+W` to close tab

**Ch 8 (Keyboard Shortcuts):**
- Read `frontend/static/js/workspace.js` to verify current shortcut bindings
- Cross-reference with `frontend/static/js/workspace-layout.js` for layout shortcuts
- Ensure all current shortcuts are listed, remove any that no longer exist
- Verify `Ctrl+K` (command palette), `Ctrl+W` (close tab), `Ctrl+Shift+S` (save layout) are documented

Use document conventions: `Ctrl+K` inline code for shortcuts, **bold** for UI elements, Basic PKM examples.
Preserve navigation footer links.
  </action>
  <verify>
    <automated>grep -c "Split.js\|split\.js" docs/guide/04-workspace-interface.md | grep -q "^0$" && echo "PASS: no Split.js refs" || echo "FAIL: stale Split.js references"</automated>
  </verify>
  <done>Ch 4 describes dockview panels with named layouts, Ch 8 lists all current shortcuts, no Split.js references remain</done>
</task>

<task type="auto">
  <name>Task 2: Update Ch 5 (Working with Objects) and Ch 7 (Browsing and Visualizing)</name>
  <files>docs/guide/05-working-with-objects.md, docs/guide/07-browsing-and-visualizing.md</files>
  <action>
Read both chapters fully. Then perform targeted surgery:

**Ch 5 (Working with Objects):**
- Replace all "flip card" / "3D flip" references with crossfade toggle description
- Describe the current read/edit flow: click Edit button, opacity crossfade transition (0.25s), edit form appears
- Add section on markdown-first read view: the read view renders markdown body content prominently, with properties and metadata below
- Add section on SHACL helptext: edit forms now show contextual help text below each field, driven by `sh:description` in the SHACL shape. Mention this is automatic when the Mental Model defines descriptions.
- Keep existing sections on creating objects, editing properties, saving — update if they reference old UI patterns
- Use Basic PKM examples: creating a Note with `dcterms:title`, editing a Project

**Ch 7 (Browsing and Visualizing):**
- Add new section on Carousel Views: the view bar at the top of type browser pages shows available views as tabs (Table, Cards, Graph, etc.)
- Describe how to switch between views using the carousel tab bar
- Keep existing table view, cards view, and graph view descriptions — just add the carousel navigation concept
- If the chapter references old view switching mechanisms, update to carousel

Use document conventions throughout. Preserve navigation footer links.
  </action>
  <verify>
    <automated>grep -c "flip card\|3D flip" docs/guide/05-working-with-objects.md | grep -q "^0$" && echo "PASS: no flip card refs" || echo "FAIL: stale flip card references"</automated>
  </verify>
  <done>Ch 5 describes crossfade toggle with helptext section, Ch 7 includes carousel view navigation, no stale flip/3D references</done>
</task>

</tasks>

<verification>
grep -r "Split.js\|flip card\|3D flip" docs/guide/04-workspace-interface.md docs/guide/05-working-with-objects.md docs/guide/07-browsing-and-visualizing.md docs/guide/08-keyboard-shortcuts.md
# Should return no results
</verification>

<success_criteria>
- Zero stale references to Split.js, flip card, or 3D flip in chapters 4, 5, 7, 8
- Ch 4 mentions dockview and named layouts
- Ch 5 mentions crossfade and helptext
- Ch 7 mentions carousel views
- Ch 8 lists current keyboard shortcuts accurately
</success_criteria>

<output>
After completion, create `.planning/phases/50-user-guide-documentation/50-01-SUMMARY.md`
</output>
