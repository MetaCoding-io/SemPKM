---
phase: 23-sparql-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/templates/browser/workspace.html
  - frontend/static/css/workspace.css
  - frontend/static/js/workspace.js
autonomous: true
requirements:
  - SPARQL-01
  - SPARQL-03

must_haves:
  truths:
    - "User can open the bottom panel SPARQL tab (Ctrl+J) and see a working Yasgui query editor"
    - "Running a query in Yasgui returns results from the triplestore via /api/sparql"
    - "Yasgui tabs and query content survive full browser close and reopen (localStorage key sempkm-sparql)"
    - "Any authenticated user (member or guest) can use the SPARQL console — not owner-only"
    - "No JavaScript errors occur when the bottom panel is closed (lazy initialization)"
  artifacts:
    - path: "backend/app/templates/browser/workspace.html"
      provides: "Yasgui CDN scripts in head block, yasgui-container div replacing placeholder, init script"
      contains: "yasgui-container"
    - path: "frontend/static/css/workspace.css"
      provides: "Height rules for #panel-sparql and #yasgui-container"
      contains: "yasgui-container"
    - path: "frontend/static/js/workspace.js"
      provides: "Yasgui resize/reinit hook on panel tab switch"
      contains: "initYasguiConsole"
  key_links:
    - from: "workspace.html Yasgui init script"
      to: "/api/sparql"
      via: "Yasgui requestConfig.endpoint"
      pattern: "endpoint.*api/sparql"
    - from: "workspace.html Yasgui init script"
      to: "localStorage"
      via: "persistenceId: 'sempkm-sparql'"
      pattern: "sempkm-sparql"
    - from: "workspace.js _applyPanelState / initPanelTabs"
      to: "window.initYasguiConsole"
      via: "panel tab click handler"
      pattern: "initYasguiConsole"
---

<objective>
Replace the placeholder SPARQL tab stub in the workspace bottom panel with a working Yasgui SPARQL console loaded from the @zazuko/yasgui v4.5.0 CDN, wired to the existing /api/sparql endpoint, with localStorage persistence (key: sempkm-sparql).

Purpose: Gives users a production-grade SPARQL query interface for exploring their knowledge base without any backend changes.
Output: Working Yasgui editor in the bottom panel SPARQL tab; queries persist across browser sessions.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-sparql-console/23-CONTEXT.md

<interfaces>
<!-- Key existing patterns the executor needs. No codebase exploration needed. -->

From backend/app/templates/browser/workspace.html (bottom panel SPARQL stub to replace):
```html
<div class="panel-pane active" id="panel-sparql">
  <div class="panel-placeholder">
    <i data-lucide="terminal" style="width:24px;height:24px;display:block;margin:0 auto 8px;"></i>
    SPARQL Console — coming in Phase 16
  </div>
</div>
```

workspace.html extends base.html. base.html has NO {% block head %} currently — the
entire <head> is inline. workspace.html has NO {% block head %} override. The Yasgui
CSS/JS must be added in a new {% block head %} override in workspace.html using {{ super() }}
if base.html gains the block, OR appended directly in {% block content %} as a <link>/<script>
before the main content. Simpler: add them as the first items inside {% block content %}.
Note: base.html loads all shared CSS/JS. workspace.html is the only template that needs Yasgui.

From frontend/static/css/workspace.css (panel CSS structure, approx lines 1842-1860):
```css
.panel-content {
  flex: 1;
  overflow: hidden;
  position: relative;
}
.panel-pane {
  display: none;
  height: 100%;
}
.panel-pane.active {
  display: flex;
  flex-direction: column;
}
```

From frontend/static/js/workspace.js — openTab function (line 63):
```js
function openTab(objectIri, label, mode) { ... }
// openTab is exposed as window.openTab at line ~700+
```

From frontend/static/js/workspace.js — panel tab switching (initPanelTabs, approx line 418):
```js
panelTabBar.addEventListener('click', function(e) {
  var btn = e.target.closest('.panel-tab');
  if (!btn) return;
  var panel = btn.dataset.panel;
  // sets active tab, calls _applyPanelState(...)
  if (btn.dataset.panel === 'event-log') { /* event log init */ }
});
```

Yasgui CDN (confirmed MIT, @zazuko fork v4.5.0):
- CSS: https://unpkg.com/@zazuko/yasgui@4.5.0/build/yasgui.min.css
- JS:  https://unpkg.com/@zazuko/yasgui@4.5.0/build/yasgui.min.js

SPARQL endpoint: POST /api/sparql accepts application/x-www-form-urlencoded (query=...).
Auth: sempkm_session cookie sent automatically (same-origin). No auth header needed.
Access: get_current_user dependency (any authenticated user). The /sparql GET page at
debug/router.py uses require_role("owner") — but the API endpoint /api/sparql uses
get_current_user (any authenticated user). Yasgui calls /api/sparql directly, not /sparql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load Yasgui CDN in workspace.html and replace SPARQL tab placeholder</name>
  <files>backend/app/templates/browser/workspace.html</files>
  <action>
Make two changes to workspace.html:

1. ADD Yasgui CSS and JS at the very top of the {% block content %} block (before any other content), so they load only on the workspace page:
```html
<!-- Yasgui SPARQL Console — Phase 23 -->
<link rel="stylesheet" href="https://unpkg.com/@zazuko/yasgui@4.5.0/build/yasgui.min.css">
<script src="https://unpkg.com/@zazuko/yasgui@4.5.0/build/yasgui.min.js"></script>
```

2. REPLACE the entire #panel-sparql div (the placeholder block) with:
```html
<div class="panel-pane active" id="panel-sparql">
  <div id="yasgui-container"></div>
</div>
```

3. ADD the Yasgui initialization script at the very end of {% block content %} (before {% endblock %}):
```html
<script>
(function () {
  'use strict';

  var DEFAULT_QUERY = [
    'PREFIX dcterms: <http://purl.org/dc/terms/>',
    'PREFIX skos: <http://www.w3.org/2004/02/skos/core#>',
    'PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>',
    'PREFIX foaf: <http://xmlns.com/foaf/0.1/>',
    '',
    '# List all objects with their types and labels',
    'SELECT ?subject ?type ?label WHERE {',
    '  ?subject a ?type .',
    '  OPTIONAL { ?subject dcterms:title ?t }',
    '  OPTIONAL { ?subject skos:prefLabel ?p }',
    '  OPTIONAL { ?subject rdfs:label ?r }',
    '  OPTIONAL { ?subject foaf:name ?f }',
    '  BIND(COALESCE(?t, ?p, ?r, ?f, STR(?subject)) AS ?label)',
    '}',
    'ORDER BY ?type ?label',
    'LIMIT 50'
  ].join('\n');

  function initYasguiConsole() {
    if (typeof Yasgui === 'undefined') {
      setTimeout(initYasguiConsole, 200);
      return;
    }
    var container = document.getElementById('yasgui-container');
    if (!container || container._yasguiInstance) return;

    var yasgui = new Yasgui(container, {
      requestConfig: {
        endpoint: '/api/sparql',
        method: 'POST'
      },
      persistenceId: 'sempkm-sparql',
      copyEndpointOnNewTab: false,
      endpointCatalogueSize: 0
    });

    // Pre-populate default query only on first ever load (no localStorage state)
    try {
      if (!localStorage.getItem('sempkm-sparql')) {
        var tab = yasgui.getTab();
        if (tab) tab.setQuery(DEFAULT_QUERY);
      }
    } catch (e) { /* localStorage may be blocked */ }

    container._yasguiInstance = yasgui;
    window._yasgui = yasgui;
  }

  // Expose for workspace.js panel tab handler
  window.initYasguiConsole = initYasguiConsole;

  // Init immediately if SPARQL pane is already active on DOMContentLoaded
  document.addEventListener('DOMContentLoaded', function () {
    var sparqlPane = document.getElementById('panel-sparql');
    if (sparqlPane && sparqlPane.classList.contains('active')) {
      initYasguiConsole();
    }
  });
})();
</script>
```

Do NOT add Yasgui to base.html — it must load only on the workspace page.
Do NOT use require_role("owner") for Yasgui access — it calls /api/sparql directly which already uses get_current_user.
  </action>
  <verify>
    <automated>grep -n "yasgui-container" /home/james/Code/SemPKM/backend/app/templates/browser/workspace.html && grep -n "sempkm-sparql" /home/james/Code/SemPKM/backend/app/templates/browser/workspace.html && ! grep -q "coming in Phase" /home/james/Code/SemPKM/backend/app/templates/browser/workspace.html && echo "PASS: placeholder removed, container present, persistenceId set"</automated>
  </verify>
  <done>workspace.html no longer contains "coming in Phase", contains yasgui-container div and sempkm-sparql persistenceId, Yasgui CDN scripts load only on workspace page</done>
</task>

<task type="auto">
  <name>Task 2: Add panel height CSS and wire panel tab init in workspace.js</name>
  <files>frontend/static/css/workspace.css, frontend/static/js/workspace.js</files>
  <action>
TWO CHANGES:

CHANGE A — workspace.css:
Yasgui requires an explicit height to render its CodeMirror editor correctly. Add after the existing `.panel-pane.active` rule (approximately line 1857):

```css
/* SPARQL Console (Phase 23): Yasgui needs explicit height to render editor */
#panel-sparql {
  overflow: hidden;
}

#yasgui-container {
  height: 100%;
  width: 100%;
}

/* Ensure Yasgui tab bar and result area fill the container */
#yasgui-container .yasgui {
  height: 100%;
}
```

CHANGE B — workspace.js:
In the `initPanelTabs()` function, inside the panel tab click event listener, after any existing `if (btn.dataset.panel === 'event-log') { ... }` block, add:

```js
// Initialize Yasgui on first SPARQL tab activation (lazy init)
if (btn.dataset.panel === 'sparql') {
  if (typeof window.initYasguiConsole === 'function') {
    window.initYasguiConsole();
  }
  // Refresh CodeMirror editor if already initialized (handles panel resize)
  if (window._yasgui) {
    var tab = window._yasgui.getTab ? window._yasgui.getTab() : null;
    if (tab && tab.yasqe && typeof tab.yasqe.refresh === 'function') {
      setTimeout(function () { tab.yasqe.refresh(); }, 50);
    }
  }
}
```

To find the correct location in workspace.js: search for the initPanelTabs function and the click handler on `panel-tab-bar`. The `if (btn.dataset.panel === 'event-log')` check is the existing pattern to follow.
  </action>
  <verify>
    <automated>grep -n "yasgui-container" /home/james/Code/SemPKM/frontend/static/css/workspace.css && grep -n "initYasguiConsole" /home/james/Code/SemPKM/frontend/static/js/workspace.js && echo "PASS: CSS height rules present, workspace.js wired"</automated>
  </verify>
  <done>workspace.css has #yasgui-container height rules; workspace.js initPanelTabs calls window.initYasguiConsole when SPARQL tab is clicked; editor refreshes on re-activation</done>
</task>

<task type="auto">
  <name>Task 3: Add user guide doc for SPARQL Console and write E2E note</name>
  <files>docs/guide/21-sparql-console.md</files>
  <action>
Create docs/guide/21-sparql-console.md documenting the SPARQL Console feature per the standing requirement that all user-visible features must be documented:

```markdown
# SPARQL Console

The SPARQL Console lets you query your entire knowledge base using SPARQL 1.1 — the standard query language for RDF data.

## Opening the Console

Press **Ctrl+J** to open the bottom panel, then click the **SPARQL** tab. The console opens with a default query showing all objects in your knowledge base.

## Running Queries

1. Type or paste a SPARQL SELECT query in the editor pane
2. Press **Ctrl+Enter** or click the **Run** button to execute
3. Results appear in the table below the editor

The editor supports syntax highlighting, prefix autocomplete, and multi-tab queries.

## Prefixes

Common prefixes are automatically injected by the server before execution — you can use them without declaring them:

- `dcterms:` — Dublin Core terms (titles, descriptions)
- `skos:` — SKOS concepts (prefLabels)
- `rdfs:` — RDF Schema (labels)
- `foaf:` — FOAF (names)
- `sempkm:` — SemPKM system namespace
- `schema:` — Schema.org

## Query Persistence

Your queries and tabs are automatically saved to browser localStorage (key: `sempkm-sparql`). They survive full browser close and reopen — you will not lose work between sessions.

## Object Links

IRI values in query results that belong to your knowledge base render as clickable links. Clicking an IRI opens that object in the active editor group — the same behavior as clicking an object in the sidebar.

## Access

The SPARQL Console is available to all authenticated users (owner, member, and guest roles). Queries are automatically scoped to your current knowledge base state — event log data does not appear in results.

## Example Queries

**All objects with labels:**
```sparql
SELECT ?subject ?type ?label WHERE {
  ?subject a ?type .
  OPTIONAL { ?subject dcterms:title ?t }
  OPTIONAL { ?subject skos:prefLabel ?p }
  BIND(COALESCE(?t, ?p, STR(?subject)) AS ?label)
}
ORDER BY ?type
LIMIT 50
```

**All notes:**
```sparql
SELECT ?note ?title WHERE {
  ?note a sempkm:Note ;
        dcterms:title ?title .
}
```
```

Note: This plan does not add Playwright E2E tests. SPARQL Console E2E coverage (sparql-console.spec.ts) is deferred to Phase 28 (POLSH-04), where all v2.2 feature E2E suites are created together. State this explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>test -f /home/james/Code/SemPKM/docs/guide/21-sparql-console.md && grep -q "sempkm-sparql" /home/james/Code/SemPKM/docs/guide/21-sparql-console.md && echo "PASS: guide doc created"</automated>
  </verify>
  <done>docs/guide/21-sparql-console.md exists with content covering how to open the console, running queries, persistence, and access requirements</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the full integration manually:

1. Start the Docker stack: `docker compose up -d` from the project root
2. Open the workspace in a browser
3. Press Ctrl+J — bottom panel opens
4. SPARQL tab is active — Yasgui editor renders (not the "coming in Phase 16" placeholder)
5. Click Run (or Ctrl+Enter) — default query executes and returns results
6. Add a second Yasgui tab, type a query, refresh the page — both tabs and queries survive
7. Close browser entirely, reopen — tabs still present (localStorage working)
8. Verify as member user: log in as non-owner, open SPARQL tab — console works
9. Verify no JS errors in browser console when panel is closed on page load
</verification>

<success_criteria>
- Yasgui editor renders inside #panel-sparql (placeholder text gone)
- POST to /api/sparql succeeds and results appear in Yasgui result table
- localStorage key sempkm-sparql is populated after first query run
- Tabs and query content survive browser close/reopen
- Console accessible to any authenticated user (not owner-only gate)
- No JavaScript errors when bottom panel is closed on page load (lazy init)
- docs/guide/21-sparql-console.md created
</success_criteria>

<output>
After completion, create `.planning/phases/23-sparql-console/23-01-SUMMARY.md` using the summary template.
</output>
