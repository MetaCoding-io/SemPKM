---
phase: 55-browser-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/templates/browser/workspace.html
  - backend/app/templates/browser/nav_tree.html
  - frontend/static/js/workspace.js
  - frontend/static/css/workspace.css
autonomous: true
requirements: [OBUI-01, OBUI-02]

must_haves:
  truths:
    - "OBJECTS section header shows refresh and plus buttons on hover"
    - "Clicking refresh reloads the nav tree and collapses all expanded type nodes"
    - "Clicking plus opens the command palette"
    - "Command palette has per-type Create entries (Create Note, Create Project, etc.)"
    - "Typing 'create' in the command palette shows all per-type create entries"
    - "Selecting a create entry opens a create form in a dockview tab"
  artifacts:
    - path: "backend/app/templates/browser/workspace.html"
      provides: "OBJECTS section header with hover-reveal action buttons (refresh, plus)"
      contains: "explorer-header-actions"
    - path: "frontend/static/js/workspace.js"
      provides: "refreshNavTree function and per-type Create palette entries"
      contains: "refreshNavTree"
    - path: "frontend/static/css/workspace.css"
      provides: "Hover-reveal CSS for explorer header action buttons"
      contains: "explorer-header-actions"
  key_links:
    - from: "workspace.html"
      to: "workspace.js"
      via: "onclick handlers on header buttons calling refreshNavTree() and openCommandPalette()"
      pattern: "refreshNavTree|openCommandPalette"
    - from: "workspace.js"
      to: "ninja-keys"
      via: "Per-type Create entries added to ninja.data from nav tree DOM"
      pattern: "Create.*ninja\\.data"
---

<objective>
Add hover-reveal action buttons (refresh, plus) to the OBJECTS section header in the nav tree, and extend the command palette with per-type "Create X" entries.

Purpose: Give users quick access to refresh the object list and create new objects without keyboard shortcuts, following the VS Code Explorer pattern.
Output: Modified workspace.html, workspace.js, workspace.css with header controls and command palette entries.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-browser-ui-polish/55-CONTEXT.md
@.planning/phases/55-browser-ui-polish/55-RESEARCH.md

<interfaces>
<!-- Key templates and functions the executor needs -->

From backend/app/templates/browser/workspace.html (lines 25-34):
```html
<div class="explorer-section expanded" id="section-objects" data-panel-name="objects">
    <div class="explorer-section-header" draggable="true"
         onclick="this.parentElement.classList.toggle('expanded')">
        <i data-lucide="grip-vertical" class="panel-grip"></i>
        <span class="explorer-section-chevron">&#9656;</span>
        <span class="explorer-section-title">OBJECTS</span>
    </div>
    <div class="explorer-section-body">
        {% include "browser/nav_tree.html" %}
    </div>
</div>
```

From backend/app/templates/browser/nav_tree.html:
```html
<!-- Type nodes with hx-get="/browser/tree/{type_iri}" hx-trigger="click once" -->
<div class="tree-node" data-type-iri="{{ type.iri }}" data-testid="nav-section" ...>
    <span class="tree-label">{{ type.label }}</span>
</div>
```

From frontend/static/js/workspace.js - command palette pattern:
```javascript
function initCommandPalette() {
    // ninja.data = [ { id, title, section, handler }, ... ]
    // Current 'new-object' entry calls showTypePicker()
    // Views added via fetch('/browser/views/available')
    // FTS entries filtered by id.startsWith('fts-')
}
function showTypePicker() { /* Opens type picker in active editor area */ }
window.showTypePicker = showTypePicker;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hover-reveal header buttons to OBJECTS section and refresh function</name>
  <files>backend/app/templates/browser/workspace.html, frontend/static/js/workspace.js, frontend/static/css/workspace.css</files>
  <action>
1. In `workspace.html`, modify the OBJECTS `explorer-section-header` div to add an `explorer-header-actions` span after the title span. Inside it, add:
   - A refresh button: `<button class="panel-btn explorer-action-btn" onclick="event.stopPropagation(); refreshNavTree()" title="Refresh"><i data-lucide="refresh-cw"></i></button>`
   - A plus button: `<button class="panel-btn explorer-action-btn" onclick="event.stopPropagation(); document.querySelector('ninja-keys').open()" title="New Object"><i data-lucide="plus"></i></button>`
   - CRITICAL: Both buttons MUST call `event.stopPropagation()` to prevent the section header's `onclick` toggle from firing.
   - Also add hidden elements for future selection UI (Plan 02 will use these): a `<span class="selection-badge" id="selection-badge" style="display:none"><span id="selection-count"></span></span>` and a trash button `<button class="panel-btn explorer-action-btn" id="bulk-delete-btn" style="display:none" title="Delete selected"><i data-lucide="trash-2"></i></button>` placed before the refresh button.

2. In `workspace.css`, add the hover-reveal pattern:
   ```css
   .explorer-header-actions {
       display: flex;
       align-items: center;
       gap: 2px;
       margin-left: auto;
       opacity: 0;
       transition: opacity 0.15s;
   }
   .explorer-section-header:hover .explorer-header-actions {
       opacity: 1;
   }
   /* Also show when selection is active (badge visible) */
   .explorer-header-actions.has-selection {
       opacity: 1;
   }
   .explorer-action-btn svg {
       width: 14px;
       height: 14px;
       flex-shrink: 0;
       stroke: currentColor;
   }
   .selection-badge {
       font-size: 11px;
       color: var(--color-text-muted);
       padding: 0 4px;
   }
   ```

3. In `workspace.js`, add a `refreshNavTree()` function:
   - Get the `#section-objects .explorer-section-body` element.
   - Use `htmx.ajax('GET', '/browser/nav-tree', { target: body, swap: 'innerHTML' })` to reload the nav tree content.
   - This replaces all type nodes and collapses them (children lazy-load again on click per `hx-trigger="click once"`).
   - After the htmx swap completes, call `lucide.createIcons()` to initialize any new Lucide icons.
   - Expose as `window.refreshNavTree = refreshNavTree;`

  Per CLAUDE.md: Size Lucide icons via CSS, not inline styles. Add `flex-shrink: 0` on SVGs in flex containers.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM/e2e && npx playwright test --project=chromium --grep "nav-tree" -x 2>&1 | tail -20</automated>
  </verify>
  <done>OBJECTS section header shows refresh and plus buttons on hover. Refresh reloads the tree. Plus opens command palette. Existing nav tree tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-type Create entries to command palette</name>
  <files>frontend/static/js/workspace.js, backend/app/templates/browser/nav_tree.html</files>
  <action>
1. In `workspace.js` `initCommandPalette()`, rename the existing "New Object" entry to "Create new object" (per CONTEXT.md locked decision). Keep its `id: 'new-object'` and handler calling `showTypePicker()`.

2. After the initial `ninja.data` setup in `initCommandPalette()`, add a function `_addTypeCreateEntries(ninja)` that:
   - Extracts type data from the existing nav tree DOM: `document.querySelectorAll('#section-objects .tree-node')` each has `data-type-iri` attribute and a `.tree-label` span with the type name.
   - For each type, adds a command palette entry:
     ```javascript
     { id: 'create-' + typeLabel.toLowerCase().replace(/\s+/g, '-'),
       title: 'Create ' + typeLabel,
       section: 'Objects',
       handler: function() { showTypePicker(); } }
     ```
   - The handler calls `showTypePicker()` which opens the type picker. This is the simplest approach since the type picker already handles opening the create form in a dockview tab.
   - IMPORTANT: All create entries must be prefixed with "Create" so typing "create" in the palette shows them clustered together.

3. Call `_addTypeCreateEntries(ninja)` at the end of `initCommandPalette()`.

4. Also call `_addTypeCreateEntries(ninja)` inside the `refreshNavTree()` function (after htmx swap completes) to re-populate type entries if the tree changes.

5. In `nav_tree.html`, no changes needed -- the type data is already in the DOM as `data-type-iri` and `.tree-label` text.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM/e2e && npx playwright test --project=chromium --grep "command-palette|nav-tree" -x 2>&1 | tail -20</automated>
  </verify>
  <done>Command palette shows "Create new object" (renamed from "New Object") plus per-type entries like "Create Note", "Create Project", etc. Typing "create" filters to show all create entries. Selecting an entry opens the type picker / create form flow.</done>
</task>

</tasks>

<verification>
1. Hover over OBJECTS section header -- refresh and plus buttons appear
2. Click refresh -- tree reloads, expanded type nodes collapse
3. Click plus -- command palette opens
4. Open command palette (Ctrl+K), type "create" -- see "Create new object", "Create Note", "Create Project", etc.
5. Select "Create Note" -- type picker / create form appears in a dockview tab
6. Existing nav tree e2e tests pass without regression
</verification>

<success_criteria>
- OBJECTS header shows hover-reveal refresh and plus buttons
- Refresh button triggers full tree reload via htmx
- Plus button opens the ninja-keys command palette
- Command palette has per-type "Create X" entries extracted from nav tree DOM
- All create entries prefixed with "Create" for cluster filtering
- No regressions in existing e2e tests
</success_criteria>

<output>
After completion, create `.planning/phases/55-browser-ui-polish/55-01-SUMMARY.md`
</output>
