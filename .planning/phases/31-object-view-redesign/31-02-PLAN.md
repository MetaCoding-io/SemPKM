---
phase: 31-object-view-redesign
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified: []
autonomous: false
requirements: [VIEW-01]

must_haves:
  truths:
    - "Opening any object tab shows the rendered Markdown body immediately without properties visible"
    - "User clicks a 'N properties' toggle badge and the full property list expands inline without a page reload"
    - "User collapses properties, reloads the page, and reopens the same object — properties remain collapsed"
    - "The existing CSS 3D flip to edit mode is unaffected and still reachable from the view header"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the object view redesign. Confirm body-first layout, properties toggle, localStorage persistence, empty-body behavior, edit mode layout, and 3D flip animation all work correctly in the browser.

Purpose: Visual and functional verification that cannot be done through automated testing alone — CSS transitions, layout correctness, animation integrity.
Output: Verified phase completion or bug list for gap closure.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-object-view-redesign/31-01-SUMMARY.md
@.planning/phases/31-object-view-redesign/31-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify object view redesign in browser</name>
  <files></files>
  <action>Human verification of the complete object view redesign from Plan 01.</action>
  <verify>Human confirms all 8 scenarios below pass.</verify>
  <done>All 8 verification scenarios pass; user types "approved"</done>
  <what-built>
    Object view redesigned: Markdown body is now primary content, properties collapsed behind toolbar badge with smooth CSS animation, localStorage persistence per object IRI, edit mode mirrors same layout.
  </what-built>
  <how-to-verify>
    Navigate to http://localhost:3901/browser/ in the browser.

    **Scenario 1: Body-first layout**
    1. Click any object with both properties and a Markdown body (e.g., a Note like "Architecture Notes")
    2. VERIFY: The Markdown body fills the view area immediately. No properties visible above it.
    3. VERIFY: A badge in the toolbar shows "N properties" (e.g., "5 properties")

    **Scenario 2: Properties toggle**
    4. Click the "N properties" badge
    5. VERIFY: Properties slide down smoothly between the toolbar and the body content
    6. Click the badge again
    7. VERIFY: Properties collapse back up with a smooth animation

    **Scenario 3: Persistence across reload**
    8. Expand properties (click badge)
    9. Press F5 to reload the page
    10. Reopen the same object
    11. VERIFY: Properties are still expanded (localStorage remembered the preference)
    12. Collapse properties (click badge)
    13. Reload and reopen
    14. VERIFY: Properties are collapsed

    **Scenario 4: Empty body auto-expand**
    15. Open an object with no Markdown body (e.g., a Person or Concept — objects that typically have properties but no body)
    16. VERIFY: Properties are expanded by default
    17. VERIFY: A "No content" placeholder appears below the properties

    **Scenario 5: Edit mode**
    18. On an object with a body, click the Edit button
    19. VERIFY: The 3D flip animation works correctly
    20. VERIFY: Edit mode shows the body editor as primary content, with properties collapsed
    21. VERIFY: The properties badge still works — click to expand/collapse the form fields

    **Scenario 6: Shared collapse state**
    22. In read mode, expand properties
    23. Click Edit to flip to edit mode
    24. VERIFY: Properties are expanded in edit mode too (shared state)
    25. Collapse properties in edit mode
    26. Click Cancel to flip back to read mode
    27. VERIFY: Properties are collapsed in read mode (state synced back)

    **Scenario 7: Type badge and toolbar**
    28. VERIFY: Title is on the left, type chip + properties badge + Edit button are on the right
    29. VERIFY: Type chip still shows the type name (e.g., "Note") and clicking copies the IRI

    **Scenario 8: No regressions**
    30. VERIFY: Edit form loads and submits correctly (save an edit)
    31. VERIFY: Ref-pills in properties are still clickable (open referenced objects)
    32. VERIFY: Settings tab and Docs tab still open correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 8 scenarios above pass visual/functional verification.
</verification>

<success_criteria>
- All 8 verification scenarios pass in the browser
- No visual regressions in existing functionality
- CSS transitions are smooth and natural
- Layout works correctly in both light and dark modes
</success_criteria>

<output>
After completion, create `.planning/phases/31-object-view-redesign/31-02-SUMMARY.md`
</output>
