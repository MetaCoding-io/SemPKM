---
phase: 54-sparql-advanced
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - backend/app/sparql/router.py
  - backend/app/views/service.py
  - backend/app/views/router.py
  - backend/app/browser/router.py
  - backend/app/templates/browser/workspace.html
  - backend/app/templates/browser/my_views.html
  - backend/app/templates/browser/promote_dialog.html
  - frontend/static/js/sparql-console.js
  - frontend/static/css/workspace.css
autonomous: true
requirements:
  - SPARQL-07

must_haves:
  truths:
    - "User can promote a saved query to a named view via the Saved dropdown or results area"
    - "Promoted views appear in a 'My Views' section in the nav tree below VIEWS"
    - "Clicking a promoted view in the nav tree renders results using existing table/cards/graph infrastructure"
    - "User can demote a promoted view back to just a saved query"
    - "Promoted views are private to the creator (other users do not see them)"
    - "Auto-detected columns from SELECT variables appear as table headers"
  artifacts:
    - path: "backend/app/sparql/router.py"
      provides: "Promote/demote endpoints"
      exports: ["promote_query", "demote_query"]
    - path: "backend/app/views/service.py"
      provides: "get_user_promoted_view_specs method, extended get_view_spec_by_iri"
      contains: "get_user_promoted_view_specs"
    - path: "backend/app/views/router.py"
      provides: "View rendering routes that handle user-promoted ViewSpecs"
    - path: "backend/app/browser/router.py"
      provides: "GET /browser/my-views endpoint"
      contains: "my_views"
    - path: "backend/app/templates/browser/workspace.html"
      provides: "My Views explorer section in nav tree"
      contains: "section-my-views"
    - path: "backend/app/templates/browser/my_views.html"
      provides: "Tree items for promoted views with demote action"
    - path: "backend/app/templates/browser/promote_dialog.html"
      provides: "Promotion dialog with name field and renderer picker"
    - path: "frontend/static/js/sparql-console.js"
      provides: "Promote icon click handler, Save as View button, promotion dialog JS"
  key_links:
    - from: "backend/app/views/service.py"
      to: "backend/app/sparql/models.py"
      via: "Query PromotedQueryView + SavedSparqlQuery from SQLite"
      pattern: "PromotedQueryView"
    - from: "backend/app/views/router.py"
      to: "backend/app/views/service.py"
      via: "get_view_spec_by_iri falls through to user specs for urn:sempkm:user-view: IRIs"
      pattern: "user-view"
    - from: "backend/app/templates/browser/workspace.html"
      to: "/browser/my-views"
      via: "htmx hx-get lazy load"
      pattern: "my-views"
    - from: "frontend/static/js/sparql-console.js"
      to: "/api/sparql/saved/{id}/promote"
      via: "fetch POST for promotion"
      pattern: "promote"
---

<objective>
Implement SPARQL query view promotion: promote saved queries into named views that appear in the nav tree, rendering results through the existing ViewSpec infrastructure.

Purpose: Allows users to "pin" frequently-used SPARQL queries as browsable views in the sidebar, turning ad-hoc queries into persistent data views without any model configuration.

Output: Promote/demote API endpoints, ViewSpecService integration, "My Views" nav tree section, promotion dialog, promote/demote UI in dropdown and results area.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-sparql-advanced/54-CONTEXT.md
@.planning/phases/54-sparql-advanced/54-RESEARCH.md
@.planning/phases/54-sparql-advanced/54-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 54-01 and existing codebase -->

From backend/app/sparql/models.py (created in 54-01):
```python
class PromotedQueryView(Base):
    __tablename__ = "promoted_query_views"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    query_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("saved_sparql_queries.id", ondelete="CASCADE"), unique=True)
    user_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), index=True)
    display_label: Mapped[str] = mapped_column(String(255), nullable=False)
    renderer_type: Mapped[str] = mapped_column(String(20), default="table")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

From backend/app/views/service.py:
```python
@dataclass
class ViewSpec:
    spec_iri: str
    label: str
    target_class: str
    renderer_type: str  # "table", "card", "graph"
    sparql_query: str
    columns: list[str] = field(default_factory=list)
    sort_default: str = ""
    card_title: str = ""
    card_subtitle: str = ""
    source_model: str = ""  # model ID or "user"

class ViewSpecService:
    async def get_all_view_specs(self) -> list[ViewSpec]  # cached, model specs only
    async def get_view_spec_by_iri(self, spec_iri: str) -> ViewSpec | None
    async def execute_table_query(self, spec, page, page_size, sort_col, sort_dir, filter_text) -> dict
    async def execute_cards_query(self, spec, page, page_size, filter_text, group_by) -> dict
    async def execute_graph_query(self, spec) -> dict
```

From backend/app/views/router.py:
```python
router = APIRouter(prefix="/browser/views", tags=["views"])
# GET /browser/views/table/{spec_iri:path} -- table rendering
# GET /browser/views/card/{spec_iri:path} -- cards rendering
# GET /browser/views/graph/{spec_iri:path} -- graph rendering
# GET /browser/views/explorer -- sidebar tree
# get_view_spec_by_iri() is called by table/card/graph endpoints
```

From backend/app/browser/router.py:
```python
# Existing nav tree endpoint pattern: renders partials via templates
# Uses request.app.state.templates for Jinja2 rendering
```

From backend/app/templates/browser/workspace.html (nav tree sections):
```html
<!-- VIEWS section pattern -->
<div class="explorer-section" id="section-views" data-panel-name="views">
    <div class="explorer-section-header" draggable="true"
         onclick="this.parentElement.classList.toggle('expanded')"
         hx-get="/browser/views/explorer"
         hx-target="#views-tree"
         hx-trigger="load"
         hx-swap="innerHTML">
        <i data-lucide="grip-vertical" class="panel-grip"></i>
        <span class="explorer-section-chevron">&#9656;</span>
        <span class="explorer-section-title">VIEWS</span>
    </div>
    <div class="explorer-section-body" id="views-tree">
        <div class="tree-empty">Loading views...</div>
    </div>
</div>
```

Existing view rendering endpoints accept `{spec_iri:path}` -- handles URN-style IRIs like `urn:sempkm:user-view:{uuid}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote/demote endpoints, ViewSpecService integration, SELECT variable extraction</name>
  <files>
    backend/app/sparql/router.py,
    backend/app/views/service.py,
    backend/app/views/router.py,
    backend/app/browser/router.py,
    backend/app/templates/browser/my_views.html,
    backend/app/templates/browser/promote_dialog.html
  </files>
  <action>
**SPARQL router -- promote/demote endpoints (router.py):**

1. `POST /sparql/saved/{query_id}/promote` -- Accepts JSON body: `{ "display_label": str, "renderer_type": str }`. Creates a `PromotedQueryView` row for the current user's saved query. Only the query owner can promote. If already promoted, return 409 Conflict. Returns 201 with `{ "id": uuid, "spec_iri": "urn:sempkm:user-view:{id}" }`. Validates `renderer_type` is one of "table", "card", "graph".

2. `DELETE /sparql/saved/{query_id}/promote` -- Demotes a promoted view. Deletes the `PromotedQueryView` row. Only the query owner can demote. Returns 204. The underlying saved query remains intact.

3. `GET /sparql/saved/{query_id}/promotion` -- Check if a saved query is promoted. Returns `{ "promoted": bool, "view_id": uuid|null, "spec_iri": str|null, "display_label": str|null, "renderer_type": str|null }`. Owner-only.

**ViewSpecService integration (views/service.py):**

1. Add `_extract_select_var_names(sparql_query: str) -> list[str]` as a module-level function:
   - Use regex to match `SELECT (DISTINCT)? ... WHERE` (case-insensitive, DOTALL)
   - Handle `SELECT *` by returning empty list
   - Extract aliases: `AS ?alias` -> use alias name
   - Extract direct variables: `?var` (outside parentheses)
   - Return variable names without the `?` prefix

2. Add `async def get_user_promoted_view_specs(self, user_id: uuid.UUID, db) -> list[ViewSpec]` method to `ViewSpecService`:
   - Query `PromotedQueryView` joined with `SavedSparqlQuery` where `PromotedQueryView.user_id == user_id`
   - For each result, build a `ViewSpec` with:
     - `spec_iri = f"urn:sempkm:user-view:{pv.id}"`
     - `label = pv.display_label`
     - `target_class = ""` (user views don't target a specific class)
     - `renderer_type = pv.renderer_type`
     - `sparql_query = sq.query_text`
     - `columns = _extract_select_var_names(sq.query_text)`
     - `source_model = "user"`
   - Do NOT cache these results (per Research pitfall 1 -- keep user specs out of TTL cache)
   - Import needed: `from sqlalchemy.ext.asyncio import AsyncSession`, `from sqlalchemy import select`, `from app.sparql.models import PromotedQueryView, SavedSparqlQuery`

3. Extend `get_view_spec_by_iri()` to accept an optional `user_id` and `db` parameter. If spec_iri starts with `"urn:sempkm:user-view:"`, query `PromotedQueryView` from SQLite instead of searching cached model specs. Return the constructed `ViewSpec` or None.

**Views router (views/router.py):**

Modify `table_view()`, `cards_view()`, `graph_view()`, and `graph_data()` endpoints:
- Add `db: AsyncSession = Depends(get_db_session)` dependency
- When calling `get_view_spec_by_iri()`, also pass `user_id=user.id` and `db=db`
- This allows these endpoints to render promoted user views using the same code path as model views
- For user views (spec.source_model == "user"): skip `get_view_specs_for_type()` call (no type switcher needed), set `all_specs = []`, set `type_label = "Custom View"`
- Add `from app.db.session import get_db_session` import

**Browser router (browser/router.py):**

Add endpoint `GET /browser/my-views`:
- Requires `get_current_user`, `get_db_session`, `get_view_spec_service`
- Calls `view_spec_service.get_user_promoted_view_specs(user.id, db)`
- Renders `browser/my_views.html` with the list of ViewSpecs
- If empty, return `<div class="tree-empty">No promoted views yet</div>`

**Template: my_views.html (NEW):**
Render each promoted view as a tree leaf entry:
```html
{% for spec in specs %}
<div class="tree-leaf view-leaf" data-spec-iri="{{ spec.spec_iri }}">
    <span class="tree-leaf-icon"><i data-lucide="{% if spec.renderer_type == 'table' %}table{% elif spec.renderer_type == 'card' %}layout-grid{% else %}share-2{% endif %}"></i></span>
    <span class="tree-leaf-label" onclick="openViewTab('{{ spec.spec_iri }}', '{{ spec.label }}', '{{ spec.renderer_type }}')">{{ spec.label }}</span>
    <button class="panel-btn tree-leaf-action sparql-demote-btn" title="Remove from views"
            hx-delete="/api/sparql/saved/{{ spec.spec_iri.split(':')[-1] }}/promote"
            hx-target="#my-views-tree"
            hx-swap="innerHTML"
            hx-confirm="Remove this view? The saved query will be kept.">
        <i data-lucide="pin-off"></i>
    </button>
</div>
{% endfor %}
```
Note: The demote hx-delete needs the query_id, not the view id. Store `data-query-id` on each entry from the PromotedQueryView, and pass it to the template. The browser/router endpoint should include query_id in the context.

Actually, simplify: the `GET /browser/my-views` endpoint should return each view with its `query_id` so the demote button can hit `DELETE /api/sparql/saved/{query_id}/promote`. After demote, htmx should re-fetch the my-views list. Use `hx-get="/browser/my-views"` as the re-fetch trigger on the demote button's `hx-swap`.

**Template: promote_dialog.html (NEW):**
A `<dialog>` element for the promotion form:
```html
<dialog id="promote-dialog" class="sempkm-dialog">
    <form method="dialog" id="promote-form">
        <h3>Promote to View</h3>
        <input type="hidden" id="promote-query-id" name="query_id">
        <div class="form-group">
            <label for="promote-label">View Name</label>
            <input type="text" id="promote-label" name="display_label" required maxlength="255" class="form-control">
        </div>
        <div class="form-group">
            <label>Renderer</label>
            <div class="renderer-picker">
                <label class="renderer-option"><input type="radio" name="renderer_type" value="table" checked> <i data-lucide="table"></i> Table</label>
                <label class="renderer-option"><input type="radio" name="renderer_type" value="card"> <i data-lucide="layout-grid"></i> Cards</label>
                <label class="renderer-option"><input type="radio" name="renderer_type" value="graph"> <i data-lucide="share-2"></i> Graph</label>
            </div>
            <div id="promote-graph-warning" class="form-hint" style="display:none; color: var(--color-warning);">
                Warning: Graph renderer requires ?source and ?target variables in the query.
            </div>
        </div>
        <div class="dialog-actions">
            <button type="button" class="btn btn-secondary" onclick="this.closest('dialog').close()">Cancel</button>
            <button type="submit" class="btn btn-primary">Promote</button>
        </div>
    </form>
</dialog>
```
Include this dialog in `workspace.html` at the bottom (before the closing `</div>` of workspace-container), using `{% include "browser/promote_dialog.html" %}`.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && rg "get_user_promoted_view_specs" backend/app/views/service.py && rg "_extract_select_var_names" backend/app/views/service.py && rg "promote" backend/app/sparql/router.py && rg "my-views" backend/app/browser/router.py && rg "promote-dialog" backend/app/templates/browser/promote_dialog.html && echo "All promotion backend + templates present"</automated>
  </verify>
  <done>Promote/demote endpoints work. ViewSpecService resolves user-view: IRIs from SQLite. View rendering endpoints handle promoted ViewSpecs. Nav tree my-views endpoint returns promoted views. Promote dialog and my_views templates exist.</done>
</task>

<task type="auto">
  <name>Task 2: Promote UI in dropdown/results, My Views nav tree section, demote action, CSS</name>
  <files>
    frontend/static/js/sparql-console.js,
    frontend/static/css/workspace.css,
    backend/app/templates/browser/workspace.html
  </files>
  <action>
**Nav tree "My Views" section (workspace.html):**

Add a new explorer section after the VIEWS section (after `</div>` closing `section-views`):
```html
<div class="explorer-section" id="section-my-views" data-panel-name="my-views">
    <div class="explorer-section-header" draggable="true"
         onclick="this.parentElement.classList.toggle('expanded')"
         hx-get="/browser/my-views"
         hx-target="#my-views-tree"
         hx-trigger="load"
         hx-swap="innerHTML">
        <i data-lucide="grip-vertical" class="panel-grip"></i>
        <span class="explorer-section-chevron">&#9656;</span>
        <span class="explorer-section-title">MY VIEWS</span>
    </div>
    <div class="explorer-section-body" id="my-views-tree">
        <div class="tree-empty">Loading views...</div>
    </div>
</div>
```

**Promote button wiring in sparql-console.js:**

1. Wire the promote button (`.sparql-promote-btn`) that was rendered in Plan 54-01's Task 2. On click:
   - Get the query_id and query name from the dropdown item
   - Open the promote dialog: set `#promote-query-id` value to query_id, pre-fill `#promote-label` with the query name
   - Show the `<dialog>` via `document.getElementById('promote-dialog').showModal()`
   - Check if the query has `?source` and `?target` -- show/hide the graph warning when "graph" radio is selected

2. Handle the promote form submission:
   - Listen for the dialog's `close` event (or intercept form submit)
   - On submit (not cancel), call `POST /api/sparql/saved/{query_id}/promote` with `{ display_label, renderer_type }`
   - On success, refresh the "My Views" nav tree section by triggering htmx: `htmx.trigger(document.getElementById('my-views-tree').parentElement.querySelector('.explorer-section-header'), 'click')`
   - Also update the promote button state in the dropdown (change icon to `pin-off` or add a visual indicator that it's promoted)

3. **"Save as View" button in results area:**
   - After a successful query execution in `executeQuery()`, check if the current query is saved (has a `currentSavedQueryId` from loading a saved query). If yes, add a "Save as View" button in the `#sparql-results-info` area alongside the row count.
   - Track `currentSavedQueryId` as module state: set it when a saved query is loaded from the dropdown, clear it when the editor content changes manually.
   - The "Save as View" button opens the same promote dialog.

4. **Add `openViewTab()` function** (accessible globally, called from my_views.html template):
   - Takes `specIri`, `label`, `rendererType` as parameters
   - Constructs the URL: `/browser/views/{rendererType}/{encodedSpecIri}`
   - Opens in the dockview editor area via `openTab(specIri, label)` (the existing workspace.js tab opener), but uses `hx-get` to load the view content. Actually, follow the existing view opening pattern: model views are opened via `openViewInEditor(specIri, label, rendererType)` if that function exists, or create it. Check how views_explorer.html currently opens views and replicate that pattern.
   - If no `openViewInEditor` exists in workspace.js, add it: calls dockview `addPanel` with the view URL as the component content source.

**CSS (workspace.css):**

- `.sparql-demote-btn svg`: width 14px, height 14px, flex-shrink 0, stroke currentColor
- `#section-my-views .tree-leaf`: display flex, align-items center, gap 6px, padding 2px 8px 2px 20px, cursor pointer
- `#section-my-views .tree-leaf-label`: flex 1, overflow hidden, text-overflow ellipsis, white-space nowrap, font-size 13px
- `#section-my-views .tree-leaf-icon svg`: width 14px, height 14px, flex-shrink 0, stroke currentColor
- `#section-my-views .tree-leaf-action`: opacity 0, transition opacity 0.15s
- `#section-my-views .tree-leaf:hover .tree-leaf-action`: opacity 1
- `.renderer-picker`: display flex, gap 12px, margin-top 4px
- `.renderer-option`: display flex, align-items center, gap 4px, cursor pointer, font-size 13px
- `.renderer-option svg`: width 16px, height 16px, flex-shrink 0, stroke currentColor
- `#promote-dialog` (extend `.sempkm-dialog` if it exists, otherwise style): background var(--color-bg-primary), border 1px solid var(--color-border), border-radius 8px, padding 20px, min-width 320px. `::backdrop` background rgba(0,0,0,0.5).

**Graph renderer warning:** When the "graph" radio is selected in the promote dialog, check the current query text for `?source` and `?target`. If missing, show `#promote-graph-warning`. Hide it for other renderer types.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && rg "section-my-views" backend/app/templates/browser/workspace.html && rg "openViewTab\|openViewInEditor" frontend/static/js/sparql-console.js && rg "promote-dialog" backend/app/templates/browser/workspace.html && rg "renderer-picker" frontend/static/css/workspace.css && echo "All promotion UI elements present"</automated>
  </verify>
  <done>"My Views" section appears in the nav tree and shows promoted views. Promote icon in Saved dropdown opens dialog. "Save as View" button appears in results area for saved queries. Promote dialog has name field and renderer picker with graph warning. Clicking a promoted view in nav tree opens it in the editor area using existing view rendering. Demote removes the view from nav tree but keeps the saved query.</done>
</task>

</tasks>

<verification>
1. Save a SPARQL query, click promote icon in Saved dropdown -- dialog opens with name pre-filled
2. Choose "table" renderer, click Promote -- "My Views" section in nav tree shows the new view
3. Click the view in nav tree -- table renders with auto-detected columns from the SELECT clause
4. Change renderer to "cards" via re-promoting (demote first, then promote again) -- cards view renders
5. Click demote (pin-off icon) on the nav tree entry -- view disappears, saved query still in Saved dropdown
6. Run a saved query, click "Save as View" above results -- same promotion dialog opens
7. Verify another user does NOT see the promoted view in their nav tree (creator-only)
</verification>

<success_criteria>
- POST /sparql/saved/{id}/promote creates PromotedQueryView, returns spec_iri
- DELETE /sparql/saved/{id}/promote removes promotion, saved query intact
- ViewSpecService.get_view_spec_by_iri handles urn:sempkm:user-view: IRIs
- Promoted views render correctly as table, cards, or graph via existing infrastructure
- "My Views" nav tree section loads via htmx, shows promoted views with renderer icons
- Promote dialog has name field, renderer picker, and graph variable warning
- Columns auto-detected from SELECT variables (including aliases)
</success_criteria>

<output>
After completion, create `.planning/phases/54-sparql-advanced/54-02-SUMMARY.md`
</output>
