---
phase: 39-edit-form-helptext-and-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/shapes.py
  - backend/app/templates/forms/object_form.html
  - backend/app/templates/forms/_field.html
  - frontend/static/css/workspace.css
  - models/basic-pkm/shapes/basic-pkm.jsonld
  - orig_specs/models/starter-basic-pkm/shapes.ttl
autonomous: true
requirements: [HELP-01]

must_haves:
  truths:
    - "Edit form shows collapsible form-level helptext below the title row when NodeShape has sempkm:editHelpText"
    - "Edit form shows ? icon next to field label when PropertyShape has sempkm:editHelpText"
    - "Clicking ? icon expands markdown helptext inline below the field"
    - "Note shape has both form-level and field-level helptext on all fields"
    - "Project, Concept, Person shapes have form-level helptext only"
  artifacts:
    - path: "backend/app/services/shapes.py"
      provides: "helptext field extraction from SHACL shapes"
      contains: "SEMPKM_EDIT_HELPTEXT"
    - path: "backend/app/templates/forms/object_form.html"
      provides: "Form-level helptext rendering"
      contains: "form.helptext"
    - path: "backend/app/templates/forms/_field.html"
      provides: "Field-level helptext rendering with ? icon"
      contains: "prop.helptext"
    - path: "models/basic-pkm/shapes/basic-pkm.jsonld"
      provides: "Helptext annotations on basic-pkm shapes"
      contains: "sempkm:editHelpText"
  key_links:
    - from: "models/basic-pkm/shapes/basic-pkm.jsonld"
      to: "backend/app/services/shapes.py"
      via: "rdflib graph extraction of sempkm:editHelpText"
      pattern: "graph\\.objects.*SEMPKM_EDIT_HELPTEXT"
    - from: "backend/app/services/shapes.py"
      to: "backend/app/templates/forms/object_form.html"
      via: "NodeShapeForm.helptext and PropertyShape.helptext fields"
      pattern: "helptext"
    - from: "backend/app/templates/forms/_field.html"
      to: "frontend/static/js/markdown-render.js"
      via: "renderMarkdownBody() call for field helptext"
      pattern: "renderMarkdownBody"
---

<objective>
Add `sempkm:editHelpText` SHACL annotation support to edit forms. NodeShape-level helptext renders as a collapsible markdown section below the form title. PropertyShape-level helptext renders via a `?` icon next to the field label that expands inline markdown below the field.

Purpose: Users see contextual guidance about what to enter and why, reducing blank-field confusion and teaching the Mental Model's vocabulary.
Output: Working helptext rendering in edit forms with seed content on basic-pkm model shapes.
</objective>

<execution_context>
@/home/james/.claude/get-shit-done/workflows/execute-plan.md
@/home/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-edit-form-helptext-and-bug-fixes/39-CONTEXT.md
@.planning/phases/39-edit-form-helptext-and-bug-fixes/39-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/services/shapes.py:
```python
@dataclass
class PropertyShape:
    path: str
    name: str
    datatype: str | None = None
    target_class: str | None = None
    order: float = 0.0
    group: str | None = None
    min_count: int = 0
    max_count: int | None = None
    in_values: list[str] = field(default_factory=list)
    default_value: str | None = None
    description: str | None = None
    # ADD: helptext: str | None = None

@dataclass
class NodeShapeForm:
    shape_iri: str
    target_class: str
    label: str
    groups: list[PropertyGroup] = field(default_factory=list)
    properties: list[PropertyShape] = field(default_factory=list)
    # ADD: helptext: str | None = None
```

From backend/app/models/registry.py:
```python
SEMPKM_NS = "urn:sempkm:"  # already defined
```

From backend/app/templates/forms/_field.html:
```html
<!-- Label rendering at line 26-28 -->
<label for="input-{{ field_id }}" title="{{ prop.path | compact_iri }}">
    {{ prop.name }}{% if is_required %}<span class="required-marker">*</span>{% endif %}
</label>
<!-- sh:description at lines 30-32 -->
{% if prop.description %}
<small class="field-help">{{ prop.description }}</small>
{% endif %}
```

From models/basic-pkm/shapes/basic-pkm.jsonld @context:
```json
{
  "@context": {
    "sh": "http://www.w3.org/ns/shacl#",
    "bpkm": "urn:sempkm:model:basic-pkm:",
    ...
  }
}
```
Note: "sempkm": "urn:sempkm:" is MISSING from @context and MUST be added.

Existing markdown render function (from frontend/static/js/markdown-render.js):
```javascript
function renderMarkdownBody(srcId, renderedId) { ... }
// Uses marked.js + DOMPurify, already loaded in workspace
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend helptext extraction + shapes annotation</name>
  <files>
    backend/app/services/shapes.py
    models/basic-pkm/shapes/basic-pkm.jsonld
    orig_specs/models/starter-basic-pkm/shapes.ttl
  </files>
  <action>
1. In `backend/app/services/shapes.py`:
   - Add `from rdflib import URIRef` if not already imported (it likely is via existing rdflib usage)
   - Define `SEMPKM_EDIT_HELPTEXT = URIRef("urn:sempkm:editHelpText")` near the top constants
   - Add `helptext: str | None = None` field to `PropertyShape` dataclass (after `description`)
   - Add `helptext: str | None = None` field to `NodeShapeForm` dataclass (after `properties`)
   - In `_extract_property_shape()` method, after the `sh:description` extraction block (around line 214), add:
     ```python
     helptexts = list(graph.objects(prop_node, SEMPKM_EDIT_HELPTEXT))
     helptext = str(helptexts[0]) if helptexts else None
     ```
     And include `helptext=helptext` in the PropertyShape constructor call
   - In `_extract_node_shape()` method, after extracting shape metadata but before returning, add:
     ```python
     helptexts = list(graph.objects(shape_node, SEMPKM_EDIT_HELPTEXT))
     helptext = str(helptexts[0]) if helptexts else None
     ```
     And include `helptext=helptext` in the NodeShapeForm constructor call

2. In `models/basic-pkm/shapes/basic-pkm.jsonld`:
   - Add `"sempkm": "urn:sempkm:"` to the `@context` block
   - Add `"sempkm:editHelpText"` to NoteShape (form-level): markdown content explaining what Notes are for, when to create one, what fields mean
   - Add `"sempkm:editHelpText"` to ALL PropertyShapes within NoteShape (field-level): brief guidance for each field (Title, Body, Tags, Related concepts, etc.)
   - Add `"sempkm:editHelpText"` to ProjectShape, ConceptShape, PersonShape (form-level only): 2-3 sentence guidance for each type
   - Helptext content should be practical PKM guidance, not lorem ipsum. Example for Note title: "A concise, descriptive title that captures the core idea. Good titles help you find notes later -- prefer specifics over generic labels."

3. In `orig_specs/models/starter-basic-pkm/shapes.ttl`:
   - Add `@prefix sempkm: <urn:sempkm:> .` to prefix declarations
   - Add matching `sempkm:editHelpText` annotations to all the same shapes (NoteShape form+fields, other shapes form-level only)
   - Use Turtle multiline string syntax (`"""..."""`) for longer helptext

Keep helptext concise -- 1-3 sentences per field, 3-5 sentences per form.
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && python -c "
from backend.app.services.shapes import PropertyShape, NodeShapeForm, SEMPKM_EDIT_HELPTEXT
ps = PropertyShape(path='test', name='test', helptext='hello')
ns = NodeShapeForm(shape_iri='s', target_class='t', label='l', helptext='world')
assert ps.helptext == 'hello'
assert ns.helptext == 'world'
print('Dataclass fields OK')
print('SEMPKM_EDIT_HELPTEXT:', SEMPKM_EDIT_HELPTEXT)
"</automated>
  </verify>
  <done>PropertyShape and NodeShapeForm have helptext fields. ShapesService extracts sempkm:editHelpText from shapes graph. basic-pkm model has helptext annotations on Note (form + all fields) and Project/Concept/Person (form-level only).</done>
</task>

<task type="auto">
  <name>Task 2: Frontend helptext rendering + CSS</name>
  <files>
    backend/app/templates/forms/object_form.html
    backend/app/templates/forms/_field.html
    frontend/static/css/workspace.css
  </files>
  <action>
1. In `backend/app/templates/forms/object_form.html`:
   - After the `.form-title-row` div (after the closing `</div>` of the title row, before the `<form>` tag), add a form-level helptext section:
     ```html
     {% if form.helptext %}
     <details class="form-helptext form-helptext-top">
         <summary class="form-helptext-summary">
             <i data-lucide="help-circle" class="helptext-icon"></i>
             Form Guide
         </summary>
         <div class="form-helptext-content markdown-body" id="helptext-form-rendered"></div>
     </details>
     <pre id="helptext-form-src" style="display:none">{{ form.helptext }}</pre>
     <script>
     (function() {
         if (typeof renderMarkdownBody === 'function') {
             renderMarkdownBody('helptext-form-src', 'helptext-form-rendered');
         }
     })();
     </script>
     {% endif %}
     ```
   - Place the `<pre>` source element OUTSIDE the `<details>` so DOMPurify processes it correctly
   - Call `lucide.createIcons()` or rely on the existing htmx:afterSettle handler that already calls it

2. In `backend/app/templates/forms/_field.html`:
   - In the `render_field` macro, modify the `<label>` element (line 26-28) to include a helptext toggle button after the label text:
     ```html
     <label for="input-{{ field_id }}" title="{{ prop.path | compact_iri }}">
         {{ prop.name }}{% if is_required %}<span class="required-marker">*</span>{% endif %}
         {% if prop.helptext %}
         <button type="button" class="btn-helptext-toggle"
                 onclick="toggleFieldHelp('{{ field_id }}')" title="Show help">
             <i data-lucide="help-circle"></i>
         </button>
         {% endif %}
     </label>
     ```
   - After the existing `{% if prop.description %}` block (line 30-32), add the field helptext block:
     ```html
     {% if prop.helptext %}
     <div class="field-helptext" id="helptext-{{ field_id }}" style="display:none">
         <div class="field-helptext-content markdown-body" id="helptext-rendered-{{ field_id }}"></div>
     </div>
     <pre id="helptext-src-{{ field_id }}" style="display:none">{{ prop.helptext }}</pre>
     {% endif %}
     ```

3. Add `toggleFieldHelp` JavaScript function. Add it as an inline script in `object_form.html` (bottom of the template, after the form):
   ```javascript
   <script>
   window.toggleFieldHelp = function(fieldId) {
       var el = document.getElementById('helptext-' + fieldId);
       if (!el) return;
       var isHidden = el.style.display === 'none';
       el.style.display = isHidden ? 'block' : 'none';
       if (isHidden && !el.dataset.rendered) {
           var srcId = 'helptext-src-' + fieldId;
           var renderedId = 'helptext-rendered-' + fieldId;
           if (typeof renderMarkdownBody === 'function') {
               renderMarkdownBody(srcId, renderedId);
           }
           el.dataset.rendered = 'true';
       }
   };
   </script>
   ```

4. In `frontend/static/css/workspace.css`, add helptext styles:
   - Form-level helptext: `.form-helptext-top` with border, border-radius, background `var(--color-surface-raised)`, margin below
   - `.form-helptext-summary` with flex display, gap, cursor pointer, muted color, align-items center
   - `.form-helptext-summary .helptext-icon` (the Lucide SVG): width 16px, height 16px, flex-shrink 0, stroke currentColor
   - `.form-helptext-content` with padding
   - Field-level toggle: `.btn-helptext-toggle` with inline-flex, no background, no border, padding 0 2px, cursor pointer, color `var(--color-text-faint)`, vertical-align middle
   - `.btn-helptext-toggle:hover` with color `var(--color-accent)`
   - `.btn-helptext-toggle svg` with width 14px, height 14px, flex-shrink 0, stroke currentColor (per CLAUDE.md Lucide rule)
   - `.field-helptext` with padding 6px 0 8px, font-size 0.85rem
   - `.field-helptext .markdown-body` with font-size 0.85rem
  </action>
  <verify>
    <automated>cd /home/james/Code/SemPKM && grep -c "form.helptext" backend/app/templates/forms/object_form.html && grep -c "prop.helptext" backend/app/templates/forms/_field.html && grep -c "btn-helptext-toggle" frontend/static/css/workspace.css</automated>
  </verify>
  <done>Edit forms render form-level helptext as collapsible section below title. Field-level helptext shows ? icon next to label, expands inline markdown on click. CSS styles follow project token conventions and Lucide flex-shrink: 0 rule.</done>
</task>

</tasks>

<verification>
1. Open any Note in edit mode -- form-level "Form Guide" section appears collapsed below title
2. Click the Form Guide disclosure -- markdown content renders with proper formatting
3. Each field label has a small ? icon
4. Click ? icon on Title field -- helptext expands inline below the field with rendered markdown
5. Click ? again -- helptext collapses
6. Open a Project in edit mode -- form-level helptext appears, but no field-level ? icons (only Note has field-level helptext)
7. Existing form functionality (submit, validation) still works
8. `cd /home/james/Code/SemPKM/e2e && npx playwright test --project=chromium` -- existing tests pass
</verification>

<success_criteria>
- SHACL shapes with `sempkm:editHelpText` render collapsible markdown in edit forms
- Note shape has form-level + field-level helptext on all fields
- Project, Concept, Person shapes have form-level helptext only
- Helptext uses marked.js + DOMPurify (existing pipeline)
- Lucide help-circle icon follows CLAUDE.md flex-shrink: 0 rule
- No regressions in existing form functionality
</success_criteria>

<output>
After completion, create `.planning/phases/39-edit-form-helptext-and-bug-fixes/39-01-SUMMARY.md`
</output>
