---
phase: 30-dockview-phase-a-migration
task: 0
total_tasks: 6
status: ready_to_execute
last_updated: 2026-03-02T04:09:02.835Z
---

<current_state>
Phase 30 (Dockview Phase A Migration) is fully planned and verified — ready to execute. Phase 29 (FTS Fuzzy Search) is complete. No tasks are mid-execution; this is a clean pause between planning and execution.
</current_state>

<completed_work>

## This session (v2.3 milestone init + Phase 29 + Phase 30 planning):

- Milestone v2.3 "Shell, Navigation & Views" initialized: PROJECT.md updated, STATE.md reset, REQUIREMENTS.md created (12 requirements: DOCK-01/02, VIEW-01/02, FTS-04, BUG-01/02/03, TEST-01/02/03/04), ROADMAP.md extended (Phases 29-34), research completed (4 parallel agents + synthesizer → SUMMARY.md)
- Phase 29 (FTS Fuzzy Search): PLANNED + EXECUTED + VERIFIED (9/9 automated must-haves pass; 5 browser items deferred to human verification)
  - `SearchService._normalize_query()` — tokens ≥5 chars get `~1`, short tokens exact
  - `/api/search?fuzzy=bool` param threaded through endpoint
  - `fuzzyPrefixLength=2` added to `sempkm-repo.ttl`
  - `search-fuzzy-toggle` command in ninja-keys Ctrl+K palette
  - `sempkm_fts_fuzzy` localStorage persistence
- Phase 30 (Dockview Phase A Migration): PLANNED + VERIFIED — 3 plans in 3 waves, all checks passed
</completed_work>

<remaining_work>

## Phase 30 — not yet executed:

**Wave 1 — Plan 30-01** (autonomous): Core dockview init
- Task 1: Rewrite `workspace-layout.js` — delete `recreateGroupSplit()` (301 lines) + entire HTML5 drag system (lines 414-609) + `renderGroupTabBar()`/`loadTabInGroup()`; add `DockviewComponent` init with `createComponent` factory (3 panel types: object-editor, view-panel, special-panel); wire dockview events to `sempkm:tab-activated` / `sempkm:tabs-empty`; `htmx.process()` in `onDidLayoutChange`; `onDidVisibilityChange` handlers for CodeMirror + Cytoscape; `fromJSON()` try/catch with `buildDefaultLayout()` fallback (NO `dv.clear()`); new key `sempkm_workspace_layout_dv`; retain `WorkspaceLayout` as thin metadata sidecar via `window._tabMeta`
- Task 2: Update `workspace.html` — empty `editor-groups-container`, add CDN links (bridge.css → dockview.css → dockview.js); remove "STATUS: Pattern file" comment from `dockview-sempkm-bridge.css`

**Wave 2 — Plan 30-02** (autonomous): Caller updates
- Task 1: Update `workspace.js` — rewrite `openTab()`, `openViewTab()`, `openSettingsTab()`, `openDocsTab()`, `closeTab()`, `markDirty()`, `markClean()`, `getActiveTabIri()` to use `window._dockview.api.addPanel()` + `window._tabMeta`; remove double-dispatch of `sempkm:tab-activated`
- Task 2: Update `object_tab.html` — replace `window._workspaceLayout.groups` iteration with `window._tabMeta[tabKey].typeIcon = typeIcon`

**Wave 3 — Plan 30-03** (checkpoint — human verify required):
- Task 1 (auto): Remove old editor-group/drag CSS from `workspace.css` (`.editor-group`, `.group-tab-bar`, `.group-editor-area`, `.gutter-editor-groups`, `.drop-indicator`, `#right-edge-drop-zone`, `.workspace-tab.dragging`); keep `.editor-groups-container`
- Task 2 (checkpoint): Human verification of 10 scenarios — tab open, multi-tab, drag-to-reorder, drag-to-split, layout persistence, event firing, htmx-after-drag, CodeMirror visibility, Cytoscape resize, view tabs

## Phases 31-34 — not yet planned:
- Phase 31: Object View Redesign (VIEW-01)
- Phase 32: Carousel Views + Bug Fixes (VIEW-02, BUG-01, BUG-03)
- Phase 33: Named Layouts + VFS Settings Restore (DOCK-02, BUG-02)
- Phase 34: E2E Test Coverage (TEST-01/02/03/04)
</remaining_work>

<decisions_made>

- v2.3 scope confirmed: DOCK-01/02, VIEW-01/02, FTS-04, BUG-01/02/03, TEST-01/02/03/04 (12 requirements, 6 phases)
- E2E testing added as dedicated Phase 34 (TEST category) per user request — covers fixing test.skip() for SPARQL/FTS/VFS + v2.3 feature coverage
- FTS fuzzy: `~1` edit distance only, tokens ≥5 chars, `search-fuzzy-toggle` ID (not `fts-*`) to avoid E2E filter collision
- Dockview Phase A: sessionStorage key changed to `sempkm_workspace_layout_dv` (old key cleared at init) to avoid fromJSON failures on stale data
- Carousel views: tab bar (no animation — animated cube-flip explicitly rejected as anti-feature per research)
- CSS scroll-snap for carousel (no Swiper.js dependency)
- Phase 29 human verification items (5 browser UI checks): deferred for user to verify manually when convenient
</decisions_made>

<blockers>

- Phase 29 has 5 browser verification items pending (not blockers for Phase 30 — independent features): toggle visible in Ctrl+K, title flip on click, state persists across reload, network URL includes &fuzzy=true, toggle not filtered during search
- Phase 30 Wave 3 is a human checkpoint — execution will pause at Plan 30-03 Task 2 for manual browser testing of 10 dockview scenarios
</blockers>

<context>
The v2.3 milestone is well-established. Research is complete (SUMMARY.md at .planning/research/). Phase 29 backend + frontend are done. Phase 30 plans are thorough — the researcher did line-level analysis of workspace-layout.js (1073 lines) and identified exactly what to delete vs retain. The three critical pitfalls (htmx silencing, CodeMirror/Cytoscape zero-size, fromJSON broken state) all have explicit guards baked into Plan 30-01 Task 1. The outer Split.js (nav/editor/right panes) is NOT touched in Phase A — only the inner editor-pane area.
</context>

<next_action>
Run: `/gsd:execute-phase 30`

This will execute Wave 1 (workspace-layout.js rewrite + workspace.html CDN) → Wave 2 (workspace.js + object_tab.html caller updates) → Wave 3 (CSS cleanup + human checkpoint).

Note: Wave 3 Plan 30-03 is `autonomous: false` — executor will pause at Task 2 for browser verification of 10 dockview scenarios.
</next_action>
